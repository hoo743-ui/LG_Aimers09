# 조건부 편차 계열 실험 누적표 (2026-08-12)

가설: **"선수의 절대 수준"이 아니라 "그 선수의 기본 수준에서 상황별로 얼마나
벗어나는가"** 는 시즌을 넘어 전이된다. 수준값은 다섯 번 죽었고(4-22·4-26·4-27·
4-28·4-29) 편차는 처음으로 살아남았다(4-30).

모든 표는 **학습 구간에서만** 만들고, 조회 키는 그 행 자신의 컬럼뿐이다 (5) 안전).
축소는 `n/(n+k)`. 판정은 3폴드 고정가중 부호일치 + 워크포워드 + 연도누설 + 제출 경로.

---

## 1. 어디에 신호가 있는가 — 교차적합 그룹 오라클

폴드 안에서 그대로 재면 `K/n` 만큼 부푼다. 교차적합으로 걷어낸 값이다 (4-30).

| 그룹 | fold2024 | fold2022 | fold2021 | 비고 |
|---|---|---|---|---|
| `pitcher x 타자손` | **+230.92** | +81.37 | +263.10 | 3/3, 1등 |
| `pitcher x batter` | +168.83 | +135.87 | +218.28 | 셀 10행, 편차형 불가 |
| `pitcher_id` | +90.83 | +20.91 | +223.67 | 4-21 의 874.8 은 거품 포함 |
| `pitcher x 카운트` | +57.83 | +31.03 | +81.22 | |
| `batter_id` | +17.87 | +0.78 | +243.16 | |

---

## 2. 편차 후보 — 3폴드 고정가중, 모델 단독 위에서

| exp | 후보 | k | w\* | 평균 | 부호 | 2021 | 2022 | 2024 | 판정 |
|---|---|---|---|---|---|---|---|---|---|
| D01 | 투수 x 타자손 | 300 | 0.40 | **+16.06** | 3/3 | +18.12 | +12.01 | +18.05 | **채택** |
| D02 | 〃 | 100 | 0.30 | +15.31 | 3/3 | +17.59 | +10.51 | +17.82 | k=300 이 최적 |
| D03 | 〃 | 200 | 0.30 | +15.60 | 3/3 | +17.95 | +11.97 | +16.88 | 〃 |
| D04 | 〃 | 600 | 0.40 | +15.80 | 3/3 | +17.97 | +12.79 | +16.64 | 〃 |
| D05 | 〃 | 1200 | 0.60 | +15.66 | 3/3 | +17.80 | +12.58 | +16.60 | 〃 |
| D06 | 투수 x 카운트 (부모=투수) | 1200 | 0.40 | +3.50 | 3/3 | +0.24 | +3.81 | +6.46 | 약함 |
| D07 | 타자 x 투수손 (거울상) | 300 | 0.20 | +2.53 | 3/3 | +0.95 | +3.14 | +3.48 | 약함 |
| D08 | 타자 x 투수손 x 카운트 | 600 | 0.20 | +1.20 | 2/3 | −1.08 | +3.47 | +1.22 | **기각** |
| D09 | 타자 x 카운트 | 300 | 0.05 | +0.30 | 2/3 | −0.33 | +0.76 | +0.47 | **기각** |
| D10 | 투수 x 주자유무 | 300 | 0.10 | +0.11 | 1/3 | −0.37 | −0.11 | +0.80 | **기각** |
| D11 | 투수 x 이닝(3구간) | 300 | 0.10 | +0.51 | 2/3 | +1.09 | +0.74 | −0.29 | **기각** |
| D12 | 투수 x 스트라이크 | 300 | 0.20 | +1.49 | 2/3 | −3.26 | +1.63 | +6.11 | **기각** |

**`축소계수 k` 는 100~1200 에서 15.3~16.1 로 평평하다.** 여기서 짜낼 것은 없다.

---

## 3. 증분 — **현행 후보(모델 + 0.199·플래툰) 위에서**

D01 을 이미 채택했으므로 델타는 그 위에서 재야 한다 (규칙 "델타는 대체 대상에
대해 재라"). 부모를 무엇으로 두느냐가 결정적이다.

| exp | 후보 | 부모 | k | w\* | 평균 | 부호 | 2021 | 2022 | 2024 |
|---|---|---|---|---|---|---|---|---|---|
| D13 | 투수 x 카운트 | 투수 | 600 | 0.20 | +2.97 | 3/3 | +0.37 | +3.44 | +5.10 |
| D14 | 투수 x 타자손 x 카운트 | 투수 | 300 | 0.40 | (+13.51) | 3/3 | +9.55 | +14.45 | +16.54 |
| D15 | 〃 | **플래툰셀** | 300 | 0.30 | +4.53 | 3/3 | +1.32 | +5.65 | +6.63 |
| D16 | 〃 | **플래툰셀** | 800 | 0.60 | **+4.92** | 3/3 | +1.87 | +5.16 | +7.74 |
| D17 | 〃 | **플래툰셀** | 2000 | 0.60 | +3.82 | 3/3 | +2.33 | +3.54 | +5.59 |

> **D14 의 +13.51 은 착시다.** 부모를 투수로 두면 플래툰 효과를 다시 세는 것이라
> 플래툰과 겹친다. 부모를 플래툰 셀로 바꾸면 순수 증분은 **+4.92** 다.
> 중첩 편차를 쓸 때는 **부모를 직전 채택 후보의 셀로** 두어야 한다.

---

## 4. 계산 불가능한 것 — 데이터 구조로 확인

| 후보 | 왜 안 되는가 |
|---|---|
| `투수 x 타자손 x 구종` 성과 편차 | `pitch_type_group` 은 `trackman_history.csv` 에만 있고 train/test 와 **1:1 결합이 안 된다.** `control_success` 는 train 에만 있으므로 `rate(p,h,구종)` 자체가 계산 불가 |
| 구종 선택 편차 (행동) | 계산은 가능하고 **이미 모델 안에 있다** — 4-9 의 `tmh_*`/`tmc_*` 8개가 손·카운트 조건부 Trackman 편차다 |
| `asof_*` 경험베이즈 축소 | 5-d 로 이미 쟀다. `rho^2` 순위 −1.04 (1/3), 증분 +2.04 (4-25, 4-26). 닫힘 |
| ID 시계 성분 분리 | `no_season` −159.33, `cat_ids` −41.30 (`rho_rank`). 4-12·4-18 에서 닫힘 |

---

## 5. LB 실측 — 전이율

| 회차 | 구성 | 예상 | 실측 | 배운 것 |
|---|---|---|---|---|
| 13 | 아핀 최적만 | 942.457753 | **942.4577639361** | 평가셋 모형이 정확 (오차 1.1e-5) |
| 14 | + 플래툰 w=0.3904 | 963.4 | **940.1357117095** | `w` 과대 + 절편 파괴. 역산으로 `k`=0.7685 획득 |
| 15 | + 플래툰 w=0.1990 | 946.50 | **946.3826029949** | 오차 0.12. `w` 최적 확정 |
| 16 | + 플래툰x카운트 w=0.20 | 948.82 | **950.0112119476** | 예상을 넘겼다. `k2`=**0.9340** |

`lambda`=0.9725 — 로컬 2차 통계(분산·공분산 구조)는 거의 그대로 옮겨진다.
**전이가 깨지는 것은 `cov(f,y)` 뿐이다.**

### 🚩 중첩이 깊을수록 전이가 좋아진다

```
2단  투수 x 타자손              k  = 0.7685
3단  투수 x 타자손 x 카운트       k2 = 0.9340
```

수준 성분이 뺄셈으로 더 많이 소거되니 당연한 방향이고, **14회차에서 세운
"낮게 걸어라"를 뒤집는다.** 그 규칙은 전이율을 *모를 때*의 것이고, 깊은 중첩은
오히려 로컬 최적에 가깝게 걸어야 한다. 16회차에서 `w2` 를 3배 낮게 걸었다
(0.20 제출, 최적은 0.5785).

### 깊이는 3단에서 끝난다

4단(부모 = 플래툰x카운트 셀)을 네 축으로 시도했다.

```
추가 축         w*    평균  부호     2021     2022     2024  중앙셀n
+아웃          0.80  +0.68  2/3   +1.21   +2.05   -1.23      11
+주자유무       0.80  +0.56  2/3   -0.79   +1.07   +1.38      15
+홈원정        0.80  +0.68  2/3   +1.61   -0.61   +1.06      16
+이닝         0.40  -0.23  1/3   -0.02   +0.70   -1.37      10
```

셀이 10~16행이라 축소를 아무리 걸어도 신호가 안 남는다. **3단이 바닥이다.**

---

## 5-b. 폭 방향도 닫혔다 — 3단 축은 카운트뿐

`k2`=0.9340 실측은 판단을 바꾼다. 깊은 중첩이 93% 전이되므로 **로컬에서 +2 인 축도
평가셋 +1.9 가치가 있다.** 그래서 로컬 수치가 약해 접었던 3단 축들을 다시 훑었다
(부모 = 플래툰 셀, 기준 = 모델 + 0.20·플래툰).

| 3단 축 | w\* | 평균 | 부호 | 2021 | 2022 | 2024 | 셀n |
|---|---|---|---|---|---|---|---|
| **카운트** | 1.00 | **+4.92** | **3/3** | +2.61 | +4.54 | +7.62 | 27 |
| 이닝(3구간) | 0.30 | +0.68 | 3/3 | +1.13 | +0.64 | +0.26 | 122 |
| 주자수 | 0.30 | +0.43 | 3/3 | +0.10 | +0.47 | +0.74 | 73 |
| 초말 | 0.30 | +0.62 | 1/3 | +2.43 | −0.33 | −0.24 | 235 |
| 점수차 3구간 | 0.30 | +0.04 | 1/3 | −0.35 | +1.87 | −1.39 | 129 |
| 주자상태 | 0.30 | −0.01 | 2/3 | −1.20 | +0.30 | +0.86 | 35 |
| 아웃 | 0.30 | −0.23 | 1/3 | +0.48 | −0.15 | −1.03 | 143 |
| 타자팀 | 0.30 | −0.30 | 2/3 | −3.24 | +1.68 | +0.67 | 86 |
| 월 | 0.30 | −0.56 | 1/3 | +0.35 | −0.86 | −1.18 | 98 |
| `li` 4분위 | 0.30 | −1.11 | 0/3 | −0.54 | −0.32 | −2.46 | 109 |
| 경기유형 | 0.60 | (+10.17) | 2/3 | +30.71 | +16.23 | **−16.42** | 116 |

> **`경기유형` 은 함정이다.** 평균이 크지만 2024 에서 −16.42 다. 정규/포스트시즌
> 구성이 해마다 달라지는 전형적인 연도 의존이라 기각한다 (4-9 의 `tmc_n` 과 같은 형태).

**카운트만 유일하게 강하다.** 폭도 닫힌다.

## 5-c. 타자 쪽 계층 — 얹을 가치가 없다

투수 계층과 구조적으로 독립인데(상관 −0.009) 현행 후보 위에서는 한 번도 안 쟀다.
기준 = 모델 + 0.20·플래툰 + 0.5785·플래툰x카운트.

| 후보 | k | w\* | 평균 | 부호 | 2021 | 2022 | 2024 |
|---|---|---|---|---|---|---|---|
| 타자 x 투수손 (부모=타자) | 600 | 0.20 | +1.42 | 3/3 | +0.36 | +1.63 | +2.27 |
| 타자 x 투수손 (부모=타자) | 300 | 0.20 | +1.18 | 2/3 | −0.35 | +1.73 | +2.16 |
| 타자 x 투수손 x 카운트 | 2000 | 0.20 | +0.21 | 2/3 | −0.37 | +0.94 | +0.06 |
| 타자 x 카운트 | 800 | 0.20 | +0.02 | 2/3 | −1.51 | +0.99 | +0.56 |

3/3 이지만 평가셋으로 환산하면 **최적 가중이 0 이다.**

```
k3 (2단 편차의 실측 전이율 = 0.7685)   최적 w3     예상
0.00 / 0.40 / 0.77                    0.0000    952.72
1.00                                  0.1763    954.22
```

분산 비용이 신호를 넘어서 **100% 전이될 때만** 가치가 생긴다. 2단 편차의 실측이
0.7685 이므로 **기각.**

## 6-b. 다음 후보 (슬롯이 열리면) — `submissions/cand_plat_cnt_w058.zip`

`k2`=0.9340 을 실측했으므로 `w2` 를 다시 풀었다. 재학습 0회.

```
p += 0.2014 * dev(투수x타자손 | 부모=투수, k=300)
p += 0.5785 * dev(투수x타자손x카운트 | 부모=플래툰, k=800)
alpha = 1.089294   center = 0.622802
예상 952.72  (현재 950.0112 대비 +2.71)
```

`w2` 곡선은 0.5~0.6 에서 평평하다 (0.50 → 952.60, 0.60 → 952.71, 0.80 → 951.81).
검증 4단계 통과, 추론 3.5초, `clip` 접촉 0%, 범위 0.2794~0.6772.

---

## 6. 16회차 제출본 — `submissions/cand_plat_cnt.zip`

```
p += 0.20 * dev(투수 x 타자손 | 부모=투수, k=300)          <- 실측 확정
p += 0.20 * dev(투수 x 타자손 x 카운트 | 부모=플래툰, k=800) <- 전이율 미측정
p' = center + 1.090437*(p - center),  center = 0.620268
```

검증: 추론 경로 == 실험 경로 **최대차 0.000e+00**, `verify_submit` 4단계 통과,
추론 3.7초, `clip` 접촉 0%, 커버리지 80.1%/79.8%.

| `k2` (플래툰x카운트의 실제 전이율) | w2=0.0 | w2=0.2 | w2=0.4 |
|---|---|---|---|
| 0.00 (신호 없음) | 946.40 | 943.24 | 938.61 |
| 0.40 | 946.40 | 946.14 | 944.39 |
| 0.77 (플래툰과 같다면) | 946.40 | **948.82** | 949.72 |
| 1.00 | 946.40 | 950.50 | 953.08 |

기준선 = 15회차 실측 **946.3826**. `w2`=0.20 은 `k2`≥0.4 에서 본전, 0.77 에서 +2.4.

---

## 6-c. 잠재 투수-타자 상호작용 (MF / FM) — **기각.** 대조군까지 세워서 확인했다

가설: `pitcher x batter` 를 그룹 평균으로 외우지 말고 저차원 `u_i · v_j` 로 표현하면
셀 희소성을 피하면서 상호작용을 쓸 수 있다. **한 번도 안 해본 inductive bias 였고
볼 가치가 있었다.** 학습 0회의 값싼 검산으로 끝났다.

### 커버리지 (학습 2019~2023 → 2024, 2025 의 유사물)

```
학습 1,221,585행  투수 711  타자 742  쌍 82,297
평가   253,507행  투수 391  타자 424  쌍 26,355
미등장 비율   투수 19.86%   타자 9.30%   쌍 48.84%
학습 쌍당 표본  중앙 9  평균 14.8   n>=10 인 쌍 46.6%
```

**쌍의 절반이 미등장**이다. 그룹 평균이 불가능한 이유이고, 동시에 MF 의 논거이기도
하다 — 임베딩은 쌍이 아니라 선수 단위라 미등장 쌍도 예측된다.

### 상호작용이 실재하는가 — 분산성분 (학습 0회)

주효과(투수·타자)를 교대최소제곱으로 뺀 잔차의 쌍 평균 `r_ij` 에 대해,
무상호작용이면 `E[n·r²]` 이 `n` 과 무관해야 한다.

```
E[n*r^2] = 0.27489 + 0.000908 * n
  절편(잡음) 0.27489   기울기(상호작용 분산 tau^2) +0.000908
  tau = 0.03013        (플래툰 편차 표준편차 0.0148 의 두 배)
```

**상호작용은 실재한다.** 여기까지는 가설을 지지했다.

### 그런데 시즌을 안 넘는다

```
[A] 같은 구간 무작위 반쪽 분할    n>=5 +0.1172   n>=10 +0.1215   n>=20 +0.1471
[B] 시즌 간 19-21 vs 22-23      n>=3 +0.0144   n>=5  +0.0245   n>=10 +0.0459
[C] 참고: 투수 주효과 (시즌 간)                            +0.5761
```

같은 잡음 수준에서 **[A] 가 [B] 의 3~10배**다. 쌍 상호작용은 실재하지만
**시즌 특이적**이다.

### 저차원 성분만 따로 — 가중 ALS

"전체 쌍 잔차는 안 넘어가도 저차원 성분은 넘어갈 수 있다"가 이 가설의 핵심이므로
따로 쟀다. 19-21 잔차에 가중 ALS(가중 = 쌍 표본수)를 적합해 22-23 에서 검증했다.

| rank | lam | 19-21 내부 | 22-23 전이 |
|---|---|---|---|
| 2 | 20 | 0.1617 | **+0.0371** |
| 4 | 20 | 0.2180 | +0.0298 |
| 8 | 5 | 0.3927 | +0.0190 |
| 16 | 5 | 0.5328 | +0.0140 |

**rank 를 올리면 내부 적합은 0.53 까지 오르고 전이는 0.014 로 떨어진다.**
시대 특이 구조에 과적합하는 전형이다.

### 🚩 대조군 — 이게 결정적이다

전이가 **실측된** 신호(플래툰, `k`=0.7685)를 **같은 지표로** 재서 비교했다.
지표 자체가 잡음에 묻혀 아무것도 못 잡는 것인지 가리기 위해서다.

```
같은 22-23 쌍 잔차와의 상관 (공통쌍 30,922)
  플래툰 편차 (전이 실측)   +0.0666
  rank-2 ALS (lam=20)     +0.0371
  rank-16 ALS (lam=5)     +0.0140
```

**지표는 전이 신호를 잡아낸다** — 플래툰이 0.0666 을 낸다. ALS 의 최고치는 그 56%다.

그리고 결정적으로, **ALS 가 학습한 잔차에는 플래툰이 그대로 들어 있다** (뺀 것은
투수·타자 주효과뿐이다). 즉 rank-2 가 하는 일은 **플래툰의 재발견**이고, 명시적
피처보다 **못 한다.** 그 너머에 전이되는 저차원 구조는 없다.

### 그래서 FM / Wide&Deep / tabular NN 도 같은 운명이다 — **논증이지 측정이 아니다**

이 계열의 장점은 정확히 **고차 상호작용을 표현하는 것**이다. 그런데 표현할 대상이
시즌을 안 넘는다. 계획의 4·5·6단계는 2·3단계의 성공을 전제로 하므로 여기서 멈춘다.

> **🚩 2026-08-13 정정.** 이 절의 제목("MF / FM — 기각")과 `STATUS.md` §5 의
> "잠재 상호작용 (MF/FM) 대조군 포함 기각" 은 **측정보다 넓게 주장하고 있었다.**
> 여기서 실제로 실행된 것은 커버리지 계수 · 분산성분 · 시즌전이 검사 ·
> **가중 ALS(rank 2/4/8/16)** 이고, **FM 도 DeepFM 도 한 번도 돌리지 않았다.**
> 위 문단은 ALS 결과로부터의 **확장 논증**이다. 빈칸은 §6-p 에서 메운다.

> **야구 도메인으로 읽으면 자연스럽다.** 투수-타자 매치업에서 시즌을 넘어 유지되는
> 저차원 구조는 **좌우 손 관계**이고, 우리는 그것을 이미 명시적으로 뽑아 썼다
> (4-30, `k`=0.7685). 그 외의 "이 투수는 저 타자에게 유독 약하다"는 대부분
> 그 시즌의 표본 잡음이다.

---

## 6-d. OOF 잔차 / blind-spot 탐색 — 곡률 하나를 찾았고, 크기가 안 넘어간다

질문을 "무슨 피처를 더할까"에서 **"현재 모델이 어디서 일관되게 틀리는가"** 로 바꿔
잔차를 직접 봤다. 기준은 현행 최고 구성(모델 + 0.20·플래툰 + 0.5785·플래툰x카운트).

> **이 지표에서 잔차 탐색은 의미가 좁다.** 아핀이 공짜이므로 `p` 의 **선형** 함수는
> 이미 다 쓴 것이다. 찾을 것은 `p` 에 직교하는 구조이거나 `E[y|p]` 의 **비선형성**뿐이다.

### ✅ 찾은 것 — `E[y|q]` 의 U자 곡률 (3/3 부호일치)

예측 10분위별 (실제 − 선형적합):

```
분위      2021       2022       2024
  1   +0.02682   +0.00133   +0.00362    <- 3/3 양수
 5~7     음수       음수       음수      <- 3/3 음수
 10   +0.02798   +0.00065   +0.00903    <- 3/3 양수
```

**양 극단을 과소예측하고 가운데를 과대예측한다.** 아핀은 직선이라 이걸 원리적으로
못 잡는다. 4-1 의 "과신하지 않기"(규제 과다)와 앞뒤가 맞는 그림이다.

### ❌ 그런데 크기가 시즌마다 30배 다르다

```
2차항 계수 (1차항으로 정규화)   2021 +0.1467   2022 +0.0048   2024 +0.0530
폴드 내 최적 이득              2021 +62.13    2022  +0.09    2024  +4.22
워크포워드                     2022 -41.75    2024  +2.08     (1/2)
```

2항 모형에서는 "모양"과 "크기"가 같은 파라미터라 분리가 안 된다 — 모양만 옮기려는
시도도 2022 에서 −53.49 였다. `alpha` 와 같은 처지이고, **크기는 평가 시즌 위에서
LB 로 풀어야 한다.**

폴드 내 최적값이 곧 LB 로 풀었을 때 얻을 값이므로(둘 다 대상 시즌 위에서 맞춘다)
기대치는 **2024 를 유사물로 보아 +4 안팎**이고, 미지수 1~2개라 **LB 점 1~2장**이 든다.

### ❌ 나머지 잔차 계열 — 신호 없음

**표본량 층화** (모델이 이력 적은 행을 과신하는가). 잔차를 예측수준으로 선형 제거한
뒤 5분위별 평균:

```
투수 이력 asof_pitcher_n   1분위  +0.01400 / -0.00211 / -0.00272   부호 갈림
타자 이력 asof_batter_n    1분위  +0.01368 / +0.00600 / -0.00225   부호 갈림
```

3/3 이 찍히는 분위는 값이 −0.0001~−0.0009 로 무시할 크기다. **blind spot 없음.**

**맥락 잔차** (교차적합 증분): 타자손 +0.23 (3/3, 무시할 크기), 카운트 −0.77 (1/3).
이미 §2·§5 에서 훑은 것과 같은 결론이다.

### 6-e. 곡률 보정 — 모양 고정 + `lambda` 축소로 재검증. **기각**

"계수를 폴드마다 맞추니 무너진 것이니, 모양을 고정하고 크기만 약하게 걸면 살지도
모른다"는 가설을 정식으로 검증했다 (`exp/curv_test.py`).

설계 — `z = (q-c)/s` 에서 `c`,`s` 는 **이전 폴드의 `q` 에서만** (타깃 미사용),
`shape = z^2 - 1` 로 평균 0 을 맞춰 중심 이동을 만들지 않는다. `q' = q + lambda*s*shape`
이므로 `lambda` 는 무차원이고 폴드 간 의미가 같다.

검증 split 을 위해 **fold 2023 을 새로 학습했다** (`plans/exp030_f2023.json`, 3시드).
2023 은 4-2 가 기록한 붕괴 시즌이 맞았다 — 원점수 **−1224.32**, `rho^2` **77.68**
(2024 는 789). 상수 예측보다 못한 해다.

```
val    lam     dBrier    d(1e5*rho^2)
2022  0.10   +0.000492      -54.89
2023  0.10   +0.001595      -39.28
2024  0.10   -0.000025       +3.73

lam    평균 dBrier   평균 d(rho^2)   부호   최악
0.00    +0.000000        +0.00      0/3     +0.00
0.10    +0.000687       -30.14      1/3    -54.89
0.25    +0.002659      -100.74      0/3   -200.21
0.50    +0.006573      -242.44      0/3   -390.49
1.00    +0.011934      -468.54      0/3   -736.24
```

**`lambda`=0 이 최선이다.** 모든 `lambda`>0 에서 평균이 음수이고, 가장 약한 0.10
에서도 2024 만 +3.73 이고 2022 −54.89 / 2023 −39.28 이다. `dBrier` 도 전부 악화다.

모양을 고정해도 안 되는 이유는 명확하다 — **곡률의 크기가 시즌 속성이지 모델
속성이 아니다.** 2021 은 강하게 휘어 있고 2022 는 거의 직선이라, 하나의 `lambda`
로는 어느 쪽도 맞출 수 없다. `alpha` 가 통한 것은 평가 시즌 위에서 **직접 풀었기**
때문이지 전이시켰기 때문이 아니다 (4-28 의 결론과 같다).

### 결론

잔차에는 **곡률 하나**가 남아 있고 그 외에는 구조가 없다. 그리고 그 곡률은
**전이시킬 수 없다** — 크기를 평가 시즌 위에서 LB 로 직접 풀어야만 쓸 수 있고,
그때의 기대치는 +4 안팎(2024 유사물)에 LB 1~2장이다. 현재 대기 후보
(`w2` 재최적화, +2.71, **LB 0장**)보다 우선순위가 낮다.

---

## 6-f. regime / 동적 키 탐색 — 신호 없음

### 평가 시즌은 붕괴형이 아니다 (먼저 확인할 것)

```
fold   rho^2      r
2021  1383.81  0.5328
2022  2361.41  0.5289
2023    77.68  0.5000   <- 붕괴형 (원점수 -1224)
2024   768.73  0.4861
평가셋 950 구간, rho=0.0971
```

평가셋은 2024 와 2022 사이다. **2023 대응은 이번 대회 점수에 기여하지 않는다**
(4-2 의 결론과 같다). 그리고 평가 시즌의 regime 을 **합법적으로 식별할 방법이 없다**
— 규칙 5) 가 test 전체 분포 참조를 금지한다. 우리가 아는 것은 LB 로 잰 값뿐이다.

### 동적 키(최근 폼) 중첩 편차 — 전부 약함

지금까지 3단 키는 전부 정적 맥락(카운트·이닝·주자)이었다. 그 행 자신의 `asof` 로
만든 **최근 폼 구간**을 키로 쓰는 것은 안 해봤다 (행 독립, 합법).

| 후보 | w\* | 평균 | 부호 | 2022 | 2023 | 2024 |
|---|---|---|---|---|---|---|
| 투수 x 최근폼5 x 카운트 | 0.50 | +0.45 | 3/3 | +0.29 | +0.40 | +0.66 |
| 플래툰 x 최근폼5 | 0.20 | +0.71 | 2/3 | −0.35 | +0.79 | +1.69 |
| 투수 x 최근폼1 | 0.20 | +0.64 | 2/3 | −2.71 | +0.70 | +3.93 |
| 투수 x 최근폼5 | 0.20 | −1.34 | 1/3 | −4.18 | +1.48 | −1.33 |
| 타자 x 타자수준 | 0.20 | −2.02 | 0/3 | −1.79 | −0.21 | −4.05 |

3/3 인 것은 +0.45 로 채택 기준(+0.5)에 미달이다.

### 시즌별 효과 크기는 regime 을 따라간다 — 그러나 예측에는 못 쓴다

```
fold   rho^2   플래툰 최적w   이득    카운트 최적w   이득
2021  1383.81     0.370    +18.26      0.410    +2.40
2022  2361.41     0.340    +12.43      0.600    +5.22
2023    77.68     1.200    +33.01      1.200    +7.32
2024   768.73     0.470    +18.42      0.710    +7.99

rho^2 와 최적w 의 상관   플래툰 -0.813   카운트 -0.744
```

**모델이 약한 시즌일수록 편차 가중이 커진다.** 구조적으로 말이 되는 관계다.
그런데 이 추세로 평가셋(`rho^2`≈950)을 외삽하면 `w`≈0.46 인데 **실측은 0.1992** 다.
전이율 0.7685 만큼 신호가 깎이기 때문이고, **우리는 이미 실측값을 갖고 있으므로
추세가 필요 없다.** regime-aware 가중은 쓸 데가 없다.

---

## 6-g. 🚩 2단 잔차 모델 — **남은 구조의 상한을 잰다.** 상한이 0 이다

개별 후보를 하나씩 스캔하는 대신 **잔차를 타깃으로 규제된 모델을 학습**시켰다
(`exp/resid_stage2.py`). 그러면 형태를 가리지 않고(선형·임계값·상호작용·비단조)
남은 구조를 잡는다. 이전 폴드의 OOF 잔차로만 학습하고 대상 폴드에서 평가한다.

```
val   모델              학습행     기준 rho^2    w      증분    상관(잔차)
2022  HGB leaf8       247,088    2376.97   0.00   +0.00    0.0049
2022  HGB leaf31      247,088    2376.97   0.00   +0.00    0.0048
2022  Ridge           247,088    2376.97   0.25   +3.73    0.0091
2023  HGB leaf8       494,560      87.67   0.00   +0.00   -0.0658
2024  HGB leaf8       740,085     788.95   0.00   +0.00    0.0035
2024  Ridge           740,085     788.95   0.25   +0.92    0.0047

모델            평균 증분   부호     2022     2023     2024
HGB leaf8         0.00    0/3    +0.00    +0.00    +0.00
HGB leaf31        0.00    0/3    +0.00    +0.00    +0.00
Ridge             1.55    2/3    +3.73    +0.00    +0.92
```

**트리는 세 폴드 모두 최적 가중이 0 이다.** 740,085행에 55개 피처 + 편차항을 주고
잔차를 학습시켰는데 시간을 건너 쓸 수 있는 것이 하나도 없다. 잔차와의 상관도
0.003~0.009 이고 2023 에서는 **음수**(−0.066)다.

그리고 이 숫자들은 **`w` 를 대상 폴드에서 고른 낙관 상한**이다. 실제로는 더 작다.

> **이것이 개별 스캔 전부를 대체한다.** `batter x count`, `pitcher x count`,
> `recent_form x hand`, 임계값, 조각별, 분위 버킷 — 어떤 형태든 트리가 표현할 수
> 있고, 트리가 0 을 냈다. **잔차에 남은 큰 구조가 없다.**

`Ridge` 만 +1.55 (2/3) 인데, 이는 잔차의 **전역 선형 성분**이다. 아핀은 `q` 에
대해서만 선형이므로 피처에 대한 선형 성분은 아직 안 쓴 몫이 맞다. 다만 상한이
+1.55 이고 2023 에서 0 이라 채택 기준에 못 미친다.

---

## 6-h. 곡률 `lambda` 를 LB 로 풀 가치가 있는가 — **없다.** 부호부터 도박이다

"크기가 전이 안 되면 `alpha` 처럼 LB 로 직접 풀면 되지 않나"를 확률로 계산했다.

### 먼저 — 3/3 U자는 프로토콜을 조이면 사라진다

이전에 본 "3폴드 부호일치"는 **폴드 내** 선형적합과 **폴드 내** shape 파라미터를 쓴
결과였다. `c`,`s` 를 이전 폴드에서만 잡고 `q` 의 선형 성분을 제대로 걷어내면:

```
fold   부분상관(shape, y | q)   폴드내 최적 w
2022        +0.00096              +0.01
2023        -0.05389              -0.50    <- 부호가 뒤집힌다
2024        +0.00652              +0.07

평균 -0.01547   표준편차 0.03339
```

**크기만이 아니라 부호가 안 맞는다.** 사전분포가 0 을 크게 걸치고 평균은 **음수**다.

### 평가셋 모형으로 계산한 확률

미지수는 곡률의 평가셋 공분산 하나뿐이다 (2차 통계는 `lambda`=0.9725 로 전이).
모형이 실측을 재현하는지 먼저 확인했다 — **950.02 vs 실측 950.0112.**

```
A = 제출본 (LB 950.0112)
 lam      기대   보수10%   낙관90%   P>950   P>952   P>955
0.00    950.02   950.02   950.02  100.0%    0.0%    0.0%
0.10    899.07   794.82  1005.60   26.3%   25.5%   24.4%
0.25    789.91   565.87  1026.97   18.4%   18.1%   17.7%
0.50    594.63   276.61   951.62   10.1%   10.0%    9.8%
1.00    340.23    42.33   719.95    3.8%    3.7%    3.7%
```

**모든 `lambda`>0 의 기대값이 기준선보다 크게 낮다.** 분산 비용은 확실히 발생하고
공분산 이득은 평균이 음수인 동전던지기이기 때문이다. 가장 약한 0.10 조차
`P(>950)` = **26.3%** 다.

> **비대칭이 반대 방향이다.** 이 프로젝트에서 통한 것들(`alpha`,`w`,`w2`)은 전부
> 부호가 확정된 뒤 크기만 풀었다. 곡률은 부호부터 미지라 LB 로 풀 대상이 아니다.

### 평가 시즌 유사물 근거

```
       rho^2      r      |rho^2-950|   |r-0.4609|
2021  1383.81  0.5328       433.8       0.0719
2022  2361.41  0.5289      1411.4       0.0680
2023    77.68  0.5000       872.3       0.0391
2024   768.73  0.4861       181.3       0.0252   <- 양쪽 모두 최근접
평가셋  ~950    0.4609
```

---

## 6-i. 구조 감사 — 정의 오류는 없다. 상한은 있는데 **시즌 안에만 있다**

### 정의 감사는 이미 5회 통과했다

target 방향·확률변환·Brier 산식·집계·행정렬·`clip` 중 하나라도 틀리면 평가셋 모형이
LB 를 재현할 수 없다. 13회차 오차 **+0.000011**, 15회차 −0.117, 16회차 +1.19 다.
**정의 계열은 감사 완료로 본다.**

### 행 구조 — 환원 불가 잡음의 직접 증거

`row_id` 는 유일하다. 그런데 피처(시즌·월·요일·이닝·초말·투수·타자·볼·스트라이크·
아웃·주자상태)가 **완전히 같은 행이 12.06%** 다.

```
중복 그룹 75,966개, 그룹당 2.34행
그룹 안에서 정답이 갈리는 비율 54.7%
```

**같은 상태에서 결과가 갈린다.** 이 문제의 잡음 바닥이 높다는 직접 증거다.

### 정보 상한 — 분산성분으로 잰다

완전일치 그룹의 그룹간 분산에서 잡음 몫을 빼면 `Var(E[y|x])` 의 하한이 나온다
(`x` = 위 11개 컬럼. `asof_*` 를 조건에 안 넣었으므로 **하한**이다).

```
                 그룹      tau^2      rho^2 상한
전체(시즌 혼합)   76,519   +0.009891      3966
2021 내부        12,323   +0.011142      4467
2022 내부        13,142   +0.012436      4986
2023 내부        12,674   +0.009091      3645
2024 내부        13,242   +0.002216       888
```

**상한이 시즌마다 5배 넘게 다르다** — 4-21 의 "해상도는 시즌이 정한다"가 독립적인
방법으로 재확인됐다. 그리고 2024 내부 상한 888 대비 우리 모델은 789 로 **89%** 를
이미 회수하고 있다.

> 시즌 간으로 짝지은 그룹(전이 가능분)도 재려 했으나 **그룹이 578개뿐이라 표준오차가
> 추정치와 맞먹는다** (상관 +0.082, SE≈0.042). 결론에 쓸 수 없어 기록만 남긴다.

### 결론 — 구조적 오류는 없고, 상한은 시즌 안에 갇혀 있다

정의도 파이프라인도 문제가 없다. 상한은 현재 성능보다 위에 있으므로 **한계는 환원
불가 잡음이 아니다.** 그런데 그 여유분은 **시즌 내부 정보**이고, 이 프로젝트가
여섯 번(4-22·4-26·4-27·4-28·4-29·6-c) 확인한 대로 시즌을 넘지 않는다.

**모델 구조를 바꿔서 회수할 수 있는 종류가 아니다.** partial pooling·계층 구조·
history decay 는 전부 *더 나은 시즌 내 추정*을 주는데, 병목은 추정이 아니라 전이다.

---

## 6-j. 최종 의사결정 — `cand_plat_cnt_w058.zip` **제출**

### LB 예측 calibration 은 파라미터 출처별로 갈린다

이것이 판단의 핵심이다. 같은 "예측 오차"라도 성격이 완전히 다르다.

| 회차 | 파라미터 출처 | 예측 | 실측 | 오차 |
|---|---|---|---|---|
| 13 | **풀림** (LB 4점) | 942.457753 | 942.4577639 | **+0.000011** |
| 15 | **풀림** (LB 5점) | 946.50 | 946.3826030 | **−0.117** |
| 14 | 가정 (`k`=1 암묵) | 963.40 | 940.1357117 | −23.26 |
| 16 | 가정 (`k2`=0.7685 차용) | 948.82 | 950.0112119 | +1.19 |

**풀린 파라미터로 예측한 두 건은 `|오차|` 최대 0.12 다.** 가정 전이율을 쓴 두 건은
23.3 이다. `w058` 은 `k2`=0.9340 을 16회차로 **실측한 뒤 푼** 값이라 앞쪽 범주다.

### 불확실성은 자기일관되게 계산해야 한다

`k2` 는 `lambda` 를 가정하고 16회차 관측에서 푼 값이므로 **둘은 독립이 아니다.**
`lambda` 를 흔들 때 `k2` 도 관측과 일관되게 다시 풀어야 한다.

```
lam    재풀이 k2   w058 점수
0.800    0.2808     945.59
0.973    0.9340     952.72
1.150    1.6062     960.06

기대 952.72 | 10분위 949.52 | 5분위 948.63 | 1분위 946.93 | 90분위 955.88
P(>950.0112) = 86.1%    P(>952) = 61.5%    P(>955) = 18.0%
```

`w2` 곡선도 평평하다 (0.40 → 952.12, 0.5785 → 952.72, 0.80 → 951.81) — `w2` 를
잘못 잡아도 비용이 작다.

### 결정

**SUBMIT.** 기대 +2.71, 10분위 downside −0.49 로 비대칭이 유리하고, 이 후보가 속한
"풀린 파라미터" 범주의 과거 calibration 이 0.12 이내다. 논리적 변경은 `w2` 하나뿐이다.

baseline artifact 3종(`grid_affine_solved` / `cand_platoon_w020` / `cand_plat_cnt`)
보존 확인, `verify_submit` 4단계 재통과 (추론 3.5초, `clip` 0%).

---

## 6-k. Champion vs Challenger 4종 — **KEEP CHAMPION**

§6-g 가 이미 상한을 줬다. 잔차에 2단 HGB(31리프, 740k행, 55피처+편차항)를 학습시켜
세 폴드 모두 최적 가중 0 이었다. Challenger 가 **같은 피처를 쓴다면** 그 고유 기여는
피처의 함수이고 그건 2단 HGB 가 표현할 수 있는 형태다 — **앙상블 가치의 상한이 이미
0 으로 측정돼 있었다.** 그래도 직접 만들어 실측했다.

### 단독 성능 (rho^2, Champion 대비)

```
val    A 계층/부분풀링    B 시계감쇠      D 정규화선형   (Champ 상관)
2022   635.54 -1741.43  920.36 -1456.61  2079.76 -297.20   0.937
2023   205.07  +117.40  305.60  +217.93    71.69  -15.98   0.925
2024   174.00  -614.95  289.32  -499.63   253.64 -535.31   0.518
```

2023 에서만 A·B 가 Champion 을 넘는데, 그건 **Champion 이 그 시즌에 87.67 로
무너져 있기 때문**이지 Challenger 가 좋아서가 아니다.

`C 잠재실력` 은 별도로 짓지 않았다 — 매치업 성분은 §6-c 에서 ALS 로 대조군까지
세워 기각했고, 주효과 성분은 `A` 가 그대로 담고 있다.

### 앙상블 (가중을 이전 폴드에서만)

```
모델   WF 평균 증분   부호      2022      2023      2024
A         -12.31    1/3     +0.00     -0.00    -36.94
B         -29.41    1/3   -110.87    +43.32    -20.69
D         -17.11    0/3    -44.96     -1.53     -4.84
```

**전부 음수다.** 그리고 **폴드내 최적(오라클 가중)조차** 2022 에서 0.00, 2024 에서
A +0.00 / B +1.45 / D +2.56 다. 가중을 정답으로 골라줘도 얻을 것이 거의 없다.

`D` 는 2024 에서 Champion 과 상관이 **0.518** 로 낮은데도 앙상블 이득이 오라클
기준 +2.56, 워크포워드 −4.84 다. **다양성이 있다고 이득이 되는 것이 아니다** —
다른 방향의 오차가 신호가 아니라 잡음이면 섞어봐야 손해다.

### 결론

`MODEL STRUCTURE HEADROOM = LOW`. 구조를 바꿔서 얻을 것이 없다.

---

## 6-l. 외부 데이터 재조사 (point-in-time 관점) — **조인이 안 된다**

§4-27 은 "주최측이 `asof_*` 로 이미 준다"는 논거로 닫았다. 이번에는 다른 각도
("역사적 평균이 아니라 예측 시점의 상태 변화")로 다시 봤고, **조인 가능성**에서 막힌다.

### 규칙 원문 — 허용 범위를 확인할 수 없다

```
제공된 train.csv, 평가 환경의 test.csv, 2019~2024년 trackman_history.csv,
그리고 "대회 규칙상 허용되는 외부 데이터" 만 사용할 수 있습니다.
```

`data_description.md` 는 그 범위를 정의하지 않는다 (순환 참조). 데이콘 대회 규정
페이지를 봐야 하는데 확인할 수 없다 → **`RULES UNVERIFIED`.**

명시적으로 금지된 것은 확인된다 — **"2025년 Trackman 데이터"**. 즉 상태 변화의
가장 강한 채널(구속·회전·무브먼트 변화)은 **규칙으로 막혀 있다.**

### 조인 장벽 1 — 평가 행에 날짜가 없다

시간 정보는 `season`/`game_month`/`game_dayofweek` 뿐이다. (월, 요일, 홈, 원정)로
경기를 특정하면 2024 기준 조합당 추정 경기 수 중앙 **1.06**, 2경기 이상 모호한 조합
**5.8%** 다. 원리적으로는 ~94% 가 특정 가능하지만 **팀 ID 가 익명**이라 그 앞에
식별 문제가 하나 더 있다.

### 조인 장벽 2 — 🚩 1군 데이터가 아니다

```
시즌별 팀 수   2019:12  2020:10  2021:11  2022:11  2023:11  2024:12
game_type      R 1,314,088 (89%)    F 161,004 (11%)
```

**KBO 1군은 2015년부터 10팀 고정인데 여기는 11~12팀이다.** `game_type` 이 둘이고
팀 ID 22·23·25 가 나타났다 사라진다 — `F` 는 **퓨처스(2군)** 로 보이며 군팀이
들어가면 팀 수가 맞는다.

이것이 외부 데이터 판단을 바꾼다. 공개 기록의 커버리지·품질이 1군과 퓨처스에서
완전히 다르고, **행의 11% 는 외부 자료가 사실상 없다.** 그리고 이 혼합 구조는
연도마다 비율이 달라 — 3단 축 탐색에서 `game_type` 편차가 +30.71/+16.23/**−16.42**
로 갈렸던 것(§5-b)이 이제 설명된다.

### 판정

| | |
|---|---|
| 규칙 허용 여부 | **RULES UNVERIFIED** (규정 페이지 확인 불가) |
| 최강 채널(2025 구속·회전) | **명시적 금지** |
| 선수 식별 | 익명 ID → 해결 필요 |
| 날짜 식별 | 팀 식별에 종속, 5.8% 모호 |
| 커버리지 | 11% 가 퓨처스로 외부 자료 희박 |
| 남는 채널 | 월 단위 집계 = `asof_*` 의 열등한 복제 (상한 +9.95, 부호갈림) |

**만들지 않았다.** 식별 문제 두 개를 풀어야 시작할 수 있고, 풀어도 최강 채널은
금지돼 있으며, 남는 채널은 이미 상한이 측정돼 있다.

---

## 6-m. 🚩 대회 규정 원문 확보 — 외부 데이터 금지 확정, **아핀 상수는 회색지대**

사용자가 데이콘 규정 원문을 확인해 왔다. 두 조항이 이 프로젝트에 직접 걸린다.

### 3) 외부 데이터 사용 금지 — 축 완전 종결

> 온라인 해커톤(Phase 2)에서 제공하는 공식 데이터 외의 외부 데이터는 사용할 수 없습니다.

§4-27 · §6-l 의 `RULES UNVERIFIED` 가 해소됐다. **명시적 금지.** 논쟁 여지 없이 닫힌다.

### 4) 추론 원칙 — 우리 아핀 상수의 출처가 걸릴 수 있다

> 평가 데이터의 다른 행이나 **전체 평가 데이터의 분포를 이용해 특정 행의 예측값을
> 보정**하거나 생성하는 방식은 정상적인 추론 절차로 인정되지 않습니다.

**추론 코드는 깨끗하다** — `script.py` 는 test 의 다른 행을 읽지 않고, 상수만 적용하며,
행 독립이다. 문제는 **상수의 출처**다. 13회차부터 우리가 한 것은:

```
r = 0.4609      <- 평가 시즌의 실제 성공률을 LB 점수에서 역산 (4-24)
m = 0.473979    <- 평가셋 예측 평균을 LB 점수에서 역산 (4-25)
center = (r - A*m)/(1 - A)     <- 그 둘을 상수로 박음
```

**제출본의 `center` 안에 평가 데이터 분포의 통계량 두 개가 들어 있다.**

| 읽기 | 판정 |
|---|---|
| 좁게 (절차 기준) | 추론 코드가 준수 → 통과. LB 기반 모델 선택은 모든 대회의 표준 |
| 넓게 (효과 기준) | 평가셋 분포를 복원해 보정값으로 썼다 → 문언에 정면으로 걸림 |

우리 방식은 "LB 보고 후보 고르기"보다 **훨씬 직접적**이다. 대수적으로 분포를
복원했다. 이건 판단이 필요한 사안이지 확신할 사안이 아니다.

### 로컬 근거만으로 만들면 얼마인가 (비용 측정)

편차항(플래툰·카운트)은 `train.csv` 로만 만들고 3폴드 워크포워드 양수라 근거가
깨끗하다. 문제는 `alpha`/`center` 뿐이므로, 그것까지 로컬 워크포워드로만 정했을 때를
평가셋 모형으로 계산했다.

```
                                        alpha    center      점수
현행 제출본 (LB 역산 상수)                1.0904  0.620268   950.01
대기 후보 w058 (LB 역산 상수)             1.0893  0.622802   952.72
로컬 근거만 (alpha/w/w2 워크포워드)        1.0700  0.523766   899.16
로컬 근거만 + 편차 없음                    1.0700  0.523766   904.34
```

**LB 역산 상수가 약 +46~48 점을 만들고 있다.** 이미 제출된 950.0112 까지 소급되는
문제라 되돌릴 수 없다.

> 로컬 근거판에서 편차를 넣은 쪽(899.16)이 뺀 쪽(904.34)보다 낮은 것은, 로컬
> 워크포워드가 고른 `w`=0.595 / `w2`=0.730 이 평가셋 최적(0.20 / 0.58)을 크게
> 넘기 때문이다 — 14회차에서 배운 오버슈팅과 같은 형태다.

### 로컬 근거만의 진짜 한계선 — 899~913

"추세 외삽으로 되찾을 수 있지 않나"를 실제로 재봤다. 성공률 추세 외삽 자체는
**아주 잘 맞는다** (최근 3시즌 선형 → `r_hat`=0.4622, 실제 0.4609, 오차 +0.0013).
그런데 `center = B/(1-A)` 는 `A`≈1.07 에서 `1-A`=−0.07 로 나누므로 **오차가 15배
증폭된다.** 예측 평균 `m` 의 외삽 오차 +0.0204 가 절편 오차 −0.0205 → **169점 손실**.

안정적 파라미터화(`A·p + B` 직접)로 바꾸고 여러 규칙을 시험했다.

```
구성                                      평가셋 점수   로컬 정당화
워크포워드 그대로 (편향차감 없음)              899.16      성립
 + 과거 폴드 평균 편향 차감                   933.95      워크포워드 1/3 (실패)
 + 편향차감 + w 를 0.5배 축소                947.08      둘 다 평가셋 보고 고름
LB 역산 대기후보 w058                       952.72      평가셋 통계량 내장
```

> **🚩 947.08 은 "편법 없는 947" 이 아니다.** 6개 변형의 **평가셋 점수를 보고 고른**
> 값이고 그 선택 자체가 LB 정보다. 편향차감 규칙은 로컬 워크포워드에서
> **2022 −62.30 / 2023 −54.18 / 2024 +17.63 로 1/3** 이라 사전 정당화가 안 된다.

**사전 규칙만 지키면 899~913 이 한계선이다.** 10회차 실측 912.41(`alpha` 1.09,
로컬 근거 보유)이 그 근처인 것이 우연이 아니다. 950 과의 격차 약 40점이 LB 역산의
몫이다.

### 권고

**주최측에 문의하는 것이 가장 싸고 확실하다.** "LB 점수로 역산한 평가셋 통계량을
학습 시점 상수로 사용하는 것이 규칙 4)에 저촉되는가" 한 문장이면 된다.
Phase 3 진출 시 방법론 설명을 요구받을 수 있으므로 어차피 정리해 두어야 한다.

---

## 6-n. 🚩 17회차로 전이율을 전부 실측 — 폭 방향이 다시 열린다

### 17회차 실측 952.4231549068 (예상 952.72, 오차 −0.30)

"풀린 파라미터" 범주의 세 번째 점이다. 과거 두 건(+0.000011 / −0.117)보다 크지만
가정 전이율 범주(최대 23.3)와는 여전히 두 자릿수 차이다.

### 점수보다 곡선 위의 세 번째 점이 소득이다

16회차까지 `w2` 곡선에는 점이 하나뿐이었다. 그래서 `k2` 를 뽑으려면 `lam` 을 플래툰
축에서 **빌려와야** 했고 그렇게 나온 값이 0.9340 이다. 17회차로 두 번째 점이 생겼고,
13회차(편차 없음)를 앵커로 두면 LB 5점에서 미지수 4개가 동시에 풀린다.

`clip` 미접촉이면 적용 아핀 `(A,B)` 에 대한 오차는 항등식이다.

```
V(w1,w2) = s^2 + 2 w1 lam1 cov(pm,d1) + w1^2 lam1 var(d1)
               + 2 w2 lam2 cov(pm,d2) + w2^2 lam2 var(d2)
               + 2 w1 w2 lam_x cov(d1,d2)
C(w1,w2) = C   + w1 k1 cov(d1,y) + w2 k2 cov(d2,y)
MSE      = A^2 V - 2 A C + base + (A m + B - r)^2
```

식 4개 = 14/15/16/17 회차. `exp/solve_k2.py`.

| | 기존 | 실측 | |
|---|---|---|---|
| `k1` | 0.7685 | **0.7689** | 원장 복원 |
| `lam1` | 0.9725 | **0.9731** | 원장 복원 |
| `k2` | 0.9340 *(가정)* | **0.9651** | |
| `lam2` | 0.9725 *(빌려옴)* | **1.0422** | 2차 적률이 오히려 **팽창**한다 |

**검산이 둘 다 통과한다** — 13회차 앵커가 942.457764 를 소수점 6자리까지 재현하고,
플래툰 축이 원장 값을 복원한다. 로컬 적률 재구성이 원 분석과 같다는 뜻이다.

`k2`↑ 와 `lam2`↑ 가 상쇄돼 17회차가 예측보다 0.30 낮았다. **`w2` 축은 닫혔다** —
진짜 최적 0.545, 재최적화 이득 +0.02.

### 첫 시도는 틀렸다 — 적률과 전이율을 섞어 쓰면 안 된다

처음에는 플래툰 전이율만 원장 값으로 고정하고 로컬 적률은 새로 계산해 섞어 썼다.
15회차(`w2`=0) 재현이 −0.47 어긋났고 그 편의가 전부 `lam2` 로 흘러들어 **1.2545**
(비물리적)가 나왔다. 로컬 적률은 폴드 구성·시드·`w1` 소수점에 따라 달라진다.
**전이율은 자기 적률 위에서 다시 풀어야 한다.**

### 5-b 의 기각을 뒤집는다 — 전이율을 낮게 가정한 판정이었다

`k2`=0.9651 로 접었던 축들을 다시 쟀다 (`exp/axis_scan.py`, 현행 구성 위, 재학습 0회).

| 3단 축 (부모=플래툰) | 축소 | `w3*` | 증분 |
|---|---|---|---|
| **주자유무 (2진)** | 2000 | 0.453 | **+1.41** |
| 주자상태 (8단) | 2000 | 0.413 | +0.91 |
| 주자수 (4단) | 2000 | 0.325 | +0.68 |
| 이닝3구간 | 2000 | 0.164 | +0.19 |
| 아웃 / 초말 / 점수차3 | — | ~0 | ~0 |

**`주자상태`(8단)가 `주자유무`(2진)에 완전히 열등하다.** 잘게 쪼개면 신호는 그대로인데
분산만 는다. 5-b 가 `주자상태` −0.01 / `주자수` +0.43 을 보고 폭을 닫은 것은
**2진화를 시도하지 않았기 때문**이다.

타자 쪽 계층(5-c)은 재평가해도 여전히 최적 가중 ≈ 0 이다. 기각 유지.

### 손익분기 k 는 틀린 질문이었다

`axis_scan.py` 는 `k` 를 바꿀 때마다 `w3` 를 다시 최적화했다. 그러면 최악의 경우
`w3`=0 이 선택되므로 **증분이 절대 음수가 안 된다.** 실제 제출은 `w3` 를 하나로
고정하므로 전이가 낮으면 분산만 늘어 손해다. 16회차 표 형식이 옳다.

### 결정적 검사 — 신호가 시즌을 넘는가

모델 예측은 2024 폴드만 있어 다폴드 점수는 못 낸다. 그러나 `k` 가 곱해지는 대상인
`cov(d,y)` 는 모델 없이 폴드별로 잴 수 있다. 표를 `<f` 로 만들고 `f` 에서 잰다.

| 축 | 2021 | 2022 | 2023 | 2024 | 부호 |
|---|---|---|---|---|---|
| **주자유무** | +1.93e−6 | +4.79e−6 | +1.05e−5 | +1.10e−5 | **4/4 단조증가** |
| 대조 — 플래툰 | +1.66e−4 | +1.85e−4 | +2.16e−4 | +2.05e−4 | 4/4 |
| 대조 — 플래툰×카운트 | +2.10e−5 | +2.76e−5 | +4.05e−5 | +4.13e−5 | 4/4 |

신호는 진짜이고 크기는 카운트 축의 약 1/4 이다.

### `w3`=0.30 — 위험표로 골랐다

`k3` 는 이 축에서 미측정이고 카운트 축에서 빌려온다. **축 간 차용은 14회차(`k`=1
암묵, −23.3)와 16회차(`k2`=0.7685 차용, +1.19)에서 두 번 다 빗나갔다.**

| 진짜 `k3` | `w3`=0.15 | **0.30** | 0.45 |
|---|---|---|---|
| 0.00 | −0.62 | **−1.53** | −2.79 |
| 0.50 | +0.10 | **−0.08** | −0.61 |
| 0.7689 (2단 실측) | +0.49 | **+0.70** | +0.56 |
| 0.9651 (3단 실측) | +0.78 | **+1.27** | +1.41 |

0.45 는 상방 +0.14 를 더 얻자고 하방을 5배 키운다 — 14회차에서 데인 형태다.

### 후보 — `submissions/cand_plat_cnt_runner.zip`

```
p += 0.20   * dev(투수x타자손        | 부모=투수,   k=300)
p += 0.5470 * dev(투수x타자손x카운트   | 부모=플래툰, k=800)   <- w2 재최적화
p += 0.30   * dev(투수x타자손x주자유무 | 부모=플래툰, k=2000)  <- 신규
alpha = 1.089163   center = 0.622907     기대 953.69 (+1.27)
```

`w3` 를 넣으면 `w2` 최적점과 아핀이 함께 움직이므로 같이 다시 풀었다 —
14회차에서 아핀을 안 고쳐 2.73점 잃은 자리다. 절편 조건 `A*m+B−r` = 0.00e+00.

**조회 키** — `num_runners_on` 은 원본 컬럼(0~3)이고 모델 features 에 있다. 2진 편차를
1/2/3 에 같은 값으로 펼쳐 담아 **원본 컬럼만으로 행 독립 조회**가 된다 (16회차에서
`cnt12` 를 `balls`/`strikes` 로 되돌린 것과 같은 처리).

검증 — `verify_submit` 4단계 통과, 추론 3.5초, `clip` 접촉 0%, 범위 0.2802~0.6812.
`exp/verify_runner.py` 로 **추론 경로 == 실험 경로 최대차 0.000e+00**, 펼침 검사 통과,
보류 커버리지 80.0% (기존 두 축 80.1%/79.9% 와 일치).

### 18회차 실측 953.7373675006 (예상 953.6943, 오차 **+0.043**)

13회차 다음으로 정확하다. 이 점으로 `k3` 를 역산한다 (`exp/solve_k3.py`).

```
2단  플래툰            k1 = 0.7689
3단  플래툰x카운트      k2 = 0.9651
3단  플래툰x주자유무    k3 = 0.9800      <- 18회차로 실측
```

**두 3단 축의 차가 0.0148 이다.** 3단 중첩의 전이율은 축을 바꿔도 같다 — 깊이의
함수이지 축의 함수가 아니다. 14·16회차에서 두 번 다 빗나갔던 **축 간 차용이
처음으로 근거를 얻었다.** 새 3단 축은 이제 `k`≈0.97 로 놓고 시작할 수 있다.

### 남은 축 2진화 훑기 — 그리고 함정

실측 `k` 로 남은 축을 2진화해 다시 쟀다. 최상위가 **`2스트라이크` +3.03**, 그 다음이
**`투수유리`(S>B) +4.78** 이었다. 그런데 둘 다 `strikes_before` 의 함수라
**이미 `플래툰x카운트` 축 안에 있다.** 같은 정보를 병렬로 두 번 넣는 것이다.

```
                 상관(d2와)   w2 변화        k=0 하방
병렬 투수유리       +0.409    0.550 -> 0.325   -17.5
중첩 투수유리       -0.027    (대체)           -0.8
```

병렬은 공선성 때문에 하방이 20배다. 16회차가 "부모를 투수로 두면 플래툰과 겹친다"며
부모를 바꿔 해결한 것과 **같은 문제**다. 편차를 자기 부모에서 재면 층이 직교한다.

### 카운트 축을 중첩 2층으로 분해 — `cand_nest_adv.zip`

```
기존 (18회차)   dev(플래툰x카운트 | 부모=플래툰, k=800)            중앙 27행
신규            dev(플래툰x투수유리   | 부모=플래툰,        k=2000)  중앙 179행
              + dev(플래툰x투수유리x카운트 | 부모=플래툰x투수유리, k=800)  중앙  29행
```

셀이 27행이면 추정 잡음이 크다. 거친 층은 표본이 6배라 안정적이고, 잔 층은 자기
부모에서 재므로 직교한다 (실측 상관 **−0.0167**). 다해상도 축소다.

**🚩 `wc` 가 작으면 오히려 손해다** — `wc`=0.30 에서 손익분기 `kc`=1.05 다. 이건 항을
*추가*하는 게 아니라 `d2` 를 *대체*하는 구조라, 거친층이 원래 `d2` 가 하던 일을
떠맡을 만큼은 실려야 한다. 지금까지의 "낮게 걸어라"가 여기서는 안 통한다.

| `wc` | `kc`=0 | 0.5 | 0.769 | 0.973 | 손익분기 `kc` |
|---|---|---|---|---|---|
| 0.30 | −7.9 | −4.1 | −2.1 | −0.6 | 1.050 |
| 0.50 | −10.7 | −4.4 | −1.1 | +1.5 | 0.853 |
| **0.825** | **−16.3** | — | — | **+3.70** | **0.793** |
| 1.16 | −23.5 | −9.2 | −1.4 | +4.46 | 0.818 |

**기대 최대가 아니라 손익분기 최소로 골랐다.** `wc`=0.825 에서 손익분기 0.793 이고
실측 3단 두 값이 0.9651/0.9800 이라 여유 0.172 다. 기대 −0.76 을 내주고 `kc`=0 하방을
−23.5 → −16.3 으로 줄인다.

```
p += 0.20  * dev(플래툰            | 부모=투수,          k=300)
p += 0.825 * dev(플래툰x투수유리     | 부모=플래툰,        k=2000)
p += 0.28  * dev(플래툰x투수유리x카운트| 부모=플래툰x투수유리, k=800)
p += 0.45  * dev(플래툰x주자유무     | 부모=플래툰,        k=2000)
alpha = 1.089079   center = 0.620698     기대 957.4367 (+3.70)
```

조회 키 — `투수유리` 는 `(balls, strikes)` 로 결정되므로 두 층 모두 원본 4컬럼으로
조회된다. 2진 편차를 12칸으로 펼쳤다.

검증 — `verify_submit` 4단계 통과 (추론 3.9초, `clip` 0%, 범위 0.2762~0.6845),
`exp/verify_nest.py` 로 **추론 경로 == 실험 경로 최대차 0.000e+00**, 펼침 검사
30,090쌍 불일치 0, 보류 커버리지 네 축 모두 79.9~80.1%.

### 🚩 19회차 실측 955.2193198652 (예상 957.4367, 오차 **−2.22**)

개선은 했다 (+1.48). 그러나 **오차가 "풀린 파라미터" 범주를 벗어났다** — 그 범주의
과거 최대는 −0.30 이다. 이 회차는 빌려온 전이율이 **둘**이었고(`k_c` 거친축 3단,
`k_n` 4단, 둘 다 0.9726 차용) 그 차용이 빗나갔다.

**18회차 결론의 적용 범위가 좁혀진다.** "3단 전이율은 축에 무관한 상수(0.9651 /
0.9800)" 는 카운트·주자유무 두 축에서 성립했지만, **`투수유리` 라는 새 축 종류에는
그대로 안 옮겨갔다.** 차용은 여전히 축마다 확인이 필요하다 — 14·16회차의 교훈이
완화됐을 뿐 없어지지 않았다.

관측이 하나라 `k_c` 와 `k_n` 을 동시에 못 푼다. 일관 궤적만 나오며, 분리하려면
**한 층만 바꾼 점이 하나 더** 필요하다 (`exp/solve_k4.py` 가 궤적을 낸다).

남은 슬롯 2장.

---

## 6-o. TrackMan 행 단위 조인 재감사 — **불가.** 단, 4-4 의 투수 대응표는 진짜다

실제 파일에서 처음부터 다시 쟀다 (`exp/tm_audit1~3.py`, `exp/tm_hand.py`).
README 4-4 가 "조인 불가" 결론을 한 번 뒤집었으므로 같은 실수를 피하려 **매핑과
행 단위 조인을 분리해서** 판정한다. 둘은 다른 질문이다.

### 4-4 의 투수 대응표는 독립 검증을 통과한다

`pitcher_id_map.csv` 592/792명(74.7%), conf 중앙 1.000, 중복 배정 0.
**매핑 제작에 쓰이지 않은 신호로** 검증했다.

| 검증 | 결과 |
|---|---|
| 투수 손 일치 (`1`=Left / `2`=Right) | **99.49%** (뒤집으면 0.51% — 인코딩 방향까지 확정) |
| 활동 시즌 집합 자카드 | 중앙 **1.000**, 완전일치 67.7% |
| 투구 수 상관 | 피어슨 **0.9709**, 스피어만 0.9515 |

**EXACT ID MAPPING = YES (투수).** 다만 **타자 대응표는 존재하지 않는다.**

### 그런데 행 단위 조인은 안 된다 — 네 겹으로 막힌다

**(1) 열쇠가 없다.** train 에 `game_date` 도 `game_id` 도 없다. 시간 해상도는
`(season, game_month, game_dayofweek)` 이고 그 한 칸에 **서로 다른 경기가 중앙
22개**(최대 62), 서로 다른 날짜가 평균 3.6개 들어간다.

**(2) 합성키가 원리적으로 유일하지 않다.** ID 매핑이 완벽하다고 **가정해도**:

```
TrackMan(1군) 안에서 season+inning+TB+B+S+O 에
  + 투수                      유일행  11.3%
  + 투수 + 타자                유일행  84.0%   <- 최대 충돌 11
  + 투수 + 타자 + 양팀          유일행  84.0%
게임ID + 투구번호               유일행 100.0%   <- train 에 둘 다 없다
```

`game_date` 를 안다고 가정해도 87.8% 다. **목표 95% 에 원리적으로 못 닿는다.**
train 쪽도 같은 키로 84.5% 만 유일하다.

**(3) 실제로 해보면 10.4% 다.** 기존 매핑을 적용한 전체 train 대비:

```
유일 대응    10.4%      <- 목표 95%
다대다       74.9%      최대 충돌 58
키 부재      12.2%
투수 미매핑   2.5%
```

**(4) 물량이 애초에 모자란다.** TrackMan 은 2군(`MIN_*` 576,848행)과
`KBO_*`/`ACE_MEX`(26,830행)를 포함한다. 1군만 남기면 **1,189,400행 < train
1,475,092행** 으로, train 의 19.4% 는 대응할 TrackMan 행이 **존재할 수 없다.**
게다가 train 은 `game_type` 이 `R`/`F` 로 나뉘는데 TrackMan 에는 그 컬럼이 없어
필터를 맞출 수도 없다.

### 결정타 — 평가셋은 2025 이고 TrackMan 은 2024 에서 끝난다

```
TrackMan 시즌 범위  2019 ~ 2024
test 시즌           2025
```

train 쪽 조인이 완벽하게 됐다 해도 **예측 대상 행에는 TrackMan 행이 하나도 없다.**
따라서 행 단위 TrackMan 피처는 원리적으로 추론에 못 쓴다. 4-4 가 이미
"2025 예측에 쓸 수 있는 trackman 은 2024 가 최신"이라고 적어둔 것과 같은 벽이다.

**TIMESTAMP RECOVERABLE = NO → AS-OF VALID = NO.** as-of TrackMan 피처 생성 금지.

### 그래서 지금 방식이 유일하게 성립하는 방식이다

현행 모델은 TrackMan 을 **이미 쓰고 있다** — `tmc_*`/`tmh_*` 8개(4-9, LB +10.98).
이것들은 행 단위 조인이 아니라 **투수 x 상황 조건부 집계를 학습 시점에 표로 굳혀
`pitcher_id` 로 조회**하는 형태다. 행 단위 조인도, 추론 시점의 TrackMan 파일도
필요 없다. 4-4(정적 프로필 −5.95) · 4-7(변화량 −29.04) · 4-10(물리량 6개 기각)에서
나머지 갈래는 전부 닫혔다.

**결론 — TrackMan 축에서 새로 만들 것은 없다.** 남은 여지는 §6-n 의 편차 계열이다.

---

## 6-p. FM / DeepFM 직접 측정 — §6-c 의 빈칸을 메운다. **KEEP CHAMPION**

§6-c 는 ALS 로 논증만 했고 FM 을 돌린 적이 없다. 직접 쟀다 (`exp/fm_probe2.py`).
FM 은 numpy 구현 — 환경에 torch/xlearn/fastFM 이 없고 피처공간이 작아(~1,600)
밀집 Adam 으로 충분하다.

### 🚩 1차 실행은 무효였다 — 그리고 원인이 이 프로젝트 전체에 걸린다

1차는 세 폴드 모두 `ep=0~1` 에서 멈췄고 2024 Brier 0.2552 가 **상수 예측(0.2498)보다
나빴다.** 대조군으로 가렸다 (`exp/fm_diag.py`) — sklearn `LogisticRegression` 이
**똑같이** `rho^2`≈2 로 실패했다. 구현이 아니라 **규제 강도**다.

피처에 정확한 공선성이 있다.

```
asof_pitcher_n == asof_pitcher_pitchmix_n          (rho^2 둘 다 64.72)
home_win_expectancy + away_win_expectancy = 100
run_total_before = run_top_before + run_bot_before
num_runners_on   = runner_on_1b + 2b + 3b
```

무규제 최소제곱은 특이행렬에서 수치 쓰레기를 낸다 (`exp/fm_diag2.py`).

| 정직한 선형 2019~2023 -> 2024 | `rho^2` |
|---|---|
| ridge `lam`=0 | **0.01** |
| ridge `lam`=100 | 296 |
| ridge `lam`=1e6 | **340** |

> **트리는 공선성에 면역이라 이 함정이 여태 안 드러났다.** 선형·잠재 계열을 쓸 때는
> 규제를 먼저 맞춰야 하고, 안 맞추면 "그 계열이 약하다" 는 **거짓 결론**이 나온다.

### 정직한 기준선 (2024)

```
단일 최강 피처 asof_pitcher_success_rate   rho^2  332   (상관 +0.0576)
규제 선형 55피처                            rho^2  340
투수 평균 성공률만                           rho^2   55
CatBoost                                  rho^2  780
Champion (편차항 포함)                      rho^2  798
```

### 결과 — 3폴드 시간순 OOF

| fold | Brier Ch | Brier FM | dBrier | `rho^2` Ch | `rho^2` FM | corr | 잔차상관 | 혼합 |
|---|---|---|---|---|---|---|---|---|
| 2022 | 0.243233 | 0.245695 | +0.002462 | 2387.1 | 1444.6 | 0.807 | −0.009 | +0.00 |
| 2023 | 0.253627 | 0.251923 | −0.001703 | 87.8 | 95.8 | 0.862 | −0.095 | +10.99 |
| 2024 | 0.247856 | 0.249638 | +0.001782 | 798.1 | 229.3 | 0.499 | +0.000 | +1.44 |

```
MEAN dBrier +0.000847   WORST +0.002462
MEAN d rho^2  -501.1    WORST -942.5
```

### 🚩 결정적 — 잠재 상호작용부가 **해롭다**

세 폴드 모두 `d=0`(순수 선형)이 `d>0`(잠재 인수분해)보다 낫다.

```
        d=0      d=4      d=8     d=16
2022   1628     1445     1443     1440
2023    107       96      104      105
2024    264      233      230      229
```

**FM 의 존재 이유가 되는 부분이 3/3 폴드에서 음수 기여다.** §6-c 가 ALS 로
논증했던 것("표현할 대상이 시즌을 안 넘는다")의 **직접 측정**이다. 이제 §6-c 의
확장 논증은 근거를 갖는다.

### 게이트 기준 정정

`d=0` 이 `rho^2` 200 을 넘어야 한다는 절대 게이트를 걸었는데 **2023 에서 부적절했다** —
그 폴드는 **Champion 자신이 87.8** 이라 어떤 모델도 200 에 못 닿는다. 2023 은 FM 실패가
아니라 **폴드 자체가 퇴화**한 것이다 (그 폴드에서는 FM 95.8 > Champion 87.8). 게이트는
절대값이 아니라 Champion 대비로 걸었어야 했다.

### 채택 기준 대조 (사용자 정의)

| 기준 | 요구 | 실측 | |
|---|---|---|---|
| 평균 OOF 개선 | > +2 `rho^2` | **−501.1** | ❌ |
| 개선 시즌 | 2/3 이상 | **1/3** | ❌ |
| worst 파국 없음 | — | **−942.5** | ❌ |
| Champion 상관 | < 0.95 (강한 후보) | 0.50~0.86 | ⭕ |
| 잔차 보완 | 명확 | **−0.095 ~ +0.000** | ❌ |

혼합 이득 평균 +4.14 는 **대상 폴드에서 가중을 고른 낙관 상한**이고, 그 대부분(+10.99)이
Champion 이 87.8 인 퇴화 폴드에서 `w`=0.60 으로 나왔다. 신뢰할 수 없다.

### DeepFM — **SKIPPED**

§6 의 실행 조건 둘 다 불충족이다. ① 평균 OOF 개선 없음(−501). ② 상관은 낮지만
잔차 보완이 없다(−0.095 ~ +0.000). 조건부 실행 규약대로 만들지 않는다.

> **DECISION = KEEP CHAMPION.** 그리고 이 결과는 §6-g(잔차 상한 0)와 독립적으로
> 같은 곳을 가리킨다 — 잔차에 남은 것이 없다.

---

## 6-q. 19회차 궤적 · 5번째 항 — 편차 계열 **종결**. 재학습 0회, LB 0장

### 전이율은 궤적으로만 잡힌다 (`exp/solve_k4.py`)

19회차는 빌려온 전이율이 둘(`k_c` 거친축 3단, `k_n` 카운트\|거친 4단)이고 관측이
하나라 동시에 못 푼다. 일관 궤적 위의 두 읽기가 **정반대 처방**을 낸다.

| 읽기 | `k_c` | `k_n` | 재최적화 `w` | 이득 |
|---|---|---|---|---|
| (a) 4단이 3단과 같다 | **0.8651** | 0.9726 *(가정)* | (0.20, 0.975, 0.275, 0.45) | +0.13 |
| (b) 거친축이 3단 실측대로 | 0.9726 *(가정)* | **0.5582** | (0.20, 1.150, 0.050, 0.45) | +1.68 |

(b) 의 기대 이득이 크다고 (b) 를 고르면 그게 14회차다. 근거는 위험표여야 한다.

### 궤적 전체의 위험표 — 재최적화의 몫은 **+0.10** (`exp/robust_w.py`)

`alpha`/`center` 도 가정한 `k` 에서 유도되므로 틀리면 그 비용까지 같이 문다.

| 구성 | `w` | 최악 | 최선 | vs 19회차 |
|---|---|---|---|---|
| 19회차 그대로 | (0.200, 0.825, 0.280, 0.450) | 955.219 | 955.219 | — |
| (a) 읽기 최적 | (0.200, 0.975, 0.275, 0.450) | 955.161 | 955.854 | −0.058 |
| (b) 읽기 최적 | (0.200, 1.150, 0.050, 0.450) | **953.062** | 957.280 | **−2.157** |
| `wn`=0 (식별 실험) | (0.200, 0.870, 0.000, 0.450) | 953.011 | 956.680 | −2.208 |
| **궤적 minimax** | (0.200, 0.920, 0.310, 0.450) | **955.315** | 955.343 | **+0.096** |

> **궤적 어디에 진짜 값이 있든 점수가 안 움직인다** — minimax 후보의 최악~최선
> 폭이 **0.03점**이다. 19회차 구성은 이미 사실상 최적이고, 재최적화로 살 수 있는
> 것은 +0.10 이다. 슬롯 값을 못 한다.

`wn`=0 은 4단 항을 빼 관측을 `k_c` 만의 함수로 만드는 **식별 실험**이 되지만
최악 −2.21 이다. 식별값을 사기에 너무 비싸다.

### 5번째 항 — 후보 22종 스캔 (`exp/axis_scan5.py`)

두 읽기가 **소수 3자리까지 같은 답**을 냈다. 새 항 평가는 `(k_c, k_n)` 모호성에
영향받지 않는다.

| 후보 | 깊이 | 증분 | 손익분기 k |
|---|---|---|---|
| 카운트 \| 플래툰x주자 | 4 | +1.32 | 0.680 |
| 초말 \| 플래툰x거친x카운트 | 5 | +1.01 | **0.059** |
| 1루 \| 플래툰 | 3 | +0.75 | 0.396 |
| 동점 \| 플래툰 | 3 | +0.65 | 0.538 |

### 🚩 그런데 손익분기 k 는 §6-n 이 이미 폐기한 질문이었다

`exp/risk_table.py` 헤더에 적혀 있는 함정을 그대로 밟았다 — `k` 마다 `w5` 를
재최적화하면 최악의 경우 `w5`=0 이 뽑혀 **증분이 절대 음수가 안 된다.** 실제
제출은 `w5` 를 고정하므로 하방이 생긴다. 손익분기 0.059 는 "하방이 없다"가
아니라 **질문이 틀렸다**는 뜻이다.

### 결정적 검사 — 신호가 시즌을 넘는가 (`exp/risk5.py`)

표는 `<f` 로 만들고 `f` 에서 `cov(d5, y)` 를 잰다. 18회차를 통과시킨 검사다.

| 후보 | 2021 | 2022 | 2023 | 2024 | 부호 |
|---|---|---|---|---|---|
| 초말 \| 플래툰x거친x카운트 | +1.05e−6 | +1.26e−7 | **−5.40e−7** | +1.37e−6 | 3/4 |
| **카운트 \| 플래툰x주자** | +5.55e−6 | +9.27e−6 | +1.54e−5 | +1.54e−5 | **4/4 단조** |
| *(대조)* 플래툰x주자유무 | +1.93e−6 | +4.79e−6 | +1.05e−5 | +1.10e−5 | 4/4 |

**초말은 기각.** 손익분기가 낮아 매력적으로 보였으나 2023 폴드가 음수다.

유일하게 통과한 카운트축은 신호가 주자유무보다 **크다**(1.54e−5 vs 1.10e−5).
그런데 카운트가 이미 `dN` 으로 모델에 들어가 있어 겹치고, 고정 `w5` 위험표가
그 대가를 보여준다.

```
w5=0.20   k5=0.00 -> -1.93   k5=0.50 -> -0.59   k5=0.7689 -> +0.13   k5=0.9651 -> +0.66
```

`k5` 를 [0.5, 1.2] 로 잡으면 **어떤 `w5`>0 에서도 최악값이 음수** — minimax 해가
`w5`=0 이다.

> **DEVIATION SERIES = CLOSED.** 깊이(§5)·폭(§6-n)·중첩(19회차)·5번째 항까지
> 전부 상한이 측정됐다. 남은 것은 재최적화 +0.10 뿐이고 그것도 슬롯 값을 못 한다.

### 부수 확인 — "노이즈로 버린 개선"은 없다

폴드 진폭이 크니(Champion `rho^2` 가 2022 2387 / 2023 88 / 2024 798) 게이트가
과하게 엄격해 진짜 개선을 버렸을 수 있다는 가설을 세웠다. **틀렸다.**
`experiment_log.csv` 67건 중 "평균 양수인데 부호갈림으로 기각"은 **4건, 합계
19.78** 이고 그 폴드 패턴은 애매한 게 아니라 진짜로 갈린다
(`exp002 2021:−6.3 2022:−6.7 2024:+32.9`). 게이트는 옳게 작동했다.

---

## 7. 1100 에 대하여 — 산술

```
점수 = 1e5 * rho^2
1100 -> rho 0.10488   950.01 대비 +7.62%
편차 계열이 실현한 것 = 942.4578 -> 950.0112,  rho +0.400%   (+ 대기 중 952.72)
=> 1100 = 지금까지 찾은 것 전부의 19배
```

편차 계열은 **깊이 3단에서 바닥**이고(§5) 폭으로도 후보가 말랐다 — 교차적합
오라클(§1)에서 플래툰 다음은 `pitcher x batter` 인데 셀이 10행이라 편차형으로
못 만든다. 그리고 새 계열마다 전이율을 재려면 LB 슬롯이 1~2장씩 든다.

**현실적 상한은 952~953** 이다. 이 세션에서 942.46 → 950.01 (+7.55) 을 올렸고
+2.71 이 더 대기 중이다.

---

## 8. 🚩 축을 잘못 팠다 — `rho` 는 아핀 불변이고, 우리는 11장을 아핀에 썼다

### 문제의 재정의

```
점수 = 1e5 * rho^2      rho = corr(예측, y)  <- 아핀 불변
```

13회차가 942.457764 를 **소수점 6자리까지** 재현한 그 아핀 최적화는 `rho` 를
**1 도 못 움직인다.** 그것은 주어진 `rho` 에서 천장에 닿는 작업이었고, 실제로
닿았다. 그 뒤 14~19회차의 편차 계열은 `rho` 를 +0.67% 올렸다.

    942.4578 -> 955.2193   =  rho +0.67%   (제출 6장)
    955.2193 -> 1120       =  rho +8.3%    =  그 12배

**LB 역산으로 번 40점과 1120 은 직교한 축이다.** 우리는 제출 11장을 전부 앞쪽
축에 썼고, 그 축은 §6-q 에서 종결됐다.

### 혼합 산술 — 필요 조건을 정확히 계산한다 (`exp/blend_spec.py`)

두 예측을 섞으면 표준화 단위에서

```
rho_blend(w) = (w*r1 + (1-w)*r2) / sqrt(w^2 + (1-w)^2 + 2w(1-w)c)
```

동급(`r2=r1`)이면 점수 배수가 `2/(1+c)` 다.

| 상관 `c` | 0.99 | 0.95 | 0.90 | 0.85 | 0.80 | **0.706** |
|---|---|---|---|---|---|---|
| 도달 점수 | 960.0 | 979.7 | 1005.5 | 1032.7 | 1061.4 | **1120** |

### 🚩 그리고 깨끗한 부등식 하나 — **`세기비 > 상관`**

`w`→0 에서 한계이득이 `r2 − c·r1` 이므로, 세기비가 상관보다 낮으면 **최적 혼합
가중이 정확히 0** 이다. 아무 일도 일어나지 않는다.

이것이 §6-k(Challenger 4종 전부 음수)와 §6-p(FM 혼합 가치 0)의 정체다 —
도전자가 나빴던 게 아니라 이 부등식을 못 넘었을 뿐이다.

| 도전자 | 세기비 | 상관 | 도달 |
|---|---|---|---|
| LightGBM | 1.000 | **0.994** | 958.1 (+2.9) |
| FM (2024 폴드) | **0.536** | 0.499 | 957.0 (+1.7) |
| FM (2022 폴드) | 0.778 | 0.807 | 955.2 (**0.0**) |

**세기와 다양성을 동시에 가진 모델을 한 번도 안 만들었다.**

### 1차 측정 — MLP (`exp/mlp_probe.py`, 2024 폴드)

GBDT 와 다른 귀납편향 셋을 의도적으로 넣었다. ① ID 임베딩 (CatBoost 는
`pitcher_id`/`batter_id` 를 정수로 넣고 CTR 은 §EXP017 에서 −31.17 로 기각),
② 주기 임베딩 PLR (연속 피처를 스칼라가 아니라 벡터로 — 축평행 분할이 원리적으로
못 만드는 표현), ③ 전역 상호작용.

```
CatBoost rho^2  780.2      MLP rho^2  430.4      세기비 0.743
상관 c = 0.6917
혼합 최적 w(cat)=0.905  ->  rho^2 784.1  ->  환산 960.0
```

> **상관 0.6917 은 1120 이 요구하는 0.706 을 이미 통과했다.** 막힌 것은 세기
> 하나뿐이다. 55피처 안에서도 GBDT 와 다른 표현이 존재한다는 직접 증거다.

### 세기가 안 나온 이유는 곡선에 그대로 있다

```
ep1 348.5   ep2 430.4 (최고)   ep5 312.3   ep11 175.7   ep15 138.8
학습 loss 는 0.694 -> 0.653 으로 계속 하락
```

**15에폭 중 2에폭이 최고** = 규제가 전혀 안 맞은 무튜닝 첫 시도다. 범인 후보는
ID 임베딩(투수 792 / 타자 830 을 32차원)이다. 세기비 목표별 도달점:

```
세기비 0.90, c=0.70 -> 1030.1      세기비 0.95 -> 1072.3      세기비 1.00 -> 1123.8
```

### 남은 위험 — 새 모델은 아핀을 리셋한다

`alpha`/`center` 는 예측의 **평가셋 적률**에서 나오는데 새 모델의 그것을 모른다.
로컬 2024 로 추정하면:

```
예측 평균     로컬 0.494858   평가셋(LB역산) 0.473979   오차 +0.020879
예측 표준편차  로컬 0.042735   평가셋(LB역산) 0.043800   오차 -0.001065
```

평균 오차 +0.0209 가 `center = B/(1-A)` 의 `1-A`≈−0.07 로 15배 증폭돼 **169점
손실**이다 (STATUS §3 의 그 숫자).

**우회로** — MLP 를 로컬에서 챔피언의 평균·분산에 맞춰 표준화한 뒤 섞으면 혼합의
평가셋 평균이 챔피언의 그것(0.473979, LB 로 이미 실측)과 거의 같아진다. 두 모델의
로컬→평가 drift 는 같은 인구 변화에서 오므로 **차이분만 남는다.** 슬롯이 2장뿐이라
아핀을 다시 풀 여유가 없으므로 이 경로를 쓴다.

### TabPFN 계열 — 매력적이지만 지금은 못 잰다

메커니즘이 근본적으로 달라 상관이 더 낮게 나올 여지가 있고, 그 구간이 정확히 돈이
되는 구간이다 (세기비 0.90 × c=0.60 → 1089.5). 그러나 **TabPFN v2 는 학습 상한이
10,000행**으로 1,221,585행의 0.82% 만 쓴다 — `rho`=0.098 짜리 약한 신호에서
데이터 99.2% 를 버리면 세기비가 무너진다. 대규모용 변형(TabDPT / TabICLv2)이
정답이나 실무 장벽이 넷이다.

| | 상태 |
|---|---|
| 가중치 라이선스 | 규정 1) 충족 여부 미확인 |
| 오프라인 번들 | 가중치를 zip 에 넣고 런타임 다운로드 차단 — 가능 |
| 추론 예산 | 245,789행 / 600초. 컨텍스트 앙상블 다회는 초과 |
| 🚩 로컬 검증 | 로컬 GPU 가 **GT 1030 (2GB)**. 세기비·상관을 로컬에서 못 잰다 |

마지막이 결정적이다. 로컬에서 못 재면 **제출 슬롯으로 재야 하는데 2장뿐**이고,
이는 19회 동안 지켜온 "슬롯은 비싼 측정 장비다"에 정면으로 어긋난다.
**MLP 로 `세기비 > 상관` 이 가능함을 먼저 확인**하는 것이 순서다 (CPU 재현 가능).

---

## 9. 혼합 축 종결 + 편차 in-model 재파라미터화 — **둘 다 기각**

### 9-a. MLP 15설정 — `세기비 > 상관` 을 못 넘는다

§8 이 낸 조건은 `세기비 > 상관` 이고 1120 은 `세기비 1.00 + c <= 0.706` 이다.
1차 탐침이 `세기비 0.743 / c 0.6917` 을 냈으므로 **상관 조건은 이미 통과**였고,
막힌 것은 세기였다. 규제 9설정(`exp/mlp_sweep.py`)과 목적함수 6설정
(`exp/mlp_corr.py`)으로 세기를 올려봤다.

| 설정 | `rho^2` | 세기비 | 상관 c | **비/상관** | 환산 |
|---|---|---|---|---|---|
| BCE 기준 (무튜닝) | 430.4 | 0.743 | 0.6917 | **1.074** | **960.0** |
| corr b32768 | 528.7 | 0.823 | 0.7872 | 1.046 | 958.5 |
| ID축소+drop0.3 | 477.4 | 0.782 | 0.7565 | 1.034 | 956.7 |
| mix lam=4 | 504.5 | 0.804 | 0.7736 | 1.039 | 957.4 |
| ID축소 8x | 435.3 | 0.747 | 0.7409 | 1.008 | 955.3 |

**15개 설정 전부 955.3~960.0 이고 `비/상관` 이 1.008~1.074 를 못 벗어난다.**
튜닝한 14개가 무튜닝 원본보다 나쁘다.

🚩 **상관 최대화 손실은 이론대로 작동했다** — `rho^2` 를 430.4 -> 528.7 로
**+23%** 올렸다. 점수가 `1e5*corr^2` 이고 `corr` 이 아핀 불변이므로 Logloss 가
보정에 쓰던 용량이 판별력으로 간 것이다. **그런데 상관이 정확히 같은 비율로
따라 올랐다** (0.6917 -> 0.7872).

> **구조적 결론 — 55피처 안에 신호가 사실상 하나다.** 규제를 바꾸든 목적함수를
> 바꾸든 두 모델이 같은 곳으로 수렴한다. 낮은 상관은 다양성이 아니라 **잡음**이고,
> 잡음을 줄이면 세기와 상관이 함께 오른다. §6-k(Challenger 4종)와 §6-p(FM)가
> 0 이었던 이유가 이것이며, 도전자가 나빴던 것이 아니다.
>
> 교과서 형태로는 `02` 덱 p37 의 배깅 분산식과 같다 —
> `Var = (1/m)(1-rho)s^2 + rho*s^2`. **상관이 앙상블 이득의 상한을 정한다.**

### 9-b. 편차를 후처리 스칼라에서 **모델 피처**로 (`exp/dev_infeat.py`)

편차 신호는 LB 로 진짜임이 증명됐는데(+12.76) 모델에게 피처로 준 적이 없었다.
`w` 가 상수 4개라 카운트·상황별로 신뢰도를 달리 못 준다. 누수는 as-of 로 막았다
(시즌 `s` 행의 편차는 시즌 `<s` 로만 — 배포 구조와 동일).

폴드 규모가 31배 다르므로(2022 `rho^2` 2371 vs 2023 76.5) **대조 대비 배수**로 본다.

| 폴드 | +편차4 | +편차4+logn | 대조+후처리 | 편차4+후처리 |
|---|---|---|---|---|
| 2022 | 1.0064 | 1.0059 | **1.0078** | 1.0044 |
| 2023 *(퇴화)* | 1.1778 | 1.1686 | 1.1412 | **1.3111** |
| 2024 | 1.0351 | **1.0499** | 1.0331 | 1.0359 |

**셀 관측수 `log(n)` 은 기각 — 1/3.** 2024 에서만 +1.43% 이고 2022 −0.05% /
2023 −0.78% 다. "축소 상수 대신 신뢰도 곡선을 학습시킨다"는 발상은 매력적이었으나
**2024 고유 현상**이었다. 14회차(로컬 최적 `w` 의 비전이)와 같은 형태다.

**in-model 대 후처리는 무승부.** 건강한 두 폴드에서 차이가 −0.14%(2022) /
+0.21%(2024) 뿐이다. 기하평균은 in-model 이 앞서나(1.0706 vs 1.0591) 그 차이가
전부 2023 에서 나오는데, 그 폴드는 Champion 이 76.5 인 퇴화 폴드라 비율 변동을
신뢰할 수 없다 (§6-p 에서 같은 이유로 게이트를 정정한 바 있다).

🚩 **결정적 대조 — in-model 판 위에 후처리를 또 얹으면 증분이 0 이다.**

```
대조 prod55    후처리 전 771.3  후 796.8   +25.5     <- 후처리가 하는 일
+편차4         후처리 전 798.4  후 799.0    +0.7     <- 이미 먹음
+편차4+logn4   후처리 전 809.8  후 809.8    -0.0     <- 완전히 먹음
```

같은 신호이고, 투입 경로만 다르며, **총량이 같다.**

> **DECISION = KEEP CHAMPION.** 편차 신호는 회수될 만큼 회수됐다.

### 9-c. 강의자료 11종 전수 조사 — 실전 아이디어 2개, 둘 다 측정 완료

| 자료 | 판정 |
|---|---|
| `01` 개론 · `02` 고전ML · 『지도학습』(156p) | 기초. 없음 |
| `03` 딥 아키텍처 (48p) | PLR + ID 임베딩 -> **구현·측정 (9-a)** |
| `04` 표현학습 (38p) | SSL/few-shot. 라벨 147만 개라 무관. 평가셋 SSL 은 규정 4) 위반 |
| `05` LLM×정형 (49p) | **CAAFE(의미 기반 피처 생성)** — 유일한 미착수 항목 |
| `06` TabPFN (41p) | 학습 상한 10,000행 (필요 1,221,585). 로컬 GPU 가 GT 1030(2GB)이라 세기비·상관을 **로컬에서 못 잰다** -> 슬롯으로 재야 하는데 2장뿐 |
| `Opt & DFL` (71p) | 볼록최적화 + 포트폴리오 DFL. 하류 결정문제가 없어 직접 적용 불가. 원리 하나 추출 -> **상관 손실, 측정 완료 (9-a)** |
| `Time Series` (87p) | 행 독립이라 시퀀스 모형이 붙을 자리 없음 |
| NLP/LLM Agent (204p) · LLM App&Eval (196p) | 토큰화·RLHF·ReAct·MCP·GPU. 없음 |

**남은 미착수 항목은 CAAFE 식 의미 기반 피처 발굴 하나뿐이다.**

### 9-d. "게이트가 과했나" — 측정했고, 아니다

폴드 진폭이 크니(2022 2371 / 2023 76.5 / 2024 771) 게이트가 진짜 개선을 버렸을 수
있다는 가설을 세웠다. `experiment_log.csv` 67건 중 "평균 양수인데 부호갈림 기각"은
**4건, 합계 19.78** 이고 폴드 패턴이 진짜로 갈린다 (`exp002 2021:-6.3 2022:-6.7
2024:+32.9`). **게이트가 막은 몫은 20점이고 1120 과의 격차는 165점이다.** 원인이 아니다.

진짜 과잉 제약은 다른 데 있었다 — **모델을 8월 11일(9회차)에 얼려놓고 제출 11장을
전부 후처리에 썼다.** 다만 9-b 가 보여주듯 그 후처리는 in-model 과 총량이 같으므로,
순서를 바꿨어도 점수는 같았을 것이다.

---

## 10. TabPFN — 부분표본으로 측정, **기각**. 마지막 모델 계열이 닫혔다

§9-c 에서 "로컬 GPU 가 GT 1030(2GB)이라 못 잰다"고 접었던 것은 **틀린 판단이었다.**
판정에 필요한 것은 세기비와 상관 두 숫자뿐이고 둘 다 상관계수라, `rho`~0.09 에서
`n`=6,000~10,000 이면 SE 0.010~0.013 으로 충분하다. 25만 행이 필요 없었다.

### 환경 · 라이선스

`tabpfn` **8.3.0 은 가중치 다운로드에 브라우저 로그인 + 라이선스 동의를 요구**한다
(`browser_auth.ensure_license_accepted`). 규정 1)의 "공식적으로 **누구에게나**
가중치가 공개"와 부딪힐 소지가 있다. 강의 노트북(`00_Hands_on_Tabular_ML.ipynb`)이
지정한 **2.0.9** 는 게이트 없이 받아진다. 채택까지 갔다면 라이선스 원문 확인이
별도로 필요했다.

로컬 RAM 15.7GB(가용 10.6GB)에서 컨텍스트 상한은 **7,000** 이다 —
`ctx^2 * 24 * 4B` 가 그 위에서 10.6GB 를 넘는다 (ctx=10,000 -> 9.6GB 요구, OOM).

### 🚩 1차 숫자에 속을 뻔했다 — 질의 2,000행은 못 쓴다

| | ctx=3000 | ctx=5000 | **ctx=7000** |
|---|---|---|---|
| `rho^2` (q=2000) | 26.5 | 56.9 | **465.1** |

ctx=7000 의 465.1 은 세기비 0.750 으로 **필요치 0.8 에 거의 닿는 값**이었다.
그러나 `q`=2000 은 `SE(corr)`=0.022 인데 `rho`=0.068 이라 95% 구간이 `rho^2`
기준 [64, 1235] 다. 그리고 5000 -> 7000 의 8배는 매끄러운 곡선이 아니다.

### 재측정 — 질의를 늘리자 무너졌다 (`exp/tabpfn_probe2.py`)

| `q` | CatBoost | TabPFN | 세기비 | 상관 `c` | 비/상관 | 환산 |
|---|---|---|---|---|---|---|
| 2,000 | 1569.0 | 23.4 | 0.122 | 0.3040 | 0.402 | 955.2 |
| 4,000 | 954.2 | 6.5 | 0.082 | 0.2486 | 0.331 | 955.2 |
| **6,000** | **829.7** | **49.5** | **0.244** | **0.2074** | **1.178** | **956.6** |

**465.1 은 운 좋은 2,000행 표본이었다.** CatBoost 자신도 같은 표본에서
1569 -> 954 -> 830 으로 흔들린다 — `q`=2000 추정이 얼마나 못 믿을 것인지가
대조군에 그대로 나온다. `q`=6000 의 829.7 이 1만 표본값 826.9 와 일치하므로
이제부터가 신뢰 구간이다.

### 결론 — 다양성은 있는데 세기가 없다

**상관 0.2074 는 오늘 측정한 것 중 압도적으로 낮다.** "메커니즘이 근본적으로
다르면 상관이 낮을 것"이라는 가설은 **맞았다.** `비/상관` 1.178 도 오늘 최고이고
부등식(`세기비 > 상관`)도 통과한다. 그런데 세기비가 0.244(필요 0.8)라 이득이
**+1.4** 에 그친다.

§6-p 의 FM(세기비 0.536 / 상관 0.499 -> +1.7)과 **정확히 같은 자리**다.
122만 행 중 7천 행(0.57%)으로는 `rho`=0.098 짜리 약한 신호를 못 잡는다.
컨텍스트 앙상블로 세기를 올리는 길이 남으나 ① 두 번째 컨텍스트가 이미 OOM
(10.58GB 요구)이고 ② §9-a 가 확정한 "잡음을 줄이면 세기와 상관이 함께 오른다"에
걸린다.

> **MODEL FAMILY = EXHAUSTED.** GBDT 3종 · HGB · RF · 선형/규제 · FM/DeepFM ·
> MLP(PLR+ID 임베딩, 15설정) · TabPFN 까지 전부 측정됐다. 남은 미착수 축은
> **CAAFE 식 의미 기반 피처 발굴 하나뿐이다.**

### 10-b. 🚩 세기를 늘리는 로직은 있다 — 그런데 추론 예산이 먼저 막는다

"세기가 없으면 늘리면 되지 않나"가 옳은 지적이라 상한을 계산했다.

`p = S + N` (S = 체계적 부분, N = 컨텍스트 표본 잡음)에서 M개 컨텍스트를 평균하면
N 만 `1/M` 로 준다. 세기비와 상관이 **같은 인자로** 곱해지므로 `비/상관` 은 불변
(1.178)이지만, 둘이 함께 커지면 혼합 이득은 커진다. 상한을 정하는 것은
`lam = corr(p_i, p_j)` 하나다.

| `lam` | M=1 | M=4 | M=16 | M=inf |
|---|---|---|---|---|
| 0.50 | 956.6 | 957.4 | 957.8 | 958.0 |
| 0.20 | 956.6 | 958.8 | 961.4 | 963.4 |
| 0.10 | 956.6 | 959.8 | 966.5 | 977.7 |
| 0.05 | 956.6 | 960.5 | 974.5 | **1137.4** |

`lam` <= 0.1 이면 축이 살아난다. 그래서 재려 했고 — **그 전에 계산했어야 할 것을
뒤늦게 계산했다.**

### 추론 예산이 `lam` 과 무관하게 닫는다

로컬 실측: ctx=7000, 질의 2,000행 -> **1,421초** (16 CPU 스레드).
평가셋은 245,789행 = **123 청크**.

| L4 가속비 | 컨텍스트 1개 | 제한 대비 | M=4 | M=16 |
|---|---|---|---|---|
| 20x | 145.5분 | **14.6x** | 582분 | 2,328분 |
| 50x | 58.2분 | **5.8x** | 233분 | 931분 |
| 100x | 29.1분 | 2.9x | 116분 | 466분 |
| 300x | 9.7분 | 1.0x | 39분 | 155분 |

**제한은 10분이다.** 16코어 CPU 대비 L4 의 트랜스포머 추론 가속은 통상 20~50x 이고,
그 구간에서 **컨텍스트 하나만 돌려도 6~15배 초과**한다. 세기를 올리는 유일한
수단(M>=4)은 여기에 다시 M 을 곱한다.

> **`lam` 이 0.05 로 나와 상한이 1137 이어도 제출이 불가능하다.** 이것은 측정
> 잡음의 문제가 아니라 대회 규정(추론 10분 / 245,789 샘플)이 정한 벽이다.

메모리 상한의 정체도 확인됐다 — `ctx^2 * 55피처 * 4B` 다.

```
ctx= 5000   5.03 GB   OK
ctx= 7000   9.86 GB   OK (가용 10.6GB 에 겨우)
ctx=10000  20.12 GB   OOM
```

**절차상의 교훈** — 이 산술은 1분이면 됐는데 측정을 2시간 돌린 뒤에 했다.
"별도 검증 과제"로 미뤄둔 것이 실은 **가장 먼저 계산했어야 할 제약**이었다.
비용 제약이 성능 가능성보다 먼저다.

---

## 11. CAAFE — 의미 기반 피처. **채택 후보 발생** (오늘 유일)

모델 계열이 전부 소진된 뒤(§9-a, §10) 남은 유일한 미착수 축이었다. 55피처 중
47개가 주최측 제공 그대로이고 야구 도메인 파생변수가 하나도 없었다.

### 11-a. 40개 -> 11개, 그리고 가산성

파생변수 40개를 만들어 3폴드에서 쟀다 (`exp/caafe.py`). 전부 **행 안의 값만**
쓰므로 규정 4)에 걸리지 않는다.

| | 2022 | 2023 | 2024 | 기하평균 | 3/3 |
|---|---:|---:|---:|---:|---|
| +CAAFE 40개 | 0.9963 | 1.0794 | 1.0325 | 1.0355 | **2/3 기각** |
| +같은손 1개 | 1.0006 | 1.0212 | 1.0145 | 1.0121 | 3/3 |
| **+생존 11개** | 1.0009 | 1.0508 | 1.0327 | **1.0279** | **3/3** |

40개는 희석이었다. 세 폴드에서 중요도가 반복적으로 높았던 것만 추리니 통과했다.
(피처를 **폴드 점수**가 아니라 **중요도 일관성**으로 골랐다 — 점수로 고르면
폴드에 과적합되고 그것이 §9-b 에서 `log(n)` 이 죽은 방식이다.)

**기존 편차 후처리 위에 얹었을 때** (`exp/caafe_stack.py`):

| 폴드 | 대조+후처리 | 대조+CAAFE+후처리 | 배수 |
|---|---:|---:|---:|
| 2022 | 2379.1 | 2379.3 | 1.0001 |
| 2023 | 87.3 | 91.3 | 1.0465 |
| 2024 | 796.8 | 821.3 | 1.0308 |
| **기하평균** | | | **1.0256  (3/3)** |

> **단독 이득 1.0279 중 1.0256(91%)이 보존된다. 부분~완전 가산이며 새 정보다.**
> 955.22 환산 **979.7 (+24.5)**. 다만 2022 이득이 1.0001 로 사실상 0 이라
> 실질 이득은 2023·2024 에서만 나온다.

### 11-b. 🚩 이득의 정체는 "퍼짐"이 아니라 **시너지**다

정보축을 분리해 재측정했다 (`exp/phase12.py`, 후처리 위 배수).

| 후보 | 2022 | 2023 | 2024 | 기하평균 | 3/3 |
|---|---:|---:|---:|---:|---|
| +CAAFE11 | 1.0001 | 1.0465 | 1.0308 | **1.0256** | 3/3 |
| +CAAFE10 (−same_hand) | 0.9971 | 0.9665 | 1.0106 | 0.9912 | 1/3 |
| +same_hand 만 | 0.9995 | 1.0192 | 1.0128 | 1.0105 | 2/3 |
| G-A 플래툰(+손쌍코드) | 1.0038 | 1.0283 | 1.0103 | 1.0141 | 3/3 |
| G-B 폼/시간편차 | 0.9974 | 1.0178 | 1.0122 | 1.0091 | 2/3 |
| G-C 실패구성·전략 | 0.9993 | 0.9794 | 0.9988 | 0.9925 | **0/3** |

`same_hand` 를 빼면 **대조보다 나쁘다**(0.9912). `same_hand` 만으로는 부족하다
(1.0105, 2/3). 그런데 합치면 1.0256 이다. **부분의 곱보다 전체가 크고 3폴드
모두에서 그렇다.**

```
2022  0.9971 x 0.9995 = 0.9966   실제 1.0001   시너지 +0.35%
2023  0.9665 x 1.0192 = 0.9851   실제 1.0465   시너지 +6.2%
2024  1.0106 x 1.0128 = 1.0235   실제 1.0308   시너지 +0.7%
```

> **폼 변화 · 실패 구성 · 구종 다양성은 그 자체로는 잡음이고, 손 조합을 조건으로
> 걸었을 때만 신호가 된다.** 투수의 난조나 실패 유형은 어느 손 타자를 상대하느냐에
> 따라 다르게 발현되며, 조건 없이 넣으면 상쇄돼 사라진다.

### 11-c. 데이터 제약 — 구종 상호작용은 구성 불가

`train.csv` 원본 50컬럼에 **해당 투구의 구종이 없다.** 구종 관련은
`asof_pitcher_{fastball,breaking,offspeed}_rate` 셋뿐이고 그 투수의 **과거 구사
비율**이다. TrackMan 의 `tagged_pitch_type` 은 ① 2024 에서 끝나 2025 평가셋에
없고 ② 행 단위 조인이 다섯 겹으로 막힌다 (§6-o). 따라서
`count x pitch_type` · `pitch_type x same_hand` 는 원리적으로 만들 수 없다.

---

## 12. 🚩 왜 955 에서 막혀 있는가 — 구조적 답

### 12-a. 투수의 "수준"은 흔들리고 "좌우 성향"은 유지된다

두 시즌 모두 200구 이상인 투수의 특성값 상관 (`exp/phase56.py`).

| 전이 | 투수수 | 성공률 corr | 기울기 | **플래툰차 corr** |
|---|---:|---:|---:|---:|
| 2019→2020 | 153 | 0.5331 | 0.4358 | 0.3563 |
| 2020→2021 | 178 | 0.3334 | 0.4446 | 0.4486 |
| 2021→2022 | 165 | 0.5864 | 0.5912 | 0.4175 |
| **2022→2023** | 168 | **0.2011** | **0.1509** | **0.4452** |
| 2023→2024 | 168 | 0.6723 | 0.6092 | 0.4467 |

전체 성공률 지속성은 0.20~0.67 로 요동친다. **2022→2023 은 0.20 / 기울기 0.15 로
거의 끊긴다 — 2023 이 퇴화 폴드인 이유가 이것이다.**

그런데 **플래툰 차(같은손 상대 − 다른손 상대)는 0.42~0.45 로 일정하고, 수준이
무너진 2022→2023 에서도 0.4452 로 멀쩡하다.** §11-b 에서 손 조합 축만 살아남은
것이 데이터로 설명된다.

### 12-b. 정보는 있다. 전이가 안 된다

검증 폴드 2024. **in-fold 오라클** = 2024 정답으로 2024 셀평균을 만들어 적용
(실전 불가, 그 분할이 담은 정보의 총량). **out-of-fold** = 같은 분할을 <2024 로
만들어 적용 (실제로 전이된 몫).

| 분할 | 셀수 | in-fold 오라클 | out-of-fold | **전이효율** |
|---|---:|---:|---:|---:|
| 손조합4 | 4 | 114.5 | 93.1 | **0.813** |
| 투수 | 391 | 990.8 | 44.7 | 0.045 |
| 투수×타자손 | 772 | 1425.5 | 109.2 | 0.077 |
| 투수×손×카운트 | 8,492 | 4593.5 | 128.6 | 0.028 |
| 투수×손×카운트×주자 | 15,680 | **7481.4** | 130.7 | **0.017** |

**세밀한 투수 구조에는 현재 성능(821)의 9배인 `rho^2` 7,481 이 실제로 들어 있다.
그런데 시즌을 넘어 살아남는 것은 1.7% 뿐이다.** 그리고 전이효율이 세밀해질수록
붕괴한다 — 81.3%(4셀) → 7.7%(772) → 2.8%(8,492) → 1.7%(15,680).

> **"병목은 추정이 아니라 전이"(§5 구조적 결론)가 숫자로 확정됐다.** 편차 계열이
> 통했던 이유도 설명된다 — 축소(shrinkage)는 그 1.7% 중 일부를 건져내는 장치다.

### 12-c. 1150 이 요구하는 것

```
현재 최고후보 821.3  ->  1000 은 1.22배 / 1050 은 1.28배 / 1150 은 1.40배
```

1.40배가 나올 수 있는 곳은 **세밀한 투수 구조(오라클 7,481)뿐**이고, 그러려면
전이효율을 1.7% → 2.4% 이상으로 끌어올려야 한다. §12-a 가 방향을 가리킨다 —
**수준(level)은 2025 `asof_*` 로 이미 관측값으로 주어지므로, 과거 시즌에서 배울
것은 수준이 아니라 형태(shape)다.**

### 12-d. 문제 재정의("다음 시즌 변화량 예측")는 성립하지 않는다

평가 시점에 2025 선수 상태로 쓸 수 있는 것은 그 행의 `asof_*` 뿐인데,
**주최측이 그것을 2025 시즌 내부 as-of 로 이미 계산해서 준다.**

```
TEST_000001  asof_pitcher_n=3465  asof_pitcher_success_rate=0.489466
```

"선수의 2025 상태"는 예측 대상이 아니라 **관측값**이다. 다음 시즌 변화량을
예측하는 구조는 이미 주어진 것을 다시 추정하는 셈이라 이득이 없다.

---

## 13. 20회차 후보 — 빌드·검증 완료 (미제출)

### 13-a. 두 판을 만든다

규정 4) 판단이 정리되기 전이므로 성능판과 클린판을 **둘 다** 유지한다.

| | 파일 | `alpha` / `center` | 근거 |
|---|---|---|---|
| **A** 성능 | `submissions/cand_caafe.zip` | 1.079724 / 0.637217 | 19회차 아핀에 적률 정합 |
| **B** 클린 | `submissions/cand_caafe_clean.zip` | 1.090000 / 0.523766 | `alpha` 는 10회차 로컬 근거, `center` 는 학습 타깃 평균 |

둘 다 모델·편차표는 동일하다 — CatBoost 3시드 × **66피처**(기본 47 + TrackMan ctx 8
+ **CAAFE 11**) + 편차 후처리 4축(19회차 가중 그대로).

B 의 `center` 0.523766 은 BEST_CONFIG 의 10회차 값과 정확히 일치한다. 그 회차가
로컬 근거만으로 912.41 을 낸 구성이고, 여기에 편차 4축 + CAAFE 이득이 얹힌다.

### 13-b. 아핀 적률 정합 (A 판)

새 모델은 평가셋 적률이 달라 19회차 `alpha`/`center` 를 그대로 못 쓴다.
로컬 2024 에서 대조판(55p)의 평균·분산에 맞추고 그 변환을 아핀에 흡수했다.

```
평균     대조 0.495443   신규 0.495577
표준편차  대조 0.044093   신규 0.044475
B0 = 0.991410   A0 = +0.004122        <- 아주 완만
alpha' = alpha*B0                        1.089079 -> 1.079724
center' = [center(1-alpha) + alpha*A0]/(1-alpha')  0.620698 -> 0.637217
```

**평균 조건 `A*m + B = r` 이 보존된다** (14회차에서 2.73점을 잃은 조건). 기울기
최적점은 rho 개선분(1.27%)만큼 어긋나나 손실이 `(dA)^2*V/BASE*1e5` = **0.15점**이다.
검산 — 새 모델 평균에서의 출력 0.483823 이 대조판 평균에서의 0.483825 와 일치한다.

### 13-c. 성능 근거

같은 코드 경로 · 같은 시드로 잰 2024 홀드아웃 (둘 다 +후처리):

```
대조 55p  rho^2 796.8      신규 66p  rho^2 816.3      배수 1.0245
```

796.8 은 캐시 기반 실험(§11-a)의 대조군과 **정확히 일치**한다 — 경로 검증.
3폴드 기하평균은 1.0256 (3/3). **기대 LB 978.6~979.7, 최악 955**
(2022 폴드가 1.0001 이라 하방이 0 에 가깝다).

### 13-d. 검증 4단계 전부 통과

```
A판  추론 6.5초/600초 (1.1%)  확률 0.2773~0.6739  평균 0.4749  clip 미접촉
B판  추론 5.3초/600초 (0.9%)  확률 0.2841~0.6845  평균 0.4836
```

### 13-e. 🚩 사고 직전까지 갔다 — 실험 환경과 제출 환경을 섞었다

첫 빌드는 **4단계에서 실패**했다.

```
AttributeError: Can't get attribute '_RemainderColsList' on sklearn.compose._column_transformer
```

```
.venv        sklearn 1.6.1   <- 오늘 torch/tabpfn 설치하며 쓴 실험 환경
venv_submit  sklearn 1.7.2   <- 제출 pin. 기존 챔피언 전부 여기서 학습
oldnp        sklearn 1.7.2   <- 검증 환경
```

1.6.1 이 피클에 심는 `_RemainderColsList` 를 1.7.2 가 못 찾는다. **로컬 추론은
3.6초에 멀쩡히 돌았으므로 4단계가 없었으면 그대로 제출해 슬롯 하나를 날렸을
것이다** (2·6회차와 같은 사고). `venv_submit` 으로 재빌드해 해결.

> **교훈 — 실험 환경과 제출 환경을 섞지 말 것.** 오늘 `.venv` 에 torch·tabpfn 을
> 설치하며 의존성이 바뀌었고, 습관적으로 같은 인터프리터로 빌드했다.

### 13-f. `script.py` 에 파생컬럼 지원 추가

`train_cat.py` 가 `--dev-feats --save` 를 거부하며 남긴 숙제("추론 경로 지원을
먼저 넣을 것")를 이번에 처리했다. `attach_caafe()` 를 추가하고 **번들의
`features` 에 `cf_*` 가 있을 때만** 호출하도록 게이트를 걸어 기존 제출본은
영향받지 않는다. 학습 쪽은 `script.py` 의 같은 함수를 import 하므로 학습·추론
경로가 원천적으로 일치한다.

---

## 14. PARTIAL POOLING — 현행 `d1` 의 결함을 특정했다

### 14-a. 문제

§12-b 가 보인 것 — 전이효율이 **손조합4 81.3% / 투수×타자손 7.7%** 다.
그런데 현행 `d1` 은 이 둘을 **하나의 `k` 로 뭉뚱그려 축소한다**.

```
d1 = n/(n+300) * (투수x손 평균 - 투수 평균)
```

이 편차 안에는 성질이 전혀 다른 두 성분이 섞여 있다 — 모집단 손조합 효과(전이
81.3%)와 투수 개인 잔차(전이 7.7%). 하나의 축소를 걸면 **안정적인 성분까지 같은
비율로 깎인다.**

### 14-b. 처방 — 안정 성분을 먼저 떼고 잔차에만 축소를 건다

```
hE[h] = H4[h] - G                                  손조합 효과 (4셀, 축소 불필요)
d1D   = hE + n/(n+k) * (투수x손 평균 - 투수 평균 - hE)
```

`corr(d, y_eval)^2 * 1e5` 로 비교했다 (적률 모형에서 최적 가중의 이득이
`corr(d,y)^2` 에 비례하므로 정확한 지표다). 전부 시즌 `<f` 로만 구축.

| 방식 | 2022 | 2023 | 2024 | 기하평균 | in-fold | 전이효율 |
|---|---:|---:|---:|---:|---:|---:|
| raw (축소 없음) | 34.1 | 48.0 | 77.2 | 50.2 | 426.5 | 0.118 |
| **count-k (현행 `d1`, k=300)** | 54.2 | 66.8 | 77.2 | **65.4** | 345.9 | 0.189 |
| 분산인지 EB | 50.3 | 64.2 | 72.6 | 61.7 | 335.6 | 0.184 |
| **계층분해 + count** | 68.3 | 84.9 | 100.9 | **83.7** | 352.0 | 0.238 |
| 계층분해 + EB | 62.1 | 94.2 | 103.0 | 84.4 | 279.2 | **0.302** |

`k` 를 훑으면 계층분해는 **k=1000 에서 91.3** 이다 (현행 65.4 대비 **+39.6%**).
3폴드 모두 개선이다 (72.8>54.2, 92.7>66.8, 112.8>77.2).

> **in-fold 오라클은 오히려 낮아지고(426.5 -> 352.0) out-of-fold 는 오른다.**
> 지시된 판정 기준("오라클이 낮아져도 전이가 오르는가")을 정확히 만족한다.

### 14-c. 아직 판정 아님 — 기저 모델과의 중복을 재야 한다

`corr(d,y)^2` 는 `d` 와 기저 모델 예측의 상관(`cm` 항)을 무시한다. `d1D` 가 품은
`hE` 는 CatBoost 가 `pitcher_hand`/`batter_hand` 로, CAAFE 판은 `cf_same_hand` 로
이미 잡고 있을 수 있다. 그래서 세 형태를 분리해 실제 파이프라인으로 재검증 중이다.

```
d1B = n/(n+300)  * (PH - P)              현행
d1D = hE + n/(n+1000)*(PH - P - hE)      계층분해
d1R =      n/(n+1000)*(PH - P - hE)      잔차만 (hE 제외)
```

**`d1R` 이 `d1B` 를 이겨야 진짜 새 정보다.** `d1D` 만 좋고 `d1R` 이 나쁘면 이득의
정체는 `hE` 이고 모델이 이미 아는 것을 후처리로 다시 넣은 것에 불과하다.

---

## 15. 🚩 20·21회차 실측 — CAAFE 는 전이가 0 이었다. 그리고 3/3 게이트가 뚫렸다

```
19회차 챔피언        955.2193
20회차 A (성능판)     952.4983    <- 챔피언보다 -2.72
21회차 B (클린판)     924.3786
예측했던 값             978.6      <- 오차 -26.1
```

### 15-a. 같은 모델을 아핀만 바꿔 두 번 냈으므로 적률이 풀린다

`alpha` 가 1.0797 vs 1.0900 으로 1% 차이라 2식이 거의 평행해 직접 해는 못 구한다.
대신 **챔피언의 로컬→평가 drift 를 이전**시켜 `m`,`V` 를 고정하고 `C` 만 풀었다.

```
챔피언 drift   평균 -0.021464   표준편차 x0.99335
신규 추정 평가적률  m=0.474113  s=0.044179
```

| | `rho` | 아핀최적 | 실측 |
|---|---:|---:|---:|
| 챔피언 19회차 | 0.097757 | 955.64 | 955.2193 |
| **A판 (성능)** | **0.097616** | 952.88 | 952.4983 |
| **B판 (클린)** | **0.097738** | 955.28 | 924.3786 |

**두 관측이 독립적으로 낸 `rho` 가 0.13% 이내로 일치한다** — 적률 가정이 타당하다.

### 15-b. 아핀은 맞았다. `rho` 가 안 올랐다

**A판은 자기 아핀 최적(952.88)에서 0.38 밖에 안 벗어났다.** §13-b 의 적률 정합은
의도대로 작동했고, 손실은 **전부 `rho` 에서** 났다.

```
rho^2 배수   실측 0.9971        로컬 3폴드 예측 1.0256
```

> **CAAFE 이득은 평가셋에서 0 이었다.** 로컬 +2.56%, 평가 −0.3%.

### 15-c. 🚩 3/3 부호 게이트가 전이를 보장하지 못했다

이 프로젝트가 19회 동안 신뢰해온 게이트가 처음으로 뚫렸다. 다시 뜯어보면 그
"3/3" 의 실체는 이랬다.

```
2022  1.0001   <- 사실상 0
2023  1.0465   <- 퇴화 폴드 (Champion rho^2 76.5, 2022 의 1/31)
2024  1.0308
```

**평가셋(2025)은 2022 처럼 행동했다.** 원장에 "2022 이득이 1.0001 로 아슬아슬하다"
고 두 번 적어놓고도 3/3 이라는 이유로 통과시켰다.

> **교훈 — 부호 개수는 게이트로 불충분하다.** 폴드 규모가 31배 다른 상황에서
> 기하평균은 퇴화 폴드에 지배되고, 부호 개수는 크기를 무시한다. **가장 건강한
> 폴드(2022)에서 이득이 0 이면 그것이 신호다.** 앞으로는 `min(폴드별 배수)` 를
> 함께 게이트로 걸어야 한다 — 이번 후보는 min=1.0001 로 즉시 걸러졌을 것이다.

### 15-d. 값비싼 소득 — 규정 4) 회색지대의 값이 실측됐다

```
A판 (LB 역산 아핀)  952.4983
B판 (클린 아핀)     924.3786
차이                 +28.12
```

**STATUS §3 이 추정하던 40~55점보다 훨씬 작다.** 그리고 **924.38 이 현재 확보된
완전 클린 최고 기록**이다 (종전 로컬 근거 최고는 10회차 912.41).

더 중요한 것 — B판의 아핀 최적은 955.28 인데 924.38 밖에 못 냈다. **클린 아핀
자체가 31점을 흘리고 있다.** 원인은 `center` 를 학습 타깃 평균(0.523766)으로 둔
것이고, **평가셋 정보 없이도 줄일 수 있다**: 챔피언의 로컬→평가 drift 가
−0.021464 로 실측됐는데 이는 시즌 간 예측평균 이동이라 **워크포워드로 추정
가능한 양**이다. 클린 노선의 상한이 924 가 아니라 940~950 대일 수 있다.

### 15-e. partial pooling 축도 같이 닫힌다

§14 의 계층분해는 `corr(d,y)^2` 로 +39.6% 였으나 파이프라인 검증에서
`d1R`(잔차만)이 현행에 크게 졌다(1.0220 vs 1.0955) — 이득의 정체는 `hE`(모집단
손조합 효과)이고 잔차 축소 개선이 아니었다. 그리고 CAAFE 판에서 `d1D` 는 2/3 다
(모델이 `cf_same_hand` 로 `hE` 를 이미 갖고 있다).

**§12-b 의 처방("전이 81.3% 짜리 상위 구조에서 빌려와라")은 방향은 맞았으나 그
정보를 CAAFE 가 이미 피처로 회수한 상태였다.** 같은 우물을 두 번 판 셈이고,
그 CAAFE 조차 평가셋에서 0 이었다.

---

## 16. 🚩 클린 아핀을 제대로 세우면 954 다 — 회색지대 없이 챔피언 동급

### 16-a. B판이 흘린 31점의 정체

21회차 B판은 924.38 인데 **그 모델의 아핀 최적은 955.28** 이다. 손실 30.9 가
전부 아핀을 잘못 세운 데서 났다. 원인은 `center` 를 학습 타깃 평균(0.523766)으로
둔 것이다 — 그건 `A*m + B = r` 조건을 전혀 안 맞춘다.

### 16-b. 필요한 두 값은 학습 데이터만으로 추정된다

```
r_hat  최근 3시즌 선형외삽        0.462179   실측 0.460900   오차 +0.001279
m_hat  r_hat + mean(m - r)      0.473769   실측 0.473979   오차 -0.000210
```

시즌별 성공률은 0.5647 → 0.5327 → 0.5328 → 0.5289 → 0.5000 → 0.4861 로 최근
3시즌이 거의 선형이라 외삽이 잘 맞는다. `m - r` 은 워크포워드에서 0.0040 /
0.0215 / 0.0093 로 평균 0.011590 이며, 이 **차이 고정** 방식이 회귀(`m=a+b*r`,
3점뿐이라 기울기 불안정)보다 훨씬 정확하다 (−0.000210 vs +0.008391).

> **STATUS §3 이 "169점 손실"의 원인으로 지목한 `m` 추정 오차(+0.0204)가
> −0.00021 로 줄었다.** 그 진단은 "추세는 맞는데 파라미터화가 불안정하다"였는데,
> 실제로는 **`m` 을 절대값으로 외삽한 것**이 문제였고 `m - r` 차이로 두면 안정적이다.

### 16-c. 손실 분해 — 챔피언 구조 기준

```
평균조건 오차 = A(m_true - m_hat) - (r_true - r_hat) = 0.000916
손실 = 1e5 * 0.000916^2 / BASE = 0.92 점
```

| `alpha` 근거 | `alpha` | `center` | 평균손실 | 기울기손실 | 예상 |
|---|---:|---:|---:|---:|---:|
| 워크포워드 중앙 | 1.0118 | 1.453141 | 0.90 | 7.82 | 946.9 |
| 2023(퇴화) 제외 평균 | 1.0294 | 0.868223 | 0.90 | 5.33 | 949.4 |
| **10회차 문서화 1.09** | **1.0900** | **0.602546** | **0.92** | **0.39** | **954.3** |

**`alpha` 의 출처를 분명히 해둔다** — 1.09 는 10회차에서 **LB 역산 이전에** 로컬
근거로 정해 BEST_CONFIG 에 문서화된 상수다. 오늘 워크포워드로 새로 뽑으면
1.01~1.03 이고 그건 949.4 다. 5점 차이가 거기서 난다. 1.09 를 쓰는 근거는
"문서화된 기존 상수"이지 오늘 재도출한 값이 아니다.

### 16-d. `submissions/cand_nest_clean.zip`

19회차 챔피언 모델(55피처, 편차 4축) **그대로** + 클린 아핀
(`alpha` 1.090000, `center` 0.602546). verify 4단계 통과, 추론 3.5초.

```
챔피언 19회차 (LB 역산 아핀)   955.2193
클린판 (평가셋 정보 0)          954.3 기대     차이 -0.9
```

> **규정 4) 회색지대를 완전히 벗어나면서 챔피언과 사실상 동급이다.** 21회차 클린판
> 924.38 대비 **+29.9**. 회색지대의 실제 가격은 28.12(§15-d)가 아니라 **약 1점**
> 이었다 — 나머지 27점은 아핀을 못 세운 탓이었다.

---

## 17. 🚩 AS-OF 분해 — 통산 누적에서 **현재 시즌 상태**를 복원한다

오늘 유일하게 **크기가 다른** 축이다.

### 17-a. 발견 — `asof_*` 는 시즌 리셋이 아니라 통산 누적이다

`data_description.md` L86 은 "해당 행의 투구 직전까지"라고만 하고 범위를 안 밝힌다.
실측으로 확인했다.

```
투수 23633  2019:2,757  2020:5,427  2021:8,378  2022:10,893  2023:13,636  2024:15,449
2024 시즌 3월 최대 n = 13,813   <- 2023 말 13,636 에서 이어진다
```

시즌마다 리셋되지 않는다. 따라서 그 선수의 **직전 시즌까지 통산**을 학습
데이터에서 빼면 시즌 내 상태가 나온다.

```
cur_n    = asof_n(행) - prior_n[선수]
cur_rate = ((asof_n * asof_rate)(행) - prior_events[선수]) / cur_n
```

### 17-b. 복원 검증

**2024 폴드** (`prior` 는 `<=2023` 에서):

```
cur_n  : 실제 시즌내 순번과 100.0000% 일치, 음수 0%
cur_rate: 평균절대오차 3.1e-6 (잔차는 저장된 rate 의 float32 반올림)
```

**test 5행** (`prior` 는 train 전체에서):

| row_id | 투수 | 월 | `asof_n` | train 행수 | `cur_n` |
|---|---:|---:|---:|---:|---:|
| TEST_000001 | 21813 | 7 | 3,465 | 3,085 | 380 |
| TEST_000017 | 24745 | 3 | 86 | 0 | 86 |
| TEST_005332 | 24713 | 3 | 0 | 0 | 0 |
| TEST_035185 | 24713 | 3 | 9 | 0 | 9 |

전부 음수 없이 정합하고 시점과 맞는다 (7월 380구 / 3월 신인 / 데뷔 첫 구).

### 17-c. 왜 새 정보인가

모델이 보는 `asof_pitcher_success_rate` 는 **통산**이라 2019~2024 이력과 현재
폼이 섞여 있고, 모델은 그 투수의 직전 시즌말 통산을 모르므로 **원리적으로 못
가른다.** 학습 데이터에서 그 상수를 빼주면 갈린다.

CAAFE 의 `prev1 - season`(경기 단위 창)과 질적으로 다르다 — **시즌 전체를 정확히
가르는 분해**이고, `cur_n` 이 현재 폼 추정의 **신뢰도**까지 준다.

### 17-d. 결과 (`exp/asof_state.py`, 시드 2개 평균, walk-forward)

| 구성 | 2022 | 2023 | 2024 | 기하평균 | **min gain** | 3/3 |
|---|---:|---:|---:|---:|---:|---|
| C 이력(prior) | 1.0005 | 1.0641 | 1.0047 | 1.0227 | 1.0005 | 3/3 |
| **D 현재(cur)** | **1.0322** | **2.3381** | **1.1291** | 1.3968 | **1.0322** | 3/3 |
| E 현재−이력 | 1.0332 | 1.7301 | 1.1070 | 1.2555 | 1.0332 | 3/3 |
| J 전부 | 1.0303 | 2.3376 | 1.1195 | 1.3918 | 1.0303 | 3/3 |

> 🚩 **`C 이력`이 거의 0 인데 `D 현재`가 크다.** 새 정보는 "그 투수가 원래 어떤가"가
> 아니라 **"지금 어떤가"** 다. 이력은 모델이 `pitcher_id` 와 통산 rate 로 이미 갖고
> 있다.

**게이트 B(min gain)가 이번엔 실질적이다** — CAAFE 는 min 1.0001 로 3/3 을
통과하고도 평가셋 이득이 0 이었다(§15-c). 여기는 **min 1.0322 로 322배 크다.**

생산 경로(train.csv)에서도 재현됐다 — 2024 홀드아웃 796.8 → 899.4, 배수
**1.1287** (캐시 경로 1.1291과 소수 3자리 일치, 대조군 796.8 은 §13-c 와 동일값).

### 17-e. 규정 5)

행 자신의 공식 `asof_*` 컬럼(`data_description.md` L182: "운영 측에서 제공한
`asof_*` 컬럼은 ... 공식 입력 피처이므로 사용할 수 있습니다") + **학습 데이터만으로
만든 선수별 상수**. 평가셋의 다른 행을 보지 않으므로 test.csv 에 그 행 하나만
있어도 값이 같다. **아핀도 클린 노선(§16)을 쓴다 — 이 후보는 규정 4) 회색지대를
완전히 벗어난다.**

### 17-f. 기대치 — 기하평균을 쓰지 않는다

```
min gain 기준 (2022)   955.6 x 1.0322 = 986
2024 기준             955.6 x 1.1287 = 1078
기하평균 기준           955.6 x 1.3968 = 1335   <- 쓰지 않음
```

기하평균 1.3968 은 2023 의 2.34배가 전부 끌어올린 값이고 그 폴드는 Champion
`rho^2` 가 76.5 인 퇴화 폴드다. **986~1078 로 본다.**

`alpha` 위험 — `A* = C/V` 이므로 `rho` 가 12.9% 오르면 최적 `alpha` 도 오른다.
챔피언 평가셋 `A*` 가 1.1125 였으니 이 모델은 1.15~1.18 일 수 있고, 1.09 로 두면
기울기 손실이 최대 6~7점이다. 그래도 1.09 를 쓴다 — 10회차에서 LB 역산 이전에
로컬 근거로 정해 문서화된 상수라 출처가 깨끗하고, 추측으로 올리면 반대로 넘친다.

### 17-g. 22회차 후보 `submissions/cand_asof.zip` — 빌드·검증 완료

```
모델    CatBoost 3시드 x 68피처 (기본47 + TrackMan ctx 8 + AS-OF 현재상태 13)
후처리  편차 4축 (19회차 가중 그대로)
아핀    alpha 1.090000  center 0.583742   <- 클린 (r_hat 추세외삽 + m_hat 워크포워드)
```

`m-r` 워크포워드 0.003040 / 0.016036 / 0.011036 → 평균 0.010037.
`r_hat` 0.462179, `m_hat` 0.472216.

**verify 4단계 통과** (추론 4.2초/600초).

### 17-h. 🚩 검증 3단계는 이 후보를 검증하지 못한다 — 별도 확인이 필요했다

3단계 확률 범위가 **0.3584~0.5800** 으로 직전 후보들(0.2773~0.6739 등)보다 좁게
나왔다. 원인 — **3단계는 2024 를 test 대역으로 쓰는데 번들의 `asof_prior` 는
train 전체(2019~2024)에서 만들었다.** 2024 행에서는 `cur_n = asof_n - prior_n` 이
음수가 되어 0 으로 클립되고 **AS-OF 피처가 전부 죽는다.** 모델이 그 피처에
의존하도록 학습됐으므로 예측이 중앙으로 수축한 것이다.

즉 3단계는 **피처가 죽었을 때의 퇴화 동작**을 검증한 셈이고(그것도 유용하다),
진짜 경로는 실제 2025 행으로 따로 확인해야 했다.

**실제 test.csv 5행 확인:**

| row_id | 투수 | 월 | `asof_n` | `cur_n` | `cur_succ` | 통산 rate | 예측 |
|---|---:|---:|---:|---:|---:|---:|---:|
| TEST_000001 | 21813 | 7 | 3,465 | 380 | **0.3579** | **0.4895** | **0.39152** |
| TEST_000017 | 24745 | 3 | 86 | 86 | 0.2907 | 0.2907 | 0.38441 |
| TEST_000213 | 24198 | 6 | 486 | 399 | 0.4461 | 0.4753 | 0.48704 |
| TEST_005332 | 24713 | 3 | 0 | 0 | NaN | NaN | 0.51954 |
| TEST_035185 | 24713 | 3 | 9 | 9 | 0.4444 | 0.4444 | 0.52351 |

> **TEST_000001 이 이 축의 존재 이유다.** 통산 0.4895 인 투수가 2025 380구에서는
> 0.3579 다. 기존 모델은 통산값만 보고 평균적인 예측을 했을 것이고, 새 모델은
> 0.39152 로 다섯 행 중 가장 낮게 준다.

신인은 `cur_rate == 통산 rate` 로 자동 일치하고, 데뷔 첫 구는 NaN 으로 안전하게
퇴화한다. `exp(cur_logn)-1 = [380, 86, 399, 0, 9]` 가 수동 계산과 정확히 일치한다.

> **교훈 — `verify_submit.py` 3단계는 2024 를 대역으로 쓰므로 "시즌 내 상태"를
> 쓰는 피처를 검증할 수 없다.** 앞으로 그런 피처를 넣을 때는 실제 `test.csv` 로
> 값 자체를 찍어 확인할 것.

### 17-i. 🚩 22회차 실측 **1040.8656** — 이 프로젝트 최대 단일 상승

```
챔피언 19회차   955.2193
22회차         1040.8656    +85.65
예측 구간       986 ~ 1078   -> 상단부 적중
```

적률을 풀면 (관측 1개 · 미지수 3개라 가정 범위로 낸다):

```
아핀 최적 범위   1041.1 ~ 1049.5     현재 손실  0.2 ~ 8.6
최적 alpha      1.020 ~ 1.160       (지금 1.09 — 구간 한가운데)
챔피언 대비 rho^2 배수   1.089 ~ 1.098
```

**전이율 확정 — 2024 폴드 배수 1.1287 중 약 78~87% 가 평가셋으로 넘어왔다.**
CAAFE(0%)와 정반대다. `min gain` 을 게이트로 쓴 판단이 맞았다.

> **아핀으로 더 짜낼 것은 없다** (최대 8점). `alpha` 1.09 는 최적 구간 한가운데에
> 있었고, §16 의 클린 절차가 의도대로 작동했다. **그리고 이 점수는 규정 4)
> 회색지대를 완전히 벗어난 상태에서 나왔다** — LB 역산 상수를 하나도 안 쓴다.

---

## 18. 🚩 AS-OF 축 확장 — 세 갈래 중 **F 만 살았다.** E·B 는 D 위에서 음수

§17 이 연 축에서 SETUP 이 남긴 세 갈래를 한 번에 쟀다 (`exp/asof_state2.py`,
시드 2개 평균, walk-forward, 폴드 2022/2023/2024).

### 18-a. 판정 기준을 바꿨다 — 분모는 A 가 아니라 **D** 다

§17-d 의 표는 전부 `A 기준 55p` 대비였다. 그때 D 는 후보였으니 맞다. 지금 D 는
**22회차 제출본(1040.8656)에 들어가 있다.** 질문이 "기준보다 나은가"에서
**"D 에 더했을 때 오르는가"** 로 바뀐다.

§17-d 가 이미 경고하고 있었다 — `J 전부`(prior+cur+E)가 D 보다 낮았다
(1.0303 vs 1.0322). **축을 더하는 것만으로는 안 오른다.**

### 18-b. 결과

기준 A(55p) 대비 배수, 괄호는 **D 대비**.

| 구성 | 2022 | 2023 | 2024 | vs A min | **vs D min** | vs D 3/3 |
|---|---:|---:|---:|---:|---:|---|
| A 기준 55p | 1.0000 | 1.0000 | 1.0000 | 1.0000 | 0.4277 | 0/3 |
| **D 현재(재현)** | 1.0322 | 2.3381 | 1.1291 | 1.0322 | 1.0000 | — |
| E 현재−이력 | 1.0315 | 2.2630 | 1.1323 | 1.0315 | **0.9679** | 1/3 |
| **F 최근vs시즌** | **1.0349** | **2.4906** | **1.1412** | **1.0349** | **1.0026** | **3/3** |
| B 타자확장 | 1.0340 | 2.2710 | 1.1271 | 1.0340 | 0.9713 | 1/3 |

> **재현 확인** — D 의 2022 1.0322 · 2023 2.3381 이 §17-d 와 소수점까지 같다.
> 환경·캐시를 재구축한 PC 에서 원 측정이 그대로 나온다.

### 18-c. E 가 닫히는 이유는 산술이다

§17-d 에서 E 의 min(1.0332)이 D(1.0322)보다 근소하게 높아 "조합하면 가산인가"가
열려 있었다. 아니다. `E = cur − prior` 이고 D 는 이미 `cur` 을 갖고 있으므로
E 를 더하는 것은 **`prior` 를 (부호만 바꿔) 더하는 것과 같은 정보**다. 그런데
`C 이력(prior)`는 §17-d 에서 1.0005 로 이미 0 이었다. **정보는 0 인데 열은 10개
늘어나니 희석만 남는다.** 2023 에서 −3.2% 가 그 값이다.

### 18-d. B(타자) 도 닫힌다 — §5-c 와 같은 자리로 수렴한다

이력 2 + 폼변화 2 + **투타 격차 2**(`cur_succ − cur_bsucc`, 축평행 분할이 한 번에
못 만드는 형태)를 넣었다. 2022 는 +1.7% 인데 2023 이 −2.9% 다. 타자 쪽 계층이
평가셋 최적 가중 0 이었던 §5-c 와 같은 결론이다.

### 18-e. 🚩 F 가 CAAFE 와 갈리는 지점 — 뺄셈이 성립하는가

CAAFE 는 `prev1 − 통산rate` 를 썼다. **통산에는 이력이 섞여 있어 척도가 다른 두
값을 뺀 것**이었고 평가셋 전이가 0 이었다(§15). 이제 `cur_*`(시즌내 누적)이
있으므로 **같은 창 안에서** 뺀다.

```
form_succ{1,3,5} = asof_pitcher_prev{k}_game_success_rate − cur_succ
form_mid{1,3,5}  = asof_pitcher_prev{k}_game_middle_rate  − cur_mid
```

"최근 k 경기가 **이번 시즌 자기 평균보다** 좋은가"가 된다. 같은 재료(prev 게임
컬럼)인데 기준선을 바꾸자 신호가 살아난다.

### 18-f. 게이트 E (시드 안정성) — 마진이 노이즈의 2배다

2022 의 여유가 1.0026(+6.4)뿐이라 CAAFE 자리(min 1.0001)와 가까워 독립 시드쌍으로
다시 쟀다.

| 시드쌍 | D | D+F | 차이 |
|---|---:|---:|---:|
| (42, 43) | 2443.3 | 2449.7 | **+6.4** |
| (44, 45) | 2446.6 | 2451.0 | **+4.4** |

**D 수준 자체가 시드쌍 사이에서 +3.3 움직이는데(공통 성분) 격차는 두 쌍 모두
같은 부호로 남는다.** 4시드 합산 ×1.0022. 게이트 A~E 전부 통과.

### 18-g. 추론 경로 — `script.py` 에 `form_*` 6개

`FORM_SPEC`/`FORM_COLS` 를 추가하고 `attach_asof_state` 끝에서 만든다. 게이팅이
`any(c in model["features"] for c in ASOF_COLS)` 라 **기존 제출본은 영향받지
않는다** (22회차 번들의 `features` 68개에 `form_*` 이 없다 — 실측 확인).

§17-h 의 교훈대로 **실제 `test.csv` 5행으로 값을 찍어 확인**했다.

| row_id | `cur_succ` | `form_succ1` | `form_succ3` | `form_succ5` |
|---|---:|---:|---:|---:|
| TEST_000001 | 0.3579 | −0.0070 | −0.0308 | −0.0221 |
| TEST_000213 | 0.4461 | −0.0053 | +0.0498 | +0.0332 |
| TEST_005332 | NaN | NaN | NaN | NaN |

`cur_succ` 0.3579 가 §17-h 기록과 일치하고 손계산 대조 최대차 **0.000e+00**.
데뷔 첫 구(NaN)와 신인은 안전하게 퇴화한다.

### 18-h. 기대치

```
2024 배수   1.0107   -> 22회차 전이율 78~87% 적용   1049.5 ~ 1050.6
min gain    1.0026   ->                              1043.6
하방        전이 0 이면 1040.87 근처 (모델 변경분만큼의 잔여 위험)
```

**기하평균(2023 이 끌어올린 값)은 쓰지 않는다** — §17-f 와 같은 규율이다.

---

## 19. 🚩 23회차 실측 **1032.3182** — F 는 전이가 0 이 아니라 **음수**였다

```
22회차 (D)      1040.8656
23회차 (D+F)    1032.3182     -8.55
예측            1051.9~1053.2  -> 오차 약 -20
```

### 19-a. 아핀이 아니다. 전부 `rho` 다

두 후보는 `alpha` 가 **같고**(1.09) `center` 만 0.583742 -> 0.578117 로 다르다.

```
B = center(1-A)     22회차 -0.05253678   23회차 -0.05203053   차 +0.00050625
절편 손실 1점어치의 오차 크기 = sqrt(BASE/1e5) = 0.001576
B 차이 단독 효과 = 0.103점 미만
```

설계가 `A*m_hat + B = r_hat` 을 매번 다시 세우므로 두 후보의 절편 오차는
`e = A(m - m_hat) + (r_hat - r)` 로 **같은 형태**다. `m_hat` 이 0.000464 밖에
차이나지 않고 두 모델이 74p/68p 로 거의 같으므로 `e` 도 거의 같다. 실제 `m` 이
예상보다 0.001 더 어긋났어도 절편 손실 차는 1.7점이다.

**따라서 -8.55 는 사실상 전부 `rho` 다.**

```
rho^2 배수  1032.3182/1040.8656 = 0.99179     평가셋 -0.82%
로컬 2024 배수                     1.0136       +1.36%
전이율                              -60%
```

### 19-b. 🚩 `min gain > 1` 은 게이트가 아니다. **크기가 게이트다**

§15-c 에서 "부호 개수는 불충분하다, `min gain` 을 함께 걸어라"로 고쳤는데
**그 교훈이 절반짜리였다.** 이제 점 세 개가 있다.

| 축 | min gain | 로컬 2024 | **평가셋 전이** |
|---|---:|---:|---:|
| CAAFE | 1.0001 | +2.56% | **0%** |
| **F 최근vs시즌** | **1.0026** | **+1.36%** | **-60%** |
| D 현재상태 | 1.0322 | +12.87% | **+78~87%** |

`min gain` 1.0026 을 "CAAFE(1.0001)의 26배"라고 읽은 것이 오독이었다. 0.26% 는
**절대 크기로 여전히 노이즈 대역**이다. 실제로 게이트 E 에서 그 증거를 손에
쥐고 있었다 —

```
폴드 2022 에서 D 수준이 시드쌍 사이에 +3.3 움직인다 (0.13%)
F 의 마진은 +6.4 / +4.4 (0.26% / 0.18%)
```

**마진이 시드 노이즈의 2배.** "부호가 두 쌍 모두 유지된다"를 통과 근거로 삼았지만,
공통 성분이 큰 두 측정에서 부호가 유지되는 것은 **약한 증거**다. 필요한 것은
부호가 아니라 **노이즈 대비 배율**이었다.

> **새 규칙 — 축 하나가 슬롯 하나를 받으려면 `min gain` 이 최소 1.01,
> 그리고 시드 노이즈의 5배 이상이어야 한다.** D 는 3.2% 로 노이즈의 25배였고
> 78~87% 가 전이됐다. 그 아래는 전부 0 이거나 음수였다.

### 19-c. 왜 음수까지 갔는가

전이 0(CAAFE)은 "새 정보가 평가 시즌엔 없다"로 설명되지만 **-60% 는 해롭다는
뜻**이다. `form_*` 은 `prev{k}_game_rate - cur_rate` 로 **두 잡음 추정량의 차**다.
`cur_rate` 는 시즌 초반에 표본이 작고(중앙값 기준 수백 구) `prev1` 은 경기 하나라
더 작다. 로컬 폴드에서는 그 잡음 구조가 학습 시즌과 검증 시즌 사이에 비슷해
모델이 활용할 수 있지만, 시즌이 바뀌면 **잡음의 모양이 옮겨가지 않는다.**
D 가 성공한 이유와 정확히 대칭이다 — D 는 `cur_rate` 자체(수준)를 줬고 F 는
`cur_rate` 의 잔차(잡음)를 줬다.

### 19-d. 남은 슬롯 1장 — 🚩 리더보드 규칙 확인이 선결이다

**최고점 유지 방식이면** 1040.8656 이 그대로 남고 23회차는 기록만 남는다.
**마지막 제출 확정 방식이면 지금 1032.3182 가 우리 점수이고, 남은 1장으로
`submissions/cand_asof.zip` 을 다시 내서 1040.8656 을 회복해야 한다.**

STATUS §7 이 이 질문을 열어두고 답하지 않은 채로 왔다. 이제 슬롯이 1장이라
**답이 곧 행동을 결정한다.**

그 외에 살아 있는 축은 없다. §18 에서 E·B 가 닫혔고 F 는 이 회차로 닫혔다.
AS-OF 축은 D 하나로 끝났다.

---

## 20. D 축 내부 분해 — 시간척도·축소는 닫히고 **맥락(X) 하나가 남는다**

§19 에서 F 가 −60% 전이를 맞은 뒤, "새 계열을 만들지 말고 **전이가 확인된 D 축
자체를 분해**하라"는 지시로 수행한 라운드다. 스크립트 `exp/d_decomp.py`,
판정은 전부 **Champion(prod55 + D) 위에서의 증분**이다.

### 20-a. D 의 정확한 구조 (코드에서 복원)

D 는 "현재 − 잡음" 형태의 **차이가 아니다.** 대수적 역산으로 얻은 **수준**과
그 수준의 **신뢰도**다.

```
asof_rate(통산) = w*prior_rate + (1-w)*cur_rate,      w = prior_n / asof_n
cur_rate = (asof_n*asof_rate - prior_events)/cur_n     <- 수준 10개
cur_logn = log1p(cur_n)                                <- 신뢰도 3개
```

### 20-b. 결과 (시드 42/43, walk-forward 3폴드)

| 구성 | 2022 | 2023 | 2024 | **min** | Brier Δ |
|---|---:|---:|---:|---:|---:|
| W2 2시즌창 | 0.9999 | 0.9744 | 1.0079 | 0.9744 | −0.000003 |
| W3 3시즌창 | 1.0020 | 0.9996 | 1.0123 | 0.9996 | −0.000028 |
| S150 축소k150 | 1.0027 | 1.0337 | 0.9991 | 0.9991 | −0.000025 |
| S500 축소k500 | 1.0050 | 1.0465 | 1.0043 | 1.0043 | −0.000023 |
| S2000 축소k2000 | 1.0019 | 0.9838 | 0.9973 | 0.9838 | +0.000019 |
| **S500R 축소교체** | 0.9735 | **0.5633** | 0.9247 | **0.5633** | +0.000478 |
| **X D×맥락** | 1.0050 | 1.0202 | **1.0314** | **1.0049** | **−0.000024** |
| XS 맥락+축소 | 1.0055 | 1.0456 | 1.0306 | 1.0055 | −0.000027 |

### 20-c. 🚩 S500R 이 D 의 작동 원리를 뒤에서 확인해 준다

원시 `cur_rate` 를 축소본으로 **교체**하면 2023 이 0.5633 으로 무너진다. 축소를
**더하면** 이득(S500 min 1.0043)인데 **바꾸면** 참사다. 즉 **원시 수준값 자체가
신호의 본체**이고 축소는 부가 정보일 뿐이다. §19-c 의 "D 는 수준을 줬고 F 는
잔차를 줬다"와 같은 이야기를 반대편에서 확인한다.

### 20-d. 시간척도 축은 닫힌다 — 방향이 원장과 일치한다

창을 늘릴수록 `prior` 에 가까워지는데 `prior` 는 §17-d 에서 이미 1.0005(=0)다.
W2 는 2023 에서 −2.6%, W3 는 2023 에서 0. **`recent 3/5/10/20 게임`·`EWMA` 는
애초에 구성 불가**다 — 2025 행이 쓸 수 있는 시즌내 정보는 그 행 자신의 `asof_*`
뿐이고(다른 test 행 참조는 규칙 4 위반), 경기 단위 창은 공식 컬럼
`prev{1,3,5}_game_*` 로 **이미 기본 47피처에 들어가 있다.**

### 20-e. 축소 `k` 는 500 근처가 최적이고 그 크기가 작다

150(1.0027) < 500(1.0050) > 2000(1.0019) 로 2022 에서 봉우리가 잡힌다.
`cur_n` 중앙값 526 과 같은 스케일이다. 그런데 2024 이득이 +0.43% 뿐이라 X 에
밀리고, **XS 결합이 가산이 아니다**(min 1.0055 vs X 단독 1.0049, 2024 는 오히려
1.0306 < 1.0314). 두 조작이 **같은 정보를 다르게 회수**하고 있었다. 축소는 X 에
흡수되므로 피처 8개를 더 쓸 이유가 없다.

### 20-f. X — 유일한 생존자, 그리고 시드 재현성

```
dx_{succ,mid}_{adv,onb,sh,bs} = cur_{succ,mid} x {카운트우위, 주자유무, 같은손, 볼-스트라이크}
```

트리는 축평행 분할이라 두 열의 곱을 스스로 못 만든다. 재료는 전부 그 행 자신의
값이라 규칙 4)에 안전하다.

| 폴드 | 시드(42,43) | 시드(44,45) | 시드 노이즈 | 마진/노이즈 |
|---|---:|---:|---:|---:|
| 2022 | +12.3 (1.0050) | **+12.9** (1.0053) | 3.3 | 3.8배 |
| 2024 | +27.5 (1.0314) | **+21.5** (1.0242) | 7.2 | 3.4배 |

**F 는 6.4 vs 4.4 로 흔들렸는데 X 는 12.3 vs 12.9 로 거의 같다.** 그리고 이득이
최근 폴드로 갈수록 커진다 (1.0050 → 1.0202 → 1.0314).

생산 경로(train.csv)에서도 재현 — **2024 홀드아웃 대조 899.4 → 924.4, 배수
1.0278**. 캐시 경로 두 시드쌍 평균 1.0278 과 소수점까지 같고, 대조 899.4 는
§17-d 기록과 같다.

### 20-g. 🚩 그럼에도 §19-b 게이트는 통과가 아니다

```
min gain      1.0049   <  1.01       (기준 미달)
마진/노이즈    3.4~3.8배  <  5배      (기준 미달)
```

증거의 질은 F 와 비교가 안 된다(2024 가 F 의 3배, 시드 재현성, Brier 최선).
하지만 **F 도 "CAAFE 보다 26배 크다"는 상대 비교로 통과시켰다가 −60% 를 맞았다.**
같은 형태의 논증을 반복하지 않는다. 후보는 만들되(`submissions/cand_asof_x.zip`)
**제출 판단은 슬롯·리더보드 방식이 확정된 뒤로 미룬다.**

### 20-h. 편차 4축 가중 재최적화 — 현행이 이미 거의 최적이었다

`WPOST`=[0.20, 0.825, 0.280, 0.45] 는 19회차(D 이전) 모델에서 최적화된 값인데
22회차가 그대로 들고 갔다. **과다 적용 가설은 틀렸다** — 후처리는 2024 에서
여전히 +25.8 을 벌고(877.7 → 903.5), in-fold 최적조차 911.0 으로 +7.5 위일 뿐이다.

| 가중 출처 | 2023 | 2024 |
|---|---:|---:|
| 현행 WPOST | 194.3 | 903.5 |
| **2022 최적** | **201.7** | **907.5** |
| 2023 최적 | 243.2 | **765.4** |
| 2022+2023 평균 | 232.9 | 867.4 |
| 후처리 없음 | 178.2 | 877.7 |

**2023 의 최적해가 [2,2,2,2] (격자 상단 전부)로 병리적**이고 그것을 2024 에 쓰면
−138 이다. 건강한 폴드(2022)의 최적만 쓰면 2/2 개선(+7.4 / +4.0)이지만,
"2023 을 출처에서 뺀다"는 판단을 **결과를 보고** 했으므로 증거 강도는 한 단계
낮게 본다 (퇴화 폴드라는 사전 지식은 §15-c 부터 있었다).

---

## 21. 🚩🚩 24회차 **1044.7656** — 그리고 전이 법칙이 풀렸다

```
22회차 (D)        1040.8656
23회차 (D+F)      1032.3182   -8.55
24회차 (D+X)      1044.7656   +3.90   <- 새 최고
```

X 는 전이됐다. 다만 예측(1063.5~1066.1)에 한참 못 미쳤다. **그 미달 자체가
정보였다** — 이제 점이 세 개라 전이의 형태를 풀 수 있다.

### 21-a. 전이율은 상수가 아니다 — 고정 손실 뒤의 85% 다

| 축 | 로컬 2024 이득 | 실측 LB 배수 | "전이율" |
|---|---:|---:|---:|
| D 현재상태 | +12.87% | 1.0897 | 70% |
| F 최근vs시즌 | +1.36% | 0.9918 | **−60%** |
| X D×맥락 | +2.78% | 1.0037 | 13% |

전이율이 −60% ~ 70% 로 튀어서 그동안 "축마다 다르다"고만 적었다. 아니다.
**로컬 이득에서 고정 크기를 빼면 남는 것이 일정 비율로 넘어간다.**

```
실측 이득 = k * (로컬 2024 이득 - c)        c = 2.34%   k = 0.851
```

**D 와 X 두 점으로 풀고 F 로 검증했다** (F 는 적합에 안 썼다).

| 축 | 예측 | 실측 | 오차 | LB 예측 (실측) |
|---|---:|---:|---:|---|
| D | +8.966% | +8.966% | — | 1040.87 (적합점) |
| **F** | **−0.834%** | **−0.821%** | **−0.013%p** | **1032.18 (1032.32)** |
| X | +0.375% | +0.375% | — | 1044.77 (적합점) |

**F 의 LB 오차 0.14점.** 자유도가 1 인 적합이라 이 한 점이 유일한 외삽 검증이고,
다음 제출이 이 법칙을 반증할 수 있다. 그래도 지금까지 세 회차를 전부 설명하는
유일한 모형이다.

### 21-b. 🚩 손익분기가 있다 — 로컬 2024 **+2.34%**

```
로컬 2024 이득 < 2.34%  ->  LB 가 내려간다 (확정)
```

**F 의 실패는 제출 전에 산술로 알 수 있었다.** 로컬 1.36% 는 손익분기 아래였다.
§19-b 에서 세운 `min gain >= 1.01` 은 방향은 맞았지만 **눈금이 없는 대리
지표**였다. 이제 눈금이 있다.

`c` 가 무엇인가 — 로컬 폴드에 붙는 **비전이 성분**이다. 워크포워드로 쟀어도
2024 폴드에서 얻은 이득의 2.34%p 는 그 시즌 고유이고 2025 로 안 넘어간다.
축의 종류와 무관하게 같은 크기라는 것이 세 점의 함의다.

### 21-c. 1100 이 요구하는 것

```
현재 1044.7656 -> 1100  이려면  로컬 2024 이득 8.55% 필요
   참고  D 가 12.87%,  X 가 2.78%
```

**D 급의 발견이 한 번 더 있어야 한다.** 정제(축소·창·맥락·가중)로는 안 된다 —
이번 라운드에서 잰 여섯 축 중 손익분기 2.34% 를 넘긴 것이 **하나도 없다**
(X 의 2.78% 가 유일하게 넘었고 그마저 여유가 0.44%p 였다).

### 21-d. 이 법칙으로 과거를 다시 보면

§20 에서 "게이트 미달이지만 후보는 만든다"고 한 판단이 결과적으로 맞았다.
X 의 로컬 2.78% 는 손익분기를 **넘었고**, 법칙이 예측한 +0.375% 가 정확히 나왔다.
반대로 §18 에서 F 를 통과시킨 것은 틀렸고, 그 오류의 크기도 이제 정확히 안다.

**다음 축을 평가하는 기준은 `min gain` 이 아니라 로컬 2024 이득이 2.34% 를
얼마나 넘는가다.** 그리고 그 값에 0.851 을 곱하면 LB 예측이 나온다.

---

## 22. Champion 1044.7656 위의 재탐색 (진행 중)

24회차로 Champion 이 바뀌었으므로 **모든 증분을 76p(D+X) 위에서 다시 잰다.**
68p 위에서 잰 값은 다른 모델 위의 값이라 그대로 못 쓴다.

### 22-a. 편차 4축 가중 재최적 — 밴드를 걸자 워크포워드가 양수로 돌아섰다

§20-h 에서 −36.1 이 나왔던 것은 **격자를 열어 둔 탓**이었다. 2023(퇴화 폴드)의
최적해가 `[2, 2, 2, 2]` 로 격자 상단에 달라붙었고 그 가중을 2024 에 쓰면 −138
이었다. 두 가지를 고쳤다.

1. 각 축을 **현행 가중의 ±40%** 안에서만 움직인다 (`BAND = 0.40`)
2. 가중 **출처를 건강한 폴드(2022)로 고정**한다 — 2023 의 in-fold 최적은 안 쓴다

| 폴드 | 후처리없음 | 현행 WPOST | in-fold 최적 | 최적 가중 |
|---|---:|---:|---:|---|
| 2022 | 2455.2 | 2471.4 | 2474.0 | [0.28, 0.78, 0.39, 0.27] |
| 2023 | 181.8 | 197.7 | 203.4 | [0.28, 1.15, 0.39, 0.63] |
| 2024 | 905.2 | 928.9 | 932.8 | [0.28, 1.15, 0.35, 0.56] |

**워크포워드 (출처 = 2022 단독)**

| 평가폴드 | 현행 | 재최적 | 증분 |
|---|---:|---:|---:|
| 2023 | 197.7 | 201.3 | **+3.6** |
| 2024 | 928.9 | 931.2 | **+2.2** |

### 22-b. 🚩 가중은 낮아지는 게 아니라 **높아지려 한다**

§20-h 에서 세운 "D 가 흡수했으니 과다 적용일 것"이라는 가설은 **방향까지
틀렸다.** 1축 0.20 → 0.28, 2축 0.825 → 1.155 로 **둘 다 +40% 상단에 붙는다.**
제약이 binding 이므로 진짜 최적은 밴드 밖이다 (지시 §8 을 지켜 넓히지 않았다 —
넓히면 2023 이 다시 달아난다).

### 22-c. 폴드 간 최적 가중이 불안정하다 — 그래서 이득이 작다

4축(플래툰×주자유무)이 2022 에서 0.27(−40%), 2024 에서 0.56(+24%)로 **부호
반대 방향**이다. 워크포워드 이득이 +2.2~+3.6 에 그치는 이유가 이것이고,
**현행 `WPOST` 가 이미 견고한 타협점**이라는 뜻이다.

판정 — §12 기준 평균 +2.9 / 최소 +2.2 로 **약한 보류(+1~+3)**. 단독으로는
슬롯 근거가 안 된다. 다만 **재학습 0회**로 얹을 수 있으므로, 다른 후보가 서면
그 위에 결합해 다시 잰다.

> 전이 가설(§21)을 여기 그대로 적용하지 않는다. 지시 §5 대로 보정 축은 다른
> transfer law 를 가질 수 있고, 무엇보다 이 값은 **이미 시즌을 건너 검증된
> 형태**다 (2022 에서 고른 가중을 2024 에 적용). 모델 피처 이득과 성격이 다르다.

### 22-d. 🚩 X 성분 marginal — 죽은 성분이 없다. 그리고 거의 직교다

X 가 실제 LB 에서 +3.90 을 냈으므로 **어느 성분이 그 일을 하는지** leave-one-out
으로 갈랐다 (지시 §9 — X 전체를 다시 찾지 않는다). 기여 = `X − (그 성분 뺀 것)`.

| 성분 | 2022 | 2023 | **2024** | 평균 | 2022+2024 |
|---|---:|---:|---:|---:|---:|
| 같은손 `sh` | +1.9 | −1.1 | **+9.6** | +3.5 | +5.8 |
| 볼−스트라이크 `bs` | +1.6 | +2.6 | **+7.0** | +3.7 | +4.3 |
| 카운트우위 `adv` | +0.9 | +3.2 | +4.5 | +2.9 | +2.7 |
| 주자유무 `onb` | −1.1 | −1.6 | +4.6 | +0.6 | +1.8 |
| `cur_succ` 쪽 4개 | +3.0 | −1.3 | +6.9 | +2.9 | +4.9 |
| `cur_mid` 쪽 4개 | **−11.1** | +3.5 | +6.3 | −0.4 | −2.4 |

**2024 에서 여덟 열이 전부 양수 기여다** (X 905.2 vs 뺀 것들 895.6~900.7).
그리고 2024 marginal 의 합이 4.5+4.6+9.6+7.0 = **25.7** 로 X 의 전체 이득
27.5 와 거의 같다 — **성분들이 사실상 직교**하고 가지치기로 얻을 것이 없다.

> **`XM-succ만`(mid 곱항 제거)이 2022 에서 2466.3 으로 X 보다 +11.1** 이다.
> 이번 세션 단일 최대값이지만 **2024 에서 −6.3** 이라 기각한다. 평가셋과 가장
> 가까운 폴드에서 지는 구성을 건강한 폴드 하나 보고 고르는 것이 §18 에서 F 를
> 태운 실수와 같은 형태다.

**소득은 방향이다** — 2024 주력은 `같은손`(+9.6)과 `볼−스트라이크`(+7.0)다.
확장은 이 둘로만 한다 (STEP 8).

### 22-e. 신뢰도 게이팅 3종 — 전부 기각. 그리고 X 가 통한 이유가 드러난다

`cur_{succ,mid,ball,rev,str} x f(cur_n)` 5열을 현 Champion 위에 얹었다.

| 구성 | 2022Δ | 2023Δ | **2024Δ** | 평균Δ | 2024 배수 |
|---|---:|---:|---:|---:|---:|
| G1 `n/(n+500)` | −2.0 | +5.8 | **−3.9** | −0.0 | 0.9957 |
| G2 `sqrt(n)/(sqrt n+sqrt 500)` | +2.4 | +2.1 | **−0.5** | +1.3 | 0.9995 |
| G3 `log1p(n)` | +1.5 | +3.7 | **−2.8** | +0.8 | 0.9969 |

셋 다 2024 에서 음수이고 평균 증분이 폐기선(+1) 아래다. **축을 닫는다.**

### 22-f. 🚩 왜 X 는 통하고 게이팅은 안 통하는가 — 이득은 **이산적 체제 전환**에서 온다

둘 다 "D × 무언가" 곱항인데 결과가 정반대다 (X 2024 +27.5, 게이팅 −0.5~−3.9).

- **신뢰도 곱항**은 `cur_n` 의 **매끄러운 단조 함수**다. 트리가 `cur_n` 으로 한 번
  쪼개고 `cur_rate` 로 쪼개면 2단 분할로 근사된다 — 이미 할 수 있는 것을 열로 준
  셈이라 잡음만 는다.
- **맥락 곱항**은 지시자가 **절반의 행에서 값을 0 으로 꺼버리는 체제 전환**이다.
  트리가 흉내내려면 맥락 분기마다 `cur_rate` 서브트리를 통째로 복제해야 하고
  `depth=6` 에서는 비싸다.

**표현이 부족한 곳은 매끄러운 재가중이 아니라 이산적 전환이다.** 이 명제는
검증 가능한 예측을 낳는다 — 강한 축(같은손 +9.6, 볼−스트라이크 +7.0)에 **이산
전환을 더 주면 더 오르고, 매끄러운 곱은 안 오른다.** STEP 8 이 이 시험이다.

### 22-g. 🚩 STEP 8 — §22-f 의 예측이 맞았다. **H1 수준확장**이 세션 최강

| 구성 | 2022Δ | 2023Δ | **2024Δ** | 평균Δ | worst |
|---|---:|---:|---:|---:|---:|
| **H1 수준확장** | **+5.4** | **+7.8** | **+15.3** | **+9.5** | **+5.4** |
| H2 카운트세분 | +3.1 | −1.3 | −1.7 | +0.0 | −1.7 |

H1 = `cur_{ball,rev,str} × {같은손, 볼−스트라이크}` 6열을 Champion 위에 추가.
H2 = `cur_{succ,mid} × {볼수, 스트라이크수, 2스트라이크}` 6열.

**§22-f 의 명제가 검증됐다** — 이득은 전환을 **더 잘게 쪼개는 데(H2)** 있지
않고 **더 많은 수준에 붙이는 데(H1)** 있다. X 는 `cur_succ`·`cur_mid` 두 수준에만
맥락을 곱하고 있었고, 투수의 **볼 비율·반대투구 비율·스트라이크 비율**에도 같은
국면 의존성이 있었다.

### 22-h. 그런데 전이 가설과 충돌한다

```
H1 의 2024 이득  920.5/905.2 = +1.69%   <   손익분기 2.34%
가설대로면       0.851 x (1.69 - 2.34)% = -0.55%  ->  LB 약 -5.7
```

**로컬로는 강한 후보(§12 기준 평균 +9.5), 전이로는 미달**이다. §21 의 식은 세 점
적합이라 법칙이 아니지만, F 를 정확히 예측한 유일한 모형이기도 하다. 이 충돌을
숨기지 않고 그대로 보고한다.

확장을 한 칸 더 밀어(`H3`) 2024 이득이 손익분기를 넘는지 본다 — H1 의 방향이
맞다면 3수준 x 4맥락으로 완성했을 때 더 커져야 한다.

### 22-i. H3 로 확장을 한 칸 더 — **포화 확인**

| 구성 | 2022Δ | 2023Δ | **2024Δ** | 평균Δ | worst | 2024 이득 |
|---|---:|---:|---:|---:|---:|---:|
| H1 수준확장 (6열) | +5.4 | +7.8 | **+15.3** | +9.5 | +5.4 | **+1.69%** |
| H3 3수준×4맥락 (12열) | +8.3 | +12.9 | +13.5 | +11.5 | +8.3 | +1.49% |

열을 두 배로 늘리면 2022·2023 은 오르는데 **2024 는 내려간다.** 이 축은 2024
기준 +1.5~1.7% 에서 **포화**한다.

### 22-j. 🚩 이번 세션의 결정적 충돌 — 로컬은 강한데 손익분기 아래다

```
H1  2024 이득 +1.69%  <  손익분기 2.34%   ->  가설 LB 1038.95
H3  2024 이득 +1.49%  <  손익분기 2.34%   ->  가설 LB 1037.21
                                              현 Champion 1044.7656
```

§12 로컬 기준으로는 둘 다 강한 후보다 (H3 평균 +11.5 는 "매우 강한" 등급, 3/3,
worst +8.3). 그런데 **LB 를 실제로 예측해 온 지표는 2024 폴드 하나**이고 거기서
둘 다 미달이다.

**제출 권고는 "하지 않는다".** §21 의 식은 세 점 적합이라 법칙이 아니지만, F 를
오차 0.14 점으로 맞힌 유일한 모형이고, 그것을 무시하고 통과시켰던 것이 §18 의
실수다. 같은 형태를 반복하지 않는다.

보존 후보는 **H1** 을 고른다 (§16 STEP 11, 하나만) — 우리가 가진 LB 관측 세 개를
전부 예측한 것이 2024 폴드이고 H1 이 거기서 앞서며, 열이 절반이라 희석 위험이
낮다. H3 의 수치는 위에 남겨 두었으니 필요하면 12열판으로 다시 만들면 된다.

### 22-k. 🚩 정정 — 전이식에는 **생산 경로** 이득을 넣어야 한다

`cand_asof_xl.zip` 빌드에서 두 경로가 갈렸다.

```
캐시 경로   905.2 -> 920.5   +1.69%   (후처리 편차 4축 없음)
생산 경로   924.4 -> 944.0   +2.12%   (후처리 편차 포함)
```

X 때는 두 경로가 소수점까지 일치해서(1.0278 / 1.0278) 구분할 필요가 없었다.
H1 은 갈린다 — **편차 4축이 들어간 기준선 위에서 이득이 더 크다.**

§21 의 계수는 **세 점 전부 생산 경로 홀드아웃 비율**로 적합됐다
(D 796.8→899.4, F 899.4→911.6, X 899.4→924.4). 따라서 생산 경로 값을 넣는다.

| 넣은 값 | 가설 LB | Champion 대비 |
|---|---:|---:|
| 캐시 +1.69% (잘못) | 1038.99 | −5.78 |
| **생산 +2.12% (맞음)** | **1042.81** | **−1.95** |

방향은 그대로지만 크기가 −5.8 에서 **−2.0** 으로 줄어든다. 손익분기 바로 아래의
접전이고, "명확한 손해"가 아니라 "기대값이 살짝 음수"다.

### 22-l. 후보 `submissions/cand_asof_xl.zip` — 빌드·검증 완료, **제출 안 함**

```
82피처 = 기본47 + ctx8 + D 13 + 맥락 8 + 수준확장 6
2024 홀드아웃 (생산)  대조 924.4 -> 944.0   배수 1.0212
클린 아핀  alpha 1.090000  center 0.585445  (평가셋 정보 미사용)
verify 4단계 통과 (추론 5.7초/600초, 범위 0.3875~0.5650)
실제 test.csv — lx_ball_sh 손계산 대조 최대차 0.000e+00, NaN 0
Champion 무결성 — 24회차 76p / 22회차 68p, 둘 다 lx 컬럼 없음
```

로컬 3/3 (평균 +9.5, worst +5.4) 인데 생산 경로 2024 이득 2.12% 가 손익분기
2.34% 를 못 넘는다. **§15 대로 제출하지 않고 보존만 한다.**

---

## 23. 🚩 25회차 **1049.9226** — 제출했고 올랐다. 그리고 §21 의 식이 깨졌다

```
24회차 (X)        1044.7656
25회차 (X+H1)     1049.9226    +5.16   <- 새 최고
가설 예측          1042.81      (-1.95)  ->  오차 +7.11
```

**§22-j 에서 내가 "제출하지 말라"고 한 판단이 틀렸다.** 그대로 따랐으면 +5.16 을
버렸다. 게이트가 과하게 보수적이었다.

### 23-a. 단조 모형이 깨진 지점

| 축 | 생산 2024 이득 | LB 이득 | 겉보기 전이 |
|---|---:|---:|---:|
| D 현재상태 | +12.87% | **+8.966%** | 70% |
| F 최근vs시즌 | +1.36% | −0.821% | −60% |
| X D×맥락 | **+2.78%** | +0.375% | 13% |
| H1 수준확장 | **+2.12%** | **+0.494%** | 23% |

**X 는 로컬이 더 큰데 LB 이득이 더 작다.** 로컬 2024 이득 하나로 LB 이득이
정해진다는 가정이 성립하지 않는다.

### 23-b. 4점 최소제곱 재적합 — 계수는 비슷한데 산포가 크다

```
k = 0.830   c = 2.07%      (기존 2점 적합 k=0.851 c=2.34%)
잔차  D -0.03 / F -2.42 / X -2.25 / H1 +4.71  (LB 점 환산)
잔차 RMS = 2.9점
```

**손익분기 자체는 2.1% 근처로 유지된다** — 세 점이든 네 점이든 비슷하게 나오고,
F(1.36%)가 아래라 실패한 것도 그대로 설명된다. 바뀐 것은 **정밀도**다.
예측 오차가 ±0.1 점인 줄 알았는데 **±3 점**이다.

### 23-c. 그래서 게이트를 이렇게 고친다

```
로컬 2024 이득 < 2.1%          기대값 음수. 강한 다른 근거 없으면 버린다
2.1% ~ 3%                    기대 +0~1 점이지만 산포가 ±3 점.
                             로컬 3/3 이고 worst 가 양수면 **낼 만하다**
> 3%                         명확한 후보
```

§22-j 에서 나는 2.12% 를 "손익분기 아래"로만 읽고 3/3·worst +5.4 라는 신호를
무시했다. **산포가 ±3 점인 모형에서 −2 점 예측은 기각 근거가 못 된다.**

### 23-d. 남은 것

D×맥락 계열은 §22-i 에서 포화가 확인됐다 (H3 12열이 2024 에서 H1 6열보다 낮다).
1100 까지 −50.1 이고, 재적합 계수로 환산하면 로컬 2024 이득 **8.1%** 가 더
필요하다. D 가 12.87%, X 가 2.78%, H1 이 2.12% 였다.

### 22-m. §12 공식 컬럼 감사 — **미검증 컬럼 0개**

| | |
|---|---|
| `test.csv` 공식 컬럼 | 48 (`row_id` 포함) |
| Champion 이 쓰는 공식 컬럼 | **47 = 전부** |
| 안 쓰는 공식 컬럼 | **0** |
| `train.csv` 전용 | `control_success` (타깃) |
| 파생 피처 | 35 (TrackMan 8 + D 13 + X 8 + H1 6) |

**"아직 한 번도 검증되지 않은 공식 컬럼"은 존재하지 않는다.** 새 정보는 기존
컬럼의 **재구성**에서만 나온다.

### 22-n. 타자 현재상태 복원 검증 — P1 의 전제 확인

P1(투수 현재상태 × 타자 현재상태)이 성립하려면 타자 쪽도 D 와 **같은 방식의
대수적 복원**이어야 한다. 확인했다.

```
cur_bsucc = (asof_batter_success_rate x asof_batter_n - prior_events[batter]) / cur_n
cur_n     = asof_batter_n(행) - prior_n[batter],  0 클립
prior     = 학습 데이터만으로 만든 batter_id 별 상수 (번들 내장)
```

| 검사 | 타자 | (투수, §17-b) |
|---|---|---|
| `cur_n` = 실제 시즌내 순번 | **100.0000%** (음수 0%) | 100.0000% |
| `cur_rate` 복원 평균절대오차 | **4.35e-03** | 3.1e-06 |
| test 행 하나만 넣어도 값 동일 | **YES** | YES |

> **타자 쪽 rate 정밀도가 투수보다 3자리 낮다.** `cur_n` 은 완전 일치하므로
> 구조는 맞고, 차이는 같은 시각 이벤트의 행 순서 정렬로 보인다. 비율 0.46 기준
> 약 1% 라 신호를 죽일 크기는 아니지만 **동등한 정밀도는 아니다.**

따라서 P1 은 "기존 `asof_batter_*` 를 current state 라 부르는 것"이 아니라
**진짜 복원값끼리의 결합**이다. 진행한다.

### 22-o. A / B2 / C — 전부 기각. 그리고 **2024 포화**의 형태가 보인다

기준선 = Champion 82p (2460.6 / 189.6 / 920.5 — H1 측정 때와 동일하게 재현).

| 구성 | 열 | 2022 | 2023 | **2024** | 평균 | worst |
|---|---:|---:|---:|---:|---:|---:|
| A gap×맥락 | +10 | +1.4 | +3.0 | **−3.3** | +0.37 | −3.3 |
| B2 타자×맥락 | +8 | −0.4 | +4.1 | **−2.9** | +0.27 | −2.9 |
| C 결합 | +18 | +5.9 | +3.4 | **−4.1** | +1.73 | −4.1 |

- **A** — 폼 변화량(`cur − prior`)은 맥락 조건부로도 안 산다. §18 에서 주효과로
  기각(E, min 0.9679)됐던 것이 맥락을 곱해도 2024 에서 음수다.
- **B2** — X/H1 을 타자 쪽으로 옮긴 것인데 2024 −2.9. §22-n 이 원인을 시사한다:
  타자 `cur_rate` 복원 정밀도가 투수보다 3자리 낮고(4.4e-3 vs 3.1e-6) 그 잡음이
  맥락으로 쪼개지며 증폭된다.
- **C** — 2022 에서 +5.9 로 A(+1.4)+B2(−0.4)보다 크니 상호작용은 있다. 그러나
  2024 −4.1 이라 방향 조건에서 탈락.

> 🚩 **반복되는 형태 — 2022·2023 은 오르고 2024 만 내려간다.** A·B2·C 가 전부
> 그렇고 앞선 G1·G2·G3·H2 도 같았다. **Champion 이 2024 에서 포화**했다는
> 신호이고, 2024 는 평가셋과 가장 가까운 폴드다.

### 22-p. P1~P4 (대형 독립축 후보) — 전부 기각

기준선 = Champion 82p (2460.6 / 189.6 / 920.5).

| 구성 | 열 | 2022 | 2023 | **2024** | 평균 |
|---|---:|---:|---:|---:|---:|
| P1 투수상태×타자상태 | +8 | −6.3 | −4.3 | **−12.7** | −7.8 |
| P2 상태 프로파일(shape) | +9 | −3.2 | +2.9 | **−3.2** | −1.2 |
| P3 상태 기하(거리·코사인) | +4 | +3.3 | −3.7 | **−1.5** | −0.6 |
| P4 상태 전이(비율·방향) | +8 | −3.3 | +2.6 | **−7.1** | −2.6 |

**P1 이 −12.7 로 세션 최악이고, §22-n 이 그 이유를 미리 말해 준다.** 타자
`cur_rate` 복원 정밀도가 투수보다 3자리 낮은데(4.4e-3 vs 3.1e-6) **두 값을 곱하면
잡음도 곱해진다.** 차항을 쓴 B2 가 −2.9, 곱항을 쓴 P1 이 −12.7 로 4배 이상 나쁜
것이 이 설명과 맞는다.

### 22-q. 🚩 누적 패턴 — **2024 폴드 부호가 LB 방향과 예외 없이 일치한다**

Champion 이 82p 가 된 뒤 시험한 7개(A·B2·C·P1~P4)가 **전부 2024 음수**다.
76p 시절의 G1·G2·G3·H2 까지 더하면 **11개 연속**이다.

그동안 2024 에서 양수를 낸 것은 **X(+27.5)와 H1(+15.3) 둘뿐**이고, 그 둘은 실제
LB 에서 각각 **+3.90 / +5.16** 을 냈다.

| 2024 폴드 | 구성 수 | 실제 LB |
|---|---:|---|
| 양수 | 2 (X, H1) | 둘 다 상승 |
| 음수 | 11 | 제출한 것 없음 (F 만 예외적으로 +1.36%였고 LB −8.55) |

**2024 폴드 부호를 1차 게이트로 쓰는 것이 지금까지 100% 맞았다.** 크기는 못
맞히지만(§23 에서 전이식이 깨졌다) 방향은 틀린 적이 없다.

### 22-r. HB (계층적 current-state 재추정) — **종료.** 분산 축소는 이득이 없다

`k` 를 고정 500 이 아니라 **시즌별 `<g` 데이터에서 적률법으로 추정**하고
(`k = mu(1-mu)/sigma^2_between`), 부모도 두 종류를 썼다.

| 구성 | 2022 | 2023 | **2024** | 평균 |
|---|---:|---:|---:|---:|
| HB-eb 교체 (부모=선수 prior) | −7.1 | +6.2 | **−2.9** | −1.3 |
| HB-hand 교체 (부모=투수손 평균) | −5.6 | +8.2 | **−0.4** | +0.7 |
| HB-add 추가 (원시 유지 + 축소본) | **+8.6** | **+9.5** | **−0.1** | +6.0 |

**교체는 EB 로 정교화해도 손해다.** §20-c 의 `S500R`(고정 k, min 0.5633)보다는
훨씬 덜 나쁜데, EB 가 `k` 를 작게 잡아 원본에 가깝게 둔 덕이다. 방향은 같다 —
**원시 `cur_rate` 의 분산은 잡음이 아니라 신호다.** 표본이 적은 투수의 극단적인
시즌내 비율 자체가 정보이고, 부모로 당기면 깎인다.

**HB-add 는 평균 +6.0 이지만 2022(+8.6)와 2024(−0.1)의 방향이 다르다.** 판정
규칙(2024 < 0 이면 종료)에 걸린다. 2024 의 −0.1 은 시드 노이즈(±7.2)의 1/70 이라
"해롭다"가 아니라 **"2024 에서 효과 없음"** 이고, 2022·2023 에서만 오르는 형태는
이번 세션 내내 반복됐으며 LB 로 전이된 적이 없다.

> **결론 — current-state 추정량의 분산 축소 축을 닫는다.** `k` 추정 방식도,
> 부모 선택도, 교체/추가 여부도 전부 재봤다.

### 22-s. TG 정보중복 감사 — TG1~TG4 는 실행하지 않는다

| 후보 | 대수적 정체 | 기존 위치 | 판정 |
|---|---|---|---|
| TG-1 `baseline(context)` 피처 | 이산 상황열의 조회표 | 그 열들이 전부 원시 피처. 트리는 저차원 이산 조회표를 정확히 표현한다 | 복원 가능 |
| TG-1' 잔차 타깃 | 잔차 재학습 | **§6-g** — 트리 최적 가중 **0/3**, 잔차 상관 0.003~0.009 | 측정 완료 |
| TG-2 잔차 위 투수 상태 | 위에 D 재투입 | §6-g 가 55피처+편차항 전부 주고 0 | 측정 완료 |
| TG-3 잔차 위 타자 정보 | 위에 타자항 | §6-g + B2(−2.9) + P1(−12.7) | 측정 완료 |
| TG-4 능력×난이도 | `cur_succ × baseline` (연속 스칼라 곱) | §22-e 의 G1·G2·G3 가 연속 곱항으로 전부 2024 음수 | 같은 계열 |
| **TG-5 margin** | `cur_succ − baseline(context)` | 차항이라 트리가 직접 못 만든다. X/H1 과 대수적으로 다르고 §6-g 의 타깃 변환과도 다르다 | **유일한 신규** |

§1("새 정보가 아닌 것은 실행하지 않는다")에 따라 **TG-5 만 실행한다.**

### 22-t. TG-5 (margin = 능력 − 상황 요구수준) — 기각. 그리고 이 세션의 종결

```
base(context) = E[y | 볼, 스트라이크, 아웃, 주자유무, 같은손]   144셀, 워크포워드
margin = cur_succ - base(context),  그리고 margin x {같은손, 볼-스트라이크, 우위, 주자}
```

| | 2022 | 2023 | **2024** | 평균 |
|---|---:|---:|---:|---:|
| TG5 margin (+5열) | +0.6 | +1.4 | **−0.7** | +0.43 |

폐기선(+1) 아래이고 2024 음수다. **TG family 종료.**

### 22-u. 🚩 세션 종결 — 12개 구성, **2024 양수 0개**

| family | 구성 | 최고 평균 | 2024 양수 |
|---|---:|---:|---:|
| A / B2 / C (gap·타자맥락·결합) | 3 | +1.73 (C) | 0 |
| P1~P4 (투수×타자·프로파일·기하·전이) | 4 | −0.6 (P3) | 0 |
| HB (계층 재추정 3종) | 3 | +6.0 (HB-add) | 0 |
| TG-5 (margin) | 1 | +0.43 | 0 |
| **합계** | **11** | — | **0** |

누적하면 Champion 이 76p→82p 로 올라온 뒤 시험한 것이 **19개이고 그중 2024
양수는 X와 H1 둘뿐**이며, 그 둘은 실제 LB 에서 각각 +3.90 / +5.16 을 냈다.

**기록 — 47개 공식 컬럼 + D/X/H1 파생 구조에서 큰 독립축이 더 이상 보이지 않는다.**

닫힌 것을 한 줄로 정리하면:

- **맥락 곱항** — X/H1 로 회수 완료. 더 잘게(H2)도, 더 많이(H3)도, 타자 쪽(B2)도,
  투수×타자(P1)도 안 된다
- **변화량** — 주효과(E)·맥락 조건부(A)·비율/방향(P4) 전부 음수
- **분산 축소** — 교체(S500R, HB-eb, HB-hand)는 손해, 추가(S500, HB-add)는 2024 에서 0
- **잔차 구조** — §6-g 에서 트리 최적 가중 0/3 으로 이미 상한이 0
- **상황 난이도 분해** — 조회표는 트리가 표현 가능(TG-1~4), 차항(TG-5)은 +0.43

> **다음은 이 축들의 정제가 아니다.** §HB와TG가모두실패하면 조항대로
> (1) 생성 메커니즘의 비선형성 (2) target noise 구조 (3) calibration 자체
> (4) 규칙 내 외부 데이터 — 이 넷으로 넘어가야 한다. 다만 (4)는 규정 3)으로
> 금지돼 있고(§6-l), (3)은 §17-i 에서 아핀 여지가 최대 8점으로 측정됐다.

### 22-v. EB 5축 leave-one-out — 어느 축도 2024 에서 일하지 않는다

기여 = `HB-add 전체 − (그 축 뺀 판)`. HB-add 전체 이득은 2022 +8.6 / 2023 +9.5 /
2024 −0.1 이었다.

| 축 | 2022 | 2023 | **2024** |
|---|---:|---:|---:|
| succ | **+20.1** | **+6.3** | **+0.7** |
| mid | +1.8 | −4.2 | +3.0 |
| ball | +10.2 | −1.6 | +1.5 |
| rev | +2.3 | −4.2 | +3.8 |
| str | +1.2 | +1.3 | −0.2 |
| (합) | +35.6 | −2.4 | +8.8 |

**세 가지가 드러난다.**

1. **`succ` 기여가 +20.1 → +6.3 → +0.7 로 최근 폴드로 갈수록 단조 감소한다.**
   EB 축소는 2022 시기의 현상이고 평가셋에 가까워질수록 사라진다.
2. **2024 에서는 다섯 축 전부 시드 노이즈(±7.2) 안이다.** HB-add 의 2024 −0.1 은
   상쇄가 아니라 **애초에 아무도 일하지 않아서**다.
3. **축별 순위가 폴드마다 뒤집힌다** — 2022 는 succ(+20.1) > ball(+10.2), 2024 는
   rev(+3.8) > mid(+3.0) > ball(+1.5) > succ(+0.7). 안정적 기여가 없다는 뜻이다.

> **2022 합(+35.6)이 전체 이득(+8.6)의 4배다.** leave-one-out 이 이렇게 부풀면
> 축들이 서로 **중복**된다는 뜻이다 — 하나를 빼도 나머지가 역할을 대신하므로 각
> "빠졌을 때 손실"이 과대평가된다. §22-d 의 X 성분 분해에서 합 25.7 ≈ 전체 27.5 로
> **거의 직교**였던 것과 정반대다.

**따라서 이 표의 값은 standalone 효과가 아니다.** `succ` 단독 구성(83p)은 아직
측정된 적이 없고, 별도로 잰다 (`exp/run_exp.py --only champ_ebsucc`).

### 22-w. 실행 최적화 레이어 (`exp/blocks.py`, `exp/run_exp.py`)

`d_decomp.py` 는 실행마다 앞단을 통째로 다시 탄다 — `build_state_lag` ×3,
등가성 검증 `build_state` ×1, `eb_shrunk` ×5, 상황 기준선. `--only` 로 구성 하나만
재도 붙고 실측 2~4분이다. 게다가 `ADD` 가 **필터 전에** 후보 25개 행렬을 전부
만든다 (구성당 ~160MB → 수 GB). 관측된 RAM 3.3GB 의 상당 부분이 이것이다.

새 드라이버는

```
RAW -> 블록 캐시(디스크 npy) -> 필요한 블록만 mmap -> 후보 = 블록 조합 -> 모델
```

- 블록 파일명에 **정의 태그**를 박아 조용한 오염을 막는다 (`D__asof13_v1.npy`)
- 후보는 **필터 후에만** 조합한다
- `thread_count` 를 여유 RAM 으로 자동 결정, 2GB 미만이면 학습을 시작하지 않는다
- 실행마다 `wall_s / cpu_s / rows / features / seeds / threads` 를
  `exp/run_resources.jsonl` 에 남긴다

**모델 정의·폴드·시드·하이퍼파라미터는 그대로다.** 바뀌는 것은 같은 값을 몇 번
계산하느냐뿐이므로 결과가 달라지지 않는다. `d_decomp.py` 는 기존 결과의 기록으로
남기고 건드리지 않는다.

---

## 24. 🚩 26회차 1045.1996 (−4.72) — 그리고 내가 적은 원인 설명이 틀렸다

```
캐시 경로 (후처리 없음, 6시드)   +5.5   (+0.60%)
생산 경로 (후처리 포함, 1시드)   -8.2   (-0.87%)
실측 LB                        -4.72  (-0.45%)
```

### 24-a. 원인 정정 — 후처리 중복이 아니다

§22 에 "K2 가 편차 3번축 `dev(플래툰x투수유리x카운트)` 와 중복돼서 갈렸다"고
적었다. **직접 검정해서 기각한다.**

| | 후처리 없음 | 후처리 포함 |
|---|---:|---:|
| champ | 916.3 | 939.0 |
| champ_k2 | 927.8 | 950.5 |
| **증분** | **+11.5** | **+11.5** |

**후처리를 넣어도 증분이 정확히 그대로다.** 다른 5개 구성(ebsucc·regB·regC·noL·noX)
에서도 증분 변화가 최대 3.2 였다. 후처리는 모든 구성에 거의 균일하게 +22.7 을
더할 뿐 **구성 간 우열을 바꾸지 않는다.**

### 24-b. 🚩 진짜 원인 — 생산 경로 홀드아웃이 **시드 1개**다

`build_asof.py` 의 2024 홀드아웃 비교는 `pipeline(features, 42)` 하나로만 잰다.
K2 의 시드쌍별 값은 **+11.5 / −5.2 / +10.2** 로 흔들린다. 단일 시드가 −8.2 를
내는 것은 그 분산 안이다.

**이것은 판정 절차의 결함이다.** 분산이 큰 후보를 우연히 죽이거나 살릴 수 있다.
K2 를 보류한 판단은 결과적으로 맞았지만(LB −4.72) **근거가 틀렸다.**

> **고칠 것 — 생산 경로 홀드아웃도 최소 2시드로 잰다.** 지금까지 D·F·X·H1·K2 의
> 생산 경로 배수가 전부 단일 시드였으므로, 그 수치들에도 같은 크기의 불확실성이
> 실려 있다.

### 24-c. 전이식 3차 붕괴 — 이제 폐기한다

| 축 | 로컬 2024 | LB 이득 | 비율 |
|---|---:|---:|---:|
| D | +12.87% | +8.97% | 0.70 |
| F | +1.36% | −0.82% | −0.60 |
| X | +2.78% | +0.37% | 0.13 |
| H1 | +2.12% | +0.49% | 0.23 |
| K2 | −0.87% | −0.45% | 0.52 |

4점 적합(k=0.830, c=2.07%)의 K2 예측이 1024.3, 실측 1045.20 으로 **오차 +20.9**.
5점 재적합은 `k=0.751, c=1.37%`, 잔차 RMS **7.8점** (4점일 때 2.9점).

**|로컬| < 3% 에서 비율이 −0.60 ~ 0.52 로 흩어진다. 부호조차 예측 불가다.**
D(+12.87%)만이 예측 가능하게 전이됐다. **로컬 수치로 ±3% 미만 후보의 우열을
논하지 않는다.**

---

## 25. 🚩 리더보드 구조 — 우리는 고원 아래 108점에 있다

2026-08-16 관측 (상위 10만 보임. 제출 횟수·참가자 수·백분위는 확인 불가).

```
1   1291.12   한 시간 전
2   1240.63   12시간 전      -50.49
3   1176.55   3일 전         -64.08   <- 절벽
4   1171.19   9~19분 전       -5.36
5   1170.70   16시간 전       -0.48
6   1164.15   14시간 전       -6.56
7   1163.62   14시간 전       -0.53
8   1163.04   18시간 전       -0.58
9   1161.04   17시간 전       -2.00
10  1157.96                  -3.08
```

**3~10위가 18.6점 안에 몰려 있다.** 8팀이 1160~1177 에 고원을 이룬다 —
**여러 팀이 독립적으로 도달한 공통 해법이 있다**는 뜻이다. 1·2위만 그 위에 있다.

```
우리 1049.9226
10위까지  +108.04   (rho^2 +10.3%,  rho +5.02%)
3위까지   +126.63   (rho^2 +12.1%)
1위까지   +241.20   (rho^2 +23.0%)
```

### 전략 함의

우리 최대 발견인 **D 가 +8.97%** 였다. **10위권에 들려면 D 급이 정확히 한 번 더
필요하다.** 밤새 시험한 +0.2~0.5% 짜리를 아무리 모아도 안 된다.

**목표를 1160 으로 잡는다** — 8팀이 도달한 지점이라 "아무도 못 한 것"이 아니라
**"우리만 아직 못 한 것"** 이다.

### 우리가 스스로 좁혀 놓은 곳 — TrackMan

TrackMan 컬럼은 `rel_speed, spin_rate, induced_vert_break, horz_break,
extension, rel_height, rel_side, zone_speed` 인데, 우리는 **앞 4개의 평균 편차
8열**만 쓴다.

- `extension` · `rel_height` · `rel_side` 는 **아예 안 쓴다**
- **분산을 어떤 형태로도 안 쓴다** — 8열이 전부 평균이다

**타깃이 "제구"이고 커맨드의 물리적 정의는 릴리스 반복성이다.** 같은 지점에서
같은 궤적으로 반복하는 투수가 제구가 좋다. `sd(rel_height)`·`sd(rel_side)` 는
그것을 직접 재는 값인데 피처에 없다.

§6-o 가 닫은 것은 **행 단위 조인**이지 집계 피처가 아니다. §4-9 의 편차 방식은
이미 검증된 경로다.

### 25-a. TR-VAR (릴리스/구질 반복성) — 기각. 내가 제안한 방향이 틀렸다

§25 에서 "가장 큰 미탐색 영역"으로 지목한 축이다. 만들어서 쟀다.

```
투수별 7열, 구종 안에서 sd 를 재고 구종 표본수로 가중평균, 워크포워드
  sd(rel_height) sd(rel_side) sd(extension)      릴리스 반복성
  sd(spin_rate) sd(ivb) sd(horz_break)           구질 반복성
  log1p(참조 투구수)
커버리지 79~86%
```

| | 2022 | 2023 | 2024 | 평균 |
|---|---:|---:|---:|---:|
| champ_trvar (89p) | +5.6 | −3.8 | **+3.3** | +1.7 |

2024 +3.3 은 Champion 자체의 시드 변동(916.3 / 919.5 / 919.1 → ±3.2)과 같은 크기다.

### 🚩 기각 이유는 중복이 아니라 **신호 자체가 약하다**

| TR-VAR 열 | asof_succ 와 상관 | pitcher_id 와 상관 | **타깃과 상관(2024)** |
|---|---:|---:|---:|
| sd_relH | 0.018 | 0.016 | **−0.0115** |
| sd_relS | 0.043 | 0.011 | −0.0053 |
| sd_ext | 0.208 | 0.116 | +0.0092 |
| sd_spin | 0.109 | 0.063 | −0.0049 |

**기존 피처와 겹치지 않는다** (0.02~0.2). 완전히 독립적인 정보인데 **타깃과의
상관이 0.01 수준**이다. Champion 의 `rho` 가 0.096 이니 한 자릿수 작다.

부호는 가설대로다 — `sd_relH` −0.0115 로 **릴리스 높이 산포가 클수록 제구 실패**.
야구적으로 맞는 방향인데 크기가 없다.

### 왜 작은가 — 시간 해상도가 다르다

**릴리스 산포는 투수의 연간 평균 특성이고 제구 성공/실패는 투구 하나의 사건이다.**
시즌 평균 산포 0.049 인 투수와 0.061 인 투수의 차이는 실재하지만, **그 투수의
특정 투구 하나가 성공할지는 거의 결정하지 못한다.**

D 와 대비하면 분명하다 — D 는 **그 시즌 그 시점의 상태**를 줬고 TR-VAR 은
**6년치 평균 특성**이다. 그리고 **TrackMan 이 2024 에서 끝나므로 2025 의 시간
변화는 원리적으로 못 담는다.** 이 축은 구조적으로 상수일 수밖에 없다.

> **교훈 — "미탐색"과 "유망"은 다르다.** 새 정보원이라는 것만으로는 부족하고,
> **타깃과 같은 시간 해상도**를 가져야 한다. 지금까지 통한 것(D·X·H1)은 전부
> 행 단위로 값이 변한다. 투수 상수는 이미 `asof_*` 요약이 담고 있다.

---

## 26. 야구 메커니즘 감사 — 그리고 두 가설의 기각

### 26-a. 타깃 정의가 제약을 만든다

```
제구 실패 = (1) 존 가운데 부근  (2) 존에서 크게 벗어남  (3) 포수 요구와 반대
```

**순수하게 "공이 어디로 갔는가"다.** 심판 판정·타자 스윙·파울·타구 결과는
**타깃에 안 들어간다.** 타자는 오직 **투수의 겨냥을 바꾸는 경로**로만 영향을 준다.

야구 예측의 보통 큰 축인 "타자 성향"과 "심판 존"이 여기서는 간접 효과만 갖는다.

### 26-b. 메커니즘 분해

```
제구 성공 = f( 어디를 겨냥했는가 , 겨냥한 곳에 얼마나 정확히 갔는가 )
              └ 전략(행 단위 변동) ┘   └ 능력(느리게 변동) ┘
```

**투수의 실제 겨냥 위치는 47개 컬럼 어디에도 없다.** 이것이 근본 한계다.
`catcher_id` 도 없다.

### 26-c. 🚩 왜 D/X/H1 만 성공했는가

| 성공 | 야구적 정체 |
|---|---|
| D | "이 투수가 **이번 시즌** 얼마나 정확한가" — 실행 정확도의 **현재값** |
| X | 그 정확도가 **어떤 전략 상황**에서 발휘되는가 |
| H1 | 위를 **실패 유형별**로 |

| 실패 유형 | 사례 | 이유 |
|---|---|---|
| 투수 상수 | TR-VAR, prior 계열 | `asof_*` 가 이미 담고 있고 행 단위로 안 변한다 |
| 두 잡음의 차 | F, P1, gap 계열 | 표본 작은 두 추정량의 차는 잡음이 곱해진다 |
| 트리가 만들 수 있는 것 | G1~G3, REGIME B | 카디널리티가 낮거나 단조라 분할로 근사된다 |

**성공한 축은 전부 "이 투수가 지금 이 상황에서"를 표현한다.**

### 26-d. 플래툰 추세 보정 — 기각. 모델 진단이 틀렸다

**데이터 증거는 진짜였다.**

```
같은손 격차   2019 +0.0069  2020 +0.0173  2021 +0.0175
             2022 +0.0134  2023 +0.0267  2024 +0.0306
```

**그러나 두 가지가 틀렸다.**

1. **"단조 증가"가 아니다.** 2022 에서 한 번 꺾인다. 3점 선형 외삽이 그 지점을
   지나면 기울기가 음수가 된다 — 폴드 2023 의 delta 가 **−0.0016 으로 부호 반전**.

| 폴드 | 학습 시즌 | 평균 격차 | 외삽 격차 | delta |
|---|---|---:|---:|---:|
| 2022 | 2019~21 | 0.0139 | 0.0244 | +0.0106 |
| 2023 | 2019~22 | 0.0138 | 0.0122 | **−0.0016** |
| 2024 | 2019~23 | 0.0163 | 0.0284 | +0.0121 |

2. 🚩 **"균등 학습이라 모델은 평균만 배운다"가 틀렸다.** 2024 폴드에서 실제 격차
   0.0306 을 모델이 **0.0257(84%)** 재현한다. `season` 컬럼이 있고 최근 시즌 행이
   더 많으며 `asof_*` 누적이 시간과 상관돼 **이미 추세를 학습한다.**

적용 결과 (폴드 2024, 저장된 예측, 재학습 0회):

| 후처리 | 보정 전 | 보정 후 | 증분 |
|---|---:|---:|---:|
| 없음 | 916.3 | 911.5 | **−4.9** |
| 포함 | 939.0 | 930.3 | **−8.7** |

예측 평균 변화 +0.00018, 범위 0.30~0.66 으로 **clipping 없음**. 효과가 음수라
편차 4축과의 대수 비교는 무의미하다. **과보정이 원인이다.**

### 26-e. TREND (prev 상호작용) — 기각. prev 축 완전 종료

대수 감사에서 제안된 6개 중 5개가 이미 CAAFE 이거나 원시 피처였다
(`prev1−prev3` = `cf_trend13`, `prev3−prev5` = `cf_trend35`,
`prev1_mid−prev3_mid` = `cf_midtrend13`, `prev5−통산` = `cf_form5`,
`prev{1,3,5} level` = 원시 6열). 남은 상호작용만 5열로 만들었다.

| | 2022 | 2023 | **2024** | 평균 |
|---|---:|---:|---:|---:|
| champ_trend (87p) | +6.5 | −3.8 | **+1.2** | +1.3 |

2024 +1.2 는 Champion 시드 변동(±3.2)의 절반도 안 된다.
**CAAFE 가 주효과로 전이 0 이었고, 상호작용으로도 안 된다. `prev` 축 종료.**

### 26-f. 🚩 타자 축 종결 — 신호가 없어서가 아니라 **이미 과다 반영**이다

"`asof_batter_*` 가 타자 능력이 아니라 **제구 난이도** proxy 아닌가" 라는 질문을
학습 없이 데이터로 답했다.

**해석은 맞다. 그런데 재해석이 필요 없다** — 설명서 원문이 이미 *"해당 타자가
상대한 투구의 제구 성공률"* 이다. 타격 능력이 아니라 **투수들이 그 타자에게 제구를
성공한 비율**이다.

**제안된 네 표현은 전부 기존 실험과 대수적으로 동일하다.**

| 제안 | 기존 | 결과 |
|---|---|---|
| batter difficulty level | `asof_batter_*` 원시 + `cur_bsucc`(D) | 이미 모델에 있음 |
| × count regime | **B2** `cur_{bsucc,bmid} x {adv,onb,sh,bs}` | 2024 −2.9 |
| × same-hand | 위 B2 의 `sh` 항 | 동일 |
| × pitcher current state | **P1** `cur_succ x cur_bsucc` 등 | 2024 −12.7 |

### 데이터 증거 — 왜 타자 축이 약한가

```
시즌 간 지속성 (200구 이상)
  타자  0.354 / 0.290 / 0.591 / **-0.142** / 0.182   평균 0.255
  투수  ...                                          평균 0.465  (1.8배)

2024 산포
  타자 210명  sd 0.0288      투수 249명  sd 0.0477  (1.7배)
```

### 🚩 그리고 모델이 이미 4배 과다 반영한다

```
2024 폴드
  정답 vs 타자난이도   +0.0463    <- 실제 신호
  예측 vs 타자난이도   +0.1826    <- 모델 반영 (4배)
  잔차 vs 타자난이도   +0.0297    <- 여전히 양수
```

잔차 상관이 **양수**라는 것은 "난이도 높은 타자 상대로 실제 성공률이 예측보다
높다" — **과다 반영을 이미 하고 있는데 더 줄여야 한다**는 뜻이다.
**타자 난이도를 더 넣으면 악화된다.**

> §5-c 의 "타자 쪽 계층 최적 가중 0" 이 **왜** 그런지가 이제 설명된다.
> 지속성이 약하고(0.255), 산포가 좁고(투수의 60%), 모델이 이미 4배 과다
> 반영 중이다. **타자 축을 종결한다.**

---

## 27. 🚩 모델 용량 재탐색 — 종료. **capacity 는 병목이 아니다**

현행 `depth=6 / border_count=32 / l2=100 / lr=0.02 / iter=1200` 은 **55피처
pre-D 시절(9회차 exp009-011)에 튜닝된 값**이고 82피처에서 재검토된 적이 없었다.
그래서 두 축만 먼저 쟀다 (폴드 2024, 2시드, 생산 경로 동일).

| depth | border | `rho^2` | 2024 증분 | 런타임 | 여유 RAM |
|---:|---:|---:|---:|---:|---:|
| **6** | **32** | **916.3** | 기준 (재현 확인) | 302s | 5.3GB |
| 6 | 128 | 908.1 | **−8.2** | 305s | 5.3GB |
| 8 | 32 | 910.9 | **−5.4** | 373s | 5.7GB |
| 8 | 128 | 909.9 | **−6.4** | 393s | 5.3GB |

**네 조합 전부 현행보다 나쁘다. family 종료.** `l2`·`lr`·`iterations` 2차 탐색도
하지 않는다 — 두 축 모두 현행이 최적으로 나온 이상 그 방향이 아니다.

### 🚩 27-a. 우리 서사가 절반만 맞았다

X/H1 을 만든 근거는 §22-f 의 *"depth 6 트리는 두 열의 곱을 축평행 분할로 못
만든다"* 였다. 그렇다면 깊이면 스스로 만들어야 하는데 **−5.4 로 나빠진다.**

정확한 설명은 **표현력이 아니라 표본 효율**이다.

```
명시적 곱항  ->  1개 열. 모든 행이 그 열을 지지한다
트리 자동    ->  깊은 분기. 같은 조합을 지지하는 표본이 쪼개진다
```

`border 32 -> 128` 이 −8.2 인 것도 같은 이야기다. `cur_*` 는 `cur_n<100` 인 행이
13.5% 라 값 자체가 거칠다. 해상도를 4배로 올리면 **잡음을 세밀하게 학습**한다.

### 27-b. 이 결론이 실패 목록을 다시 설명한다

| 실패 | 표본 효율 관점의 이유 |
|---|---|
| P1 (투수상태×타자상태) | 두 잡음 추정량의 **곱** — 잡음이 곱해진다 |
| K2 (2스트라이크 곱항) | 지시자가 행의 절반을 0 으로 꺼서 **유효 표본 반감** |
| TR-VAR | 투수당 1값. 행 단위 정보가 없다 |
| HB (EB 축소) | 압축은 맞으나 원시를 유지해 **정보 중복** |
| REGIME B/C | 저카디널리티 곱이라 트리가 이미 만든다 |

**D 가 성공한 이유도 여기서 설명된다** — 통산값에서 상수를 빼 **모든 행에
적용되는 새 수준**을 만들었다. 표본 손실이 0 이다.

### 27-c. 다음 후보의 조건 (갱신)

```
1. 모든 행에 값이 있을 것        (지시자로 절반을 끄지 않는다)
2. 여러 원천을 하나로 압축할 것   (열 수를 늘리지 않는다)
3. 시즌을 넘어 지속될 것          (2019~2024 로 먼저 진단)
4. 기존 피처의 선형·단조 변환이 아닐 것
```

**capacity 가 병목이 아니라는 것은, 1160 고원이 모델 설정이 아니라 우리가 못 본
데이터 해석에 있다는 뜻이다.**

---

## 28. 27회차 1036.0902 (−13.83) — 그리고 **음수 게이트가 처음으로 검증됐다**

`cand_asof_eb.zip`(87p, champ + EB 축소 5열). 생산 경로 2024 가 0.9934(−0.66%)로
음수라 **권고하지 않았으나 제출됐다.** 실측 −13.83 으로 예측(약 1043)보다 나빴다.

### 28-a. 소득은 점수가 아니라 규칙 검증이다

§22 에서 지적한 논리적 결함이 이것이었다 — *"우리는 2024 양수 후보만 제출했고
음수 후보는 한 번도 안 내봤다. 그 규칙으로 17개를 버렸는데 검증된 적이 없다."*

**이제 메워졌다.**

| 축 | 생산 2024 | LB 이득 | 비율 | 부호 |
|---|---:|---:|---:|:---:|
| D | +12.87% | +8.97% | 0.70 | O |
| F | +1.36% | −0.82% | −0.60 | **X** |
| X | +2.78% | +0.375% | 0.13 | O |
| H1 | +2.12% | +0.494% | 0.23 | O |
| K2 | −0.87% | −0.45% | 0.52 | O |
| **EB** | **−0.66%** | **−1.32%** | 2.00 | **O** |

```
생산 경로 2024 부호 == LB 부호      5/6
  양수 4건  D O / X O / H1 O / F X   (F 만 어긋남, +1.36% 로 작았다)
  음수 2건  K2 O / EB O              **2/2**
```

### 28-b. 확정된 판정 규칙

```
생산 경로 2024 음수   ->  제출하지 않는다      (2/2 실증)
생산 경로 2024 +3% 이상 ->  제출한다           (D 만 해당, 1/1)
생산 경로 2024 0~+3%  ->  예측 불가            (F − / X + / H1 + 로 갈림)
크기                  ->  못 맞힌다. 비율 −0.60 ~ 2.00
```

**부호는 쓸 수 있고 크기는 못 쓴다.** 이것이 6점으로 확정된 전부다.
§21·§23·§24 에서 세 번 무너진 선형 전이식은 폐기 상태를 유지한다.

### 28-c. 남은 슬롯 3장

현재 우리에게 **생산 경로 2024 양수 후보가 하나도 없다.** 그 상태로 슬롯을 쓰는
것은 2/2 로 실증된 규칙을 어기는 것이다.

---

## 29. risk 압축 — 기각. **표현이 아니라 정보가 없다**

§27 에서 "병목은 표현력이 아니라 표본 효율"이라 결론 내고, 처음으로 **열을 줄이는**
방향을 시험했다. `cur_{mid,ball,rev}` 세 실패 유형을 학습 구간 추정 가중으로
1열 압축 (시즌 g 가중은 시즌 <g 최소제곱, 검증 타깃 미사용).

| 구성 | 2022 | 2023 | **2024** |
|---|---:|---:|---:|
| champ_risk_add (83p, 원시 유지 + 압축) | +2.4 | +0.2 | **+0.4** |
| champ_risk_rep (80p, 원시 3열을 압축으로 대체) | +6.8 | +2.4 | **−4.8** |

### 무엇이 반증됐나

가설은 *"정보를 여러 열에 흩으면 트리가 조합을 찾느라 표본을 소모하니 미리
합치면 이득"* 이었다. **`add` 가 +0.4 (시드 변동 ±3.2 의 1/8)** 라는 것은
**트리가 이미 그 조합을 찾고 있었다**는 뜻이다. 미리 합쳐도 새로 알려주는 것이 없다.

§26 진단표에서 `cur_mid`·`cur_ball`·`cur_rev` 의 반영배율이 0.77 / 0.93 / 1.00 으로
**이미 적정 반영**이었던 것과 정확히 일치한다.

`rep` 의 2022 +6.8 / 2024 −4.8 은 이번 세션 내내 반복된 형태다 — **옛 폴드에서만
좋고 최근 폴드에서 지는 구성은 지금까지 한 번도 전이된 적이 없다.**

> **결론 — 표현 방식(열을 늘리든 줄이든)으로 짜낼 것이 남아 있지 않다.**
> depth/border 를 올려도(§27), 열을 합쳐도(§29), 곱해도(X/H1 이후 전부),
> 현행 82p 를 못 넘는다. 정보가 없는 것이지 표현이 부족한 것이 아니다.

---

## 30. 프레임 감사 — objective · 재보정 · 다양성 · 게이팅, 전부 상한이 0

새 피처 없이 **모델링 프레임 자체**에 사각지대가 있는지 감사했다.

### 30-a. OBJECTIVE MISMATCH — 없다

```
예측 범위    0.3105 ~ 0.6541
p(1-p) 범위  0.2141 ~ 0.2500   (비 1.17)
```

logloss 의 가중은 `1/(p(1-p))` 인데 **우리 작동 구간에서 1.17배 안에서만 변한다.**
사실상 상수라 MSE·Brier 와 거의 같은 문제를 푼다. objective 를 바꿔 얻을 것이 없다.

### 30-b. 🚩 비선형 재보정 — **내가 한 번 속았다**

`rho` 를 최대화하는 함수는 `g(p)=E[y|p]` 이고 아핀은 그중 선형만 쓴다. 그래서
분위 재보정을 재봤더니 200분위에서 **+94.3** 이 나왔다. **in-sample 이었다.**

같은 시즌을 절반으로 갈라 A 에서 곡선을 뽑고 B 에서 평가하면:

| 분위 | in-sample | **out-of-sample** | 원본 |
|---:|---:|---:|---:|
| 20 | 957.4 | 920.3 | 949.2 |
| 50 | 986.4 | 893.4 | 949.2 |
| 100 | 1031.4 | 846.8 | 949.2 |
| 200 | **1112.0** | **788.6** | 949.2 |

**in-sample 이 오르는 만큼 out-of-sample 이 정확히 떨어진다.** 순수 과적합이다.
선형 재보정은 원본과 **차이 −0.000** (rho 아핀 불변 확인). §6-h 의 곡률 기각이
재확인된다.

### 30-c. PREDICTION DIVERSITY — 없다

| 구성 | 단독 `rho^2` | champ 와 상관 | 0.5:0.5 혼합 |
|---|---:|---:|---:|
| champ_k2 | 950.5 | 0.99613 | 946.5 |
| champ_ebsucc | 941.0 | 0.99481 | 942.4 |
| champ_noX | 906.5 | 0.98639 | 929.1 |

**상관 0.986~0.996.** §8 의 혼합 산술(`2/(1+c)`)로 c=0.996 이면 이득 0.2% 다.
실제로 어떤 혼합도 최선 단독을 못 넘는다.

### 30-d. 선택 규칙 백테스트 — 미탐이 많다

train 2019 → 선택 2020, train 2019~20 → 답 2021.

```
부호 일치 5/10 (50%)
FALSE REJECT 4건  noL +25.8 / noX +24.0 / ebsucc +22.8 / risk_rep +47.4
FALSE ACCEPT 1건  trvar -17.7
BEST SELECTED regB (2020 +11.1 -> 2021 +32.7)   ORACLE risk_rep +47.4   GAP +14.7
```

**규칙은 나쁜 것을 거르는 데는 작동하나 좋은 것을 많이 버린다.** 다만 이 백테스트는
(1) 선택 폴드가 1개 (실제는 3폴드), (2) 학습량이 5배 차이(237k vs 481k),
(3) 2021 의 `rho^2` 규모가 2020 의 2.3배라 증분 척도가 다르다 — 세 결함이 있다.

미탐 4건이 전부 **모델을 단순하게 만드는 방향**(열 제거·압축·최소 추가)이었는데,
현 Champion 에서 같은 방향을 3폴드로 재면 `noL` −2.4/−9.0/−15.3,
`noX` −11.5/−13.6/−35.6 로 **전부 명확히 음수**다. 2021 의 우위는 그 시즌 특유였다.

### 30-e. 🚩 ROW-LEVEL GATING — 오라클 상한이 **0** 이다

"데이터 상태에 따라 최적 예측기가 달라진다"를 직접 시험했다.

**시즌 수준** — 5폴드 중 4폴드에서 `D+X+H1` 이 최선. 2021 만 `D+X` 이고 그 차이도
1.6%(1660.7 vs 1634.9)다.

**행 수준** — 폴드 2024 에서 게이트 축 4종으로 갈라 봤다.

| 게이트 | 오라클 이득 (달성 불가 상한) |
|---|---:|
| `cur_n` 4분위 | **+0.7** |
| 2스트라이크 | +0.0 |
| 같은손 | +0.0 |
| 3볼 | +0.0 |

**네 축 모두 거의 모든 구간에서 `D+X+H1` 이 이긴다.** `cur_n` 3분위 하나만 `D+X` 가
0.5% 앞서고, 라벨을 보고 골라도 전체 이득이 +0.7 이다.

**원인** — expert 들의 예측 상관이 0.986~0.996 이다. 게이팅은 예측기가 **서로 다른
곳에서 틀릴 때** 이득이 나는데, 우리 expert 는 같은 피처의 부분집합이라 거의 같은
것을 예측한다. **2021 의 부호 반전도 "최적이 바뀐다"가 아니라 "차이가 잡음보다
작아 순위가 흔들린다"** 이다.

가설이 확인되지 않았으므로 **게이팅 모델을 만들지 않는다.**

---

## game_type F — 2023 년 라벨 정의 변경. 2023 퇴화 폴드의 원인

**결론: F 는 오염원이 아니다. 오염원은 "스테일한 F" 다.**

`--drop-f`(F 를 학습에서만 제외, 검증 폴드 유지, 2시드):

| 폴드 | Champion | F 제외 | 차이 |
|---|---|---|---|
| 2022 | 2458.4 | 1134.2 | −1324.2 |
| 2023 | 192.0 | 768.0 | **+576.0** |
| 2024 | 916.3 | 909.4 | −6.9 |

부호가 폴드마다 정반대다. 원인은 **2022 와 2023 사이의 체제 전환**이다.

```
F 성공률   2019 .689  2020 .588  2021 .704  2022 .709 | 2023 .473  2024 .459
R 성공률          .550       .527       .513       .504 |       .503       .490
F-R 격차        +.140      +.061      +.191      +.205 |      -.030      -.030
```

### 이것이 실력 변화가 아니라는 증거

```
2022 와 2023 양쪽에 F 로 등판한 공통 투수 92명
  2022 평균 0.7018 -> 2023 평균 0.4757   변화 -0.2261
  하락한 투수 비율 98.9%
투수 교체율 2022->2023 은 40.0% 로 6년 중 **가장 낮다**
상황 변수(inning/balls/strikes/runners) F 행 평균은 6시즌 내내 동일
```

**같은 투수 92명 중 91명이 같은 방향·같은 크기로 떨어졌다.** 구성 변화도
상황 변화도 아니다. 2023 년에 F 경기의 "제구 성공" **판정 기준 또는 기록
방식이 바뀌었다**. 즉 2019~2022 의 F 라벨과 2023~2024 의 F 라벨은 **같은
타깃이 아니다**.

### 폴드별 부호가 전부 설명된다

- **2022** 검증셋 F 기저율 0.709 vs R 0.504. F 를 빼면 12.3% 의 행을 통째로
  못 맞혀 상관이 무너진다 -> −1324. **F 자체는 필수 정보다**
- **2024** 검증셋 F 0.459 vs R 0.490 으로 거의 같다. 그런데 **모델은 이미
  적응해 있다** (아래 측정 참조)

### 정정 — "모델이 F 에 +0.20 보너스를 얹는다"는 틀렸다

저장된 `pred_champ_2024.npy` 로 직접 쟀다.

```
구간        n        실제      예측      편향
R     223,497    0.4897   0.4999   +0.0102
F      30,010    0.4593   0.4775   +0.0183
오라클로 F 평균 편향 완전 제거   ->  rho2 913.0  (-3.3)
F 를 R 수준으로 평행이동         ->  rho2 879.7  (-36.6)
```

F 편향은 R 보다 **+0.008 클 뿐**이고, 수준 보정의 오라클 이득은 **음수**다.
원인은 D 블록이다 — `cur_succ` 가 **현재 시즌** 성공률을 주므로 2024 년 F
투수는 0.46 으로 읽히고, 모델은 `game_type` 이 아니라 그 경로로 적응한다.

따라서 폴드 2023 의 +576 은 **학습셋에 신체제가 한 시즌도 없을 때만** 나타나는
현상이다. 2024 는 2023 한 시즌이 들어오며 거의 소진됐고(−6.9), 2025 는 두
시즌이 들어간다.

**이 발견의 가치는 2023 퇴화 폴드의 원인 규명이지 LB 이득이 아닐 공산이 크다.**

모델에는 시즌 피처가 없으므로 "F 관계가 바뀌었다"를 표현할 수단이 원리적으로
없고, 구체제 4시즌과 신체제 2시즌을 그냥 평균한다.

### 처방과 스테일 비중

`--drop-f-before N` — 시즌 N 미만의 F 행만 학습에서 제외. 신체제 F 는 유지해
F 행 예측 능력을 잃지 않는다.

```
폴드 2023   구체제 F 4시즌 : 신체제 0시즌   스테일 100%  ->  +576
폴드 2024   구체제 F 4시즌 : 신체제 1시즌   스테일  80%  ->    -6.9 (전량 제외 기준)
2025 생산   구체제 F 4시즌 : 신체제 2시즌   스테일  65%
```

폴드 2024 는 **보수적인 대리가 아니라 약간 낙관적인 대리다** — 2025 는 신체제를
한 시즌 더 갖고 있어 스테일 피해가 그만큼 작다. 방향은 같고 크기는 더 작을 것.

### 조건 변수 감사 2단계 (동시 수행)

조건 9종 x 핵심신호 3종의 `corr(신호, 정답 | 조건)` 을 세 시즌 비교.
**3시즌 일관 + 격차 0.010 초과 + 미회수** 통과는 `game_type` 하나뿐.
`runner_on` 은 통과했으나 `dx_succ_onb` 가 이미 그것이고, `outs`/`inning`/
`2strike` 는 REGIME B/C/K2 에서 기각, `cur_n` 은 2022 부호 반대로 지속성 없음.
**조건 변수 축 소진.**

### 결론 — F 행 선택 축은 닫는다

| 폴드 2024 | rho2 | vs champ |
|---|---|---|
| champ | 916.3 | — |
| `--drop-f` (F 전량 제외) | 909.4 | −6.9 |
| `--drop-f-before 2023` (구체제 F 만 제외) | 906.6 | **−9.7** |

스테일한 것만 골라 빼도 **전량 제외보다 나쁘다**. 판정 폴드에서 둘 다 음수이므로
2024 부호 규칙에 따라 **기각**한다. 구체제 F 10만행은 라벨 수준이 달라도
조건부 구조(어떤 상황에서 성공률이 오르내리는지)는 여전히 유효한 학습 신호였고,
그걸 버린 손해가 수준 오차를 없앤 이득보다 컸다.

**남은 가치는 LB 이득이 아니라 진단이다.** 25회차까지 설명하지 못했던 "2023 폴드가
왜 퇴화하는가"(rho2 192 vs 2022 의 2458)의 원인이 확정됐다 — 2023 년 F 라벨 정의
변경이고, 학습셋에 신체제가 한 시즌도 없는 폴드 2023 에서만 치명적이다.
**앞으로 2023 폴드의 낮은 값을 모델 결함으로 읽지 않는다.**

### F/R 타입별 표현 분리도 기각 — game_type 축 완전 종료

`--only "champ,champ_xt,champ_ht,champ_st"` (3폴드 x 2시드, champ 대비 증분):

| 후보 | 정체 | 2022 | 2023 | **2024** |
|---|---|---|---|---|
| `champ_xt` | CTX(X) x is_F | +8.7 | −2.6 | **−0.5** |
| `champ_ht` | LVL(H1) x is_F | +8.6 | −3.1 | **+3.1** |
| `champ_st` | `cur_*` x is_F | +2.1 | −11.0 | **−6.7** |

게이트(2024 에서 +5 이상)를 셋 다 미달. `champ_ht` 의 +3.1 은 2024 폴드
시드 노이즈 하한(±3.2)보다 작아 신호가 아니다.

**2022 에서만 +8.7/+8.6 이 나오는 것이 진단과 정확히 맞는다.** 2022 는 F−R
격차가 +0.205 로 최대인 해라 타입 분리가 실제로 이득이고, 격차가 −0.03 으로
사라진 2024 에는 나눌 차이가 없다.

**따라서 `game_type` 축은 두 방향 모두 닫혔다** — 행 선택(−6.9 / −9.7)과
표현 분리(−0.5 / +3.1 / −6.7). 조건 변수 감사에서 유일하게 살아남았던 축이
실험에서 소진됐다. **관계 구조 축 전체 종료.**

같은 판에서 재확인된 것들(2024 증분): `ebsucc` +2.0, `trend` +1.2,
`trvar` +3.3, `risk_add` +0.4, `regB` −0.0, `regC` −3.0 — 전부 노이즈 대역.
`k2` +11.5 는 기존대로 시드 안정성에서 이미 기각.

---

## PN — 비율의 숨은 분모에서 **직전 경기 투구 수**를 역산

### 발견

`asof_pitcher_prev{1,3,5}_game_{success,middle}_rate` 는 **비율만 주고 분모를
주지 않는다.** 47개 컬럼 어디에도 경기당 투구 수가 없다.

원본 CSV 의 rate 는 **소수 6자리**로 저장돼 있다 (자리수 분포: 6자리 130만행,
5자리 12.6만행). 작은 분모의 기약분수는 이 정밀도에서 **유일하게 역산된다**.

```
n=1501, rate=0.576949  ->  정확히 866/1501
```

`success` 와 `middle` 은 **같은 창의 같은 투구 집합**을 공유하므로, 둘을
**동시에** 정수로 만드는 최소 분모가 참 투구 수다. (한쪽만 쓰면 기약분수의
분모, 즉 참값의 약수가 나온다 — 단독 복원의 완전일치가 51% 인 이유다.)

### 검증 — 네 가지가 모두 맞는다

```
1) 복원율          prev1/3/5 모두 100.00%
2) 우연 배제        분모 400 이하 기약분수의 평균 간격 2e-5, 허용오차 5.1e-7
                   -> 무작위 실수가 맞을 확률 약 5%. 실측 100%
3) 창 단조성        p1<=p3 95.49%,  p3<=p5 87.41%  (물리적 필연)
4) 경기당 환산 3중 일치
                   p1 중앙값 41.0 / p3/3 37.3 / p5/5 35.2
                   95분위    105.0 /     101.3 /     100.0
                   분포는 불펜(1~30)과 선발(90~119) 이봉형, 최대 151
```

**규정 4 준수** — 그 행의 자기 값 하나만 쓴다. 다른 test 행도, 전체 분포도
참조하지 않는다. 행 하나만 줘도 같은 값이 나온다.

### 신호가 있는가

모델은 `prev1_rate` 를 갖고 있지만 그게 5구짜리인지 100구짜리인지 모른다.
실제 신뢰도는 크게 갈린다:

```
corr(prev1_rate, y | 직전 경기 투구수)      2022      2023      2024
  1~5구                                  +0.0586   +0.0250   +0.0142
  91~200구                               +0.0583   +0.0719   +0.0577
```

2023/2024 에서 3~4배 차이. 다만 **2022 는 평평하다** — 지속성이 완전하지 않다.

모델이 이미 얼마나 회수했나 (폴드 2024):

```
투구수      corr(r1,y)  corr(r1,pred)  corr(r1,res)
1~5           +0.0142       +0.2992      -0.0109   (과신)
16~30         +0.0442       +0.3477      +0.0148   (과소신)
91~200        +0.0577       +0.5554      +0.0058
```

**모델은 이미 투구수를 간접 추정해 신뢰도를 0.30->0.56 으로 조절하고 있다.**
실제 신호는 4배 벌어지는데 조절은 1.85배뿐이라 미세한 오조정이 남아 있으나
잔차 크기는 0.01 대다.

### 파생 — 겹치지 않는 창으로 분해 (D 축과 같은 시도)

`p3n-p1n`, `p5n-p3n` 으로 게임 2-3, 4-5 의 독립 구간 비율을 만들었다.
건전성은 좋다 (유효 91.3%, 범위이탈 0.88%, 중앙 0.5217/0.5224).

```
피처            2022      2023      2024    2024잔차
g1(직전1)     +0.0770   +0.0405   +0.0365   +0.0018
g23(2-3)      +0.0211   +0.0170   +0.0178   +0.0010
g45(4-5)      +0.0393   +0.0143   +0.0266   +0.0030
기존 prev5     +0.0908   +0.0551   +0.0574   +0.0043
```

**분해 조각이 기존 누적보다 약하다.** D 와 갈리는 이유가 분명하다 — D 는 누적
창이 통산 수천 구라 시즌 증분이 진짜 다른 정보였는데, `prev5` 는 5경기라
조각들이 전부 "최근 폼"으로 서로 비슷하다. **분해 축은 약하다.**

### 결과 — 기각

```
champ_pn (85p = Champion 82p + log1p(p1n,p3n,p5n))
  2022 +6.8    2023 +3.2    2024 -1.4
```

판정 폴드 2024 가 음수. 크기는 시드 노이즈(±3.2~7.2) 안이라 "해롭다"보다
**"효과 없음"**이고, 어느 쪽이든 게이트 미달이다.

**진단이 이미 예고했다** — 모델은 투구수를 간접 추정해 `prev1_rate` 신뢰도를
0.30 -> 0.56 으로 조절하고 있었고 미회수 잔차는 0.01 대였다. 정보는 진짜로
새것이었지만 **모델이 이미 거의 다 뽑아 쓰고 있었다.**

### 이 건에서 배운 것 — "새 정보"와 "쓸 수 있는 정보"는 다르다

D 축이 통한 이유를 지금까지 "모델이 원리적으로 못 만드는 값을 줬기 때문"으로
정리해 왔는데, PN 은 그 조건을 **똑같이** 만족하면서 실패했다. 조건이 하나 더
있다.

```
D  : 모델이 못 만든다 + **대리 신호도 없다**(prior_events 는 학습셋 상수)
PN : 모델이 못 만든다 + 대리 신호가 있다(비율의 granularity, 이닝, 역할 관련 피처)
```

앞으로 후보를 볼 때 **"모델이 이 값을 간접 추정할 수 있는 경로가 있는가"** 를
먼저 묻는다. 값싼 판정법이 있다 — 그 피처의 **잔차 상관**을 이미 쓰고 있는
피처의 잔차 상관과 나란히 놓는다. 같은 대역이면 회수된 것이다.

```
PN 잔차상관 +0.0016~+0.0036   vs   이미 쓰는 asof_pitcher_n +0.0034
-> 같은 대역. 학습 전에 기각할 수 있었다.
```

**역산 방법 자체는 유효하고 재사용 가능하다.** 다른 반올림된 비율 컬럼에서
숨은 분모를 복원해야 할 일이 생기면 이 절을 참조한다.

---

## 타깃 생성 과정 감사 — 폴드 2021/2022 는 대부분 "F 공짜 신호"였다

학습 없이 진단만 수행. 질문은 하나 — `control_success` 가 2019~2024 동안 같은
의미와 생성 규칙을 유지했는가.

### A. 이상은 하나뿐이고 F 에 갇혀 있다

```
R 만의 기저율   .5495  .5269  .5128  .5037  .5031  .4897
연도별 변화     -.0226 -.0142 -.0091 -.0006 -.0134
                                    2022->23 이 5개 중 **가장 작다**
```

전체 기저율의 2022->23 급락(-0.0290)은 전부 F 효과다 (0.11 x 0.236 = 0.026).
**`control_success` 의 R 부분에는 불연속이 없다.**

축별 감사(game_type / hand / count / runners / outs / inning / month / dow /
base_state, 64개 구간)에서 전역 표류를 뺀 '수준 효과'의 최대 점프는 **0.029**
로 F 의 0.236 보다 한 자릿수 작다 (중앙값 0.0075, 90분위 0.0182).
상위에 몰린 `2019->2020` 카운트 항목들은 **구조 변화가 아니다** — 카운트 수준
효과의 퍼짐이 .0128 .0114 .0120 .0134 .0136 .0142 로 매끄럽다.

### B. 능력 변화와 정의 변화의 구분 (반복 관측)

```
                    공통   평균이동   한방향비율   표준편차
R 투수 최악 전이     131   -0.0207     74.0%     0.0365
R 타자 최악 전이     127   -0.0224     66.9%     0.0356
F 2022->2023         92   -0.2261     98.9%
```

서명이 질적으로 다르다. **R 은 전 구간 리그 표류 + 개인차, F 만 정의 변화.**

F 의 2020 딥은 정의 변화가 **아니다** — 2019->20 은 -0.0746(78.7%)였다가
2020->21 에 +0.1126(하락 13.0%)로 **복귀**했다.

### C. 시점 — 시즌 경계의 계단 변화

```
F-R 격차 월별
2022   4월 +.249  5월 +.197 ... 10월 +.229    전 구간 구체제
2023   4월 +.008  5월 -.027 ... 9월 -.045     4월이 전환월
2024   3월 -.020 ... 10월 -.031               16개월 안정, 추세 없음
```

2020 딥은 시즌 **안에서** 점진적으로 흘렀고(+.061 -> +.018) 복귀했다.
2023 은 시즌 **경계**에서 한 번에 갈아탔고 두 시즌 유지됐다. 서명이 다르다.

### D. **가장 중요한 발견 — 폴드 점수의 정체**

`game_type` 하나만 알고 그룹 평균을 찍었을 때 얻는 점수:

| 시즌 | F−R격차 | rho2_gt | 실제 폴드 | **공짜 비중** |
|---|---|---|---|---|
| 2020 | +0.061 | 128.0 | 704.5 | 18.2% |
| 2021 | +0.191 | 1374.4 | 1634.9 | **84.1%** |
| 2022 | +0.205 | 1820.9 | 2458.4 | **74.1%** |
| 2023 | −0.030 | 34.2 | 192.0 | 17.8% |
| 2024 | −0.030 | 38.7 | 916.3 | **4.2%** |

**폴드 2021/2022 의 높은 점수는 모델 실력이 아니라 F 공짜 신호다.**
이것으로 여러 미스터리가 한꺼번에 설명된다.

- 2022 가 2458 이고 2024 가 916 인 것은 **난이도 차이가 아니라 사라진 공짜 신호**
- 2024 부호가 유일한 LB 지표인 이유 — **2025 가 2024 와 같은 신체제**
- `xt`/`ht` 가 2022 에서만 +8.7/+8.6 인 이유 — 거기만 나눌 격차가 있었다
- 2023 이 "퇴화"한 이유 — 사라진 공짜 신호에 모델이 계속 베팅했다

### E. 모델 노출도 (폴드 2024, Champion 예측 기준)

시즌 내 축적량별 F 초과편향(R 대비):

```
cur_n 0~20      +0.0214   (3,069행, 검증셋의 1.2%)
cur_n 20~60     +0.0016
cur_n 60~150    -0.0012
cur_n 150~400   +0.0125
cur_n 400+      +0.0091
```

노출은 **시즌 초반에 집중**된다. 올해 이 투수가 어떤지 아직 모를 때만 구체제
기억에 기댄다. D 블록의 `cur_succ` 가 시즌 중 자동 보정하기 때문이다.

### F. 2025 전이 위험

과거 전이 두 건으로 판단 기준을 만들 수 있다.

```
              크기      쏠림     시점       지속
2020 딥     -0.075    78.7%   시즌 내 점진   1시즌 후 복귀
2023 전환   -0.226    98.9%   시즌 경계 계단  2시즌 유지, 추세 없음
```

2023 전환은 세 배 크고, 거의 만장일치고, 행정적 변경의 서명(경계 계단)이며,
16개월간 표류가 없다. **2025 지속 가능성이 높다.**

복귀 시 손해 상한 (모델이 전혀 적응 못 한다고 가정):

```
2025 F-R 격차 +0.000 이면  rho2 924.5  (+8.2)
              +0.100 이면       842.4  (-73.9)
              +0.205 이면       720.3  (-196.1)
```

**상한이다.** 실제로는 `cur_succ` 가 2025 값을 읽어 시즌 중 보정하므로 피해는
E 의 초반 구간에 집중된다. 그리고 **이 위험은 대비할 수단이 없다** — 어느
쪽으로 갈지 모르는데 한쪽에 맞추면 반대쪽에서 잃는다. 기록만 해 둔다.

---

## 경로 배분 재최적화 — **기각.** K2 중복 가설은 틀렸다

생산 경로(단일 시드 42, 2019~2023 학습, 2024 홀드아웃, 아핀 이전 rho^2).
**정합성 검증** — Champion 944.0, champ+K2 935.8 으로 원장 `local_2024` 와 일치.

편차 축을 빼는 것은 모델을 바꾸지 않으므로 **학습 2회로 12가지**를 전부 쟀다.

### 1단계 — 단일 제거

| 구성 | 2024 | vs champ |
|---|---|---|
| Champion (편차 4축) | 944.0 | — |
| − B dev_platoon (투수x타자손) | 932.1 | −11.9 |
| − C dev_plat_cnt (플래툰x투수유리) | 936.6 | −7.4 |
| − A dev_count (플래툰x투수유리x카운트) | 942.6 | −1.5 |
| − D dev_runner (플래툰x주자유무) | 942.4 | −1.7 |
| − 편차 전부 | 921.4 | −22.7 |

**어떤 축도 제거로 +3 을 주지 않는다.** 그리고 개별 기여의 합 22.5 가 전체
22.7 과 같다 — **편차 4축은 서로 거의 직교한다.**

### 2단계 — K2 in-model x 편차 제거

```
편차 4축 전부 있을 때   944.0 -> 935.8   K2 기여 -8.2
dev_count 뺐을 때      942.6 -> 934.4   K2 기여 -8.2
편차 전부 뺐을 때       921.4 -> 913.5   K2 기여 -7.9
```

**K2 의 손해가 편차 구성과 무관하게 -8.2 로 고정이다.**

### 정정 — 26회차의 원인 설명이 틀렸다

26회차 원장은 K2 의 LB −4.72 를 "후처리 편차 3축과 중복"으로 설명했다.
**그 설명은 틀렸다.** 겹친다면 편차 축을 뺐을 때 K2 의 손해가 줄어야 하는데
줄지 않는다. K2 는 후처리와 무관하게 생산 경로에서 나쁜 피처다.

이 오진단을 근거로 "in-model 과 후처리가 중복돼 흘리는 몫이 있다"는 가설을
최우선으로 세웠으나 **그런 몫은 존재하지 않는다.** 편차 4축은 서로 직교하고
in-model 과도 직교한다. **경로 배분 family 종료.**

### 부수 소득 — 편차 축의 기여 서열이 처음 측정됐다

```
dev_platoon    +11.9   (투수x타자손, k=300)
dev_plat_cnt    +7.4   (플래툰x투수유리, k=2000)
dev_runner      +1.7   (플래툰x주자유무, k=2000)
dev_count       +1.5   (플래툰x투수유리x카운트, k=800)
합계           +22.7
```

15~19회차에 LB 로 관측된 누적 +15.08 과 대역이 맞는다(생산 경로 2024 기준
+22.7). **가장 큰 것은 플래툰이고 카운트 계열은 합쳐도 +8.9 다.**

---

## TMX — 상황 조건부 TrackMan 편차 6열 (spin/ivb/hb). **기각**

```
WHY_NEW        = cols.json 의 extra 에 이미 생성돼 있으나 원장에도 run_exp.py
                 에도 시험 이력이 없다. 사전 선별에서 tmc_spin_dev 의 2024
                 잔차상관 -0.0092 가 채택 8열 최대치(+0.0077)를 초과했다.
                 이번 세션에서 사전 선별을 통과한 유일한 후보였다.
TRACKMAN_SOURCE= spin_rate, induced_vert_break, horz_break
                 (TrackMan 30컬럼 중 Champion 이 쓰는 원천은
                  pitch_type_group 과 rel_speed 둘뿐이다)
WHAT_IS_NEW    = 물리량 자체가 아니라 **투수 x 맥락(카운트/타자손) 조건부 편차**.
                 행마다 카운트·손으로 조회되므로 행 단위로 값이 변한다.
                 25-a TR-VAR(투수 상수인 산포)이 기각된 지점과 갈린다.
WHY_CURRENT_8P = 현행 8열은 구종 선택 비율과 구속만 담는다. 회전·수직무브·
  DOES NOT HAVE  수평무브는 같은 구종 안에서도 투수마다 다른 독립 축이다.
2022           = +3.3
2023           = +1.2
2024           = -0.9
MEAN           = +1.2
WORST          = -0.9
SEED_VARIANCE  = 6.5 (champ 자체는 10.1)
DECISION       = 기각. 게이트 "2024 < 0 -> family 종료" 에 해당.
                 내부 분해(spin/ivb/hb)와 tmh_ivb 중복 검사는 실행하지 않았다.
```

경로 — 생산 경로 3폴드 x 2시드, 후처리 편차 4축 유지, 아핀 이전 `rho^2`.
정합성 확인: 폴드 2024 champ 시드42 = 944.0 으로 원장 `local_2024` 와 일치.
행 정렬 검증 통과 (캐시 X 와 생산 df, 1,475,092행 불일치 0).

### 관찰 — 사전 선별이 처음으로 헛짚었다

잔차 상관 사전 선별은 지금까지 PN·잔여질량을 정확히 걸러냈는데, TMX 는
**통과시켰고 결과는 −0.9** 였다. 규칙을 이렇게 고친다.

> **잔차 상관은 "이미 회수됐는가"의 기각용 지표이지 "쓸모 있는가"의 채택용
> 지표가 아니다.** 대역이 낮으면 버려도 되지만, 높다고 통과시켜서는 안 된다.

`tmc_spin_dev` 의 잔차상관 −0.0092 는 실재했으나 그 신호가 모델이 쓸 수 있는
형태가 아니었다. 결측 33~36% 도 한몫했을 것이다.

### 부수 관찰 — TMX 는 신호가 아니라 안정화로 작동했다

```
폴드 2024 시드별   champ     944.0  935.7   (폭 8.3)
                  champ_tmx 939.3  939.4   (폭 0.1)
```

TMX 를 넣으면 시드 편차가 거의 사라지는데 수준은 낮아진다. 신호를 더한 것이
아니라 **분산을 줄이면서 평균도 끌어내린** 모양이다. 시드 2개라 과대해석하지
않는다.

### TrackMan 컬럼 provenance — 남은 미검증 항목

```
사용중   pitch_type_group (구종비율 6열) · rel_speed (구속 2열)
기각     행 조인(6-o) · 산포 TR-VAR(25-a) · 구종 상호작용(11-c) · TMX(여기)
미검증   extension/rel_height/rel_side 의 **평균** 편차 (TMR)
         zone_speed, rel_speed-zone_speed 구속유지율 (TMZ)
         tagged vs auto pitch_type 불일치율
```

TMX 가 −0.9 인 이상 TMR/TMZ 도 같은 계열이므로 기대치를 낮춰 잡는다.

---

## 시드 앙상블 확대 (3 -> 7) — **기각.** 그리고 추론 시간 확정

생산 경로 폴드 2024, Champion 82p, 후처리 4축 유지.

```
단일 시드   944.0  935.7  936.7  941.0  940.1  937.1  932.4   (평균 938.1, 폭 11.6)

k         1      2      3      4      5      6      7
rho2    944.0  943.8  944.0  945.1  945.6  945.5  944.6
vs k=3    -      -     +0.0   +1.1   +1.7   +1.5   +0.6
```

**3 -> 7 은 +0.6, 최대(k=5)도 +1.7 이다.** 곡선이 단조가 아니라(k=7 < k=5)
차이 자체가 잡음 대역이다. `+3 이상만 후보` 게이트 미달. **시드 축 종료.**

주의 — k=1 값 944.0 은 7개 중 가장 좋은 시드가 우연히 첫 번째였던 것이다.
평균 단일 시드는 938.1 이므로 "앙상블이 무의미"한 것이 아니라 **현행 3시드가
이미 수확체감 구간**이라는 뜻이다.

### 부수 확정 — 추론 시간

```
253,507행 예측에 시드당 0.7~0.8초.  7시드도 약 5초.
대회 제한은 245,789행 10분.
```

**추론 시간은 어떤 구성에서도 제약이 아니다.** 앞으로 이 항목으로 후보를
탈락시키지 않는다.

### 함께 확정 — 아핀/center 에는 남은 여지가 없다

```
Champion 예측 범위 [0.322, 0.650], 아핀 후 [0.299, 0.655]  -> clip 미접촉
alpha 0.5~2.0 에서 rho^2 동일 (916.3112, 소수 4자리)
temperature T=2.0 도 +0.069 (잡음)
```

점수는 최적 아핀에서 `1e5*rho^2` 를 천장으로 갖는 R^2 계열이고(원장 §8),
손실은 평균 오프셋의 **제곱**으로 커진다.

```
손실 = 1e5 * (mean(q) - mean(y))^2 / Var(y)
오프셋 0.003 -> 4점,   0.016 -> 102점
```

워크포워드 추정 2025 성공률 **.4622** vs 11회차 LB 역산 **.4609** — 차이
0.0013 이므로 현재 아핀 손실은 **약 0.7점**이다. 22회차의 독립 추정
"0.2~8.6" 과 일치한다. **건드리면 잃기만 한다.**

참고로 F 체제 전환을 반영해 R-only 로 외삽하면 .4794 가 나오는데 이는
.4609 대비 **137점 손해**다. **전 행 외삽이 맞고 F 보정 아이디어는 틀렸다.**

---

## TMR 기각 — 그리고 **TrackMan 맥락편차 family 전체 종료**

릴리스 기하(extension / rel_height / rel_side)의 상황 조건부 편차 6열.
생산 경로 3폴드 x 2시드, 후처리 편차 4축 유지.

```
WHY_NEW           = 25-a 가 기각한 것은 같은 컬럼의 **산포(sd)** 였고
                    **평균 편차**는 미검증이었다.
BASEBALL_MECHANISM= 제구는 반복성. 카운트별 릴리스 이동은 조정 또는 무너짐이다.
CURRENT_MODEL_GAP = 현행 TrackMan 8열은 구종 비율 + 구속뿐. 릴리스 기하 없음.
2022              = +9.8
2023              = +1.5
2024              = -3.9
MEAN              = +2.5
WORST             = -3.9
SEED_VARIANCE     = 10.0  (champ 자체 10.1)
PRODUCTION_RUNTIME= 폴드당 학습 196~315s x 2시드, 추론 0.7s / 253,507행
DECISION          = 기각 (게이트 2024 < 0)
```

### 🚩 근본 원인 — 결측 패턴이 `game_type` F 를 대리한다

TMX 와 **서명이 같다.**

```
TMX   2022 +3.3   2023 +1.2   2024 -0.9
TMR   2022 +9.8   2023 +1.5   2024 -3.9
```

둘 다 2022 에서 크게 양수, 2024 에서 음수다. 우연이 아니다.

```
시즌     F비중   TMR결측   F중결측   R중결측    상관    F-R격차
2022    12.3%    20.7%    40.3%    17.9%   +0.181   +0.205
2023    10.5%    16.8%    25.0%    15.8%   +0.075   -0.030
2024    11.8%    22.7%    39.9%    20.4%   +0.151   -0.030

corr(결측, y)   2022 +0.0258    2023 -0.0078    2024 +0.0006
corr(F, y)      2022 +0.1349    2023 -0.0185    2024 -0.0197
```

**F 행은 40% 결측, R 행은 18% 결측이다.** 그래서 TrackMan 피처를 넣으면
결측 지시자를 통해 **F 지시자가 부분적으로 딸려 들어간다.** 2022 에서는 그것이
공짜 신호(F-R 격차 +0.205)를 실어 나르고, 2024 에서는 격차가 -0.030 으로
무너져 **+0.0006 으로 죽는다** — 신호는 없고 잡음만 남는다.

**신호를 준 것이 아니라 죽은 공짜 신호를 되살린 것이다.**

### 결론 — TMZ 는 설계하지 않는다

TMZ(`rel_speed - zone_speed`)는 **같은 TrackMan 조인, 같은 결측 패턴**을 쓴다.
원인이 물리량이 아니라 **조인 구조**이므로 결과가 달라질 이유가 없다.
설계 전에 기각 근거가 확보됐다. **TrackMan 맥락편차 family 전체 종료.**

### 남은 함의 — 현행 TrackMan 8열도 죽은 무게일 수 있다

Champion 의 `tmc_*`/`tmh_*` 8열은 **7회차(LB 825 시절)에 채택**됐다. 그때는
F-R 격차가 살아 있던 구체제다. 같은 결측 오염을 지금도 지고 있을 수 있다.

**다음 실험 후보 = 8열 제거** (새 피처가 아니라 제거). 생산 경로 3폴드 x 2시드.
TMX -0.9 / TMR -3.9 였으므로 제거가 양수일 개연성이 있다.


---

## 현행 TrackMan 8열 제거 — **기각.** 죽은 무게가 아니었다

`exp/drop_tm.py`, 생산 경로 3폴드 x 2시드, 후처리 편차 4축 유지.
champ 이 `tmx.json` 값을 소수점까지 재현했다 (2475.8 / 211.8 / 943.8) —
틀은 검증된 상태에서 나온 숫자다.

| 구성 | 피처 | 2022 | 2023 | **2024** | 평균 | 최악 |
|---|---:|---:|---:|---:|---:|---:|
| champ | 82 | 2475.8 | 211.8 | 943.8 | — | — |
| champ_notm (8열 전부 제거) | 74 | -2.4 | +1.6 | **-11.7** | -4.2 | -11.7 |
| champ_nocnt (`tmc_*_dev` 4열) | 78 | -6.1 | -10.0 | **-7.8** | -8.0 | -10.0 |
| champ_nohand (`tmh_*_dev` 4열) | 78 | -0.6 | -4.9 | **-0.4** | -2.0 | -4.9 |

### 가설이 틀렸다 — 그리고 어디까지 틀렸는지가 분명하다

TMX/TMR 기각 후 세운 가설은 "결측이 F 를 대리하므로 **어떤 TrackMan 파생도**
2024 에서 죽는다, 현행 8열도 같은 죽은 무게일 것"이었다. 제거가 2024 에서
**-11.7** 이므로 그 확장은 **성립하지 않는다.**

오염(결측->F 대리)은 8열에도 똑같이 실려 있다. 그런데도 제거가 손해라는 것은
**그 오염을 상쇄하고 남는 진짜 신호가 있다**는 뜻이다. 구분선은 물리량이다.

```
tmc_*_dev  카운트별 구종 성향 편차   3폴드 전부 음수(-6.1/-10.0/-7.8)  진짜 신호
tmh_*_dev  타자손별 구종 성향 편차   전부 노이즈 대역(-0.6/-4.9/-0.4)  중립
TMX/TMR    회전/무브/릴리스 기하     2024 -0.9 / -3.9                   오염만
```

**카운트 조건부 구종 성향은 메인 데이터에 아예 없다** (`asof_pitcher_*_rate` 는
투수별 전체 평균 하나뿐). 그래서 트리가 만들 수 없고 대리 신호도 없다 —
CLAUDE.md 4절의 "못 만든다 + 대리 없다" 를 만족한다. 반면 회전/무브는 제구
성공과의 연결이 약해 결측 오염만 남는다.

### 정정 — "TrackMan family 전체 종료" 는 과했다

닫힌 것은 **TrackMan 맥락편차 신규 파생**(회전/무브/릴리스 기하)이고,
채택된 8열은 유지된다. 기록을 그렇게 고친다.

`tmh_*` 4열은 세 폴드 전부 노이즈 대역이라 **제거해도 무해**하다. 다만 이득도
없으므로 슬롯을 쓸 이유가 없다 — 추론 시간을 줄일 일이 생기면 그때 쓴다.


---

## 오라클 진단 — 남은 점수의 **소재**를 특정하고, 그 자리가 닫혀 있음을 확인

닫힌 축 목록이 소진돼 다음 축을 추측으로 고를 수 없게 됐다. 그래서 축을
고르기 전에 **어디에 정보가 남아 있는지**를 먼저 쟀다. `exp/oracle_probe.py`,
학습 0회 (`prod_champ_2024.npy` 7시드 예측 재사용), 폴드 2024 기준선 944.0.

그룹 `G` 마다 Champion 잔차의 그룹 평균을 예측에 더해 다시 잰다. 더하는 값을
**무엇으로 추정했는가**가 셋을 가른다 — `infold`(2024 자신, 과대), **`cv2`
(2024 를 반으로 갈라 교차적합, 정직한 상한)**, `transfer`(학습 시즌 라벨 편차).

| 그룹 | 셀수 | infold | **cv2** | cv2 - 기준 |
|---|---:|---:|---:|---:|
| pitcher x 타자손 | 772 | 1467.2 | **1020.8** | **+76.8** |
| pitcher x 월 | 1,688 | 1880.2 | 1005.2 | +61.2 |
| pitcher x 시즌내십분위 | 1,975 | 2008.2 | 996.2 | +52.2 |
| pitcher | 391 | 1157.8 | 961.9 | +17.9 |
| 타자팀 | 12 | 960.9 | 955.6 | +11.6 |
| pitcher x count | 4,512 | 2853.3 | 948.9 | +4.9 |
| batter | 424 | 1116.1 | 944.4 | +0.4 |
| count(12) / 시즌내십분위 / 월 / 이닝 / base_state / 투수팀 / 구종믹스 | | | 기준선 이하 | -0.8 ~ -20.2 |

### 읽는 법 두 가지

**하나 — 타자·국면 축은 회수가 끝났다.** `batter` +0.4, `count` -4.1,
`이닝` -5.5, `base_state` -0.8. 그 시즌 라벨을 쥐고 셀 평균을 더해도 안 오른다.
**정보가 없는 것이지 표현이 부족한 것이 아니다.** 타자 축을 다시 열지 않는다.

**둘 — 남은 것은 전부 투수 단위이고 시즌 내에서 변한다.** 그것도 후처리
`dev_platoon`(+11.9)이 **이미 얹힌 위**의 잔여분이다. `infold`(1467)와
`cv2`(1021)의 큰 차이는 셀 평균의 상당 부분이 잡음이라는 뜻이지만, 교차적합
후에도 +76.8 이 남는다.

### 그래서 결정적 질문 — 그 정보가 시즌을 건너는가

`exp/resid_table.py`. 폴드 `f` 모델의 `f` 잔차로 셀 표를 만들어 **다음 시즌**
행에 조회한다. (학습 2019~2024) -> 2025 와 구조가 같다. 축소 `n/(n+k)` 를 훑는다.
기준선은 세 폴드 모두 원장 값을 정확히 재현했다 (2475.8 / 211.8 / 944.0).

| 원천 -> 목표 | 최적 k | 이득 | k=500 |
|---|---:|---:|---:|
| 2023 -> 2024 | 5000 | **+6.8 (+0.72%)** | -74.9 |
| 2022+2023 -> 2024 | 5000 | **+6.9 (+0.73%)** | -61.1 |
| 2022 -> 2023 | 500 | +39.9 | +39.9 (단조 증가) |

2022->2023 만 크게 양수다. **2023 이 퇴화 폴드**라 그렇다 — F 라벨 정의 변경으로
모델이 그 해에 어긋나 있어 투수 단위 보정이면 무엇이든 듣는다. 규칙대로 2023 을
채택 근거로 쓰지 않는다.

2024 를 목표로 한 두 판은 서로 일치한다 — 최적 k 약 5000 에서 **+0.7%**.
축소를 약하게 하면(k=500) -60 ~ -75 로 크게 손해다. **셀 평균의 대부분이 그
시즌 고유의 잡음**이라는 뜻이다.

### 결론 — 오라클의 91% 는 시즌을 건너지 못한다

```
시즌 내 상한 (cv2)        +76.8   (+8.1%)
시즌 간 전이 실측          +6.9   (+0.73%)   -> 9.0%
손익분기                          (+2.1%)
```

+0.7% 는 손익분기 아래이고, 그 `k` 조차 2024 를 보고 고른 값이라 실제로는 더
낮다. **기각.** 그리고 이것은 단순 기각보다 큰 정보다 —

**남은 점수는 "투수의 그 시즌 현재 상태"에 있는데, 평가셋(2025)에는 라벨이 없고
규정 4) 때문에 다른 행도 못 본다.** D 가 +85.65 를 낸 이유가 여기서 다시 보인다.
D 는 라벨 없이 `asof_*` 통산 카운터에서 시즌내 상태를 **대수적으로** 복원했다.
같은 자리를 여는 유일한 열쇠는 라벨이 아니라 **행 자신이 들고 있는 카운터**다.
그런데 `asof_*` 에는 **타자손 분할이 없다** — 그래서 +76.8 짜리 자리(투수x타자손)에
닿는 합법 경로가 현재 데이터에 존재하지 않는다.

### 남긴 도구

| 파일 | 무엇 | 비용 |
|---|---|---|
| `exp/oracle_probe.py` | 그룹별 infold/cv2/transfer 상한 | 학습 0회, 20초 |
| `exp/resid_table.py` | 잔차 표의 시즌 간 전이 | 폴드당 학습 2회 (캐시됨) |
| `exp/prod_champ_{2022,2023,2024}.npy` | 생산 경로 폴드별 예측 | 재사용 |

새 그룹 가설이 생기면 `oracle_probe.py` 의 `GROUPS` 에 한 줄 더해 20초에
"거기 정보가 있는가"를 먼저 묻는다. **학습 전에 축을 기각하는 두 번째 도구**다
(첫째는 잔차 상관 사전 선별).


---

## `asof_*` 전수 감사 — D 가 꺼내지 못한 독립 상태 정보는 **없다**

`exp/asof_audit.py` + `exp/asof_resid.py`. 학습 0회 (세 폴드 예측 캐시 재사용,
기준선 2475.8 / 211.8 / 944.0 전부 재현). 라벨·미래행·다른 test 행·test 분포
미사용, 계수는 학습 구간에서만 적합.

### 1. 대수적 종속성 — 두 가지가 확정됐다

```
투수 5비율 합 1.7064 (표준편차 0.0441), 5비율+절편 특이값비 1.09e-2
   -> 정확한 선형 종속이 없다. 5개는 서로 독립 측정이다 (구간이 겹친다:
      succ+mid 0.677, ball+str 0.814)
구종 3비율 합 1.0000 (표준편차 0.0000)
   -> 정확한 단체. 셋 중 하나는 나머지로 결정된다
```

🚩 **`asof_pitcher_pitchmix_n` 은 `asof_pitcher_n` 과 완전히 같은 열이다.**
최대 절대차 0.000000, 일치율 **100.0000%**. 따라서 D 의 `cur_logn_mix` 는
`cur_logn_pitch` 와 일치율 100% 인 **헛열**이다 (82피처 중 1열이 중복).
공식 컬럼 19개 중 실질은 18개다.

### 2. 원시 = 경력 성분 + 현재시즌 성분 (정확한 분해)

```
asof_n * asof_rate = prior_events + cur_n * cur_rate
=> asof_rate = w * prior_rate + (1-w) * cur_rate ,   w = prior_n / asof_n
```

D 가 꺼낸 것이 뒤쪽이므로 "D 를 걷어낸 나머지"는 근사가 아니라 `prior_rate`
(경력 성분)로 **정확히** 특정된다. D 가 원시 분산의 44~85% 를 설명한다
(`D설명 R2` — succ .557 / mid .436 / ball .576 / rev .588 / str .516 /
구종 .735~.851 / 타자 .387~.464).

### 3. 남은 성분의 신호 — 세 폴드 전부 잡음 대역

폴드 2024 는 253,507행이므로 상관의 표준오차가 **0.0020** 이다. in-model 기준
피처 `asof_pitcher_n` 이 +0.0033(1.7SE)이고, 이것이 "회수됨" 대역이다.

| 성분 | 2022 | 2023 | **2024** |
|---|---:|---:|---:|
| 사영잔차 ~ 모델잔차 (최대 절대값) | 0.0039 | 0.0384 | **0.0046** |
| 경력성분 ~ 모델잔차 (최대 절대값) | 0.0042 | 0.0426 | **0.0044** |
| 사영잔차 오라클 십분위 (2024) | | | **전부 음수** (−3.2 ~ −15.3) |

**2023 만 크다. 퇴화 폴드라 그렇다** — F 라벨 정의 변경으로 모델이 그 해에
어긋나 있어 투수 단위 비율이면 무엇이든 걸린다. 채택 근거로 쓰지 않는다.

### 4. 카운터에서 만든 후보 10종 — 전부 잡음 대역

`prior_logn`(경력 투구수) +0.0023 · `cur_share`(통산 중 이번 시즌 몫) −0.0026 ·
`mix_cover_{cur,car}` +0.0002/+0.0026 · `cur_ev_{succ,ball}`(성공 **개수**)
+0.0005/+0.0003 · `pace`(월당 투구수) +0.0022 · `d_succ`(현재−경력) +0.0001 ·
`bat_share` −0.0042 · `prev1_gap_prior` +0.0003.

전부 |2.1SE| 이하이고 오라클 십분위는 전부 음수다. `prior_logn` 은 선수내
분산비가 **0.00** — 완전한 `pitcher_id` 상수라 4-4(정적 프로필) 족보다.

### 5. 남은 최대치의 크기

전 감사에서 모델 잔차와의 상관이 가장 큰 것은 **가운데율 계열**이다.

```
asof_pitcher_prev5_game_middle_rate   -0.0076  (3.8SE)  선형상한 +5.2
asof_pitcher_prev3_game_middle_rate   -0.0071  (3.6SE)  선형상한 +4.5, 오라클 +5.8
asof_pitcher_middle_rate              -0.0062  (3.1SE)  선형상한 +3.5
```

셋 다 **이미 모델에 든 원시 컬럼**이다 — 새 정보가 아니라 모델이 그 방향을
덜 쓰고 있다는 뜻이다. 상한 계산이 서로 맞는다 (`1e5 * 0.0076^2 = 5.8`).

**손익분기는 2024 에서 +2.1% = 약 +19.8 점이다. 최대 후보의 원리적 상한이
+5.2 점(+0.55%)이므로 이 축에서 손익분기를 넘는 것은 불가능하다.**

### 결론 — asof 축 종료

```
A (raw 약한데 D 후 강함)   : 없음 (D 의 이득은 이미 22회차에 실현됨)
B (raw 와 D 가 같은 정보)  : 투수 5비율 · 구종 3비율 · prev 6열 · 카운터
C (D 걷어내도 신호 남음)   : **없음**
D (잔차도 없음)            : 카운터 파생 10종 · 경력 성분 · 타자 3열
```

D 가 +85.65 를 낸 이유는 그것이 `asof_*` 에서 뽑을 수 있는 **유일하게 큰 축**
이었기 때문이고, 그 축은 22회차에 이미 전량 회수됐다. **`asof_*` 는 닫는다.**

### 부수 항목 — `cur_logn_mix` 는 헛열이다

제거해도 정보 손실이 0 인 것이 대수적으로 확정됐다. 이득을 기대할 근거는
없지만 (트리는 중복열을 대체로 무시한다) 82 -> 81 피처가 된다. 값싼 확인
항목으로 남긴다.


---

## D 의 한계 감사 — 잃은 것은 **둘뿐**이고, F/R 혼합은 그중에 없다

`exp/d_limits.py`. 학습 0회, 폴드 2024 기준선 944.0. `pitcher` 기준값이
`oracle_probe.py` 와 소수점까지 일치(+17.9)해 도구가 교차검증됐다.

### 위약 대조를 먼저 놓는다

그룹을 잘게 쪼개면 저절로 이득이 나는 것 아니냐는 의심을 먼저 없앤다.

```
pitcher 단독            391셀   +17.9
위약 pitcher x 무작위2   782셀    +9.4   <- 같은 셀수인데 오히려 낮다
위약 pitcher x 무작위10 3,838셀   +5.0
```

**세분화는 이득을 만들지 않는다.** 무작위 분할은 표본만 쪼개 값을 떨어뜨린다.
그러므로 아래 양수들은 셀 세분화의 산물이 아니라 정보다.

### 한계 후보 7개의 실측

| # | 한계 후보 | 그룹 | cv2 | 판정 |
|---|---|---|---:|---|
| 5 | **타자손 분할 부재** | pitcher x 타자손 | **+76.8** | **진짜 한계** |
| 1 | **궤적 상실** (시즌 평균만) | pitcher x 시즌내십분위 | **+52.2** | **진짜 한계** |
| 7 | 생성 메커니즘 | pitcher x cur_bb 십분위 | +42.1 | 시즌내 투수 변동의 재현 |
| 3 | **F/R 혼합** | pitcher x game_type | **+18.3** | **한계 아님** (단독 +17.9 대비 +0.4) |
| 2 | 작은 cur_n 분산 | cur_n 십분위 | −2.0 | 한계 아님 |
| 4 | 타입별 의미차 | game_type x cur_succ 십분위 | −11.7 | 한계 아님 |
| 6 | 실패유형 겹침 | rev/ball/mid 몫 십분위 | −3.7 / +0.3 / +1.4 | 한계 아님 |

`pitcher x game_type x 타자손` 은 +73.3 으로 `pitcher x 타자손`(+76.8)보다
**낮다** — game_type 을 더하면 셀만 갈라져 손해다. 궤적도 마찬가지다
(+52.5 vs +52.2).

### 🚩 최우선 질문의 답 — F/R 분리 복원은 두 겹으로 막혀 있다

**(1) 대수적으로 불가능하다.** D 가 푼 식은 하나뿐이다.

```
asof_n * asof_rate = prior_events + cur_n * cur_rate        미지수 2, 식 1
F/R 로 가르면 미지수가 4 (cur_n_F, cur_rate_F, cur_n_R, cur_rate_R), 식은 그대로 1
```

공식 카운터는 F/R 이 **섞인 단일 계열**이고, 규정 4) 로 평가셋의 다른 행을 못
보므로 2025 행에서 그 행의 시즌내 F 투구수를 알 방법이 없다. 합법으로 남는 것은
**학습 이력으로 만든 정적 표 D_F/D_R** 뿐이다.

**(2) 그런데 그 정보 자체가 없다.** 2024 라벨을 쥐어 준 오라클로도 +0.4 다.

**(3) 정적 표의 실현 가능치도 0 에 가깝다.** 투수별 F−R 격차를 시즌마다 쟀다.

| 시즌 | 2019 | 2020 | 2021 | 2022 | 2023 | 2024 |
|---|---:|---:|---:|---:|---:|---:|
| F−R 격차 평균 | +0.196 | +0.127 | +0.216 | +0.244 | **+0.012** | **+0.006** |
| 표준편차 | 0.097 | 0.087 | 0.096 | 0.084 | 0.088 | 0.077 |

```
격차 상관 (시즌 s -> s+1)   +0.05 / +0.12 / +0.27 / -0.21 / +0.31   (공통 투수 16~26명)
```

**신체제(2023~)에서 F−R 수준차는 사라졌다.** 2022 까지 +0.24 였던 것이 +0.01 로
붕괴했다 — 이미 규명한 2023 라벨 정의 변경과 정확히 같은 사건이다. 남은 것은
투수별 편차뿐인데 시즌 간 상관이 −0.21~+0.31 로 불안정하다.

**결론 — D_F/D_R 은 설계하지 않는다.** 못 만들어서가 아니라 **가져올 것이 없어서**다.
`game_type` 축 완전 종료를 다시 확인한 셈이다 (앞서 `champ_st` = cur_* x is_F 가
2024 −6.7 이었던 것과 일관된다).

### 남은 진짜 한계 둘 — 그리고 둘 다 이미 측정된 벽에 막혀 있다

```
타자손 분할 부재  +76.8   카운터에 손 분할이 없다 -> 합법 경로 없음
궤적 상실        +52.2   시즌 간 전이 9% (resid_table.py) -> 손익분기 아래
```

D 의 한계는 **표현이 부족한 것이 아니라 입력 카운터의 해상도**다. 공식
`asof_*` 는 (투수, 통산) 한 축으로만 집계돼 있고, 우리가 원하는 것은
(투수, 시즌, 타자손) 또는 (투수, 시즌, 시점)이다. 그 축은 데이터에 없다.


---

## 궤적(trajectory) 진단 — **경로 모양에는 정보가 없다.** 학습 없이 종료

`exp/traj_probe.py`. 학습 0회. A(오라클) / B(시즌 간 전이) / C(잔차 신호)를
분리해 재고, **B 가 충분하지 않으면 학습하지 않는다**는 사전 규약을 따랐다.

### 구조 — 네 점은 시간축의 점이 아니라 중첩된 창이다

```
cur (시즌 전체) ⊃ prev5 ⊃ prev3 ⊃ prev1
d1 = p5 - cur ,  d2 = p3 - p5 ,  d3 = p1 - p3     세 증분의 부호 8가지 = 경로 모양
```

F(차분 하나)와 달리 **모양 등급**을 1급 표현으로 뒀다 (이산 전환 원칙).
경로 결측률 2.1%.

### 🚩 먼저 전제를 정정한다 — "궤적 오라클 +52.2" 는 궤적의 값이 아니다

그 수치는 `pitcher x 시즌내십분위` 였다. **투수 정체성 x 시점**이라 값의 대부분이
투수 성분이고, 경로 모양이 표현할 수 있는 몫이 아니다. 모양을 직접 그룹으로
쓰면 다음이 나온다 (폴드 2024, 기준선 944.0).

| 그룹 | 셀 | cv2 상한 |
|---|---:|---:|
| cur 십분위 (기준) | 10 | −4.8 |
| 모양 8등급 | 9 | −4.6 |
| 단조성 3등급 | 4 | −4.7 |
| 반전 지시자 | 3 | −0.7 |
| **cur 십분위 x 모양** (같은 상태·다른 경로) | 90 | **+0.1** |
| 기울기 / 곡률 / 변동성 / 최근집중도 십분위 | 10 | −1.5 / +1.4 / **+2.0** / +0.2 |
| mid 모양 8등급 · succ모양 x mid모양 | 9 / 65 | −2.6 / −0.4 |

**라벨을 쥐어 준 상한이 최대 +2.0 이다** (참고: `pitcher x 타자손` +76.8).
같은 `cur` 안에서 경로로 갈라도 +0.1 이다.

**왜 그런가** — 모양 등급별 평균 `y` 는 0.4785~0.4926 으로 갈리는데, 같은 등급의
평균 `cur` 이 0.4775~0.5061 로 **같은 방향으로 갈린다.** 모양이 설명하는 것은
수준이고 그 수준은 이미 모델에 있다.

### B. 시즌 간 전이 — 2024 를 목표로 하면 전부 음수

| 원천 -> 목표 | 모양 8등급 | 단조성 | cur x 모양 | 곡률 |
|---|---:|---:|---:|---:|
| 2022 -> 2023 | +8.4 | +0.6 | +25.9 | +0.8 |
| **2023 -> 2024** | **−6.5** | +1.9 | **−8.1** | −8.0 |
| **2022+2023 -> 2024** | **−9.4** | +0.9 | **−10.5** | −9.0 |

2022->2023 만 양수인데 퇴화 폴드라 근거로 쓰지 않는다 (앞선 두 감사와 같은 서명).

### B-2. 지침 7의 핵심 — "관계"는 유지되는가

값의 자기상관이 낮아도 **관계**가 안정적일 수 있다. 그래서 시즌별
[모양 -> 잔차] 벡터를 직접 비교했다. **결측 등급을 포함하면 +0.63 으로 보이지만
그것은 결측 셀 하나가 만든 착시**다. 빼고 다시 재면:

```
모양 8등급 (평균 제거 후)   2022~2023 +0.58   2023~2024 -0.25   2022~2024 -0.16
cur 십분위 x 모양 (80셀)    2022~2023 +0.22   2023~2024 -0.02   2022~2024 -0.10
진폭                       2022 0.0064 -> 2023 0.0046 -> 2024 0.0032 (줄어든다)
```

**가장 중요한 전이(2023->2024)에서 관계가 뒤집힌다.** 관계 안정성 논거로도
살릴 수 없다.

### C. 잔차 신호와 값의 지속성

```
잔차상관 (SE 배수)   slope  2022 +1.8 / 2023 +0.5 / 2024 +0.6
                    curv   +3.0 / +2.1 / +0.3
                    vol    +2.0 / -4.8 / -2.2      부호가 폴드마다 뒤집힌다
                    conc   +1.1 / +4.6 / -2.9

투수 수준 값의 순위 지속성 (스피어만 중앙값)
    slope +0.053    curv -0.123    vol +0.329
```

`vol`(변동성)만 투수 형질로서 지속된다(+0.33). 그런데 그 오라클이 +2.0 이고
잔차상관 부호가 폴드마다 뒤집힌다 — **형질이지만 타깃 정보가 아니다.**

### 결론

```
TRAJECTORY 축 종료. 학습 없이 닫는다 (사전 규약: B 부족 -> 학습 금지).
```

D 의 두 병목 중 궤적 쪽은 "표현이 부족한 문제"가 아니었다. 경로 모양이 담는
것은 수준이고 수준은 이미 회수돼 있다. 남는 병목은 **타자손 해상도 하나**이며
그것은 공식 카운터에 분할이 없어 합법 경로가 없다.


---

## 생성 가정 감사 — 가정 하나가 틀렸고, 파생 8열이 정보가 없다

`exp/gen_audit.py`. 학습 0회, 새 피처 0개. 원자료를 직접 검사했다.

### Q3. 생성 순서 — D 의 전제는 **정확히** 맞다

```
투수-시즌 조합 2,260개
  asof_n 중복 0개 (0.0%)
  빈틈(span-size)=0 인 조합 100.0%          <- 모든 투구가 행으로 존재한다
시즌 경계 1,330쌍
  다음 시즌 첫 asof_n = 이번 시즌 마지막 +1  100.0%   <- 통산 누적이 정확하다
```

**관측되지 않은 투구가 없다.** `cur_n` 을 시즌내 순번으로 읽어 온 근거가
원자료 수준에서 확인됐다.

### 🚩 Q3-b. 그런데 카운터는 **F 와 R 을 섞는다** — 우리가 틀린 가정

```
전체(F+R)  빈틈0 비율 100.0%
R 행만     빈틈0 비율  91.4%     <- R 만 보면 구멍이 난다
F 행만     빈틈0 비율  92.8%     <- F 만 봐도 구멍이 난다
한 시즌에 F 와 R 을 모두 던진 투수-시즌 867 / 2,260 (38.4%)
```

합집합에서만 연속이므로 **하나의 카운터가 두 체제의 투구를 함께 센다.**
따라서 `cur_succ` 는 성공률이 다른 두 모집단의 **혼합 추정치**다.

구체제(2022)에서 F−R 격차가 +0.205 였으므로 그 시절 혼합 오차는 컸다. 다만
신체제에서는 격차가 −0.03 이라 **지금은 오염이 거의 없다** (`pitcher x game_type`
오라클 +0.4 와 정확히 일치한다). 그리고 F/R 분리 복원이 대수적으로 불가능하다는
앞선 결론의 **원자료 근거**가 이것이다 — 카운터가 애초에 하나다.

### Q1. F 는 야구가 아니라 **라벨링**이다 (커버리지도 아니다)

| 시즌 | R 성공률 | F 성공률 | F−R | R TM커버 | F TM커버 |
|---|---:|---:|---:|---:|---:|
| 2019 | .5495 | .6892 | +.140 | 0% | 0% |
| 2022 | .5037 | .7087 | **+.205** | 82% | 60% |
| 2023 | .5031 | .4729 | **−.030** | 84% | 75% |
| 2024 | .4897 | .4593 | −.030 | 80% | 60% |

TrackMan 커버리지로 층화해도 F 는 양쪽 다 무너진다 (있음 .715->.483,
없음 .700->.443). R 은 어느 쪽도 안 움직인다. **커버리지 아티팩트가 아니다.**
2019 는 TrackMan 커버가 **0%** 다 (2020 부터 65% -> 79%).

### Q2. 같은 종류의 구분자는 **없다**

전체 추세를 제거한 뒤 시즌 간 최대 급변:

```
game_type  0.207   <- 압도적
투수팀      0.066 (R 행만 0.080)   타자팀 0.065
game_month 0.036   TM커버 0.034   나머지 0.02 미만
```

**팀 13 의 2023 급변(+0.080)은 진짜지만 체제가 아니다.** 홈(+0.086)과
원정(+0.074) 양쪽에 똑같이 나타나므로 구장 효과가 아니고, 공통 투수 16명이
93.8% 같은 방향으로 움직였으므로 로스터 교체(15.8%)도 아니다. 구단 단위
사건이고 2023 한정이다.

### 구장(홈팀) 축 — 신규 검사, 그리고 종료

`top_bottom` 이 T 면 투수팀이 홈이므로 **구장이 정확히 복원된다** (12개, 100%).
원장에 있던 "홈원정"은 후처리 4단 축(셀 10~16행)이었고 구장 정체성은 시험된
적이 없었다. 폴드 2024 오라클:

```
구장(홈팀)      +0.1      홈/원정 단독   -1.2      구장 x 홈원정  +0.2
투수팀          -6.8      타자팀        +11.6      구장 x 투수손  +2.6
```

`타자팀` +11.6 만 양수였으나 **전이가 음수**다 (2023->2024 −9.7,
2022+2023->2024 −10.0). **팀·구장 축 전체 종료.**

### 🚩 Q6. 47열 중 **8열은 독립 정보가 없다**

정확한 항등식으로 확인 (전부 100.0000%):

```
run_total_before      = run_top + run_bot
score_diff_home       = run_bot - run_top
score_diff_pitcher_team = 수비팀 기준 점수차
num_runners_on        = 1b + 2b + 3b
base_state            = 주자 3플래그의 문자열 표현
asof_pitcher_pitchmix_n = asof_pitcher_n          (앞선 감사)
```

그리고 `li` · `home_win_expectancy` · `away_win_expectancy` 는
**(이닝, 초말, 아웃, 주자상태, 홈기준점수차)의 완전한 결정함수**다 —
30행 이상 셀 5,045개에서 **값이 유일한 셀 100.0%**, 셀내 표준편차 0.0000.
`home + away` 는 반올림 오차(최대 0.1) 안에서 항상 100 이다.

**즉 승률기대·중요도는 팀 전력이나 구장 같은 숨은 정보를 담고 있지 않다.**
경기상태 5열의 압축일 뿐이다 (트리에게는 깊이를 아껴 주므로 유용하지만
새 정보는 0 이다).

### 목표 정의의 구조 — 5비율은 분할이 아니다

```
succ + rev + mid + ball + str = 1.7064 (표준편차 0.0441)
succ + mid = 0.677    ball + str = 0.814
```

다섯 결과 차원은 **서로 겹치는 주변분포**이고 결합분포는 관측되지 않는다.
그래서 "실패 구성비"(rev/fail, ball/fail)가 죽은 것이다 — 존재하지 않는
분할을 가정한 값이었다 (오라클 −3.7 / +0.3 / +1.4).

### Q4. train 과 test 는 같은 규칙이다

test 5행의 `(li, home_we)` 가 train 상태표 값과 float32 정밀도까지 일치하고,
`pitchmix_n == pitcher_n` 이 test 에서도 100%, `n=0` 일 때 rate 결측도 동일하다.

### Q5. 고해상도 상태의 합법 대리 — 없다

`pitcher x 타자손` 오라클 +76.8 에 대해 합법으로 닿는 것은 학습 이력으로 만든
정적 표뿐이고 그 실현치는 +6.8~+6.9 (9%)로 이미 측정됐다. 구장·홈원정·팀·
game_type 어느 것도 대리가 되지 못한다 (전부 오라클 ≤ +0.2 또는 전이 음수).


---

## 형식(formulation) 감사 — D->확률 사상에는 남은 것이 없다

D 구성 고정, 피처 0개, 하이퍼파라미터 무변경. `exp/form_audit.py`, 학습 0회.
§30 에서 이미 닫힌 것(objective · 비선형 재보정 · 다양성 · 행 게이팅 오라클
+0.7)은 반복하지 않고 **시험된 적 없는 두 형식**의 상한만 쟀다.

### 시험 1 — 구간별 아핀 (상태 의존 확률 모형·조건부 예측함수의 상한)

`rho` 는 전역 아핀에 불변이지만 **구간마다 다른 아핀**은 불변이 아니다. 2024 를
반으로 갈라 교차적합했다 (§30-b 에서 in-sample 재보정에 속은 적이 있다).

| 구간 축 | 아핀 상한 | 기울기만 | 기울기 범위 |
|---|---:|---:|---|
| game_type (F/R) | **−3.6** | −134.7 | 0.89 ~ 1.05 |
| cur_n 십분위 | **−9.1** | −501.7 | 0.90 ~ 1.15 |
| cur_n 4분위 | −8.2 | −299.3 | 0.97 ~ 1.11 |
| 같은손 | **+3.9** | −263.1 | 0.96 ~ 1.13 |
| 카운트 우위 | +1.0 | −10.2 | 1.03 ~ 1.04 |
| 2스트라이크 | −0.1 | −296.0 | 0.96 ~ 1.07 |
| cur_succ 십분위 | −12.3 | −329.4 | 0.72 ~ 1.29 |
| 예측값 십분위 | −17.4 | −920.0 | 0.12 ~ 1.61 |
| 투수 (391) | −131.7 | −923.0 | −0.22 ~ 1.83 |

**기울기가 사실상 1 이다.** 의미 있는 축들의 범위가 0.89~1.15 이고, 라벨을 보고
구간별로 다시 맞춰도 최대 이득이 **+3.9**(같은손)다. 손익분기 +19.8 의 5분의 1이다.

`예측값 십분위`의 기울기 0.12~1.61 은 이질성이 아니라 **예측자 자신으로 층화할
때의 감쇠 편향**이고, 결과가 −17.4 로 해롭다.

### 시험 2 — 잠재 상태 (10개 cur_* 를 독립 축으로 주는 것이 손실인가)

**전제부터 틀렸다.** 10축의 주성분 설명력이

```
0.21 0.20 0.16 0.13 0.11 0.09 0.06 0.03 0.02 0.00      누적 1~3성분 0.56
```

**하나의 잠재 상태가 아니라 실질 5~7차원이다.** 성공률·가운데율·볼률·반대율·
구종믹스·타자쪽은 서로 다른 것을 재고 있다.

그리고 대각 방향에도 남은 것이 없다. 학습 시즌(2022+2023) 잔차로 적합한 최적
선형결합의 2024 잔차상관이 **−0.0043 (−2.1SE)**, 선형 상한 **+1.8점**,
사영 십분위 오라클 **−10.0** 이다.

### 다섯 질문의 답 — 전부 상한이 손익분기 아래다

| 질문 | 측정된 상한 | 출처 |
|---|---:|---|
| 1 D 를 단순 벡터로 취급 | 잔차 2단 모델 0/3 폴드 | §닫힌 축 |
| 2 10축을 독립으로 처리 | +1.8 (사영 오라클 −10.0) | 시험 2 |
| 3 cur_n 을 피처로 vs 상태의존 모형 | −9.1 (게이팅 +0.7, EB −6.2) | 시험 1 · §30-e |
| 4 game_type 을 예측함수 조건으로 | −3.6 (in-model 분리 −6.7) | 시험 1 · champ_st |
| 5 2단계 잠재->조건부 확률 | 투수 오라클 +17.9, 전이 +6.9 | oracle_probe · resid_table |

**5번이 핵심이다.** 어떤 2단계 구조든 회수하려는 것은 "투수의 현재 상태"이고,
그 시즌 라벨을 그대로 쥐어 준 상한이 **+17.9**(+1.9%), 시즌을 건너오는 몫이
**+6.9**(+0.73%)다. 손익분기 +2.1% 아래이므로 **구조를 바꿔도 도달점이 이미
손익분기 밑에 있다.**

### 결론

D 를 확률로 잇는 현재 사상(단일 CatBoost + 후처리 편차 4축)은 **이 데이터에서
남은 것을 이미 다 쓰고 있다.** 형식을 바꿔 얻을 것이 없다. D 구성은 고정한다.


---

## 해상도 격차 조사 — 고원(1158~1177)은 **투수 상태 해상도로 설명되지 않는다**

새 피처 0개, 학습 0회, LB 제출 없음. 규정 원문과 이미 측정된 상한만 쓴다.

### 1. 규정 원문 (추측 없음)

**외부 데이터 — 금지 확정** (§6-m 에 원문 확보):

> 3) 외부 데이터 사용 금지 — 온라인 해커톤(Phase 2)에서 제공하는 공식 데이터
> 외의 외부 데이터는 사용할 수 없습니다.

`data_description.md` L196 의 "대회 규칙상 허용되는 외부 데이터"는 이 조항에
종속되며, 그 조항이 허용하는 범위는 **공집합**이다.

**평가 행 간 참조 — 금지** (`data_description.md` §5):

> 평가 데이터의 각 행은 독립적으로 예측해야 합니다. (선수별·팀별·월별 누적,
> 빈도/분포, target encoding, 행 순서 기반 rolling/expanding, 전체를 보고 만든
> 사후 보정값)

**사용 금지 정보** (§6): 현재 투구 이후 확정 정보 · 실제 위치/코스 · 판정/결과 ·
실제 구종 · 현재 투구의 Trackman 측정값 · **2025년 Trackman 데이터**.

**허용 확인**: `asof_*` 는 "각 행의 투구 직전 시점까지의 과거 기록"이므로 사용 가능.

### 2. 우리가 가진 정보의 시간 해상도

```
행 자체의 시간 축   season · game_month · game_dayofweek        <- 날짜·경기ID 없음
투수 내부 시간 축   asof_pitcher_n (통산 투구 일련번호, 연속성 100% 검증)
없는 것            경기 식별자 · 타석 내 투구순번(pitch_of_pa) · 경기 내 투구수
```

`trackman_history.csv` 에는 `game_date` · `trackman_game_id` · `pitch_no` ·
`pitch_of_pa` · `batter_hand` 가 **있다.** 그러나 (1) 2019~2024 뿐이고
**2025 는 규정으로 금지**, (2) 메인 데이터와 1:1 결합 테이블이 아니며
(`trackman_game_id` 는 `row_id` 와 직접 대응하지 않음), (3) 결측이 `game_type` F 를
대리해 어떤 파생도 2024 에서 죽는다(TMX −0.9 / TMR −3.9). **추론 시점(2025)에
쓸 수 있는 채널이 아니다.**

### 3. 필요한 rho 역산 — 그리고 결정적 비교

```
Champion 1049.9226  ->  rho = 0.102466
```

| 목표 | rho | Δrho 상대 | rho^2 상대 | 필요한 **직교 신호** c | 필요 로컬 2024 이득 |
|---|---:|---:|---:|---:|---:|
| 10위 1157.96 | 0.107609 | +5.02% | +10.29% | **0.0329** | **+14.47%** |
| 고원 중앙 1170 | 0.108167 | +5.56% | +11.44% | 0.0347 | +15.85% |
| 1위 1291.12 | 0.113627 | +10.89% | +22.97% | 0.0491 | +29.75% |

D 자체가 로컬 +12.87% / LB +8.97% 였다. **10위에 들려면 D 보다 큰 발견이
하나 더 필요하다.**

### 🚩 결정적 비교 — 최대 오라클을 100% 전이시켜도 고원에 못 닿는다

측정된 모든 상한을 같은 자(로컬 2024, 기준선 944.0)로 환산했다.

| 상한 | 로컬 이득 | 전이식 예상 LB |
|---|---:|---:|
| `pitcher x 타자손` 오라클 (**라벨 사용, 불법**) | +8.14% | 1102.8 |
| `pitcher x 시즌내십분위` 오라클 | +5.53% | 1080.1 |
| `pitcher` 오라클 | +1.90% | 1048.4 |
| 그 전이 실측 | +0.73% | 1038.3 |
| 가운데율 계열 / 구간별 아핀 / 잠재축 | ≤ +0.55% | ≤ 1036.7 |

**전이 손실을 0 으로 가정하고 최대 오라클을 그대로 얹어도 LB 1135.3 이고,
10위(1157.96)에 −22.6 이다.**

즉 **고원 8팀의 우위는 "투수의 현재 상태를 더 잘 복원했다"로 설명되지 않는다.**
그 축은 라벨을 쥐어 줘도 부족하다. 이것이 이번 조사의 핵심 결과다.

### 4. A / B / C 분류

**A. 현재 데이터에서 합법적으로 복원 가능** — 남은 것이 전부 +5점 미만이다.

```
가운데율 계열 잔차 방향        상한 +5.2 (이미 모델에 든 원시 열)
같은손 구간별 아핀            상한 +3.9 (2024 라벨로 얻은 값)
10축 잠재 사영               상한 +1.8 (2024 잔차상관 -0.0043)
투수 단위 잔차표 전이          실측 +6.9  (손익분기 아래)
```

**B. 외부 허용 데이터로 확보 가능 — 공집합.** 규정 3)이 명시적으로 금지한다.
설령 허용됐더라도 §6-l 의 세 장벽이 남는다: 선수·팀 ID 익명, 평가 행에 날짜
없음(월·요일뿐), 행의 11% 가 퓨처스라 공개 기록이 희박.

**C. 데이터에 존재하지 않아 원천 불가능** — 여기에 큰 것들이 전부 있다.

```
2025 시즌내 pitcher x 타자손 상태   카운터에 손 분할이 없다 (오라클 +76.8)
2025 시즌내 궤적 상태              시즌 평균만 복원 가능 (오라클 +52.2)
경기 내 투구수·타석 내 순번         경기 식별자가 없다. 다른 test 행 참조는 규정 4) 금지
2025 Trackman                    규정 6) 명시적 금지
현재 투구의 위치·구종·판정          규정 6) 명시적 금지
5개 결과 차원의 결합분포            주변분포만 제공된다 (합 1.706, 분할 아님)
```

### 결론

A 는 바닥났고(전부 +5 미만), B 는 규정상 공집합이며, C 는 원천적으로 막혀 있다.
그리고 **C 의 가장 큰 항목을 불법으로 100% 실현해도 1135.3 으로 고원에 못 닿는다.**

고원의 정체는 우리가 측정한 축들 밖에 있다. 남은 가능성은 세 가지이고
전부 이 저장소의 자료로는 판별할 수 없다 — (1) 우리가 착상하지 못한 행 내부
신호, (2) 규정 경계의 다른 해석, (3) 같은 피처에서 **더 잘 적합하는 모델**.
(3)만 로컬에서 판별 가능하며 그 진단이 다음 최소 행동이다.


---

## 정보 한계 vs 적합 한계 — **적합은 병목이 아니다.** 표본이 한계다

`exp/fit_limit.py`. 새 피처 0개, Champion 82p 고정, 제출 없음. 시드 42.

### 1. train / OOS (모델 단독, train 은 학습 행 20만 표본)

| 폴드 | 학습행 | train | OOS | 격차 | OOS+후처리 |
|---|---:|---:|---:|---:|---:|
| 2022 | 728,588 | 2542.8 | 2450.7 | +92.1 | 2465.6 |
| 2023 | 976,060 | 2637.0 | 190.6 | +2446.4 | 206.6 |
| 2024 | 1,221,585 | 2313.5 | 921.4 | +1392.2 | 944.0 |

**이 격차를 그대로 과적합으로 읽으면 안 된다.** 시즌마다 난이도가 다르다 —
2022 의 *정직한* OOS 자체가 2450.7 이다. 그래서 폴드 2024 모델의 in-sample 을
**시즌별로** 갈라 같은 행의 정직한 OOS 와 비교했다.

| 시즌 | in-sample | 정직한 OOS | 기억화 배수 |
|---|---:|---:|---:|
| 2022 | 3045.1 | 2450.7 | **1.24** |
| 2023 | 1682.3 | 190.6 | 8.83 |

**진짜 과적합은 1.24배다.** 2023 의 8.83 은 기억화가 아니라 **F 라벨 정의 변경**
때문이다 — 2023 행을 학습에 넣으면 신체제를 맞출 수 있지만, 2022 이하만으로
학습한 모델은 원리적으로 못 맞춘다. 이미 규명한 사실의 정량적 재확인이다.

### 2. 반복수 곡선 (같은 적합의 부분 앙상블, 폴드 2024)

| 나무 | 100 | 200 | 400 | 600 | 800 | 1000 | 1200 |
|---|---:|---:|---:|---:|---:|---:|---:|
| train | 1643.6 | 1760.6 | 1893.0 | 1995.1 | 2097.6 | 2207.9 | 2313.5 |
| OOS+후처리 | 803.0 | 851.7 | 893.0 | 917.2 | 936.7 | 943.1 | **944.0** |
| 증분 | — | +48.7 | +41.3 | +24.2 | +19.5 | +6.4 | **+0.9** |

**포화했다.** 마지막 200그루가 OOS 에 +0.9 를 주는 동안 train 은 +105.6 오른다.
추가 용량은 전부 기억화로 간다. **적합 여지가 없다 -> C 기각.**

### 3. 표본 크기 곡선 (폴드 2024)

| 비율 | 학습행 | train | OOS+후처리 | 배가당 증분 |
|---|---:|---:|---:|---:|
| 0.125 | 152,698 | 4992.7 | 774.9 | — |
| 0.250 | 305,396 | 3491.5 | 843.8 | +68.9 |
| 0.500 | 610,792 | 2847.6 | 891.2 | +47.4 |
| 1.000 | 1,221,585 | 2391.3 | **944.0** | **+52.8** |

**아직 가파르다.** 표본을 두 배로 하면 여전히 약 +50 이 오른다. 즉 현재 모델은
**표본 한계** 상태이고, 정보를 다 뽑았다기보다 **추정 오차가 남아 있다.**

### 판정

```
A 과적합       부분적으로만 참 (1.24배). 그러나 용량을 줄여도 늘려도 나빠진다(§27)
B 정보 한계     현 표현에서 실질적으로 참 — 반복수 곡선이 944 에서 포화
C 적합 병목     기각 — 마지막 200그루가 +0.9 다
```

**새 모델 탐색을 시작하지 않는다** (사전 규약: C 일 때만 시작).

### 부수 소득 — 제출본은 이미 이 곡선의 이득을 받고 있다

최종 학습은 2019~2024 전량 **1,475,092행**으로 하고, 폴드 2024 측정치보다 21%
많다. 곡선 기울기(배가당 +52.8)로 환산하면 약 **+14.6점**에 해당한다. 이것은
이미 실현된 몫이고 추가로 할 일이 없다는 뜻이기도 하다 — 데이터는 고정이고
외부는 규정 3)으로 금지다.


### 검증 — Champion 아티팩트는 **이미 전량 학습본**이다 (2026-08-17)

앞 절에서 "최종 학습은 1,475,092행이라 곡선상 +14.6 을 이미 받고 있다"고 적었다.
가정이 아니라 **아티팩트를 열어 확인**했다.

```
submissions/cand_asof_xl.zip 내부 model/rf.pkl  sha256 ed600d7fc515794a
model_cand/cat_asof_xl.pkl                      sha256 ed600d7fc515794a   <- 동일
  (zip 시각이 pkl 보다 8시간 늦은 것은 재포장일 뿐 내용은 같다)
```

번들 내부:

| 항목 | 값 |
|---|---|
| 시드 | `cat-s42` · `cat-s43` · `cat-s44` (3개) |
| 나무 수 | 1200 x 3 (lr 0.02, depth 6) |
| 피처 | 82 |
| alpha / center | 1.09 / 0.5854452601930041 |
| 후처리 편차 | 4축, 셀 1,571 / 18,753 / 17,767 / 6,185 |

**학습 행 수는 `asof_prior` 로 직접 셀 수 있다** — `prior_tables` 의 표본수는
그 구간의 행 수이므로 선수별 합계가 곧 학습 행 수다.

```
kind=pitch  792명   합계 1,475,092
kind=mix    792명   합계 1,475,092
kind=bat    830명   합계 1,475,092
```

**정확히 1,475,092 = train.csv 전량이다.** 폴드 학습분(1,221,585)이 아니다.
`build_asof.finish()` 가 `prior_tables(tr, 전체마스크)` 와 `mm.fit(Xtr, y)` 에
같은 `tr` 을 쓰므로 모델과 prior 의 행 집합도 동일하다.

### 결론 — 추가 표본 이득은 **없다. 중복 계산하지 않는다**

```
LB 1049.9226 = 전량(1,475,092행) 학습 x 3시드 프로덕션 결과
표본 곡선이 말한 +14.6 은 **이미 이 점수 안에 들어 있다**
재학습할 것도, 새로 만들 아티팩트도 없다
```

표본 곡선의 기울기(배가당 약 +50)는 "더 넣을 데이터가 있었다면"의 값이고,
데이터는 고정이며 외부는 규정 3)으로 금지다. **이 축은 닫힌 상태로 확정된다.**


---

## LOCAL -> FUTURE 전이 연구 — **깨끗한 의사미래 쌍이 0개다**

`exp/transfer_study.py`(창 5개 x 구성 6개 x 2시드, 약 100분) +
`exp/transfer_fit.py` / `transfer_fit2.py`. 새 피처 0개, 제출 없음.

### 설계

창 `w` 는 시즌 `<w` 로 학습하고 **이후 모든 시즌**에 예측한다 (예측은 초 단위라
시간 거리가 공짜로 나온다). 목표 `w` 가 이미 1시즌 앞이므로 `거리 = s-w+1`.

    LOCAL_t  = gain%(창 t,   목표 t)     FUTURE_t = gain%(창 t+1, 목표 t+1)

후보는 실제 LB 앵커가 있는 5개를 그 앵커와 같은 중첩으로 만들었다
(BASE55 -> D68 -> DX76 -> CH82, DF74, K286). 검증 — 창 2024/목표 2024 에서
D 이득 +13.2% (기존 측정 +12.87%), CH=943.8 (원장 943.8) 로 재현됐다.

### 🚩 왜 이 연구가 원하는 답을 줄 수 없는가

시즌별 기준선(BASE) `rho^2` 를 보면 즉시 드러난다.

```
창 2020:  2020 645.2   2021 1460.8   2022 1978.7   2023 30.0   2024 22.8
창 2021:  2021 1403.3  2022 1815.2   2023 179.6    2024 131.3
창 2022:  2022 2384.8  2023 96.2     2024 95.0
창 2023:  2023 87.1    2024 60.3
창 2024:  2024 802.9
```

**분모가 22~2400 으로 100배 흔들린다.** 그래서 상대 이득(%)조차 불안정하다 —
같은 D 가 (창2022,목표2023)에서 **+122.83%**, (창2024,목표2024)에서 +13.24% 다.

20개 쌍을 등급으로 나누면:

```
소창(창<=2021, 학습 237k~481k)   10쌍
퇴화(2023 이 LOCAL 또는 FUTURE)  10쌍
정상(둘 다 아님)                  0쌍   <- 하나도 없다
```

**2019 는 TrackMan 0%, 2020 은 표본 최소, 2023 은 F 라벨 정의 변경.** 6시즌 중
4시즌이 비정상이라 "충분한 학습량 + 정상 시즌 -> 정상 시즌" 쌍이 만들어지지 않는다.
**이것이 전이식이 세 번 깨진 진짜 이유다** — 앵커 자체가 서로 다른 체제에서 왔다.

### 그래도 얻은 두 가지 수치

**(1) 소창이지만 2023 을 안 건드리는 10쌍** (가장 덜 오염된 부분집합)

```
원점통과 기울기 b = +0.512    잔차 RMS 4.93%p
비율 중앙값 +0.179            corr(LOCAL, FUTURE) +0.471
부호 보존 70%
```

**(2) 실제 LB 앵커 6건** (생산 경로 로컬% -> 실제 LB%)

| 후보 | family | 로컬% | LB% | 비율 |
|---|---|---:|---:|---:|
| D | current-state | +12.87 | +8.97 | **+0.697** |
| H1 | context-interaction | +2.12 | +0.49 | +0.233 |
| X | context-interaction | +2.78 | +0.37 | +0.135 |
| F | recent-history | +1.36 | −0.82 | **−0.605** |
| K2 | regime | −0.87 | −0.45 | +0.518 |
| EB | shrinkage | −0.66 | −1.32 | +2.006 |

```
비율 중앙값 +0.375   평균 +0.497   표준편차 0.789
부호 보존 5/6 (로컬+ 4건 중 LB+ 3건, 로컬- 2건 중 LB- 2건)
```

**두 독립 경로가 중심값 0.2~0.5 에서 일치한다.** 그러나 산포가 그만큼 크다.

### 가설 A/B/C 판정

```
A  Future = b x Local          b=+0.145 (전체) / +0.512 (2023 미관여)   RMS 4.9~27.8%p
B  Future = a + b x Local      a=+8.4 b=+0.05  — 절편이 데이터 이질성을 흡수할 뿐
C  임계 가설                    LOCAL>=0.5% 12건 평균 전이 +3.46
                               LOCAL<0.5%  8건 평균 전이 −21.05
```

**C(임계)가 가장 잘 지지된다.** 작은 로컬 이득은 전이가 아니라 **잡음**이다.
A 의 기울기는 부분집합에 따라 3배 이상 흔들려 점추정으로 못 쓴다.

### family 별 (실제 LB 앵커 기준이 더 신뢰도 높다)

```
current-state (D)        LB 비율 +0.697   가장 높다
context-interaction      LB 비율 +0.135 ~ +0.233
regime (K2)              +0.518 (음수를 음수로 전이)
recent-history (F)       −0.605   부호까지 뒤집힌다
shrinkage (EB)           +2.006   음수를 더 크게 전이
```

**"수준은 전이되고 차분은 안 된다"가 정량화됐다.** current-state 0.70 vs
recent-history −0.61 은 CLAUDE.md 4절의 원리와 정확히 같은 순서다.

### 시드 SNR

```
corr(SNR, 전이비율)      +0.130   (약함)
corr(LOCAL 시드sd, |델타|) +0.521   (시드 산포가 큰 후보일수록 전이가 더 많이 어긋난다)
SNR 낮은 절반 전이 평균 −16.9  vs 높은 절반 +4.2
```

시드 산포는 **전이의 크기를 못 예측하지만 어긋남의 폭을 예측한다.**

### 시간 거리 — 이 설계로는 측정 불가

거리별 평균 이득이 D +33%(거리1) -> +96%(거리5)로 **증가**한다. 감쇠가 아니라
먼 창의 기준선이 붕괴(BASE 22.8)해서 분모가 작아진 것이다. 창을 옮기면
**학습량과 시간거리가 동시에** 바뀌므로 둘을 분리할 수 없다. 이 항목은 미해결로 남긴다.

### 실무 규칙 (LB 앵커 기준, 더 신뢰도 높은 쪽)

```
EXPECTED_FUTURE% ~ 0.4 x LOCAL%   (중앙값 0.375, 사분위 0.14~0.70)
불확실성: LB 잔차 RMS 0.3~0.75%p (= 3~8점)

양수를 2시그마로 확신하려면  0.4 x LOCAL% > 1.5%p  ->  LOCAL% > 3.8%
```

기존 손익분기(로컬 2.1%)와 같은 방향이고 **더 보수적**이다. 우리가 오늘 측정한
모든 후보(최대 로컬 +0.18%)는 이 문턱의 20분의 1이다.

### 최종 판정

```
전이의 크기는 예측 불가 (계수가 부분집합에 따라 3배 흔들림, 정상 쌍 0개)
전이의 부호는 준예측 가능 (실제 LB 5/6, 의사미래 70~86%)
family 가 체계적으로 다르다 (current-state 0.70 > context 0.2 > recent -0.6)
가장 큰 불확실성 = 역사적 기록에 정상 쌍이 없다는 구조적 결함
```


---

## PIPELINE B (state-first) 기각 — 그리고 `+17.9` 오라클의 분해

새 피처 0개, 제출 없음. `exp/pipeline_b.py`, `exp/oracle_decomp.py`,
`exp/oracle_legal.py`(로그).

### 1. PIPELINE A vs B — 혼합값을 빼면 손해다

D 는 **학습이 없는 결정론적 추정기**다(`cur_rate` 는 대수 항등식). 그래서
"추정기를 분리한다"는 조작 자체로는 정보가 늘지 않는다. 실제로 다를 수 있는
곳은 Stage 2 의 입력뿐이고, 원시 `asof_rate` 가 정의상 **혼합**이라는 점이
미검증 지점이었다.

```
asof_rate = w * prior_rate + (1-w) * cur_rate ,   w = prior_n / asof_n
A 는 트리에게 혼합값과 분해값을 동시에 준다 -> 트리가 cur_n 으로 분기해 스스로 푼다
```

| 구성 | 2022 | 2023 | **2024** | 2024 % | 시드폭 |
|---|---:|---:|---:|---:|---:|
| B1 원시 asof 비율 16열 제거 (66p) | −34.7 | −13.3 | **−16.4** | −1.74% | 8.2 |
| B2 원시 asof 19열 전부 제거 (63p) | −37.2 | −23.1 | **−10.6** | −1.13% | 3.4 |

**3폴드 전부 음수.** 게이트(B < +1% -> 종료)대로 **state-first family 종료.**

의미 — 경력 성분(prior)은 잔차상관이 잡음 대역(|0.0044|)이지만 그것은
**모델이 이미 쓰고 있어서** 남은 것이 없다는 뜻이었다. 빼면 −1~2% 손해다.
"혼합을 푸는 데 용량을 낭비한다"는 가설은 **반증됐다.**

### 2. `+17.9` 오라클이 정확히 무엇이었나 (코드 재구성)

```
resid = y2024 - Champion예측
2024 행을 무작위 반으로 갈라, 한쪽 절반의 라벨로 투수별 잔차평균을 구하고
n/(n+k) 로 축소해 반대쪽 절반에 조회해 더한다. k 를 훑어 최댓값. -> k=2000 에서 +17.9
```

**오라클이 아는 것 = 그 투수의 그 시즌 나머지 절반에서의 평균 잔차.**
행 자신의 라벨도, 시즌 전체 평균도 아니다. 최적 `k=2000` 이라는 것 자체가
경고다 — 절반 시즌 650투구인 투수도 셀 평균의 **24%만** 반영된다.

### 3. 분해 — 그 잔차평균의 대부분은 잡음이다

| 절반당 최소 투구 | 투수 | 절반 A~B 상관 | 관측 sd | 잡음 sd | **신호 sd** | 신호분산비 |
|---:|---:|---:|---:|---:|---:|---:|
| 50 | 294 | +0.121 | 0.0363 | 0.0348 | 0.0101 | **0.08** |
| 100 | 247 | +0.206 | 0.0310 | 0.0280 | 0.0132 | **0.18** |
| 300 | 166 | +0.250 | 0.0243 | 0.0219 | 0.0106 | **0.19** |

**투수별 잔차평균 분산의 81~92% 가 이항 잡음이다.** 진짜 시즌내 효과의
표준편차는 0.010~0.013 에 불과하다.

시즌 간 안정성 (300투구 이상):

```
2023~2024  +0.193   감쇠보정 +0.774      <- 인접 시즌은 진짜 효과가 꽤 유지된다
2022~2024  -0.016   감쇠보정 -0.064      <- 두 시즌 건너면 사라진다
2022~2023  +0.069
```

**진짜 효과는 인접 시즌에 유지되지만 크기가 너무 작다.** 그래서 과거 시즌
표로 만든 보정의 실측 이득이 **+1.0(2023->2024) / +0.5(2022+2023->2024)** 다.

### 🚩 4. 합법 대리 검증에서 한 번 속을 뻔했다

학습 시즌에서 [투수 수준 합법 피처 -> 잔차평균] 사상을 적합해 2024 에 적용하니
**+19.5** 가 나왔다. 손익분기(+19.8) 코앞이다. **그러나 세 겹으로 무효다.**

```
(1) 가중 0.25 를 2024 에서 골랐다 (선택 편향)
(2) 투수별 평균은 **평가셋의 다른 행**을 봐야 만들어진다 -> 규정 4 위반
(3) 시즌 평균은 그 행보다 **미래의 투구**를 포함한다 -> 규정 6 위반
```

합법 형태(행 단위, 그 행 자신의 피처만, 가중은 2023 에서 선택)로 다시 재면:

| 가중 | 2023 이득 | **2024 이득** |
|---:|---:|---:|
| 0.10 | +1.6 | **−1.4** |
| 0.50 | +7.5 | **−9.2** |
| 1.50 (2023 최적) | +20.4 | **−42.6** |

**부호가 정반대다.** 2023 에서 좋아 보일수록 2024 에서 더 나쁘다. 이는 닫힌
축인 잔차 2단 모델(트리 최적 가중 0/3)과 같은 서명이다.

반면 규정 위반 형태는 2024 에서 **+46.3** 까지 오른다. **그 격차(+46 vs −43)가
곧 "다른 행/미래를 보는 것"의 가격**이고, 우리가 넘지 않는 선이다.

### 결론

```
ORACLE INFORMATION      투수 x 그 시즌 라벨 집계 (교차적합, k=2000)      +17.9
  A 합법 행 단위 대리    측정 -42.6 (부호 반대)                           0
  B 과거 학습으로 추정   실측 +1.0 / +0.5                                +1.0
  C 라벨 없이는 불가     나머지 (신호분산비 0.19, 인접시즌만 유지)        ~+16.9
```

`+17.9 -> +1.0` 은 **전이 구조가 아니라 잡음 축소의 결과**다. 셀 평균의 81~92%가
잡음이고, 남은 진짜 효과는 인접 시즌에 유지되지만(감쇠보정 +0.774) 크기가
0.01 수준이라 보정으로 환산하면 1점이다. **기대 현실 이득 = +1.0점 (+0.11%)**
으로 게이트(+3.8%)의 30분의 1이다.


---

## 🚩 경기 단위 상태 — 압축돼 사라진 것의 크기는 **+187.8** 이다 (합법 경로 없음)

`exp/game_state.py`. 학습 0회, 2분. 새 피처 0개.

### 경기 경계 복원이 맞는지부터

`prev1_game_*_rate` 는 한 경기 안에서 상수이고 경기가 바뀔 때만 변한다.
`asof_pitcher_n` 은 투구마다 1씩 증가한다(연속성 100% 검증). 둘을 합쳐
투수별로 정렬하고 prev 값이 바뀌는 지점을 끊었다.

```
복원된 (투수,경기) 8,017개   경기당 투구수 중앙값 19, 90분위 89, 최대 151
분위 10/25/50/75/90/99 = 9 / 13 / 19 / 35 / 89 / 105
PN 감사의 독립 추정: 불펜 1~30, 선발 90~119, 최대 151    <- 정확히 일치
```

복원이 맞다. 1투구 단위는 0.4% 뿐이다.

### 결과 — 위약 대조가 결정적이다

| 그룹 | 셀 | 오라클 |
|---|---:|---:|
| **(투수, 경기)** | 8,017 | **+187.8** |
| 위약: 투수 x 무작위(**같은 셀 수**) | 8,017 | **+1.9** |
| 투수 (참조) | 391 | +17.9 |
| 경기 내 순번 십분위 | 10 | −5.1 |
| 경기 크기 십분위 | 10 | +0.6 |

**같은 셀 수의 무작위 분할이 +1.9 인데 실제 경기 단위는 +187.8 이다.**
세분화의 산물이 아니라 **진짜 경기 단위 상태**다. 지금까지 잰 어떤 것보다 크다
(투수x타자손 +76.8 의 2.4배).

이것이 "데이터가 압축해 버린 상태"의 크기다 — 포수(라벨 정의에 직접 들어간다) ·
심판 · 구장 · 날씨 · 휴식 · 그날의 컨디션이 전부 여기 들어 있다.

### 그런데 합법 경로가 없다

```
경기 내 순번(피로)   잔차평균 1~5 −0.0112 / 61~90 −0.0132 / 91~200 −0.0220
                    corr(순번, 잔차) −0.0024 (−1.2SE)  -> 피로 신호 없음
경기 첫 투구 지시자   오라클 −1.9
```

그리고 **그 행 하나로는 경기 경계를 알 수 없다.**

```
0-0 카운트 행이 25.8% 인데 그중 실제 경기 첫 투구는 12.2%
1회 0아웃 주자없음 0-0 (0.67%) 만 97.0% 로 식별된다  -> 선발 첫 투구뿐
```

경기 단위 보정을 쓰려면 **같은 경기의 다른 투구 결과**를 봐야 한다. 2025 에서
그것은 (1) 라벨이 없고 (2) 규정 4)가 금지하는 평가셋 행 간 참조다.
train 으로 만든 표도 소용없다 — 경기는 그 시즌에만 존재한다.

### 🚩 고원과의 크기 대조 (관찰이지 주장이 아니다)

```
+187.8 / 944.0 = +19.9%  ->  LB 1049.9226 x 1.199 = 약 1259
실제 상위권          1위 1291.12   2위 1240.63   3~10위 1158~1177
```

**우리가 넘지 않기로 한 선(평가셋 행 간 집계)의 크기가 고원~1위 구간과 겹친다.**
이것은 상위권이 그렇게 했다는 증거가 아니다 — 다른 설명도 가능하다. 다만
"우리가 못 보는 정보가 그 정도 규모로 존재한다"는 사실은 처음으로 정량화됐다.
우리는 규정을 지키므로 이 축은 **관측만 하고 닫는다.**

### 판정

```
GAME_BOUNDARY_SIGNAL   경기 단위 상태 +187.8 (위약 +1.9) — 실재하고 매우 크다
                       그러나 경계 지시자 자체는 −1.9, 경기 내 피로는 없다
RESIDUAL_SIGNAL        같은 값. 지금까지 측정된 최대 압축 상태
ALREADY_CAPTURED       경기 내 순번·경기 크기는 이미 없거나 회수됨(−5.1 / +0.6)
NEW_INFORMATION        합법적으로는 0. 그 행 하나로 경계를 식별할 수 없고
                       (0-0 중 12.2%만 첫 투구), 보정에는 같은 경기 다른 행이 필요
DECISION               종료. 추가 실험 없음
```


---

## `+187.8` 분해 — 행 하나로 사전 추정 가능한 몫은 **0 이다**

`exp/game_decomp.py`. 학습 0회. 오라클 값을 피처로 만들지 않았고 같은 경기의
다른 행도 참조하지 않았다 (경기 경계 복원은 train 진단 도구로만).

### 1. 경기 잔차평균의 대부분은 잡음이다

| 절반당 최소 투구 | 경기 | A~B 상관 | 관측 sd | 잡음 sd | **신호 sd** | 신호분산비 |
|---:|---:|---:|---:|---:|---:|---:|
| 5 | 6,223 | +0.129 | 0.1563 | 0.1479 | 0.0508 | **0.11** |
| 15 | 2,083 | +0.241 | 0.1008 | 0.0880 | 0.0492 | **0.24** |
| 40 | 881 | +0.249 | 0.0851 | 0.0726 | 0.0445 | **0.27** |

**73~89% 가 이항 잡음이다.** 진짜 경기 단위 효과의 sd 는 0.045~0.051 로,
투수 단위 효과(0.010~0.013)의 **약 4배**다. 경기 단위 상태는 실재하고 투수
단위보다 훨씬 크다 — 그러나 그 위에 잡음이 세 배로 얹혀 있다.

### 2. 🚩 중첩해서 얹으면 3분의 1로 줄어든다

각 층을 **이전 보정 위에서** 다시 적합해 쌓았다 (그룹 키는 전부 행 컬럼).

| 단계 | 셀 | 누적 | 증분 |
|---|---:|---:|---:|
| 투수 | 391 | +17.9 | +17.9 |
| + 투수 x 월 | 1,688 | +45.0 | +27.0 |
| + 투수 x 월 x 구장 | 4,538 | +46.0 | **+1.0** |
| + (투수, 경기) | 8,017 | **+68.4** | +22.4 |

**단독으로 잰 경기 오라클은 +187.8 인데 층을 쌓으면 총합이 +68.4 다.**
앞 층의 추정 잡음이 뒤 층의 적합 대상에 그대로 얹히기 때문이다. **라벨을
쥐고도 순차 보정으로는 3분의 1밖에 못 가져온다** — 오라클 값을 실현 가능치로
읽으면 안 되는 이유가 하나 더 늘었다.

구장은 투수x월 위에서 **+1.0** 뿐이다 (앞서 단독 오라클 +0.1 과 일관).

### 3. 결정적 — 경기 시작 행의 값으로는 경기 상태를 못 맞힌다

경기별 잔차평균을 그 경기 **첫 행의 합법 값**(cur_* 5, cur_logn, 통산 성공률,
prev1/3/5, 월)으로 예측하는 선형 사상을 2022+2023 에서 적합했다.

```
2023 경기 6,658개   corr(예측, 실제) = +0.208     <- 적합에 쓰인 시즌이라 낙관적
2024 경기 6,888개   corr(예측, 실제) = +0.002     <- **out-of-sample 은 0 이다**

적용 이득 (2024)   가중 0.10 −1.7 / 0.25 −8.3 / 0.50 −29.0 / 1.00 −97.5
```

**상관 +0.002.** 경기 단위 잠재상태는 행이 들고 있는 어떤 값으로도 사전에
예측되지 않는다. 포수·심판·날씨·휴식은 데이터에 없고, 투수의 상태값(cur_*)은
그날의 컨디션을 담지 못한다.

### 판정

```
GAME_ORACLE            +187.8 (단독) / +68.4 (층을 쌓았을 때 실제로 회수되는 몫)
ROW_ONLY_RECOVERABLE   ~0   (out-of-sample 상관 +0.002, 적용하면 −1.7 ~ −97.5)
UNRECOVERABLE          사실상 전부. 진짜 신호 sd 0.045~0.051 인데 그것을 가리킬
                       행 단위 관측치가 없다
LEGAL_PROXY            없음. 구장 +1.0, 타자팀 전이 −9.7, 월/요일 음수,
                       투수x월은 그룹은 합법이나 추정에 그 시즌 라벨이 필요
BEST_NEW_INFORMATION   없음
EXPECTED_UPSIDE        0
NEXT_TEST              없음 — 승격 조건(+3.8%) 근처에도 못 간다
```

**경기 축 종료.** 이로써 "압축된 상태"의 크기는 정량화됐고(신호 sd 0.05,
단독 오라클 +187.8), 동시에 **행 단위로는 그 방향을 가리킬 관측치가 하나도
없다**는 것도 확정됐다.


---

## 🚩 정보 해상도 감사 — 데이터는 타석/경기 해상도인데 타깃은 투구 해상도다

`exp/info_coverage.py`. 학습 0회. 47열 전부의 분산을 계층으로 분해했다
(투수 / 시즌|투수 / 경기|시즌 / 타석|경기 / 투구|타석). 경기·타석 경계는
train 진단으로만 복원했다 (경기 45,755, 타석 381,289, 타석당 3.87투구).

### 결과 — 투구 해상도를 가진 컬럼은 **둘뿐이다**

```
투구 수준 분산이 과반인 컬럼   balls_before 0.63,  strikes_before 0.68
그 외 투구 성분               asof 비율 0.02~0.08 (매 투구 갱신되는 몫), 나머지 0.00
```

계층별 대표:

| 수준 | 대표 컬럼 (해당 수준 분산 비중) |
|---|---|
| 투수 상수 | `pitcher_id` 1.00 · `pitcher_hand` 1.00 · 구종비율 0.85~0.93 · 통산 성공률 0.68 |
| 시즌 | `season` 0.57 · `asof_pitcher_n` 0.46 |
| 경기 | `top_bottom` 0.95 · `game_dayofweek` 0.94 · `batter_team_id` 0.91 · `prev1_middle` 0.87 · 승률기대 0.82 · `game_month` 0.79 |
| 타석 | `outs_before` 0.88 · `batter_hand` 0.82 · 주자 0.71~0.76 · `batter_id` 0.72 · 타자 asof 0.49~0.56 |
| **투구** | **`strikes_before` 0.68 · `balls_before` 0.63** — 끝 |

**타깃은 그 투구의 위치로 정해지는데, 데이터에서 투구마다 움직이는 것은
볼카운트 둘뿐이다.** 이것이 이 프로젝트의 천장을 설명한다.

### 해상도 사다리 — 오라클과 셀 크기

| 수준 | 셀 | 셀당 행 | 오라클 |
|---|---:|---:|---:|
| 투수 | 391 | 648.4 | +17.9 |
| **(투수, 경기)** | 8,017 | 31.6 | **+187.8** |
| (투수, 경기, 타석) | 65,374 | 3.9 | +75.8 |
| 위약: 경기 내 무작위 타석 | 64,094 | 4.0 | +11.8 |

타석 수준에도 **진짜 구조가 있다**(+75.8 vs 위약 +11.8). 경기보다 낮게 나오는
것은 정보가 적어서가 아니라 **셀이 3.9행뿐이라 추정 잡음이 이기기 때문**이다.

### 타깃 자체의 계층 분산

```
투수 0.010   경기|투수 0.042   타석|경기 0.233   투구|타석 0.715
```

타석 수준 구조가 경기 수준의 **5.5배**, 투수 수준의 23배다. 그런데 우리가 가진
타석 수준 컬럼(타자, 주자, 아웃, 손)은 전부 **오라클이 0 이하**로 이미 회수됐다
(batter +0.4, base_state −0.2, outs +0.2, 타자손 −1.2).

### 생성 과정에서 제거된 것 (원자료 확인)

```
제공 안 됨   경기 식별자 · 정확한 날짜 · 포수 · 심판 · 투구 위치 · 투구 구종 ·
             타석 내 투구 순번(pitch_of_pa) · 투구 순서
2019~2024 만  trackman_history 에 game_date · trackman_game_id · pitch_no ·
             pitch_of_pa · 구종 · 물리량이 있다 (위치·포수·심판은 **거기에도 없다**)
2025          Trackman **규정으로 금지** (data_description 6절)
```

`pitch_of_pa` 는 파울까지 세므로 볼카운트로 복원되지 않는다. 타석 내 **순서**
(1-1 이 볼-스트라이크인지 스트라이크-볼인지)도 한 행으로는 알 수 없다.
이것이 타석 수준 +75.8 의 정체 중 관측 가능했던 부분이다.

### 판정

```
OBSERVED INFORMATION
  47열. 실질 39열 (8열은 독립 정보 0 — 항등식 5 + pitchmix_n 중복 + li/승률기대 결정함수)
  해상도는 투수/시즌/경기/타석에 몰려 있고 투구 해상도는 볼카운트 둘뿐

UNOBSERVED BUT EXISTING (생성 시점에는 존재했으나 제공 안 됨)
  경기 식별자 · 날짜 · 포수 · 심판 · 투구 위치/구종 · 타석 내 순번과 순서
  크기: 경기 수준 오라클 +187.8, 타석 수준 +75.8 (둘 다 그 시즌 라벨 필요)

LEGALLY RECOVERABLE
  **0.** 행 하나로 경기·타석 경계를 식별할 수 없고(0-0 카운트 행 중 경기 첫
  투구는 12.2%), 경기 잔차평균의 row-only OOS 예측 상관은 +0.002 다.
  trackman 은 2025 가 금지 + 위치/포수/심판이 애초에 없다.

IRRECOVERABLE
  위 전부. 그리고 타깃 분산의 71.5% 는 투구 수준이라 원리적으로 관측 밖이다.

UNEXPLORED_HIGH_VALUE_RELATIONS = 0
```

**정보공간은 다 썼다.** 남은 것은 데이터에 없거나(포수·심판·위치), 규정이
금지하거나(2025 trackman, 행 간 참조), 그 시즌 라벨이 필요한 것(경기·타석 상태)뿐이다.


---

## latent 대리 탐침 — 경기 잠재축은 **D 와 직교한다**

`exp/latent_proxy.py`. 학습 0회. 오라클을 피처화하지 않았고, 단순 보정 대신
**방향 정합**부터 쟀다.

    정합도 = corr(그 관계의 교차적합 보정, 경기 잠재축 보정)

경기 잠재축 보정의 sd 는 0.0165 (이득 +187.8).

### 1. 방향 정합 (폴드 2024)

| 관계 | 셀 | 오라클 | **정합도** | 설명 분산 |
|---|---:|---:|---:|---:|
| 투수 x 월 | 1,688 | +61.2 | **+0.492** | 0.242 |
| 투수 x 점수차 | 2,094 | +20.5 | +0.285 | 0.081 |
| 투수 x 타자 매치업 | 26,355 | +61.3 | +0.272 | 0.074 |
| 투수 (참조) | 391 | +17.9 | +0.217 | 0.047 |
| **cur_ball 십분위 x 월** (미검증) | 80 | +0.1 | **+0.108** | 0.012 |
| **cur_succ 십분위 x 투수팀** (미검증) | 115 | −3.6 | +0.110 | 0.012 |
| **cur_succ 십분위 x 월** (미검증) | 80 | −4.7 | +0.094 | 0.009 |
| cur_succ 십분위 (참조) | 10 | −7.8 | +0.038 | 0.001 |

🚩 **D 의 현재상태(cur_*)를 어떤 맥락과 곱해도 정합도가 0.09~0.11 이다.**
경기 잠재축은 **투수가 그날 어떤 상태였는가**의 축이고, D 가 재는 것은
**시즌 누적 수준**이다. 둘은 사실상 직교한다 (설명 분산 1%).

잠재축과 그나마 겹치는 것은 전부 **정체성 x 시간** 계열이다 —
투수x월 0.492, 투수x점수차 0.285, 매치업 0.272. 그러나 전부 그 시즌 라벨로
추정해야 한다.

### 2. 미검증 관계의 시즌 간 전이 (2022+2023 -> 2024)

| 관계 | 최적 k | 2024 이득 | k=0 |
|---|---:|---:|---:|
| cur_succ x 월 | 50000 | **−0.8** | −32.0 |
| cur_ball x 월 | 50000 | **−0.8** | −47.1 |
| cur_succ x 투수팀 | 50000 | **−1.4** | −70.2 |
| 투수 x 타자 매치업 | 500 | **+3.9** | −765.4 |

### 3. 부수 — (투수 x 타자) 매치업 축의 첫 측정

행 단위 합법 그룹이고 원장에 시험 이력이 없었다.

```
2024 매치업 26,355개 (투구 중앙값 7)   오라클 +61.3   위약(투수x무작위타자) -0.0
학습 2시즌에 같은 매치업이 있던 2024 행 44.8%
전이 +3.9 (k=500) = +0.41%
```

**위약이 정확히 0 이므로 매치업 신호는 진짜다.** 그러나 전이가 +0.41% 로
게이트(+3.8%)의 9분의 1이다. 타자 단독 오라클이 +2.2 인데 매치업이 +61.3 인
것은 **상호작용이지 타자 주효과가 아니라는 뜻**이다 — 다만 그 상호작용의
대부분이 그 시즌에만 존재한다.

### 판정

```
LATENT_PROXY     투수x월 (정합도 0.492) 이 최선. cur_* 계열은 0.09~0.11 로 무관
ORACLE_CORR      0.492 / 0.285 / 0.272 (정체성x시간 계열) vs 0.038~0.11 (D 계열)
OOS_CORR         경기 잔차평균의 row-only 예측 상관 +0.002 (game_decomp)
TRANSFER         cur x 월 −0.8 · cur x 팀 −1.4 · 투수x월 −1.1~−1.5 · 매치업 +3.9
EXPECTED_GAIN    최대 +3.9점 = +0.41%  (게이트 +3.8% 의 1/9)
LEGAL            전부 합법 (행 자신의 컬럼 + 학습 구간 상수)
DECISION         승격 없음. **경기 잠재축의 row-only 대리는 존재하지 않는다**
```

이로써 "큰 잠재축을 간접적으로 잡는다"는 경로도 닫힌다. 이유가 분명하다 —
잠재축은 **그날의 상태**인데 우리 데이터의 고해상도 축은 볼카운트 둘뿐이고,
D 는 시즌 누적이라 그날을 가리키지 못한다.


---

## 매치업 축 종료 — 시즌 간 지속성이 **0** 이다. 그리고 EB 축소가 왜 실패했는지 밝혀졌다

`exp/matchup_audit.py`. 학습 0회. 원시 매치업 비율을 피처화하지 않고
표본 구조 -> 계층 분해 -> 적률법 축소 -> 전이 순으로 갔다.

### 1. 표본 구조

```
2024  pair 26,355개   투구/pair 중앙 7  평균 9.6  90분위 20  최대 84
                      1~3투구 pair 가 19.0%
학습 2시즌 pair 41,756개, 두 시즌 모두 등장하는 pair 21.3%
2024 커버리지 44.8%  — 그중 학습 표본 10투구 이상은 29.7% 뿐
```

적률법 분해: 관측 pair 평균 분산 0.02507 = **잡음 0.02093 + 신호 0.00414**
-> 신호비 **0.165**. 즉 원시 매치업 평균의 83.5% 가 이항 잡음이다.

### 2. 계층 분해 (학습 시즌 잔차, 적률법 k)

| 성분 | 셀 | k | 신호비 |
|---|---:|---:|---:|
| 투수 주효과 | 483 | 241 | 0.809 |
| 타자 주효과 | 507 | 523 | 0.650 |
| **pair 잔차** | 41,756 | 89 | **0.118** |

주효과를 걷어내도 pair 잔차에 신호가 남긴 한다(11.8%). 앞서 잰 오라클
+61.3(위약 −0.0)과 일관된다 — **매치업 상호작용은 실재한다.**

### 🚩 3. 그런데 시즌을 못 건넌다

같은 pair 의 잔차평균이 시즌 간에 유지되는지 직접 쟀다.

| 시즌쌍 | nmin | 공통 pair | 상관 |
|---|---:|---:|---:|
| 2022~2023 | 5 / 10 | 5,521 / 2,347 | **+0.017 / +0.037** |
| 2023~2024 | 5 / 10 | 5,277 / 2,176 | **+0.032 / +0.024** |
| 2022~2024 | 5 / 10 | 3,472 / 1,390 | −0.007 / +0.003 |

**전부 0 이다.** 매치업 효과는 그 시즌 안에서만 존재한다 (투수 단위 효과가
인접 시즌 감쇠보정 +0.774 였던 것과 대조적이다).

### 4. A/B/C 실측 (표는 학습 시즌으로만 제작)

| 구성 | 2024 이득 |
|---|---:|
| A 원시 pair (적률 k=60) | **−85.9** |
| B 주효과 제거 후 pair 잔차 (적률 k=89) | **−46.9** |
| C 투수 + 타자 + pair 잔차 (전부 적률 k) | **−173.8** |
| 참고: 주효과만 (투수+타자, 적률 k) | −154.3 |

### 🚩 5. 가장 값진 소득 — EB 축소가 왜 실패했는지

적률법이 고른 k 는 투수 241 / 타자 523 / pair 89 다. 그런데 **전이에 최적인
k 는 투수 20,000, pair 500 이상**이다 (앞선 `resid_table` 과 아래 민감도).

```
pair 잔차 축소 민감도 (2024 이득)
  k x 0.5 = 44    −137.9
  k x 1.0 = 89     −46.9      <- 적률법이 고른 값
  k x 2.0 = 177     −8.9
  k x 5.0 = 443     +2.4
```

**경험적 베이즈는 "이번 시즌의 효과를 이번 시즌 데이터로 추정"하는 문제를
풀지만, 우리가 푸는 것은 "다음 시즌의 효과를 예측"하는 문제다.** 후자는 효과가
시즌을 건너며 감쇠하는 만큼 **훨씬 세게 축소해야** 한다 — 실측으로 약 100배다.

이것이 27회차 EB 축소가 LB −13.83 으로 무너진 이유를 설명한다. 그때 우리는
적률법으로 k 를 뽑아 "데이터가 정하게 했다"고 적었는데, 적률법이 푸는 문제가
우리 문제가 아니었다.

### 판정

```
MATCHUP_ORACLE              +61.3 (위약 −0.0) — 실재하는 상호작용
PAIR_COUNT_DISTRIBUTION     중앙 7투구, 1~3투구가 19%, 2024 커버리지 44.8%
                            (학습 10투구 이상은 29.7%)
RAW_SIGNAL                  신호비 0.165 (83.5% 가 이항 잡음)
PITCHER/BATTER_MAIN_EFFECT  신호비 0.809 / 0.650 — 주효과는 잘 추정된다
MATCHUP_RESIDUAL            신호비 0.118 — 주효과를 걷어내도 남는다
TRANSFER                    시즌 간 pair 상관 **+0.017 ~ +0.037 (사실상 0)**
                            적용 이득 A −85.9 / B −46.9 / C −173.8
PARTIAL_POOLING_POTENTIAL   적률법 k 는 전이 문제에 **100배 부족**하다.
                            k 를 5배로 키워야 겨우 +2.4 (=+0.25%)
EXPECTED_GAIN               최대 +2.4 ~ +3.9점 (+0.25~0.41%)
DECISION                    **종료.** 게이트(+3.8%)의 10분의 1
```

**매치업 축 종료.** 신호는 실재하지만 그 시즌 고유이고, 시즌을 건너는 순간
사라진다.


---

## 🚩🚩 매치업 근본 원인 — 오라클의 절반 분할이 **투구 단위였던 것**이 문제였다

`exp/matchup_sens.py`, `exp/matchup_root.log`. 학습 0회.

### 가설

pair 당 중앙 7투구는 **타석 2개** 남짓이다. 그런데 오라클의 교차적합은 절반을
**투구 단위 무작위**로 갈랐다. 그러면 같은 타석·같은 경기의 투구가 양쪽 절반에
나뉘어 들어간다. pair 셀 평균이 "그 pair 의 고유 효과"가 아니라 **같은 타석/경기의
상태**를 실어 나른 것 아닌가.

### 검정 — 분할 단위를 거칠게 하면

| 그룹 | 투구 분할 | 타석 분할 | **경기 분할** | 셀 커버리지(경기) |
|---|---:|---:|---:|---:|
| (투수 x 타자) | **+61.3** | +16.2 | **+4.0** | 45.1% |
| (투수, 경기, 타석) | +95.7 | — | 0 (구조상 불가) | 0% |
| 투수 | +10.9 | +10.5 | **+0.8** | 98.6% |

**가설이 확인됐다.** 그리고 경기 분할값이 **시즌 간 전이 실측과 거의 같다.**

| 그룹 | 투구 분할(기존) | **경기 분할(정직)** | 전이 실측 |
|---|---:|---:|---:|
| 투수 x 타자손 | +76.8 | **+20.7** | +6.9 |
| 투수 x 월 | +67.6 | **+2.8** | −1.1~−1.5 |
| 투수 x 점수차 | +4.9 | **+0.1** | +1.7 |
| 투수 | +11.9 | **+1.0** | **+1.0** |
| 투수 x 타자 (pair) | +61.3 | **+4.0** | **+3.9** |

### 이것이 "큰 오라클 vs 0 전이"의 정체다

지금까지 우리는 "오라클은 큰데 전이가 0" 을 **"그 시즌에만 존재하는 효과"** 로
해석해 왔다. 절반은 틀렸다. 진짜 이유는 **측정 프로토콜의 누수**다.

```
투구 단위 무작위 분할  ->  같은 타석/경기의 투구가 양쪽에 들어간다
                     ->  셀 평균이 그 경기의 상태를 실어 나른다
                     ->  오라클이 부풀려진다
```

경기 단위로 가르면 투수 +1.0 = 전이 +1.0, pair +4.0 = 전이 +3.9 로 **정확히
일치**한다. 즉 **전이 측정은 처음부터 옳았고, 오라클이 틀렸다.**

남는 진짜 격차는 `투수 x 타자손` 하나다 (경기 분할 +20.7 vs 전이 +6.9) — 이것만
"그 시즌 고유"로 부를 수 있고, 나머지는 애초에 없던 것이다.

### 매치업 감도(sensitivity) 축 — 별도로 기각

`batter_id` 대신 타자를 저차원 형질로 표현하고 투수별 기울기를 쟀다
(투수당 200투구 이상, 249명).

| 타자 형질 | 기울기 지속성 22~23 / 23~24 / 22~24 | 오라클 | 전이 |
|---|---|---:|---:|
| 타자 통산 성공률 | −0.050 / −0.070 / −0.154 | −159.0 | −1.0 |
| 타자 통산 가운데율 | −0.000 / −0.089 / −0.219 | −180.6 | −2.9 |
| 타자 시즌 성공률 | +0.041 / −0.037 / −0.025 | −161.7 | +0.5 |
| **같은손** | **+0.200 / +0.152** / +0.052 | −36.6 | +3.0 |
| 타자 경험 | −0.224 / +0.000 / +0.037 | −170.7 | −0.9 |

**오라클이 전부 음수다** — 시즌 안에서조차 기울기가 잡음이다. 같은손만 지속성이
+0.15~0.20 인데 그것은 후처리 `dev_platoon`(투수x타자손)이 이미 하는 일이다.

### 최종

```
MATCHUP ROOT CAUSE      +61.3 은 pair 고유 효과가 아니라 **같은 타석/경기 상태의
                        누수**였다. 경기 단위로 가르면 +4.0 이고 이는 전이 +3.9 와 같다.
                        pair 의 41% 는 시즌에 타석 1회만 만난다.
STABLE MATCHUP SIGNAL   경기 분할 기준 +4.0. 그 이상은 없다
BEST REPRESENTATION     원시 pair (축소 k>=500). 감도·계층 분해는 전부 더 나쁘다
ORACLE                  +4.0 (정직) / +61.3 (누수 포함, 폐기)
TRANSFER                +3.9
REALISTIC UPSIDE        +0.41%  (게이트 +3.8% 의 1/9)
DECISION                **매치업 상호작용은 실재하지만 future-predictable 구조가
                        아니다.** family 종료.
```

### 🚩 방법론 수정 — 앞으로 오라클은 **경기 단위로 가른다**

이 발견은 매치업만의 문제가 아니다. 지금까지의 모든 `cv2` 오라클이 투구 단위
분할이었고 따라서 **전부 부풀려져 있었다.** 결론(전이 기반 판정)은 바뀌지 않지만
해석은 바뀐다 — 우리는 "그 시즌 고유 효과"라고 불렀던 것의 상당 부분이
**클러스터 누수**였음을 이제 안다.


---

## TRANSFER-AWARE SHRINKAGE 이론 — **재현되지 않는다.** 그리고 그 이유가 중요하다

`exp/ta_shrink.py`. 학습 0회 (폴드 2020~2024 예측 재사용). 제출 없음.

검증 대상: `θ̂_future = ρ x θ̂_current` (EB 추정값에 전이상관 ρ 를 곱한다).
`ρ` 는 각 전이에서 **그 이전 전이들로만** 적합했다.

### 결과 — ρ 가 전혀 안정적이지 않다

| 전이 | 적률 k | 사후최적 ρ | 과거로 적합한 ρ̂ |
|---|---:|---:|---:|
| 2020->2021 | 294 | **−0.462** | (앞선 전이 없음) |
| 2021->2022 | 289 | **+0.030** | −0.462 |
| 2022->2023 | 1,381 | **+0.893** | −0.256 |
| 2023->2024 | 90 | **+0.144** | −0.181 |

**ρ 가 −0.46 에서 +0.89 까지 흩어진다.** 안정 계수가 존재하지 않는다.
적률 k 자체도 90~1,657 로 18배 범위다.

### 다음 시즌 실제 이득

| 전이 | RAW | STANDARD EB | TRANSFER-AWARE (과거 ρ̂) | 참고: 사후최적 ρ |
|---|---:|---:|---:|---:|
| 2020->2021 | −708.3 | −330.8 | — | +15.7 |
| 2021->2022 | −410.4 | −119.7 | −37.5 | +0.1 |
| 2022->2023 | +44.1 | +25.1 | −7.4 | +22.7 |
| 2023->2024 | −400.2 | −258.0 | −40.1 | −1.1 |

**사후최적 ρ 를 써도(반칙) 이득이 0 근처다.** 과거로 적합한 ρ̂ 는 부호가 틀려
표준 EB 보다 나은 전이도 있고(−258 -> −40) 나쁜 전이도 있다(+25 -> −7).

### 🚩 왜 ρ 가 음수/불안정한가 — 우리 모델이 스스로 흡수한다

폴드 `s+1` 의 모델은 **시즌 s 를 학습 데이터에 포함**한다. 그래서 시즌 s 에서
관측된 투수 효과 중 **지속적인 성분은 다음 폴드 모델이 이미 배워 버린다.**
남는 잔차는 정의상 **모델이 못 배운 부분**이고, 그것은 지속되지 않는 성분이다.

```
시즌 s 잔차의 지속 성분  ->  폴드 s+1 모델이 학습에 흡수  ->  s+1 잔차에서 사라짐
                                                        (때로 과보정되어 음의 상관)
```

이것이 지금까지 관측한 모든 것을 하나로 설명한다.

```
투수 잔차 표 전이   +1.0      매치업 전이 +3.9      투수x월 −1.1      팀 −9.7
```

**"큰 오라클, 0 전이"의 두 번째 원인이다.** 첫 번째는 클러스터 누수(측정 문제)
였고, 두 번째는 **모델의 자기 흡수**(구조 문제)다. 둘을 걷어내면 잔차 표로
가져올 것이 원리적으로 거의 남지 않는다.

### 판정

```
IS THEORY EMPIRICALLY VALID?   아니오. 4개 전이에서 ρ 가 −0.46 ~ +0.89 로 흩어진다
ρ STABILITY                    없음. 과거로 적합한 ρ̂ 는 3개 전이 모두 음수
BEST TRANSFER ESTIMATOR        0 으로 강하게 축소하는 것 = **표를 아예 얹지 않는 것**
FUTURE GAIN                    ~0 (사후최적으로도 +0.1 ~ +22.7, 그 +22.7 은 퇴화 폴드)
SEED STABILITY                 해당 없음 (학습 0회, 결정론적 표)
GENERALIZABLE RULE             **잔차 표(hierarchical effect table)를 만들지 않는다.**
                               모델을 재학습하면 지속 성분은 모델이 가져가고
                               표에는 비지속 성분만 남는다
```

**이론 종료.** 다만 소득은 크다 — 앞으로 "이 효과는 크니 표로 만들자"는 발상
자체를 접을 근거가 생겼다. 후처리 편차 4축이 통했던 이유도 이제 분명하다:
그것은 잔차 표가 아니라 **라벨 편차 표**이고, 모델과 **동시에** 만들어져
흡수 대상이 아니었다.


---

## 투수 x 타자손 축 최종 — **유일하게 4/4 지속되는 효과**, 그러나 +0.85%

`exp/hand_axis.py` + 차등 검정. 학습 0회. 모든 오라클은 **경기 단위 분할**.

### 전제

이 축은 **이미 Champion 에 있다** — 후처리 1번축 `dev_platoon`
(nested_dev, 부모=투수, 자식=투수x손, k=300, w=0.20), 2024 기여 +11.9.
아래는 전부 **그 위의 잔여분**이다.

### 1. 표본 구조

| 시즌 | 투수 | 양손 50투구 이상 | 손별 중앙 투구 |
|---|---:|---:|---:|
| 2022 | 390 | 276 (71%) | 177 |
| 2024 | 391 | 267 (68%) | 222 |

```
cold start   2024 행 중 학습 2시즌에 없던 투수 22.9%
             학습 2시즌에 양손 50투구 이상인 투수의 행 74.4%
```

pair(중앙 7투구)와 비교하면 **표본이 25~30배**다. 이것이 이 축만 살아남은 이유다.

### 🚩 2. 손 차등의 지속성 — 이 프로젝트에서 가장 안정적이다

`d_p = (같은손 잔차평균) − (반대손 잔차평균)`

| 전이 | 공통 투수 | 상관 | 가중 회귀 기울기 |
|---|---:|---:|---:|
| 2020->2021 | 196 | **+0.102** | +0.058 |
| 2021->2022 | 187 | **+0.076** | +0.133 |
| 2022->2023 | 191 | **+0.104** | +0.114 |
| 2023->2024 | 181 | **+0.105** | +0.182 |

**4개 전이 전부 같은 부호이고 범위가 +0.076~+0.105 로 좁다.** 비교하면
투수 주효과의 ρ 는 −0.46~+0.89 로 흩어졌고 pair 는 +0.02~+0.04 였다.
**손 차등만이 재현되는 구조다.**

### 3. 수준(level)은 안 되고 차등(differential)만 된다

| 전이 | RAW 수준 | 표준 EB 수준 | 과거 ρ̂ |
|---|---:|---:|---:|
| 2021->2022 | −539.2 | −126.0 | −0.287 |
| 2022->2023 | +17.8 | +41.7 | −0.125 |
| 2023->2024 | −429.6 | −227.5 | −0.024 |

수준을 그대로 옮기면 무너진다 — 모델 자기 흡수 때문이다. **차등만 분리하면
부호가 뒤집힌다.**

```
차등만 적용 (수준 제외), 학습 2시즌 -> 2024
  k=50 −49.2 · k=150 −20.5 · k=500 +2.6 · **k=2000 +8.0** · k=10000 +3.1

전이별 일관성 (k=2000)
  2021->2022 +1.5   2022->2023 +3.9   2023->2024 +7.6      <- 3/3 양수
```

`k` 는 과거 두 전이에서 골라도 2000 이 최선이라 **선택 편향이 없다**.

### 판정

```
HAND_ORACLE                경기 분할 +12.8 ~ +20.7 (분할 난수에 따라)
HAND_TRANSFER              차등 k=2000 에서 **+8.0** (3/3 전이 양수)
PAIR_COUNT                 손별 중앙 177~222투구 (pair 의 25~30배)
COLD_START                 신규 투수 행 22.9% / 양손 50투구 이상 74.4%
STANDARD_ESTIMATOR         수준 RAW −429.6, 표준 EB −227.5  (전부 붕괴)
TRANSFER_AWARE_ESTIMATOR   차등 + 강한 축소(k=2000)  -> +8.0
2024_OOS                   +8.0 = **+0.85%**
EXPECTED_FUTURE            +0.85%  (게이트 +3.8% 의 4분의 1)
DECISION                   **종료.** 게이트 미달
```

### 남기는 관찰 하나

이 +8.0 은 현행 `dev_platoon`(k=300) **위에** 얹어 잰 값이다. 즉 현행 축이
**덜 축소돼 있을** 가능성을 시사한다 (EB 가 k 를 작게 잡는다는 오늘의 발견과
같은 방향). 다만 편차가중·k 재최적은 이미 닫힌 축이고 크기가 +0.85% 라
승격하지 않는다. **기록만 남긴다.**


---

## 🚩 HAND-DIFFERENTIAL — **연구 후보로 보관** (persistent weak signal)

`exp/hand_diff.py`, `exp/hand_diff2.log`. 학습 0회 (후처리만 바꿈), 제출 없음.

### 먼저 갈린 것 — 라벨 기반과 잔차 기반은 다른 물건이다

| | 라벨 기반 차등 | **잔차 기반 차등** |
|---|---|---|
| 현행 `dev_platoon` 과의 상관 | **+0.9151** | +0.5259 |
| 현행 잔차와의 상관 | +0.0065 | +0.0094 |
| 정체 | 현행 축 그 자체 | 현행 축이 **못 잡은 몫** |

**라벨 기반은 새 축이 아니다.** 실제로 1번축 `k` 만 바꿔 보면 4폴드 전부 악화된다.

```
k       2021   2022   2023   2024
300     +0.0   +0.0   +0.0   +0.0      <- 현행
500     -0.7   -0.6   -1.1   -1.0
2000    -4.1   -3.8   -4.7   -5.1
20000   -8.4   -8.7   -8.6  -10.6
```

**현행 k=300 이 이미 최적이다** (편차가중 재최적이 닫힌 축이라는 기존 결론 재확인).

### 잔차 기반 차등 — 3/3 전이 양수

현행 4축 **위에** 얹는다. 원천 시즌의 **잔차**로 투수별 손 차등을 만들어
유효표본 `n_eff = n0·n1/(n0+n1)` 로 축소한다.

| k | 21->22 | 22->23 | 23->24 | 22+23->24 | 평균 | 최악 | 부호 |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 300 | −16.0 | +10.6 | +0.3 | −5.2 | −1.7 | −16.0 | 2/3 |
| 500 | −6.7 | +8.9 | +6.8 | +2.6 | +3.0 | −6.7 | 2/3 |
| **1000** | −0.3 | +6.3 | **+9.3** | **+7.8** | +5.1 | −0.3 | 2/3 |
| **1500** | +1.1 | +4.8 | +8.6 | +8.4 | +4.9 | +1.1 | **3/3** |
| **2000** | +1.5 | +3.9 | +7.6 | +8.0 | +4.4 | +1.5 | **3/3** |
| 5000 | +1.3 | +1.8 | +4.2 | +5.1 | +2.4 | +1.3 | 3/3 |

**k 는 과거 2전이(21->22, 22->23)로만 골랐고 1000 이 나왔다** — 2024 를 보고
고르지 않았다. 그 k 에서 23->24 실측 **+9.3 (+0.99%)**, 22+23->24 **+7.8 (+0.83%)**.
k=1500 이면 3/3 양수에 최악 +1.1 이고 2024 +8.4 (+0.89%) 다.

### 시드 안정성

```
시드42 단독  기준선 944.0 -> 951.5   +7.4
시드43 단독  기준선 935.7 -> 944.0   +8.3
2시드 평균   기준선 943.8 -> 951.6   +7.8
```

시드폭 0.9 로 **Champion 자체 시드폭(8.3)보다 훨씬 작다.**

### 메커니즘 — 투수마다 다른 플래툰 감도

```
전체 차이(같은손 − 반대손)   −0.0306
  R 경기 −0.0267 · F 경기 −0.0493 · cur_succ 상위 −0.0296 · 하위 −0.0279
투수별 차등의 sd            0.1278   <- 전체 평균차의 4.2배
```

**투수마다 다르다.** 상태(cur_succ)에는 의존하지 않고 game_type 에 약하게
의존한다. 라벨 기반 축(dev_platoon)이 이미 수준을 가져갔고, 잔차 기반은
**모델과 X/H1(cur x 같은손)이 흡수하고 남긴 투수별 편차**를 잡는다.

### 판정 — 연구 정책 적용

```
LOCAL             +0.83 ~ +0.99%   -> 정책 3) research candidate (>= +0.5%)
TRANSFER          k>=1500 에서 3/3 양수, 최악 +1.1
SEED              폭 0.9 (Champion 8.3 대비 작음)
REDUNDANCY        dev_platoon 과 corr 0.526 — 관련되지만 같지 않다
MECHANISM         투수별 플래툰 감도. 명확
STRONG 기준       +1.5% 미달
LB 기준           +3.8% 미달

DECISION          **KEEP AS RESEARCH CANDIDATE** (persistent weak signal)
```

### 승격하려면 확인해야 할 것

이 표는 **폴드 모델의 잔차**로 만든다. 2025 배치 시에는 폴드 2023/2024 잔차로
표를 만들어 **2024 까지 학습한 생산 모델** 위에 얹게 된다. 구조는 같지만
생산 모델은 2024 를 이미 학습했으므로 **자기 흡수**로 이득이 줄 수 있다.
승격 전에 그 배치 구조를 그대로 재현해 재측정해야 한다.


---

## HAND-DIFF 생산 경로 검증 — 자기흡수를 실측했다. **연구 후보 유지**

`exp/prod_hand.py` + `prod_hand2.log`. 학습 2회(흡수 검정용 목표 모델), 제출 없음.

### 잔차 생성 구조 (명시)

```
표 제작 잔차 = strictly out-of-fold
  2022 잔차 <- 2019~2021 학습 모델   2023 잔차 <- 2019~2022 학습 모델
생산 모델의 in-sample 잔차는 쓰지 않는다
적용 대상 = 그 표의 원천 시즌을 **이미 학습한** 모델
  측정: 표(2022+2023) -> 2019~2023 학습 모델의 2024
  배치: 표(2023+2024) -> 2019~2024 학습 모델의 2025      <- 동일 구조, 한 시즌 이동
```

**자기흡수는 이미 측정 안에 들어 있다.** 남은 차이는 생산 모델의 학습량이 21%
더 많다는 것뿐이고, 그것을 따로 쟀다.

### 1. 자기흡수 실측 — 같은 표(2022 잔차)를 학습량이 다른 목표에

| 목표 모델 | 학습행 | 2022 를 학습했나 | k=1000 | k=1500 |
|---|---:|---|---:|---:|
| A ≤2021 | 728k | 아니오 | **+4.2** | +3.4 |
| B ≤2022 | 976k | 예 | **+3.2** | +2.5 |
| C ≤2023 | 1.22M | 예(+2023) | −0.9 | +1.0 |

**학습량이 34% 늘 때(A->B) 이득이 24% 줄었다.** 흡수가 실재한다.
C 의 −0.9 는 흡수에 더해 **표가 낡았기 때문**이다 (2022 표를 2024 에 적용).
그래서 배치에서는 **직전 두 시즌** 표를 쓴다 — 그것이 +7.8 짜리 측정이다.

### 2. 구성 비교 (표는 그 폴드 직전 시즌들 잔차로만)

| 폴드 | 기준선 | k=1000 | k=1500 | 시드42 | 시드43 |
|---|---:|---:|---:|---:|---:|
| 2023 | 211.8 | +6.3 | +4.8 | +6.2 | +6.3 |
| 2024 | 943.8 | **+7.8** | **+8.4** | +7.9 | +7.9 |

**시드폭 0.0~0.1.** Champion 자체 시드폭 8.3 과 비교하면 사실상 결정론적이다.

### 3. 독립성 — 대체재가 아니라 보완재다

```
corr(dev_platoon, hand_diff)  +0.5475
corr(현행 잔차, hand_diff)     +0.0094
dev_platoon 제거              −12.0
  거기에 hand_diff 만 얹기      +3.4     <- 대체하지 못한다
현행 4축 + hand_diff (D 구성)  +7.8     <- 얹으면 는다
```

### 판정

```
PROD_HAND_DIFF_k1000    +7.8  (+0.83%)   폴드 2024, 배치와 동일 구조
PROD_HAND_DIFF_k1500    +8.4  (+0.89%)
COMBINED (4축 + diff)   +7.8  — 대체가 아니라 가산
SELF_ABSORPTION_LOSS    학습량 +34% 당 이득 −24% (A->B 실측)
                        생산은 C 대비 +21% 이므로 **추정 −15%, 약 +6.6 (+0.70%)**
SEED_STABILITY          시드42 +7.9 / 시드43 +7.9  (폭 0.0)
DECISION                **PERSISTENT WEAK SIGNAL 유지** (연구 후보)
                        정책 3구간(+0.5~1.5%)에 해당. STRONG(+1.5%)·LB(+3.8%) 미달
```

### 남은 위험

`k` 는 과거 전이 규칙으로 고정했고 2024 를 보고 고르지 않았다. 다만 흡수 보정
후 기대치가 **+0.70%** 라 게이트의 5분의 1이다. 이 신호는 **크기가 아니라
안정성이 특징**이다 — 3/3 전이 양수, 시드폭 0, 메커니즘 명확(투수별 플래툰 감도).
LB 슬롯을 쓸 근거는 아직 없다.


---

## 🚩🚩 28·29회차 — **1057.3394 새 최고.** 잔차 차등 family 가 전이됐다

```
cand_submit_3   1057.3394030999   Champion 대비 +7.4168   (2026-08-18 02:50)
cand_submit_2   1053.5950918323                 +3.6725   (02:54)
```

25회차(1049.9226, 08-15) 이후 **첫 상승**이고, 오늘 세운 방법론이 통째로 검증됐다.

### 전이율 — 세 측정이 모인다

| 후보 | 로컬 | LB | 전이율 |
|---|---:|---:|---:|
| C2 손 차등 1축 | +0.826% | +0.350% | **0.423** |
| C3 3축 | +1.526% | +0.706% | **0.463** |
| C3−C2 증분 (2S+주자) | +0.699% | +0.355% | **0.508** |

기존 앵커는 −0.605 ~ +2.006 으로 흩어졌는데 **이 family 는 0.42~0.51 에 모인다.**

```
D current-state +0.697 · K2 +0.518 · H1 +0.233 · X +0.135 · F −0.605 · EB +2.006
잔차 차등 family                    +0.423 / +0.463 / +0.508
```

### 무엇이 검증됐나 — 오늘의 네 가지 방법론

1. **오라클은 경기 단위로 가른다.** 투구 단위 분할은 클러스터 누수로 부풀려진다
   (pair +61.3 -> +4.0, 투수 +17.9 -> +1.0). 경기 분할값이 전이 실측과 일치했다.
2. **축소 `k` 는 적률법이 아니라 과거 전이로 고른다.** EB 는 "이번 시즌 추정"을
   풀고 우리는 "다음 시즌 예측"을 푼다. k=1000/2000 은 과거 전이에서 골랐다.
3. **수준이 아니라 차등만 표로 만든다.** 모델은 재학습에서 수준을 흡수하므로
   (수준 전이 −227.5) 차등만 남는다(+8.0).
4. **독립인 약한 신호는 누적된다.** 상호 상관 0.004~0.007, 양방향 증분 모두 양수,
   합계 +14.4 = 단독 합(+7.8+5.0+1.6)과 거의 같다.

### 게이트 재조정 — 이 family 에 한해

`+3.8%` 게이트는 전이율 0.375 와 큰 산포를 전제로 만든 값이다. 이 family 는
**전이율 0.45 ± 0.04** 로 안정적이므로 문턱이 훨씬 낮다.

```
LB +5 점을 2시그마로 확보하려면  0.45 x LOCAL% > 0.48%p  ->  **LOCAL > 1.1%**
```

C3 는 로컬 +1.53% 로 이 기준을 넘었고 실제로 +7.42 가 나왔다. 다만 이것은
**후처리 잔차 차등 family 전용 규칙**이고, 새 피처(in-model) 후보에는 기존
+3.8% 를 유지한다 — 그쪽은 재학습 분산과 자기흡수가 겹친다.

### 새 Champion 아티팩트

```
submissions/cand_submit_3.zip   sha256 82799a5c...  (7축)
model_cand/cat_submit_3.pkl     모델은 Champion 그대로 (재학습 없음)
구성 = 기본47 + TM8 + D13 + X8 + H1 6 (82p)
     + 후처리 4축 + 잔차차등 3축(손 k1000 / 2S k1000 / 주자 k2000)
     + 아핀 alpha 1.09 center 0.585445
```

**절대 덮어쓰지 않는다.** 표는 2023·2024 의 strictly out-of-fold 잔차로만 만들었고
조회 키는 그 행 자신의 컬럼뿐이다 (행 독립성 실측 0.000e+00).


---

## COUNT-ADVANTAGE 차등 기각 — 그리고 맥락 차등 family 소진 확인

`exp/count_diff.py`. 학습 0회. **기준선을 C3(7축)으로 바꿔서** 측정했다.

### 기준선 재현 확인

```
폴드 2022  2475.8 -> 2476.7  (+0.8)      표는 2020+2021 잔차
폴드 2023   211.8 ->  220.1  (+8.2)      표는 2021+2022
폴드 2024   943.8 ->  958.1  (+14.4)     표는 2022+2023   <- 제출 측정과 일치
```

### 카운트 우위 4정의 — 전부 기각

| 정의 | 오라클 | 위약 | 21→22 | 22→23 | 23→24 | 최적k | **C3 증분** |
|---|---:|---:|---:|---:|---:|---:|---:|
| adv (S>B) | +0.5 | −1.9 | +0.1 | +0.5 | −0.7 | 5000 | **−0.7** |
| adv_x2s (S>B & S<2) | −1.9 | −0.7 | +0.5 | −0.1 | −0.5 | 3000 | **−0.6** |
| margin (≥1 vs ≤−1) | +0.5 | −2.3 | +0.7 | +2.3 | −0.5 | 1000 | **−0.1** |
| disadv (B−S≥2) | −2.4 | −2.0 | +4.8 | +5.1 | +0.1 | 500 | **−3.4** |

`disadv` 는 과거 두 전이가 +4.8/+5.1 로 컸으나 23→24 에서 +0.1 로 죽고 C3
위에서는 −3.4 다. **과거 전이만으로 고른 k 가 최근 시즌에 안 통하는 전형**이다.

**오라클이 위약과 같은 대역이다** — 정보가 없다는 뜻이지 표현 문제가 아니다.
`adv` 와 기존 2스트라이크 축의 상관이 **+0.30** 이라, 겹치는 부분은 이미
C3 가 갖고 있고 겹치지 않는 부분(`adv_x2s`)에는 신호가 없다.

### 남은 맥락 5종도 함께 확인 — 전부 소진

| 맥락 | 오라클 | 위약 | C3 증분 |
|---|---:|---:|---:|
| 홈/원정 (투수팀 홈) | −2.3 | −1.9 | −0.1 |
| 득점권 (2·3루) | −1.4 | −0.7 | −0.2 |
| 타자 베테랑 (경험 상위) | −2.0 | −2.3 | +0.2 |
| 투수팀 뒤짐 (점수차<0) | −1.6 | −2.0 | −1.7 |
| 이닝 7회 이상 | −1.7 | −1.6 | +0.1 |

### 결론 — 이 family 는 세 축으로 끝났다

투수별 **이진 맥락 차등**을 13가지 정의로 훑었고 살아남은 것은
`손 / 2스트라이크 / 주자` 셋뿐이며 **전부 C3 에 들어가 있다.**

```
살아남음   손 (+7.8)  ·  2스트라이크 (+5.0)  ·  주자 (+1.6)
죽음       카운트우위 4정의 · 3볼 · 2아웃 · 시즌후반 · F경기
           홈원정 · 득점권 · 타자경험 · 점수차 · 이닝후반
```

**왜 셋만 살았나** — 셋 다 **투수의 물리적 행동이 실제로 바뀌는 국면**이다
(좌우 대응, 결정구 선택, 세트포지션). 나머지는 투수 행동이 아니라 **경기 상황의
분류**여서 투수별 차등이 생길 이유가 없다.

### K2 와 무엇이 달랐나 (기록)

```
K2        cur_{ball,rev} x {2S, 2S&b<=1}   in-model 전역 곱   LB −4.72
2S 차등    투수별 [2S − 그외] 잔차 차등        후처리 표          LB 기여 +3.7 중 일부
```

같은 "2스트라이크"를 봤지만 K2 는 **모든 투수 공통의 곱항**이었고 이번 것은
**투수마다 다른 상수**다. 전자는 트리가 이미 만들 수 있었고 후자는 아니다.


---

## 연속 맥락 기울기(CONTINUOUS CONTEXT SENSITIVITY) — 기각. 이진이 이미 다 가져갔다

`exp/slope_axis.py`. 학습 0회. 기준선 C3(7축) = 958.1, 오라클은 경기 단위 분할.

보정 형태를 이진과 명확히 구분했다 — `b_p x (맥락 − 그 투수의 맥락 평균)` 로
**수준을 빼고 기울기만** 실었다. 축소는 기울기의 정보량 `n*var(ctx)` 기준이다.

| 맥락 | 전체 기울기 | 투수 sd | **위약 sd** | 오라클 | 위약 | 21→22 | 22→23 | 23→24 | **C3 증분** |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| count margin (S−B) | +0.00151 | 0.0623 | 0.0488 | +0.4 | −0.0 | +0.9 | +0.7 | +0.3 | **+0.6** |
| runner load (0~3) | +0.00031 | 0.0782 | 0.0798 | +2.3 | +0.2 | +0.2 | +0.1 | +0.8 | **+0.0** |
| out state (0~2) | +0.00165 | 0.0735 | 0.0840 | −0.1 | +0.1 | −0.3 | +0.2 | −0.1 | **−0.5** |
| score pressure (±4) | −0.00042 | 0.0365 | 0.0278 | −9.1 | +3.2 | −2.0 | −1.0 | −4.3 | **−5.0** |

### 🚩 핵심 — 투수별 기울기 이질성이 거의 없다

**위약 sd 와 비교하는 것이 이 실험의 핵심이다.** 투수를 무작위로 섞어도 기울기
산포가 거의 같게 나온다.

```
count margin   투수 0.0623 vs 위약 0.0488   ->  1.28배
runner load    0.0782 vs 0.0798            ->  0.98배 (완전히 잡음)
out state      0.0735 vs 0.0840            ->  0.87배 (위약보다 작다)
score pressure 0.0365 vs 0.0278            ->  1.31배, 그러나 오라클 −9.1
```

**보이는 기울기 차이의 대부분이 추정 잡음이다.** 이진 차등에서는 같은 검사에서
분명한 신호가 있었다(위약 −0.0 vs 실제 +12.8 등).

### 왜 이진은 되고 연속은 안 되나

`runner load` 의 보정 벡터가 기존 **이진 주자 축과 상관 +0.74** 이고 C3 증분이
**+0.0** 이다. `count margin` 도 2스트라이크 축과 +0.20 이다.

```
이진 차등이 이미 다 가져갔다. 연속 구조는 그 위에 남는 것이 없다.
```

야구적으로도 말이 된다 — 투수의 행동은 **국면 전환**(2스트라이크가 되면 결정구,
주자가 나가면 세트포지션)이지 **매끄러운 반응**이 아니다. `CLAUDE.md` 4절의
"이산 전환은 통하고 매끄러운 곱은 안 통한다"가 차등 family 에서도 성립한다.

### 유일한 생존자와 그 크기

`count margin` 만 3/3 전이 양수(+0.9/+0.7/+0.3)이고 C3 증분 **+0.6 = +0.063%** 다.
손 차등(+0.83%)의 **13분의 1**이고 Champion 자체 시드폭(±8)에 묻힌다.
전이가 +0.9 -> +0.3 으로 **감소 추세**인 것도 좋지 않다.

```
DECISION  CLOSE.  기록만 남긴다 (0~+0.5% 구간, 3/3 양수이나 크기가 잡음 이하)
```


---

## NON-PITCHER SUBJECT DIFFERENTIAL — 종료. 차등 family 전체가 닫힌다

`exp/subject_diff.py`. 학습 0회. 기준선 C3(958.1), 오라클은 경기 단위 분할.
주체를 타자·투수팀·타자팀으로 바꾸고 조건 4종을 걸어 12조합을 쟀다.

| 주체 x 조건 | 셀 | 오라클 | 위약 | 21→22 | 22→23 | 23→24 | **C3 증분** |
|---|---:|---:|---:|---:|---:|---:|---:|
| 타자 x 같은손 | 816 | +4.2 | +0.4 | −0.2 | +0.2 | +0.3 | −0.1 |
| 타자 x 2스트라이크 | 843 | +0.2 | −1.5 | +0.0 | −0.1 | −0.1 | +0.1 |
| 타자 x 주자 | 830 | +0.7 | −1.2 | +1.0 | +0.7 | −1.9 | −1.6 |
| 타자 x F 경기 | 591 | +0.0 | −3.2 | +1.7 | −1.2 | −0.1 | +0.5 |
| 투수팀 x 같은손 | 24 | +1.8 | −9.5 | −2.0 | −0.7 | +4.9 | +1.9 |
| 투수팀 x 2스트라이크 | 24 | +4.8 | −4.4 | −2.0 | +0.5 | −1.9 | −2.9 |
| 투수팀 x 주자 | 24 | −3.8 | −6.9 | +0.1 | −0.6 | −1.3 | −0.9 |
| 투수팀 x F 경기 | 22 | +7.6 | −4.4 | +6.9 | **−10.8** | **−13.6** | +2.6 |
| 타자팀 x 같은손 | 24 | +9.1 | −5.3 | −2.3 | −0.3 | +1.5 | +1.4 |
| 타자팀 x 2스트라이크 | 24 | +10.8 | −8.4 | −0.6 | −0.0 | −1.4 | −3.1 |
| 타자팀 x 주자 | 24 | +2.2 | −3.9 | +0.6 | −0.4 | −0.8 | +0.1 |
| 타자팀 x F 경기 | 22 | +9.1 | −4.4 | +8.5 | **−10.6** | **−12.5** | +2.7 |

### 판정 — 12조합 전부 탈락

```
타자 주체    오라클 최대 +4.2, C3 증분 −1.6 ~ +0.5    -> 정보 자체가 없다
팀 주체      오라클은 최대 +10.8 로 커 보이나 전이가 무너진다
```

**팀 x F 경기가 함정이다.** C3 증분이 +2.6/+2.7 로 유일하게 커 보이지만
전이가 **+6.9 -> −10.8 -> −13.6** 으로 급격히 뒤집힌다. 2023 년 F 라벨 정의
변경 때문이고, `game_type` 축은 이미 완전 종료된 축이다. 3/3 양수 요건을
만족하는 조합이 **하나도 없다**.

`상대 같은손` 팀 조합(+1.9 / +1.4)도 전이가 2/3 음수다.

### 결론

```
NON-PITCHER DIFFERENTIAL FAMILY = CLOSED
```

이로써 **차등(differential) family 전체가 닫힌다.**

```
주체 x 조건 탐색 총계
  투수 x 이진 맥락 13정의   -> 손 · 2스트라이크 · 주자 3개 생존 (전부 C3 에 채택)
  투수 x 연속 맥락 4종       -> 전부 기각 (투수별 기울기가 위약 수준)
  타자 x 조건 4종            -> 전부 기각 (오라클 최대 +4.2)
  팀 x 조건 8종              -> 전부 기각 (전이 붕괴)
```

**살아남은 것의 공통점** — 셋 다 **투수의 물리적 행동이 실제로 바뀌는 국면**이다.
타자·팀은 그 행동의 주체가 아니고, 연속 맥락은 행동이 바뀌는 방식이 아니다.


---

## 🚩 GAME-ORACLE 분해 — `+187` 은 **경기 내 상태**다. 경기 간에는 0 이다

`exp/game_ladder.py`. 학습 0회, 제출 피처 없음. 진단 전용.

### 1. 분할 사다리 — 같은 오라클, 분할 단위만 변경

| 키 | 투구 무작위 | 타석 | 경기 | **시간(전/후반)** |
|---|---:|---:|---:|---:|
| (투수,경기) | +176.5 | **+152.5** | 구조상 불가 | **+0.0** |
| (투수,타자) 매치업 | +62.2 | +20.3 | +1.2 | +1.1 |
| 투수 | +16.0 | +6.7 | +2.8 | −3.7 |
| (투수,경기,타석) | +49.8 | 구조상 불가 | 구조상 불가 | +0.0 |

**타석 분할로도 경기 오라클이 +152.5 로 살아 있다** — 타석은 경기 안에 있으므로
같은 경기가 여전히 양쪽에 들어간다. 반면 **시간 분할(시즌 전반 경기 -> 후반
경기)에서는 +0.0** 이다. 경기 효과는 경기를 넘어가지 않는다.

### 2. 반복 홀드아웃 50회 — 단발 측정의 함정

```
매치업   중앙 +2.4   5~95% +0.5~+5.5   최소 −0.0  최대 +6.1  sd 1.69
투수     중앙 −0.0   5~95% −3.2~+2.2   최소 −6.0  최대 +2.8  sd 1.67
```

앞서 단발로 잰 매치업 +4.0 / 투수 +0.8 은 **분포 안이지만 위쪽**이었다.
정직한 중앙값은 매치업 **+2.4**, 투수 **−0.0** 이다. 전이 실측(+3.9 / +1.0)과
같은 대역이다.

### 3. 경기 내 예보 — 앞 절반이 뒤 절반을 맞힌다

```
k=0 −475.3 · k=50 +64.4 · **k=150 +135.3** · k=500 +75.7   (뒤 절반 128,794행)
```

**같은 경기 앞부분의 잔차가 뒷부분을 예측한다.** 시간 순서를 지킨 측정이므로
미래 누수가 아니다. 그러나 2025 에서는 같은 경기의 다른 행을 볼 수 없다
(규정 4). **제출에 쓸 수 없다.**

### 4. 지속성 — 경기 안에서만 산다

```
같은 경기 내 절반끼리   +0.129 ~ +0.249
다음 경기 (lag-1)      +0.0287        <- 사실상 0
같은 투수 임의 두 경기   +0.0070
투수 효과의 다음 시즌    +0.193 (감쇠보정 +0.774)
```

### 6. 계층 분산분해 (이항 잡음 제거)

| 수준 | 셀 | 셀당 | 관측 sd | 잡음 sd | **진짜 sd** | 신호비 |
|---|---:|---:|---:|---:|---:|---:|
| 투수 | 391 | 648.4 | 0.0497 | 0.0194 | 0.0458 | 0.848 |
| (투수,경기) | 7,986 | 31.7 | 0.1142 | 0.0848 | **0.0764** | 0.448 |
| (투수,타석) | 57,841 | 4.3 | 0.2566 | 0.2017 | **0.1586** | 0.382 |

### 판정

```
BIG_SIGNAL_SOURCE            **경기 내(within-game) 공유 상태**
IS +187 REAL?                **partially** — 경기 내 효과로는 실재한다
                             (앞 절반 -> 뒤 절반 +135.3, 절반 간 상관 0.13~0.25)
                             그러나 사전에 알 수 있는 신호로는 0 이다
                             (시간 분할 +0.0, 다음 경기 상관 +0.029)
PERSISTENT_GAME_EFFECT       **no**
PERSISTENT_MATCHUP_EFFECT    **no** (경기 분할 중앙 +2.4, 시즌 간 상관 +0.02~0.04)
ESTIMATED LEGAL RECOVERABLE  매치업 +2.4 (중앙) ~ +3.9 (전이 실측) = +0.25~0.41%
                             투수 −0.0 ~ +1.0
NEXT RESEARCH DIRECTION      없음. 가장 큰 잔여 신호가 경기 내 상태인데
                             규정 4)가 그 접근을 금지한다
```

**이 진단으로 "큰 오라클" 문제가 완결된다.** `+187` 은 측정 오류도 아니고 순수
잡음도 아니다 — **실재하지만 그 경기 안에서만 존재하고, 우리는 2025 경기 안을
볼 수 없다.** 앞서 "클러스터 누수"라고 부른 것의 정확한 정체가 이것이다.


---

## 관계형 국면 6종 기각 + FINAL 아티팩트 (표 원천 5시즌)

### 관계형 국면 — 같은 행 안의 **비교**를 국면으로 (C3 기준, 전부 기각)

지금까지의 국면은 전부 경기 상황이었다. 비교 자체를 국면으로 쓴 적이 없어
6종을 정의했다 (트리는 `a > b` 같은 대각 경계를 원리적으로 못 만든다).

| 후보 | 잔차상관 | 오라클 | 위약 | 21→22 | 22→23 | 23→24 | C3 증분 |
|---|---:|---:|---:|---:|---:|---:|---:|
| R1 cur_succ > cur_bsucc | −0.0021 | +0.4 | −1.9 | +0.1 | −0.3 | +0.6 | +0.6 |
| R2 cur_mid < cur_bmid | +0.0005 | −2.0 | −0.7 | −0.3 | −0.1 | +0.0 | +0.2 |
| R3 투수경력 > 타자경력 | +0.0001 | −3.0 | −2.3 | +0.2 | −0.1 | +1.0 | +0.8 |
| R4 prev1 결측(첫 등판) | −0.0027 | −3.2 | −2.0 | +0.1 | −0.1 | −0.2 | −0.6 |
| R5 cur_succ > 통산 성공률 | −0.0013 | −5.5 | −1.6 | −0.2 | −0.3 | +0.1 | +0.6 |
| R6 cur_ball > cur_str | −0.0022 | −2.7 | −0.9 | +1.8 | −0.8 | −2.4 | −1.7 |

잔차상관이 전부 잡음 대역(|0.002|)이고 오라클이 위약 수준이다. **기각.**

### 🚩 FINAL — 표 원천을 5시즌으로 늘린다

구조는 C3 그대로, **표 원천만 2시즌 -> 5시즌(2020~2024 OOF 잔차)**.

```
원천 시즌 수별 폴드 이득 (k 고정 1000/1000/2000, w=1.0)
  원천   2023      2024
   1    +10.5    +15.2
   2     +8.2    +14.4     <- C3 (제출본)
   3    +11.6    +16.3     <- 두 폴드 모두 2개보다 낫다
   4      —      +19.1
시드 안정성(원천3): 시드42 +16.1 / 시드43 +16.8   (원천2 는 +14.0/+15.0)
```

**이것은 하이퍼파라미터 튜닝이 아니라 가용 데이터를 다 쓰는 것**이다. 표본이
늘면 차등 추정 잡음이 준다는 메커니즘과 일치한다.

**반대로 기각한 것 두 가지** (과거 폴드로 골랐는데 2024 에서 무너진다):

```
전역 가중 w   과거 최적 1.5 -> 2024 +8.4  (w=1.0 은 +16.3)   기각
cold-start 대체(리그 평균)   과거 동률, 2024 +15.8 (없을 때 +16.3)   기각
```

### 검증

```
verify_submit          1,2,3,4 전부 통과   확률 범위 0.3716~0.5972
추론 == 분석            최대차 1.041e-17
행 독립성(200행)         최대차 0.000e+00     (규정 4)
표 원천                 2020~2024 strictly out-of-fold 잔차 1,237,679행
표 항목                 7,921  (손 719명 / 2S 727 / 주자 716)
zip sha256             5152edc7332064c66b569d75d6e6902881214ac664f061651a95a0b3b809c018
```

**로컬 2024: C1 대비 +19.1 (C3 는 +14.4) — C3 대비 +4.7 (+0.50%)**
전이율 0.45 를 적용하면 예상 LB **약 +2.3점 (1059.6 내외)**.
