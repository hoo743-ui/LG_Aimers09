# CLAUDE.md — 새 세션이 먼저 읽는 파일

이 파일은 세션마다 자동으로 읽힌다. **사용자가 매번 규칙을 다시 설명하지 않아도
되도록** 여기에 유지한다. 새로 배운 규칙이 생기면 여기에 반영한다.

---

## 1. 이 프로젝트가 무엇인가

LG Aimers 9기 Phase 2 — KBO 투구 단위 **제구 성공 확률** 예측. 코드 제출 대회라
평가셋(2025 시즌 245,789행)은 서버에만 있다.

```
점수 = 1e5 * rho^2 ,  rho = corr(예측, 정답)
```

`rho` 는 **아핀 불변**이다. 스케일·절편 보정으로는 점수가 오르지 않는다
(13회차에서 아핀 천장에 소수점 6자리까지 도달함).

## 2. 현재 상태 — 먼저 확인할 것

**로컬 클론이 원격보다 뒤처져 있을 수 있다.** 사용자는 여러 PC 에서 작업한다.
현황을 묻는 질문에는 파일만 읽지 말고 먼저:

```bash
git fetch && git log --oneline origin/main -5
```

읽는 순서: `SETUP.md`(현황·다음 할 일) → `PROGRESS.md`(시각별 중간 결과)
→ `RELATION_LEDGER.md`(**47열의 관계를 어디까지 검증했는가** — 새 축을 생각할 때
먼저 본다) → `DEVIATION_LEDGER.md`(실험 기록, 최신 절이 끝) → `LB_LEDGER.csv`.
`STATUS.md` 는 오래됐다.

## 3. 지금 Champion

```
28회차  cand_submit_3.zip   LB 1057.3394030999   82피처   (2026-08-18)
= 기본47 + TrackMan ctx 8 + D 13 + X 8 + H1 6
후처리 편차 4축 w=[0.20, 0.825, 0.280, 0.45]
  + 🚩 잔차 차등 3축 (모델 재학습 없음, 표만 추가)
      손 차등      k=1000   키 (투수, 투수손, 타자손)
      2스트라이크   k=1000   키 (투수, strikes_before)
      주자유무      k=2000   키 (투수, num_runners_on)
아핀 alpha 1.09  center 0.585445  (클린)
```

**이력** 1040.8656(22) -> 1044.7656(24) -> 1049.9226(25) -> **1057.3394(28)**

**잔차 차등 3축은 2023·2024 의 strictly out-of-fold 잔차로만 만든다.**
조회 키는 그 행 자신의 컬럼뿐이고 행 독립성을 실측했다(200행 배치 vs 단독 0.000e+00).

### 🚩 표 원천은 **직전 2시즌 고정** (30회차 실패로 확정)

`cand_final`(원천 5시즌)이 **1047.3863** 으로 C3 대비 −9.95, 원래 Champion
보다도 −2.54 낮았다. 로컬 +0.50% -> LB −0.94%, **전이율 −1.890**.

```
원천 시즌 곡선이 비단조였다 (두 폴드 모두 n=1 > n=2)  <- 잡음의 신호였다
폴드 2023  1개 +10.5  2개 +8.2   3개 +11.6
폴드 2024  1개 +15.2  2개 +14.4  3개 +16.3  4개 +19.1
```

**최신성이 표본량보다 중요하다.** 오래된 시즌의 차등은 낡아서 해롭다.

### 🚩 두 종류의 변경은 전이 성질이 완전히 다르다

```
축을 추가하는 변경         새 정보를 더한다      전이율 +0.42 ~ +0.51 (C2, C3)
표의 추정 방식을 바꾸는 변경  같은 정보를 다시 잰다  **전이 불안정, 부호 반전**
```

표 제작 파라미터(원천 시즌 수 · k · 가중)는 **과거 폴드로 고를 수 없다.**
같은 판에서 전역 가중도 과거 최적 1.5 가 2024 에서 +8.4 로 붕괴했다.
**현행값(직전 2시즌, k=1000/1000/2000, w=1.0)을 건드리지 않는다.**

### 🚩 이 family 의 전이율은 안정적이다 (28·29회차로 검증) — 단 **축 추가에만**

| 측정 | 로컬 | LB | 전이율 |
|---|---:|---:|---:|
| C2 손 차등만 | +0.826% | +0.350% | **0.423** |
| C3 3축 | +1.526% | +0.706% | **0.463** |
| C3−C2 증분 | +0.699% | +0.355% | **0.508** |

기존 앵커(−0.605~+2.006)와 달리 **0.42~0.51 에 모인다.** 그래서 이 family 는
`LOCAL > 1.1%` 면 LB +5 를 기대할 수 있다 — **+3.8% 게이트는 이 family 에
적용하지 않는다** (새 피처 in-model 후보에는 유지).

**Champion 아티팩트(`submissions/cand_asof*.zip`, `model_cand/*.pkl`)는 절대
덮어쓰지 않는다.** 새 후보는 `cand_*` 새 이름으로만 만든다.

## 4. 통한 것과 그 원리

| 축 | LB | 정체 |
|---|---|---|
| **D** | +85.65 | `asof_*` 가 통산 누적이라는 데서 현재 시즌 수준을 대수적으로 복원 |
| **X** | +3.90 | `cur_{succ,mid} x {카운트우위, 주자유무, 같은손, 볼−스트라이크}` |
| **H1** | +5.16 | `cur_{ball,rev,str} x {같은손, 볼−스트라이크}` |

**공통 원리 — 모델이 원리적으로 못 만드는 것을 준다.**
D 는 모델이 알 수 없는 학습셋 상수(`prior_events[선수]`)를 빼서 만든 값이고,
X/H1 은 축평행 트리가 못 만드는 곱항이다.

**그것만으로는 부족하다 — 대리 신호가 없어야 한다.** PN(직전 경기 투구 수를
비율의 숨은 분모에서 역산)은 "모델이 못 만든다"를 똑같이 만족했는데 2024 −1.4
로 실패했다. 모델이 비율의 granularity 등으로 **간접 추정**하고 있었기 때문이다.

```
D  : 못 만든다 + 대리 신호도 없다   -> +85.65
PN : 못 만든다 + 대리 신호가 있다   ->  기각
```

**🚩 오라클은 반드시 `경기` 단위로 절반을 가른다 (2026-08-18 확정).**
투구 단위 무작위 분할은 같은 타석·경기의 투구를 양쪽에 나눠 넣어 셀 평균이
**그 경기의 상태를 실어 나른다.** 그만큼 오라클이 부풀려진다.

| 그룹 | 투구 분할(옛) | **경기 분할(정직)** | 전이 실측 |
|---|---:|---:|---:|
| 투수 | +11.9 | **+1.0** | **+1.0** |
| 투수 x 타자(pair) | +61.3 | **+4.0** | **+3.9** |
| 투수 x 월 | +67.6 | +2.8 | −1.1~−1.5 |
| 투수 x 타자손 | +76.8 | +20.7 | +6.9 |

**경기 분할값이 전이 실측과 거의 같다.** 옛 기록의 큰 오라클 수치는 누수가
섞인 값이니 그대로 인용하지 말 것 — 결론(전이 기반 판정)은 바뀌지 않는다.

**학습 전 값싼 선별 ① 오라클 진단 — "거기 정보가 있는가"를 20초에 묻는다.**
`exp/oracle_probe.py` 의 `GROUPS` 에 한 줄 더하면 된다 (학습 0회). 그룹 잔차
평균을 2024 를 반으로 갈라 교차적합해 더한 `cv2` 가 **정직한 상한**이다.
기준선 이하면 그 축은 **정보가 없는 것**이지 표현이 부족한 것이 아니다.
실측(2026-08-17, 기준선 944.0) — 타자 +0.4, 카운트 −4.1, 이닝 −5.5,
base_state −0.8 은 **회수 완료**. 남은 것은 전부 투수 x 시즌내(최대 +76.8).

**학습 전 값싼 선별 ② 잔차 상관을 이미 쓰는 피처와 나란히 놓는다.**
**단 이것은 기각용이지 채택용이 아니다** — TMX 는 통과시켰는데 2024 −0.9 였다.
대역이 낮으면 버려도 되지만, 높다고 통과시키지 않는다.
폴드 2024 Champion 예측의 잔차와 후보 피처의 상관을 재서, 이미 모델에 들어
있는 피처(`asof_pitcher_n` +0.0034 등)와 **같은 대역이면 회수된 것**이다.
PN 은 +0.0016~+0.0036 이었다 — 15분 학습을 돌리기 전에 기각할 수 있었다.

**이산 전환은 통하고 매끄러운 곱은 안 통한다.** `cur_rate x n/(n+k)` 류 3종이
전부 2024 음수였다 — `cur_n` 의 단조 함수라 트리가 2단 분할로 이미 근사한다.

**수준은 전이되고 차분/잔차는 안 된다.** `prev − cur`(F)은 LB 전이 −60%.

## 5. 판정 규칙 (가장 중요)

**2024 폴드 부호가 LB 방향과 지금까지 예외 없이 일치한다.** 평가셋 `rho`(≈0.1025)가
2024(0.096)와 가장 가깝기 때문으로 보인다 (2022 0.156, 2023 0.044).

```
로컬 2024 음수      -> 강한 폐기 신호
로컬 2024 양수      -> 필요조건일 뿐 (F 는 +1.36% 였는데 LB −8.55)
```

**크기는 못 맞힌다.** 한때 `LB이득 = 0.851 x (2024이득 − 2.34%)` 가 세 점을 맞혔으나
네 번째(H1)에서 깨졌다. 4점 재적합 `k=0.830, c=2.07%`, **잔차 RMS ±2.9점**.
−2점 예측은 기각 근거가 못 된다 (그걸로 보류했다가 +5.16 을 놓칠 뻔했다).

> ⚠️ **이 전이식은 2026-08-17 전이 연구로 폐기됐다.** 단일 배수를 쓰지 않는다.
> 아래 5-b 가 현행 규칙이다. 위 식은 이력으로만 남긴다.

## 5-a. 🚩🚩 모든 변경을 두 종류로 나눈다 (30회차 실패로 확정)

이 구분을 **모든 보고서에 명시한다.** 섞으면 30회차처럼 실패한다.

| | A. SIGNAL INNOVATION | B. ESTIMATION / CONSTRUCTION CHANGE |
|---|---|---|
| 무엇 | **새 정보축**을 더한다 | 같은 정보를 **어떻게 추정할지** 바꾼다 |
| 예 | 손·2S·주자 차등, 새 entity x context | 원천 시즌 수 · 창 · k · 가중 · 평활 · cold-start · 보정 순서 |
| 실측 전이율 | **+0.42 ~ +0.51** (C2, C3) | **−1.89** (30회차), 부호 반전 |
| 판정 기준 | 5-b 게이트 | **아래 전용 규칙** |

### B 유형 전용 규칙 — 기본은 "바꾸지 않는다"

```
1  기존 방식 vs 새 방식을 **직접 짝지어** 비교한다
2  최소 2개 이상의 의사미래 전이에서 안정적이어야 한다
3  목표 시즌을 숨긴 상태로만 고른다 (2024 에서 k/창 최적화 금지)
4  생산 경로에서 재측정한다
5  성능뿐 아니라 **부호 안정성**을 본다
하나라도 부족하면 현행 유지
```

**"데이터가 더 많다 / 표본이 더 크다 / 추정이 더 부드럽다"는 채택 근거가 아니다.**

### 30회차가 가르친 것

```
표 원천 2시즌 -> 5시즌   로컬 +4.7 (+0.50%)   LB −9.95 (−0.94%)   전이 −1.890
원천 시즌 곡선이 비단조였다 (두 폴드 모두 n=1 > n=2)  <- 잡음의 신호였다
표본 증가 = 분산 감소  뿐 아니라  오래된 체제 혼입 = 편향 증가 도 동시에 일어난다
```

**현행 고정값** — 표 원천 **직전 2시즌**, k = 1000/1000/2000, w = 1.0,
strictly OOF 잔차, 생산 경로 동일. **새 근거 없이 건드리지 않는다.**

## 5-b. 🚩 후보 탐색 정책 (2026-08-18 확정) — 전이 연구의 결론

**목표가 바뀌었다: `TRANSFERABLE SIGNAL > LARGE LOCAL SIGNAL`.**
로컬 +10 / 전이 불안정보다 **로컬 +5 / 전이 안정**을 우선한다.

### 확정된 사실 (`exp/transfer_study.py`, 창 5 x 구성 6 x 2시드)

```
단일 전이 배수는 없다     부분집합에 따라 b=+0.145 ~ +0.512 로 3배 흔들린다
부호는 준예측 가능        실제 LB 5/6, 의사미래 70~86%
작은 로컬 이득은 무의미    LOCAL<0.5% 8건 평균 전이 −21.05 (잡음)
정상 의사미래 쌍 0개      6시즌 중 4시즌이 비정상이라 계수를 정할 자료가 없다
```

### family 별 전이 (실제 LB 앵커 — HISTORICAL_TRANSFER_SIGN 조회표)

| family | LB 전이비율 | 부호 | 대표 |
|---|---:|---|---|
| **current-state** | **+0.697** | 안정 + | D (+85.65) |
| regime | +0.518 | 음수를 음수로 | K2 (−4.72) |
| context-interaction | +0.135 ~ +0.233 | 약한 + | X, H1 |
| **recent-history** | **−0.605** | **부호 반전** | F (−8.55) |
| shrinkage | +2.006 | 음수를 확대 | EB (−13.83) |

### 새 후보는 학습 **전에** 이 표를 채운다

```
FAMILY =
HISTORICAL_TRANSFER_SIGN =     위 조회표. 없는 family 면 "미지"
TRANSFER_STABILITY =
LOCAL_GAIN =                   생산 경로 2024, Champion 대비 %
LOCAL_SEED_STD =
SNR =                          LOCAL_GAIN / LOCAL_SEED_STD
EXPECTED_FUTURE_SIGN =
EXPECTED_FUTURE_RANGE =
```

### HARD GATES (2026-08-18 세분화)

```
LOCAL < 0                 해당 표현 종료. 구조적 근거가 강하면 기록만 남긴다
LOCAL 0 ~ +0.5%           원칙 종료. 단 3/3 전이 양수 + 메커니즘 명확이면 연구 후보 유지
LOCAL >= +0.5%            RESEARCH CANDIDATE
LOCAL >= +1.5% + 전이 안정 + 시드 안정   STRONG CANDIDATE
생산 경로 >= +3.8% + 독립 정보 확인       **LB CANDIDATE** (제출은 여기부터)
```

**작아도 버리지 않는 조건** — 3/3 이상 전이 양수 · 지속성 안정 · 시드 산포 작음
· Champion 과 중복 낮음 이면 **persistent weak signal 로 보관**한다.

**승격 전 필수 5종** — 오라클(경기 분할) · 전이 · 생산 경로 · 시드 산포 · 중복.

**현재 보관 중인 persistent weak signal**
```
잔차 기반 손 차등 (투수별 플래툰 감도, 현행 4축 위에 가산)
  k 는 과거 전이로 선택 -> 1000~1500,  2024 +7.8~+8.4 (+0.83~0.89%)
  전이 3/3 양수(k>=1500), 시드폭 0.9, dev_platoon 과 corr 0.526
  승격 전 확인 — 배치 구조(생산 모델은 2024 를 이미 학습)에서 자기 흡수 여부
```

`+3.8%` 의 근거 — LB 앵커 비율 중앙값 0.375 에 잔차 RMS 0.3~0.75%p 를 얹어
2시그마 양수를 요구하면 `0.375 x LOCAL > 1.5%p`. 기존 손익분기 2.1% 보다 보수적이다.

**`0.4 x LOCAL` 을 일반 법칙으로 쓰지 않는다** — 문턱 계산의 근거일 뿐이고
개별 후보의 점추정에 쓰면 안 된다 (그래서 세 번 깨졌다).

**게이트 4 의 한계** — 의사미래 쌍은 전부 오염돼 있다(정상 0개). 그러므로
새 family 의 전이 부호 검사는 **기각용이지 채택용이 아니다**. 사전 선별 두
도구(잔차 상관, 오라클)와 같은 규율로 쓴다.

### 시드 산포의 의미

```
corr(시드sd, |전이 어긋남|) = +0.521      corr(SNR, 전이비율) = +0.130
```

시드 산포는 전이의 **크기를 예측하지 못하고 어긋남의 폭을 예측한다.**
SNR 이 낮으면 그 후보의 로컬 값 자체를 믿지 않는다.

### 🚩 축소 상수 `k` 는 **적률법(EB)으로 정하지 않는다** (2026-08-18 확정)

집단 평균을 축소해 쓰는 모든 것 — 후처리 편차축 · 잔차 표 · 매치업 · EB 열 —
에서 `k` 는 **전이 실측으로** 고른다. 경험적 베이즈로 뽑으면 **약 100배 작다.**

| 대상 | 적률법 k | **전이 최적 k** | 배율 |
|---|---:|---:|---:|
| 투수 잔차 표 | 241 | **20,000** | 83x |
| 매치업(투수x타자) 잔차 | 89 | **500+** | 6x 이상 |

```
pair 잔차 축소 민감도 (2024 이득)
  k x 0.5 = 44   −137.9      k x 1.0 = 89   −46.9   <- 적률법이 고른 값
  k x 2.0 = 177    −8.9      k x 5.0 = 443   +2.4
```

**왜 그런가** — EB 는 "**이번** 시즌의 효과를 **이번** 시즌 데이터로 추정"하는
문제를 푼다. 우리가 푸는 것은 "**다음** 시즌의 효과를 예측"하는 문제다. 효과가
시즌을 건너며 감쇠하는 만큼 더 세게 축소해야 하는데, EB 는 그 감쇠를 모른다.

**이것이 27회차 EB 축소가 LB −13.83 으로 무너진 이유다.** 당시 원장에
"고정 500 이 아니라 데이터가 정하게 했다"고 적었는데, 데이터가 정한 그 값이
**다른 문제의 답**이었다.

**How to apply**
```
1  k 후보를 훑되 선택은 반드시 과거 시즌 전이로 한다 (목표 폴드에서 고르면 편향)
2  전이 최적 k 는 대개 EB 값의 10~100배다. 작은 k 에서 크게 음수면 정상 신호다
3  현행 후처리 4축의 KSH = [300, 2000, 800, 2000] 은 이 규칙과 부합한다
   (재최적화 시도는 이미 닫혔다 — 현행이 최적 근처)
```

**따름 정리 — 효과의 지속성으로 축을 고른다.** 오늘까지의 실측은 일관된다:

```
투수 주효과       시즌내 신호비 0.809   인접시즌 감쇠보정 +0.774   전이 +1.0
타자 주효과       0.650               —                      오라클 +2.2
투수x타자 상호작용  0.118 (오라클 +61.3) 시즌간 상관 +0.017~0.037  전이 +3.9
경기 잠재축       —    (오라클 +187.8) 그 시즌에만 존재         전이 불가
```

**지속되는 것은 작고, 큰 것은 지속되지 않는다.** 새 축을 볼 때 오라클 크기보다
**시즌 간 상관**을 먼저 본다.

**시드 노이즈** — 2022 ±3.3, 2024 ±7.2. 마진을 이 배율로 읽는다.

**생산 경로 편차 4축의 기여 (2024, 실측)** — `dev_platoon` +11.9,
`dev_plat_cnt` +7.4, `dev_runner` +1.7, `dev_count` +1.5, 합 +22.7. 서로
직교하고 in-model 과도 직교한다. **26회차의 "K2 가 편차와 중복" 설명은 틀렸다** —
K2 의 손해는 편차 구성과 무관하게 −8.2 로 고정이다.

**생산 경로 재현 도구** — `exp/path_alloc.py`. 학습 2회로 편차 축 조합 12가지를
잰다 (편차 제거는 모델을 바꾸지 않으므로). Champion 944.0 재현 확인.

**2023 은 퇴화 폴드** (Champion `rho^2` 190 vs 2022 의 2450). 평균을 지배하게
두지 말고 catastrophic failure 여부만 본다. **원인 규명됨** — 2023 년에 `game_type`
F 의 라벨 정의가 바뀌어(F 성공률 .709 -> .473, 같은 투수 92명 중 98.9% 가 동일
방향 하락) 학습셋 2019~2022 에 신체제가 한 시즌도 없다. 폴드 2023 에서만 치명적이고
2024 이후로는 거의 소진된다. **낮은 2023 값을 모델 결함으로 읽지 않는다.** 2023 의 in-fold 최적 가중을 2024 에
쓰면 −138.

**캐시 경로와 생산 경로가 갈릴 수 있다.** 전이식 계수는 **생산 경로**
(`build_asof.py` 2024 홀드아웃)로 적합됐으므로 그 값을 넣는다.

## 5-c. 🚩 2026-08-18 자율 연구가 확정한 다섯 규칙

### 1. 최신성 vs 표본량의 부호는 **추정 대상의 복잡도**가 정한다

| 대상 | 자유도 | 지배 항 | 결과 |
|---|---|---|---|
| 잔차 표 (셀 평균) | 투수당 2셀, n 작음 | **편향** (낡은 체제) | 직전 2시즌 > 5시즌 (5시즌은 LB −9.95) |
| 모델 (82열) | 매우 큼 | **분산** | 전체 > 감쇠 (γ=0.7 −27.6, γ=0.5 −54.6) |

`exp/exp012_seasonw.json`. 같은 "최신성" 직관이 한쪽에선 맞고 한쪽에선 정반대다.
**새 추정량을 만들 때 어느 쪽인지 먼저 정한다.**

### 2. 학습 창은 자유 모수가 아니다 — D 가 묶는다

`cur_n = asof_n − prior_n[투수]` 의 `prior_n` 이 학습 창 끝 기준이라, 창을
자르면 목표 시즌 행의 `cur_n` 이 '이번 시즌'이 아니게 된다. 창 실험(958→252)은
최신성 효과가 아니라 **D 파손**이다. 최신성은 **표본가중으로만** 잰다.

### 3. 이항잡음 보정 분산분해는 작은 셀에서 무효

```
투수 x 타자 (셀당 10행)   분산분해 상한 4099.7   실제 경기분할 오라클 +4.0
```
셀 안 행이 같은 경기라 진짜 잡음이 `p(1−p)/n` 보다 크다. 덜 빼면 남은 것이
가짜 신호다. **셀당 600행 이상만 인용한다.**

### 4. 🚩 과거 2폴드 선택은 다음 시즌에 일반화되지 않는다

246개 국면을 2022·2023 **둘 다 양수**로 걸러 43개를 묶었다.

```
선택 통과 43 / 246       우연 기대 62 -> **우연보다 적다** (폴드 간 음의 상관)
선택된 축의 2024 평균     −0.898  (양수비율 34.9%)
포트폴리오 (w=0.5)       2022 +22.7   2023 +20.8   숨긴 2024 **−16.9**
```

**과거에 도운 축은 미래에 해치는 경향이 있다.** 이것이 잔차 학습기가 2023 에서
상관 −0.0230 으로 부호가 뒤집힌 것과 같은 기제다. 그러므로 **"여러 약신호를
모으면 된다"는 접근은 이 문제에서 작동하지 않는다.** 살아남은 3축은 예외적으로
지속성이 있는 것들이고, 그래서 크기가 아니라 지속성으로 골라야 한다.

### 5. 🚩 규정 4 가 지키는 것은 **라벨이 아니라 배치 추론**이다 (+169.0)

접근 수준을 셋으로 갈라 폴드 2024 에서 직접 쟀다 (`research/exp015_access.py`).

| 수준 | 무엇을 보는가 | C3 증분 |
|---|---|---:|
| A 현행 | 그 행 + 과거 train | −5.5 |
| **B 전이적** | + 평가셋 **다른 행의 피처** (라벨 없음) | **+163.6** |
| C 라벨 | + 평가셋 라벨 | +113.0 (B 보다 낮다) |

**왜 라벨이 필요 없는가** — `asof_pitcher_n` 은 매 투구 1씩 오르는 통산
카운터다. 평가 시즌에서 그 투수의 `asof_n` **최대 행**의 `cur_succ` 가 곧
그 시즌 실현 성공률이다.

```
corr(피처만으로 얻은 최종 cur_succ, 실제 시즌 성공률) = +0.9986   MAE 0.0017
```

즉 **피처가 라벨을 포함한다.** 이 대회의 무결성은 전적으로 행 독립성
조항("행 하나만 있어도 같은 값") 하나에 걸려 있다.

**따라서 로컬에서 큰 수가 나오면 먼저 이 채널을 의심한다.** EXP005 의
+39.3 이 정확히 이것이었다 (합법 판본 −13.4). 그리고 제출물은 반드시
**단독 행 vs 배치** 동일성을 실측한다 (현 Champion 은 0.000e+00 확인).

### 6. 🚩 지속성만으로는 신호가 아니다 — **오라클과 함께** 봐야 한다

이득 대신 `d_p` 의 시즌 간 상관으로 246개를 재측정했다(`exp016_persist.py`).
검정력이 훨씬 높다 — 투수 ~200명에 걸친 상관이라 자유도가 크다.

```
[대조] 같은손  8위 / 206   인접상관 +0.1345   반분신뢰 0.307   z=1.91
1~7위는 전부 손의 재표현 (lx_*_sh · tmh_* · batter_hand 자체)
```

**LB 로 검증된 진짜 신호조차 z≈1.9 다.** 그러므로 이 통계량도 유의성으로는
못 가르고 **순위로만** 읽는다. 그리고 손이 사실상 1위다.

그런데 `_bs` 계열 5종이 손과 맞먹는 지속성을 보이면서 **오라클은 위약 이하**
였다. 시즌 내 정보가 0 인데 시즌 간에는 지속되는 모순이다. 원인은 구성이었다.

```
_bs 국면 구성차 (볼−스트라이크 기준)   +1.78     <- 사실상 카운트 우위 그 자체
[대조] 같은손 구성차                  −0.05     <- 손은 카운트와 직교
```

분위로 만든 국면은 투수마다 **다른 카운트에서 걸린다.** 투수 수준이 안정적이니
그 구성도 매 시즌 재현되고, 정보가 없어도 `d_p` 가 지속된다.

**How to apply**
```
1  지속성 높음 + 오라클 <= 위약  ->  구성 교란이다. 버린다
2  새 국면은 반드시 **구성차**를 함께 보고한다 (기존 축과의 겹침 진단)
3  분위 기반 국면은 특히 의심한다 — 정의가 다른 축을 몰래 싣는다
```

### 7. 🚩🚩 타깃은 **투수 쪽 운동 결과**다 — 이것이 모든 결과를 설명한다

`control_success` 는 "던진 공이 의도한 곳에 갔는가"다. 타자는 그 공이 어디로
갈지에 **인과적으로 거의 관여하지 않는다** — 오직 투수의 *선택*을 통해서만
들어온다. 실측이 정확히 그 모양이다.

| 축 | 지속성 z | 섞기 z | 오라클 | 해석 |
|---|---:|---:|---:|---|
| **투수 x 타자손** | **1.91** | −0.55 | +11.8 | 투수가 대응을 **바꾼다** |
| 투수 x 2스트라이크 | 1.62 | 0.77 | −0.2 | 결정구 **선택**이 바뀐다 |
| 타자 x 상대 투수손 | 0.45 | 0.40 | +4.2 | 타자 형질 — **섞기와 구별 불가** |
| 타자 x 2스트라이크 | −0.66 | −0.75 | +0.2 | 〃 |
| 타자 x 주자유무 | 0.66 | −0.16 | +0.7 | 〃 |
| 타자 x 카운트우위 | 0.25 | −0.50 | +0.0 | 〃 |

**그래서 살아남은 축이 정확히 셋인 이유**가 설명된다. 투수의 **투구 동작을
실제로 바꾸는 국면**이 데이터 안에 그 셋뿐이다.

```
좌우 대응    타자손        -> 공략 코스와 구종 배합을 바꾼다
결정구 국면  2스트라이크    -> 유인구/결정구로 전환한다
세트포지션   주자유무       -> 투구 동작 자체가 바뀐다
```

**예측력 있는 규칙 — 새 축을 보기 전에 이걸 먼저 묻는다.**
```
"이 국면에서 투수의 동작이나 선택이 물리적으로 바뀌는가?"
  아니오 -> 오라클을 재지 말고 버린다 (타자 형질 · 팀 · 구장 · 점수차 · 이닝)
  예     -> 남은 후보는 이미 소진됐다. 투구수 피로는 데이터에 없고(PN 종료),
            구종은 투구 단위로 없다
```

이 원리는 사후 설명이 아니라 **이미 닫힌 60여 개 축 전부와 부합**한다 —
타자/팀/구장/점수차/이닝/월 축이 모두 "투수 동작이 안 바뀌는 국면"이다.

## 6. 검증 프로토콜

```
워크포워드 3폴드   2019~21 -> 2022,  ~22 -> 2023,  ~23 -> 2024
최소 2시드,  random CV 금지
판정은 항상 **현 Champion 위의 증분** (과거 기준선 대비는 무의미)
```

제출 후보는 `verify_submit.py` 4단계 + **실제 `test.csv` 로 값 직접 확인**.
3단계는 2024 를 test 대역으로 써서 "시즌 내 상태" 피처를 검증하지 못한다 —
`cand_asof*` 는 3단계 확률 범위가 좁게 나오는 것이 정상이다.

**규정 4)** — `test.csv` 의 다른 행이나 전체 분포를 참조하면 안 된다. 행 하나만
있어도 같은 값이 나와야 한다.

## 7. 닫힌 축 — 반복 금지

```
외부 데이터(규정 3 금지) · TrackMan 행 조인(불가) · FM/DeepFM(3폴드 음수)
LightGBM · MoE · 곡률 · 잔차 2단 모델(트리 최적 가중 0/3)
F(recent−season) · 시간척도 창 확장(W2/W3) · EB 축소(k=150/500/2000)
축소로 원시값 교체(참사) · 신뢰도 게이팅 3종 · 카운트 세분 · D×맥락 12열
gap×맥락 · 타자×맥락 · 투수상태×타자상태 · 상태 프로파일/기하/전이
F 행 선택(전량 제외 −6.9, 구체제만 제외 −9.7) · 조건변수 축(game_type 외 없음)
F/R 타입별 표현 분리(xt −0.5 / ht +3.1 / st −6.7) · **game_type 축 완전 종료**
PN 직전경기 투구수 역산(2024 −1.4, 대리 신호 존재) · prev 창 분해(누적보다 약함)
경로 배분(편차축 제거x K2 복귀 12조합 전부 음수, 편차 4축은 서로·in-model 과 직교)
TMX TrackMan 회전/무브 맥락편차 6열(생산 3폴드x2시드, 2024 −0.9)
TMR TrackMan 릴리스기하 맥락편차 6열(2024 −3.9) · **TrackMan 맥락편차 family 전체**
TMZ(구속유지율) — 같은 조인/결측이라 설계 전 기각
TG1~TG5 · 편차가중 재최적(현행이 이미 최적 근처)
현행 TrackMan 8열 **제거**(2024 −11.7) — 죽은 무게가 아니었다. 양방향 종료
투수 단위 잔차 표(시즌 간 전이 +0.7%, 손익분기 아래) · 타자/카운트/국면 그룹
(오라클 cv2 가 기준선 이하 — 정보 자체가 없다)
궤적 family 전체(경로 모양 오라클 최대 +2.0, 2023->2024 관계 반전)
구장(홈팀) 정체성(오라클 +0.1) · 팀 축(타자팀 오라클 +11.6 이나 전이 −9.7)
**F-specific X/H1** (생산 경로 2024 −4.9 / −1.8 / −1.5 — 캐시 경로 +8.7 은 착시)
후처리 5번째 편차축(투수x점수차 등 4종, 2024 최대 +1.7)
형식 축 전체 — 구간별 아핀(최대 +3.9) · 잠재 사영(+1.8) · 2단계 잠재 구조
🚩 **투수 x 이진 국면 전수 소진** (2026-08-18, `research/exp003_sweep.py`)
  47열에서 정의 가능한 이진 국면 **243개를 기계적으로 열거**해 전부 쟀다.
  위약(+0.0)+3 을 넘은 12개가 **전부 `batter_hand` 의 재표현**이었고
  (`*_sh` 결측패턴 · `tmh_*_dev` · `batter_hand` 자체) 전이 프로파일이
  대조군 `같은손` 과 소수점까지 같았다(−0.6/+0.3/+0.1, k=20000).
  **47열 안에서 투수별 차등 정보를 가진 국면은 타자손 하나뿐이다.**
투수 x 국면 2차 상호작용 3종(손x2S · 손x주자 · 2Sx주자) — C3증분 +0.5/+0.1/−2.4
  손x2S 셀 오라클은 +14.3(위약 −0.5)로 **시즌 내엔 실재하나 전이 0**.
  2025 에서는 규정 4로 시즌 내 구조에 못 닿는다 — 도달 불가 신호
새 국면 6종(타자 수준·중앙성향·이닝 선두타자·시즌 워크로드·주자배치 세분)
  전부 오라클 <= 위약. 전이가 안 된 게 아니라 **정보 자체가 없다**
```

**family 단위로 닫힌 것** — 반복 금지:
`recent-history` · `shrinkage` · `generic regime` · `simple calibration` ·
`trajectory` · `gating` · `F-specific` · `team/park identity` ·
**`pitcher x binary context`(전수 증명)** · `context interaction`

## 7-c. 🚩 남은 점수는 어디에 있고 왜 못 닿는가 (2026-08-17)

```
시즌 내 상한 (오라클 cv2, 투수x타자손)   +76.8  (+8.1%)
그중 시즌을 건너오는 몫 (실측)            +6.9  (+0.73%)   -> 9%
손익분기                                        (+2.1%)
```

남은 점수는 **"투수의 그 시즌 현재 상태"** 에 있다. 그런데 2025 에는 라벨이 없고
규정 4) 로 다른 행도 못 본다. **D 가 +85.65 를 낸 이유가 정확히 여기다** — 라벨
없이 `asof_*` 통산 카운터에서 시즌내 상태를 대수적으로 복원했다.

**그러므로 유일하게 열려 있는 문은 `asof_*` 카운터다.** 다만 거기엔 **타자손
분할이 없어** 최대 자리(투수x타자손 +76.8)에 닿는 합법 경로가 현재 없다.

**그리고 그 문도 전수 감사로 닫혔다** (2026-08-17, `exp/asof_audit.py` ·
`exp/asof_resid.py`). `asof_rate = w*prior_rate + (1-w)*cur_rate` 로 정확히
갈리고 D 가 뒤쪽을 전량 가져갔다. 남은 경력 성분·카운터 파생 10종은 2022/2024
에서 전부 잡음 대역(|2.2SE| 이하, 오라클 십분위 음수)이다. 최대 후보(가운데율
계열, 잔차상관 −0.0076)의 **원리적 상한이 +5.2점(+0.55%)** 으로 손익분기
+19.8점의 4분의 1이다. **C 등급 0건 — `asof_*` 축 종료.**

🚩 **`asof_pitcher_pitchmix_n` 은 `asof_pitcher_n` 과 100.0000% 같은 열이다.**
그래서 D 의 `cur_logn_mix` 는 헛열이고, 공식 19열의 실질은 18열이다.
(제거해도 정보 손실 0 — 값싼 확인 항목으로만 남긴다.)

**잡음 대역의 기준** — 폴드 2024 는 253,507행이라 상관의 SE 가 0.0020 이다.
in-model `asof_pitcher_n` 이 +0.0033(1.7SE). 잔차상관 c 의 원리적 상한은
`1e5 * c^2` 이고 실측 선형상한과 일치한다 (0.0076 -> +5.8 vs +5.2).

**공식 47개 컬럼은 전부 사용 중이다.** 미검증 raw 컬럼은 0개이므로 새 컬럼
탐색을 반복하지 않는다.

## 7-b. 🚩 TrackMan 결측이 `game_type` F 를 대리한다

TMX(−0.9)와 TMR(−3.9)이 **같은 서명**을 보였다 — 2022 크게 양수, 2024 음수.

```
F 행 결측 40%  vs  R 행 결측 18%
corr(결측, y)   2022 +0.0258   2023 −0.0078   2024 +0.0006
corr(F, y)      2022 +0.1349   2023 −0.0185   2024 −0.0197
```

TrackMan 피처를 넣으면 **결측을 통해 F 지시자가 딸려 들어간다.** 2022 에서는
공짜 신호(F−R 격차 +0.205)를 실어 나르고 2024 에서는 죽는다.

**어떤 TrackMan 파생도 이 오염을 피할 수 없다** — 조인 구조가 원인이다.
그래서 TMZ 도 설계하지 않는다.

**단 "그러므로 현행 8열도 죽은 무게"는 틀렸다** (2026-08-17 실측). 제거하면
2024 −11.7 이다. 오염은 8열에도 실려 있지만 **상쇄하고 남는 신호가 있다.**

```
tmc_*_dev  카운트별 구종 성향   제거시 -6.1 / -10.0 / -7.8   진짜 신호 (일하는 4열)
tmh_*_dev  타자손별 구종 성향   제거시 -0.6 /  -4.9 / -0.4   노이즈 대역 (중립)
TMX/TMR    회전·무브·릴리스     2024 -0.9 / -3.9              오염만
```

구분선은 조인이 아니라 **물리량**이다. 카운트별 구종 성향은 메인 데이터에
아예 없어서(`asof_pitcher_*_rate` 는 투수 평균 하나뿐) 못 만들고 대리도 없다.
**TrackMan 축은 신규·제거 양방향으로 종료.**

## 8. 실행

```powershell
# 실험 (블록 캐시 사용, 권장)
.\.venv\Scripts\python.exe -u exp\run_exp.py --only "champ,새후보" --out r.json

# 후보 빌드 — 반드시 venv_submit
.\venv_submit\Scripts\python.exe -u exp\build_asof.py --ctx --lvl --build

# 검증
.\.venv\Scripts\python.exe -u verify_submit.py --zip submissions\후보.zip
```

캐시는 2단계다 — `exp/prep.py` 다음 `exp/prep_tmx.py` 까지 돌려야 `cols.json` 에
`prod` 키가 생긴다. 없으면 대부분의 실험이 `KeyError` 로 죽는다.

## 9. 이 PC 에서의 운영 규칙

**백그라운드 작업의 "completed" 알림이 실제 종료보다 먼저 온다.** 이걸 믿고
같은 빌드를 세 개 겹쳐 띄웠다가 가용 RAM 이 1.6GB 로 떨어져 전부 실패한 적이 있다.

```
완료 판단 = Get-Process 로 PID 부재 확인 + 산출물 파일 확인
```

- 실험은 **직렬**. 이전 PID 종료 확인 후 다음을 시작한다
- 프로세스를 정리할 때 **런처 셸을 죽이면 자식 파이썬도 죽는다** — 대상 PID 를
  직접 지정한다
- Bash 에서 `패치 && 실험` 으로 체인하지 않는다. 패치가 assert 로 죽어도 실험이
  그대로 실행된다
- 전체 DataFrame 을 `astype(float32)` 하지 않는다 — 범주형이 문자열이라 죽는다.
  수치 컬럼만 내린다
- 가용 RAM 2GB 미만이면 새 전체학습을 시작하지 않는다

## 10. 사용자와의 약속

- **LB 제출은 사용자만 할 수 있다.** 나는 대회 사이트에 접근할 수단이 없다.
  후보 빌드·검증·보존까지가 내 몫이고 제출 판단은 사용자가 한다
- 남은 슬롯을 모르면 **묻는다.** 그 숫자가 전략의 전제다
- 결과가 나올 때마다 `PROGRESS.md`(시각 포함) → `DEVIATION_LEDGER.md`
  → `SETUP.md` → `LB_LEDGER.csv` 순으로 갱신한다
- 자리비움 모드에서 절전을 막았다면 **복귀 즉시 해제**한다
  (`.keepawake_pid` 에 PID 를 남긴다. `powercfg` 는 건드리지 않는다)
