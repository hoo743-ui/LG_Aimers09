# 다음 제출 계획 (2026-08-12) — 탐침 끝. 평가셋이 풀렸다

현재 **934.10** (12회차, `alpha` 1.20 + 이동 −0.005). 이 점수가 절편 탐침이었고
**평가 시즌이 완전히 풀렸다.** 남은 카드는 최적 아핀 하나다.

## 확정된 것

| | 값 | 근거 |
|---|---|---|
| 모델 설정 | CatBoost depth6 / l2=100 / border32 / i1200 | 4-11, 두 경로 통과 |
| 평가 시즌 성공률 | `r` = **0.4609** | LB 점 3개의 2차식 상수항에서 역산 (4-24) |
| 평가셋 예측 평균 | `m` = **0.473979** | 12회차 절편 탐침 (근이 하나뿐, 모호성 없음) |
| 예측 분산 / 공분산 | `s²` = 0.00191773, `C` = 0.00211915 | 같은 해 |
| 이동 0 일 때 잔여 편차 | **+0.0031** | 위 값에서 |

이야기 두 개 중 **(i)는 기각됐다.** (i)이면 탐침이 ≈998 이어야 했는데 934.10 이다.
실제 `m`=0.4740 은 (ii)의 0.472 에 가깝고, 잔여 편차도 +0.0031 로 작다.

## 1번 제출 — 완료 (12회차)

`submissions/exp_007_a120_shift005.zip` → **934.10** (+2.57). 목적은 점수가 아니라
독립적인 네 번째 식이었고, 그 역할을 했다.

## 2번 제출 — 완료 (13회차). **942.4577639361**

`submissions/grid_affine_solved.zip` — `alpha` = 1.105030, `center` = 0.598664.

```
예측  942.457753
실측  942.457764        오차 +0.000011
```

`clip` 이 안 닿으면 오차식은 근사가 아니라 **항등식**이라 이렇게 맞는다. 평가셋은
완전히 풀렸고 **아핀 천장에 정확히 도달했다.** 이후 점수 = `1e5 * rho^2` 이고
현재 `rho` = **0.097080** 이다. 남은 것은 `rho` 뿐이다.

> **새 모델은 아핀을 다시 풀어야 한다.** `r`=0.4609 만 모델과 무관하게 남고
> `m`,`s^2`,`C` 는 모델마다 다시 미지수다 — 미지수 3개니 **LB 점 3개**가 더 든다.
> 즉 새 모델의 `rho` 이득이 아핀 어긋남보다 크지 않으면 **순손실**이다.
> 모델을 건드릴 거면 작은 개선이 아니라 큰 개선이어야 한다.

이동만 최적화하면 935.50 (+1.4) 뿐이다. **기울기를 1.20 에서 1.105 로 되돌리고
절편을 올리는 쪽**이 이득의 대부분이다 — 최소제곱 해라 이동 단독보다 항상 낫다.

> 이미 만들어 둔 이동 격자(`shift010/015/019`)는 **전부 폐기.** 저건 이야기 (i)을
> 가정한 값이라 지금은 손해다 — 예컨대 `shift010` 은 잔여 편차를 +0.0031 에서
> −0.0069 로 지나쳐 **916 근처**로 떨어진다. `grid_affine_i_opt.zip`(A\*=0.9651)도
> 마찬가지로 (i) 전용이니 내지 말 것.

## 그 다음 — 남은 게 없다

아핀 변환은 `p' = A p + B` 안에서 **최적**이다. 여기서 더 가려면 변환이 아니라 모델이
좋아져야 하는데(= `C²/s²` 를 키워야 하는데) 모델 축은 열두 개를 재서 전부 닫혔다.
942 근처가 이 아티팩트의 천장이다.

## 3번 제출 — 없다. 시드 축도 닫혔다 (2026-08-11 기록)

시드 3 → 6 을 실제로 빌드해 제출 경로에서 쟀다 (2026-08-11).

```
시드 3:  748.77  ->  alpha 1.20 적용  767.16
시드 6:  746.29  ->  alpha 1.20 적용  756.77      (-2.48 / -10.4)
```

실험 경로는 보정 후 시드 6개가 3폴드 모두 좋다고 했지만(+3.60/+3.01/+1.23) 제출
경로에서 **−10.4** 다. s45/s46/s47 단일이 739.30/738.99/729.46 으로 낮아 누적이
내려간다. 시드 운이지만, 제출 경로는 폴드가 하나뿐이라 기대값을 잴 수 없고 **LB 로
검증된 아티팩트는 3시드 판**이다. 측정 가능한 유일한 값이 음수이므로 3시드를 유지한다.

시드 축은 이로써 세 번째로 닫힌다 (3→5 −0.63, 3→6 −2.48, 보정 후 −10.4).
`model_cand/cat_a120_s6.pkl` 은 남겨 두지만 zip 은 만들지 않았다.

**그래서 남은 카드는 보정 축 하나였고, 12회차 탐침이 그것을 다 썼다.** 위 2번이
마지막 카드다.

## 규칙 관점 — 기록해 둘 것

- `alpha`·`center` 는 **학습 시점에 정해져 pkl 에 담기는 상수**다. 추론은 행 독립이고
  제출 코드는 `test.csv` 를 읽지 않는다. `data_description.md` 5) 가 금지하는
  "추론 시점 평가 데이터 참조"에 해당하지 않는다
- 다만 **값을 LB 피드백으로 고르는 것**은 4-3 에서 철회한 중심 보정과 *효과*가 같다.
  `Private = Public`(테스트 100%)이라 과적합 손실은 없지만, Phase 3 에서 방법론을
  볼 수 있으니 이 문서와 커밋 기록을 근거로 남긴다
- `alpha` 1.20 자체는 **로컬 근거가 독립적으로 있다** — 워크포워드 2/2 승, 3폴드 전부
  양수 (4-23). LB 없이도 정당화된다. 이동은 그렇지 않다
- **아핀 최적(A\*=1.105, center=0.598664)은 로컬 근거가 전혀 없다.** `r`·`m`·`s²`·`C`
  네 값이 전부 LB 점수 4개에서 역산된 것이고, 그중 `r` 은 주최측이 비공개로 둔 값이다.
  기전은 여전히 "학습 시점 상수"라 규칙 5) 의 문언에는 걸리지 않지만, 이건 **모델을
  좋게 만든 게 아니라 평가셋을 역산한 것**이다. Phase 3 에서 방법론을 설명해야 하면
  이 문단과 `exp/shift_solve.py` 의 주석이 그 근거다 — 숨기지 말고 그대로 낼 것

## 이미 닫힌 것 (다시 열지 말 것)

오늘 열두 축을 재서 통과는 `alpha` 하나였다. 보정 이후 순위를 다시 매겨도
(`exp/postcal_rank.py`) 부호가 일치하는 설정은 없었다 — `oh_team` 이 2024 에서
+25.0 이지만 2021 에서 −47.0 이다.

| 축 | 결과 |
|---|---|
| lr 0.01 / 0.005 / 0.04 | 제출 경로 −2.88 / 미달 / −14.43 |
| 시드 (alpha 없이) | 제출 경로 −0.63 |
| `cat_features` (CTR) | −31.17 |
| 목적함수 RMSE | +2.64 부호갈림 |
| 잔차 타깃 | −19.56 |
| `boosting_type` Ordered | −255.16 |
| 표본 수 컬럼 제거 | −19.20 |
| `random_strength` / `leaf_estimation` | 부호갈림 / 무시됨 |
| 팀 원핫, `dev_*`, `sk200_*` | 보정 전후 모두 부호갈림 |
