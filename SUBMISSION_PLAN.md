# 다음 제출 계획 (2026-08-12)

현재 **931.53** (11회차, `alpha` 1.20). 오늘 제출 횟수를 다 썼으므로 내일 쓸 것을
미리 만들어 두었다. 순서가 중요하다 — **첫 제출이 나머지를 결정한다.**

## 지금 확정된 것

| | 값 | 근거 |
|---|---|---|
| 모델 설정 | CatBoost depth6 / l2=100 / border32 / i1200 | 4-11, 두 경로 통과 |
| `alpha` (기울기) | **1.20** — 최적 1.1941, 잔여 +0.06 | LB 점 3개로 2차식 정확 적합 (4-24) |
| 평가 시즌 성공률 | **r = 0.4609** (주최측 비공개 값을 역산) | 같은 적합의 상수항 |
| 남은 미지수 | `m` (평가셋 예측 평균) 하나 | 기울기만 바꾼 점으로는 안 풀린다 |

`m` 이 안 풀려서 **잔여 중심 편차의 크기와 부호를 모른다** (−0.017 ~ +0.031).
두 이야기가 모두 자기정합적이다.

| | `m` | 잔여 편차 | 이동만 최적 | 아핀 최적 | 로컬 근거 |
|---|---|---|---|---|---|
| (i) | 0.487 | +0.019 | 1072 | **1140** | 없음 |
| (ii) | 0.472 | +0.001 | 932 | 933 | 폴드 3개와 일치 |

## 1번 제출 — 반드시 이것부터

**`submissions/exp_007_a120_shift005.zip`** (`alpha` 1.20, 이동 −0.005)

이동은 기울기가 아니라 **절편**을 움직이므로 독립적인 네 번째 식이 된다. 이 한 번으로
`m`·`s`·`cov` 가 전부 확정되고 최적 아핀 변환이 계산된다.

```
결과가 ≈998 (+67)  -> 이야기 (i). 최적 아핀 1140 구간
결과가 ≈926 (-6)   -> 이야기 (ii). 이미 최적, 931.53 으로 되돌린다
```

받은 점수를 그대로 넣으면 최적값이 나온다.

```powershell
.\.venv\Scripts\python.exe exp\shift_solve.py --probe <받은 점수>
```

## 2번 제출 — 1번 결과에 따라

풀이기가 출력한 `alpha`/`center` 를 pkl 상수로 박으면 된다 (재학습 없음, 약 1분).
자주 나올 값은 미리 만들어 두었다 (`submissions/GRID_INDEX.json`).

| 1번 결과 | 2번에 낼 것 |
|---|---|
| ≈998 | `grid_affine_i_opt.zip` (A*=0.9651) 또는 풀이기 출력값으로 새로 빌드 |
| 970~990 | `grid_a120_s3_shift019.zip` → 풀이기로 미세조정 |
| 940~970 | `grid_a120_s3_shift015.zip` |
| 932~940 | `grid_a120_s3_shift010.zip` |
| 926 근처 | 이동 축을 닫는다. 아래 3번으로 간다 |

> **주의** — 이동을 크게 걸면 `clip` 에 닿기 시작한다. 닿은 만큼 보정이 잘려 이득이
> 줄어든다. `GRID_INDEX.json` 의 `clip_pct` 를 확인할 것 (현재 격자는 전부 0%).

## 3번 제출 — 없다. 시드 축도 닫혔다

시드 3 → 6 을 실제로 빌드해 제출 경로에서 쟀다 (2026-08-11).

```
시드 3:  748.77  ->  alpha 1.20 적용  767.16
시드 6:  746.29  ->  alpha 1.20 적용  756.77      (-2.48 / -10.4)
```

실험 경로는 보정 후 시드 6개가 3폴드 모두 좋다고 했지만(+3.60/+3.01/+1.23) 제출
경로에서 **−10.4** 다. s45/s46/s47 단일이 739.30/738.99/729.46 으로 낮아 누적이
내려간다. 시드 운이지만, 제출 경로는 폴드가 하나뿐이라 기대값을 잴 수 없고 **LB 로
검증된 아티팩트는 3시드 판**이다. 측정 가능한 유일한 값이 음수이므로 3시드를 유지한다.

시드 축은 이로써 세 번째로 닫힌다 (3→5 −0.63, 3→6 −2.48, 보정 후 −10.4).
`model_cand/cat_a120_s6.pkl` 은 남겨 두지만 zip 은 만들지 않았다.

**따라서 내일 쓸 카드는 이동 보정 하나다.** 그것이 (ii)로 판명되면 931.53 에서
끝이고, 그 뒤로는 해상도 축의 구조적 발견만 남는다.

## 규칙 관점 — 기록해 둘 것

- `alpha`·`center` 는 **학습 시점에 정해져 pkl 에 담기는 상수**다. 추론은 행 독립이고
  제출 코드는 `test.csv` 를 읽지 않는다. `data_description.md` 5) 가 금지하는
  "추론 시점 평가 데이터 참조"에 해당하지 않는다
- 다만 **값을 LB 피드백으로 고르는 것**은 4-3 에서 철회한 중심 보정과 *효과*가 같다.
  `Private = Public`(테스트 100%)이라 과적합 손실은 없지만, Phase 3 에서 방법론을
  볼 수 있으니 이 문서와 커밋 기록을 근거로 남긴다
- `alpha` 1.20 자체는 **로컬 근거가 독립적으로 있다** — 워크포워드 2/2 승, 3폴드 전부
  양수 (4-23). LB 없이도 정당화된다. 이동은 그렇지 않다

## 이미 닫힌 것 (다시 열지 말 것)

오늘 열두 축을 재서 통과는 `alpha` 하나였다. 보정 이후 순위를 다시 매겨도
(`exp/postcal_rank.py`) 부호가 일치하는 설정은 없었다 — `oh_team` 이 2024 에서
+25.0 이지만 2021 에서 −47.0 이다.

| 축 | 결과 |
|---|---|
| lr 0.01 / 0.005 / 0.04 | 제출 경로 −2.88 / 미달 / −14.43 |
| 시드 (alpha 없이) | 제출 경로 −0.63 |
| `cat_features` (CTR) | −31.17 |
| 목적함수 RMSE | +2.64 부호갈림 |
| 잔차 타깃 | −19.56 |
| `boosting_type` Ordered | −255.16 |
| 표본 수 컬럼 제거 | −19.20 |
| `random_strength` / `leaf_estimation` | 부호갈림 / 무시됨 |
| 팀 원핫, `dev_*`, `sk200_*` | 보정 전후 모두 부호갈림 |
