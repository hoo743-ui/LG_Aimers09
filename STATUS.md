# 현재 상태 요약 (2026-08-12)

한 장으로 보는 현황. 상세 근거는 `README.md` §4 와 `DEVIATION_LEDGER.md` 에 있다.

---

## 1. 점수 현황

| | 값 |
|---|---|
| **최고 실측 LB** | **955.2193198652** (19회차) |
| 대기 후보 | 없음 |
| 남은 제출 | **2장** (2026-08-13 기준) |
| 1등 | 1000+ (2026-08-11 기록 시점) |
| 평가셋 `rho` | 0.0977 (점수 = `1e5 * rho^2`, 아핀 최적일 때만 성립) |

### 제출 이력 (후반부)

| 회차 | 구성 | 예측 | 실측 | 오차 |
|---|---|---|---|---|
| 9 | CatBoost depth6 l2=100 border32 s3 | 866 | 864.92 | −1.1 |
| 10 | + `alpha` 1.09 (로컬 근거 보유) | — | 912.41 | — |
| 11 | + `alpha` 1.20 | — | 931.53 | — |
| 12 | + 이동 −0.005 (절편 탐침) | — | 934.101431079 | — |
| 13 | 아핀 최적 (LB 4점으로 역산) | 942.457753 | **942.4577639361** | **+0.000011** |
| 14 | + 플래툰 `w`=0.3904 | 963.4 | 940.1357117095 | −23.26 |
| 15 | + 플래툰 `w`=0.1990 | 946.50 | 946.3826029949 | −0.117 |
| 16 | + 플래툰×카운트 `w2`=0.20 | 948.82 | **950.0112119476** | +1.19 |
| 17 | + `w2` 재최적화 0.5785 | 952.72 | **952.4231549068** | −0.30 |
| 18 | + 플래툰×주자유무 `w3`=0.30 | 953.6943 | **953.7373675006** | **+0.043** |
| 19 | 카운트축 중첩 2층 분해 | 957.4367 | **955.2193198652** | **−2.22** |

> **19회차 오차 −2.22 는 "풀린 파라미터" 범주가 아니다** (과거 최대 −0.30). 빌려온
> 전이율 두 개(`k_c` 거친축 3단, `k_n` 4단, 둘 다 0.9726 차용)가 빗나갔다.
> 18회차에서 얻은 "3단 전이율은 축에 무관"이 **새 축 종류(투수유리)에는 그대로
> 안 옮겨갔다.** 관측이 하나라 `k_c`·`k_n` 을 동시에 못 풀고 일관 궤적만 나온다 —
> 분리하려면 한 층만 바꾼 점이 하나 더 필요하다.

**예측 오차는 파라미터 출처로 갈린다** — LB 로 *푼* 값은 `|오차|` ≤ 0.30,
전이율을 *가정한* 값은 최대 23.3.

---

## 1-b. 🚩 17회차의 진짜 소득 — 전이율 4개를 전부 실측

점수 +2.41 보다 **`w2` 곡선 위의 세 번째 점**이 중요했다. 16회차까지는 점이 하나뿐이라
`k2` 를 뽑으려면 `lambda` 를 플래툰 축에서 **빌려와야** 했다. 이제 LB 5점(13~17)으로
미지수 4개를 동시에 푼다 (`exp/solve_k2.py`, 재학습 0회).

| | 기존 | **실측** | |
|---|---|---|---|
| `k1` 2단 `cov(f,y)` 전이 | 0.7685 | **0.7689** | 원장 복원 ✅ |
| `lam1` 2단 2차 적률 | 0.9725 | **0.9731** | 원장 복원 ✅ |
| `k2` 3단 `cov(f,y)` 전이 | 0.9340 *(가정)* | **0.9651** | 전이가 더 좋다 |
| `lam2` 3단 2차 적률 | 0.9725 *(빌려옴)* | **1.0422** | 분산 비용이 7% 더 크다 |

13회차 앵커가 942.457764 를 **소수점 6자리까지 재현**하고 플래툰 축이 원장 값을
복원하므로 로컬 적률 재구성이 원 분석과 일치한다. `k2`↑ 와 `lam2`↑ 가 상쇄돼
17회차가 예측보다 0.30 낮았던 것이 설명된다.

> **`w2` 축은 닫혔다.** 진짜 최적은 0.545 이고 재최적화 이득은 +0.02 뿐이다.
> 0.5785 는 사실상 정확했다.

---

## 2. 🚩 미해결 — 규칙 4) 저촉 여부

### 공식 규정 (2026-08-12 확인)

> **1) 사전학습모델 및 가중치 사용 가능 범위**
> 공식적으로 누구에게나 가중치가 공개되었으며, 최소한 비상업적 이용이 허용된
> 라이선스(MIT, Apache 2.0 등) 하에 배포된 모델 및 가중치만 사용 가능합니다.
>
> **2) 외부 API 사용 제한**
> 원격 서버 기반의 API 형태로만 접근 가능한 모델(OpenAI API, Gemini API 등)은
> 사용이 불가합니다. 모든 작업은 로컬 환경에서 직접 코드로 실행 및 재현
> 가능해야 하며, 외부 서버에 의존하는 방식은 제한됩니다.
>
> **3) 외부 데이터 사용 금지**
> 온라인 해커톤(Phase 2)에서 제공하는 공식 데이터 외의 외부 데이터는 사용할 수
> 없습니다.
>
> **4) 추론 코드의 평가 데이터 예측 원칙**
> 평가 데이터(test.csv)의 각 행은 하나의 독립적인 예측 대상입니다. 참가자는 각
> 행에 포함된 입력 변수와 주최 측이 제공한 공식 학습 데이터만을 이용하여 해당
> 행의 예측값(제구 성공 확률)을 추론해야 합니다. 평가 데이터의 다른 행이나 전체
> 평가 데이터의 분포를 이용해 특정 행의 예측값을 보정하거나 생성하는 방식은
> 정상적인 추론 절차로 인정되지 않습니다.

### 3) 은 해소 — 외부 데이터 축 완전 종결

`RULES UNVERIFIED` 였던 것이 **명시적 금지**로 확정. `DEVIATION_LEDGER.md`
§4-27 · §6-l 의 판단과 일치한다. 논쟁 여지 없이 닫힌다.

### 4) 는 회색지대 — 판단 필요

**추론 코드는 준수한다.** `script.py` 는 `test.csv` 의 다른 행을 읽지 않고,
상수만 적용하며, 행 독립이다.

**문제는 상수의 출처다.** 13회차부터:

```
r = 0.4609      <- 평가 시즌의 실제 성공률을 LB 점수에서 역산
m = 0.473979    <- 평가셋 예측 평균을 LB 점수에서 역산
center = (r - A*m)/(1 - A)      <- 그 둘을 상수로 박음
```

제출본의 `center` 안에 **평가 데이터 분포의 통계량 두 개가 들어 있다.**

| 읽기 | 판정 |
|---|---|
| 좁게 (절차 기준) | 추론 코드 준수 → 통과. LB 기반 모델 선택은 모든 대회의 표준 |
| 넓게 (효과 기준) | 평가셋 분포를 복원해 보정값으로 사용 → 문언에 걸림 |

우리 방식은 "LB 보고 후보 고르기"보다 **직접적**이다. 대수적으로 분포를 복원했다.

### 되돌릴 수 없는 부분

13~**17**회차는 이미 제출됐다. 기록은 남는다 (3·4회차의 국면 보정 건과 같은 상황).
17회차(`w058`)는 이 문서가 "문의 먼저"를 권고한 **뒤에** 제출됐다 — 대기 후보가
아니라 확정 기록이다. 18회차 후보도 같은 아핀 상수 구조를 그대로 쓴다.

---

## 3. 로컬 근거만으로 만들면 얼마인가

`alpha`/`center` 를 LB 없이 정했을 때를 평가셋 모형으로 계산한 결과.

| 구성 | 평가셋 점수 | 로컬 정당화 |
|---|---|---|
| 워크포워드 그대로 (편향차감 없음) | **899.16** | ⭕ 성립 |
| + 과거 폴드 평균 편향 차감 | 933.95 | ❌ 워크포워드 1/3 |
| + 편향차감 + `w` 0.5배 축소 | 947.08 | ❌ 평가셋 보고 고름 |
| LB 역산 대기후보 `w058` | 952.72 | ❌ 평가셋 통계량 내장 |

> **947.08 은 "정당한 947" 이 아니다.** 6개 변형의 평가셋 점수를 보고 고른 값이다.
> 편향차감 규칙은 로컬 워크포워드에서 2022 −62.30 / 2023 −54.18 / 2024 +17.63 로
> **1/3** 이라 사전 정당화가 안 된다.

**사전 규칙만 지키면 899~913 이 한계선이다.** 10회차 실측 912.41(`alpha` 1.09,
로컬 근거 보유)이 그 근처인 것이 우연이 아니다. **950 과의 격차 약 40점이 LB 역산의
몫이다.**

> 참고 — 성공률 추세 외삽 자체는 매우 잘 맞는다 (최근 3시즌 선형 → `r_hat`=0.4622,
> 실제 0.4609, 오차 +0.0013). 그런데 `center = B/(1-A)` 는 `A`≈1.07 에서
> `1-A`=−0.07 로 나누므로 오차가 15배 증폭된다. 예측 평균 `m` 의 외삽 오차
> +0.0204 가 절편 오차 −0.0205 → **169점 손실**. 추세는 맞는데 파라미터화가 불안정하다.

---

## 4. 다음 행동 — 두 갈래

### A. 주최측에 문의하고 답에 따른다 (권고)

문의 문안:

> 리더보드 점수로부터 평가셋의 통계량(성공률·예측 평균)을 역산하여, 그 값을 학습
> 시점 상수로 모델 파일에 담아 추론에 사용하는 것이 규칙 4)에 저촉됩니까?
> 추론 코드는 `test.csv` 의 다른 행을 참조하지 않습니다.

- **저촉 안 됨** → `cand_plat_cnt_runner.zip` 제출 (기대 953.69)
- **저촉됨** → 로컬 근거판을 새로 제작. 900대 초반이 현실적 상한

권고 이유는 점수가 아니라 **정보와 시점**이다. Phase 3 진출 시 방법론 설명을
요구받을 수 있고, "미리 문의해 확인받았다"와 "지적받고 알았다"는 다른 상황이다.
저촉이라는 답이 오면 **지금은 로컬 근거판을 만들 시간이 있다.**

### B. 문의 없이 현 상태 유지

952.4232 는 이미 기록됐고 되돌릴 수 없다. 추가 제출만 하지 않는 것이며 리스크를
키우지도 줄이지도 않는다. 다만 17회차가 이미 나간 이상 "한 번 더"의 한계 비용은
낮아졌고, 그만큼 **문의를 미룰 이유도 약해졌다** — 남은 4장을 어떻게 쓰든 방법론
설명 요구는 같은 구조에 대해 들어온다.

---

## 5. 탐색 종결 현황

1100 을 목표로 한 탐색은 **수렴했다.** 모든 축이 상한까지 측정되어 닫혔다.

| 축 | 결과 | 근거 |
|---|---|---|
| 모델 설정 (56개) | 고갈 | `rho^2` 재순위 §4-25 |
| 계층 편차 — 깊이 | 3단이 바닥 (4단 전부 2/3 이하) | §5 |
| 계층 편차 — 폭 | 🚩 **재개봉 — 주자유무가 살아난다** | §6-n |
| 타자 쪽 계층 | 평가셋 최적 가중 0 | §5-c |
| 잠재 상호작용 (MF/ALS) | 대조군 포함 기각 | §6-c |
| **FM / DeepFM** | **직접 측정 완료 — 기각.** 잠재항이 3/3 폴드에서 음수 | §6-p |
| 외부 데이터 | **규칙 금지** | §6-l, 규정 3) |
| 조건부 아핀 | WF 평균 양수 0/10 | §4-28 |
| 곡률 | 부호부터 불안정, `P(>950)`=26.3% | §6-h |
| 잔차 2단 모델 | 트리 최적 가중 0 (상한 확정) | §6-g |
| Challenger 4종 | 전부 음수, 앙상블 가치 0 | §6-k |
| regime / 동적 키 | 최대 +0.45 | §6-f |

**구조적 결론** — 한 시즌에서 맞춘 관계는 다른 시즌으로 넘어가지 않는다.
정보 상한은 현재 성능 위에 있지만(2024 기준 888 vs 789) 그 여유분은 **시즌 내부
정보**다. 병목은 추정이 아니라 전이이며, 모델 구조 변경으로 회수할 수 있는
종류가 아니다.

---

## 6. 보존 아티팩트 (덮어쓰기 금지)

```
submissions/grid_affine_solved.zip      13회차 942.4578
submissions/cand_platoon_w020.zip       15회차 946.3826
submissions/cand_plat_cnt.zip           16회차 950.0112
submissions/cand_plat_cnt_w058.zip      17회차 952.4232   <- 현 Champion
submissions/cand_plat_cnt_runner.zip    대기 후보 (미제출, 기대 953.69)
```

`verify_submit.py` 4단계 전부 통과 확인 (추론 3.5초, `clip` 접촉 0%).

신규 후보 추가 검증 (`exp/verify_runner.py`) — **추론 경로 == 실험 경로 최대차
0.000e+00**, 보류 커버리지 80.0% (기존 두 축 80.1%/79.9% 와 일치), 범위
0.2802~0.6812.

---

## 7. 다음 후보 — `cand_plat_cnt_runner.zip` (18회차)

```
p += 0.20   * dev(투수x타자손        | 부모=투수,   k=300)
p += 0.5470 * dev(투수x타자손x카운트   | 부모=플래툰, k=800)    <- w2 재최적화
p += 0.30   * dev(투수x타자손x주자유무 | 부모=플래툰, k=2000)   <- 신규
alpha = 1.089163   center = 0.622907
기대 953.69  (17회차 대비 +1.27)
```

### 왜 주자유무인가

실측 `k2`=0.9651 로 원장 5-b 가 접었던 축들을 다시 쟀다 (`exp/axis_scan.py`).
원장의 기각은 전이율을 낮게 가정한 판정이었다.

폴드 검사 — 표를 `<f` 로 만들고 `f` 에서 `cov(d,y)` 를 쟀다 (`exp/risk_table.py`).

| 축 | 2021 | 2022 | 2023 | 2024 | 부호 |
|---|---|---|---|---|---|
| **주자유무** | +1.93e−6 | +4.79e−6 | +1.05e−5 | +1.10e−5 | **4/4 단조증가** |
| *(대조)* 플래툰 | +1.66e−4 | +1.85e−4 | +2.16e−4 | +2.05e−4 | 4/4 |
| *(대조)* 플래툰×카운트 | +2.10e−5 | +2.76e−5 | +4.05e−5 | +4.13e−5 | 4/4 |

신호 크기는 카운트 축의 약 1/4. **`주자상태`(8단)가 `주자유무`(2진)에 완전히
열등하다** — 잘게 쪼개면 신호는 그대로인데 분산만 는다.

### 왜 `w3` = 0.30 인가 — 위험표로 골랐다

`k3` 는 이 축에서 미측정이고 카운트 축에서 빌려온다. **축 간 차용은 14·16회차에서
두 번 다 빗나갔다.** 그래서 고정 `w3` 위험표가 판단 근거다 (17회차 대비 증분).

| 진짜 `k3` | `w3`=0.15 | **`w3`=0.30** | `w3`=0.45 |
|---|---|---|---|
| 0.00 (신호 없음) | −0.62 | **−1.53** | −2.79 |
| 0.50 | +0.10 | **−0.08** | −0.61 |
| 0.7689 (2단 실측) | +0.49 | **+0.70** | +0.56 |
| 0.9651 (3단 실측) | +0.78 | **+1.27** | +1.41 |

`w3`=0.45 는 상방 +0.14 를 더 얻자고 하방을 5배 키운다 — 14회차에서 데인 형태다.
`k3` < 0.5 일 때만 손해인데, 3단 축에서 그건 실측 패턴(2단 0.769 → 3단 0.965)에
반한다.

> **주의** — 이 판단의 기대값은 `k3`=0.9651 이라는 **차용 가정** 위에 있다.
> 근거로 삼을 것은 기대값이 아니라 위험표다.
