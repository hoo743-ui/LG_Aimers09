# Phase 3 실전 플레이북

1박 2일, 오프라인, 야구 데이터, 세부 과제 미공개. **과제가 뭐로 나오든 쓸 수 있는
것만** 적는다. Phase 2 에서 실제로 점수를 만든 순서와, 비싸게 배운 함정들이다.

---

## 0. 먼저 — Phase 2 에서 무엇이 실제로 점수였나

LB 원장(`LB_LEDGER.csv`)에서 구간별로 잘라 보면 순서가 직관과 반대다.

| 한 일 | LB 변화 | 든 시간 |
|---|---|---|
| 베이스라인 → 시드앙상블 + ID 피처 | 549.64 → 814.50 (**+264.86**) | 반나절 |
| **캘리브레이션** (`alpha` → 아핀 최적) | 864.92 → 942.46 (**+77.54**) | 재학습 **0회** |
| 모델류 교체 (HGB → CatBoost) | 825.48 → 864.92 (+39.44) | 하루 |
| 피처 추가 (상황 조건부 Trackman) | 814.50 → 825.48 (+10.98) | 하루 |

> **캘리브레이션이 모델류 교체의 2배, 피처 작업의 7배였다. 그리고 재학습이 0회다.**
> 우리는 이걸 **10회차에 가서야** 처음 건드렸다. 다음엔 3회차 안에 한다.

그리고 열두 개가 넘는 모델/피처 축을 재서 통과한 것은 `alpha` 하나였다.
**손잡이 돌리기는 거의 값이 없었다.**

---

## 1. 시간표

시간은 예산이다. 각 구간 끝에 **넘어갈지 말지**를 정한다.

### T+0 ~ T+2h — 지표와 검증 설계 (여기가 제일 중요하다)

이 두 시간이 나머지 전부를 결정한다. **모델을 만지지 않는다.**

- [ ] **평가 산식을 확정한다.** 공식이 문서에 없으면 역산할 각을 미리 잡는다.
      Phase 2 에서 우리는 `점수 = 1e5 x R^2` 라는 것을 **13회차에 가서야** 알았다.
      알고 나니 "아핀 보정은 공짜"라는 것과 "비교는 `rho^2` 로 해야 한다"가 따라왔다
- [ ] **금지 조항을 그대로 옮겨 적는다.** 무엇이 누설인지의 정의다. Phase 2 는
      "평가셋 내부 행을 이용한 누적/빈도/rolling/target encoding" 이 금지였고,
      우리는 이걸 어긴 제출로 862.75 를 찍었다가 폐기했다 (4-3)
- [ ] **시간 분할 검증을 만든다.** 랜덤 분할 금지. 폴드는 **3개 이상**, 서로 다른
      연도로. 폴드 하나로 판정하면 반드시 틀린다
- [ ] **타깃 비율이 시간에 따라 움직이는지 먼저 센다.** Phase 2 는 해마다 떨어졌고
      (0.5647 → 0.4861), 이게 가장 중요한 구조적 사실이었다 (4-2)

### T+2 ~ T+5h — 베이스라인으로 제출 경로를 관통한다

**점수가 아니라 경로를 뚫는 게 목적이다.** Phase 2 에서 실패한 제출 2건이
전부 환경 문제였지 모델 문제가 아니었다.

- [ ] 가장 단순한 모델로 **끝까지** 제출해 본다
- [ ] `verify_submit.py` 4단계를 그대로 이식한다 (§3)
- [ ] 제출 슬롯이 하루 몇 개인지 확인하고 **예산으로 취급한다**

### T+5 ~ T+9h — 모델류를 정한다

- [ ] 2~3개 계열을 같은 폴드에서 비교한다 (Phase 2: HGB vs CatBoost, 6/6 부호일치로 결정)
- [ ] **여기서 정하면 다시 안 돌아온다.** 이후 모든 측정의 기준선이 된다

### T+9 ~ T+13h — 캘리브레이션 (가장 남는 구간)

재학습 0회. §2 로.

### T+13 ~ T+18h — 피처·하이퍼파라미터

- [ ] 자동 실험 큐로 돌린다 (§3의 `auto.py`/`runner.py`)
- [ ] 이 구간의 기대값은 낮다. **캘리브레이션이 안 끝났으면 여기 오지 말 것**

### T+18h ~ — 제출본 확정, 문서화

- [ ] 마지막 제출 2시간 전에 **후보를 동결한다**
- [ ] 재현 명령과 판단 근거를 남긴다 (Phase 3 은 방법론을 볼 수 있다)

---

## 2. 캘리브레이션 — 제일 먼저, 제일 싸게

### 왜 이렇게 큰가

정답 비율이 시간에 따라 움직이는 문제에서, 모델은 **학습 구간의 수준**을 배운다.
평가 구간의 수준이 다르면 그만큼이 그대로 손실이다. Phase 2 기준으로 중심이 `d` 만큼
어긋나면 `d^2 / r(1-r) x 100000` 점을 잃었고, 2024 폴드의 편차 0.0119 가 **57점**이었다.

그리고 규제를 세게 건 모델(또는 시드 평균)은 예측이 **중심으로 오므라든다.** 그
퍼짐을 되돌리는 것만으로 Phase 2 에서 +47.49 가 나왔다.

### 순서

1. **퍼짐(기울기)부터.** `p' = c + alpha*(p - c)`, `c` 는 **학습 구간** 평균.
   폴드별 최적 `alpha` 를 찍어 보고, **워크포워드로** (이전 폴드에서 고른 값을
   다음 폴드에 적용) 검증한다. 그 폴드 정답으로 고른 `alpha` 는 낙관 상한이다
2. **중심(절편).** 평가셋 평균을 보고 옮기면 대개 **규칙 위반**이다. 로컬로만 고른다
3. **LB 로 푸는 방법** — 산식을 알고 `clip` 이 안 닿으면 오차식이 항등식이라
   LB 점 몇 개로 평가셋 통계를 **정확히 역산**할 수 있다. Phase 2 에서는 4점으로
   풀었고 예측과 실측 오차가 **0.000011** 이었다 (`exp/shift_solve.py`)

> **`clip` 접촉률을 반드시 찍을 것.** 닿기 시작하면 보정이 잘려 이득이 준다.

### 규칙 관점 — 미리 정리해 둘 것

`alpha`/`center` 가 **학습 시점에 정해져 모델 파일에 담기는 상수**면 추론은 행
독립이고 평가 데이터를 참조하지 않는다. 그건 안전하다. 반면 **값을 LB 피드백으로
고르는 것**은 기전이 다르지만 효과가 같아질 수 있다 — 로컬 근거가 따로 있는지를
구분해서 기록해 둘 것. Phase 2 에서 `alpha` 1.20 은 로컬 근거가 독립적으로 있었고
(워크포워드 2/2, 3폴드 전부 양수), 아핀 최적 1.105 는 **전혀 없었다.**

---

## 3. 가져갈 코드 자산

과제가 달라도 뼈대는 그대로 쓴다. 우선순위 순.

| 파일 | 무엇 | 이식 비용 |
|---|---|---|
| `verify_submit.py` | **제출 전 4단계 자동 점검.** 빈 폴더 압축해제 → 격리 실행 → 실규모 추론 시간 → 낮은 numpy 로드 | 경로만 고치면 됨 |
| `exp/prep.py` | 피처 행렬을 float32 npy 로 한 번만 캐시. 실험당 1~2분씩 절약 | 컬럼 정의만 |
| `exp/runner.py` | temporal fold x seed 로 재고 **예측을 캐시**한다 | 거의 그대로 |
| `exp/auto.py` | `plans/queue/*.json` 을 무인으로 돌려 델타·표준오차·부호일치까지 기록 | 거의 그대로 |
| `exp/rho_rank.py` | 캐시된 예측을 **아핀 불변 지표**로 재순위. 재학습 0회 | 산식만 |
| `exp/calib.py`, `exp/alpha_pick.py` | 캘리브레이션 워크포워드 검증 / 제출본 상수 고르기 | 거의 그대로 |
| `exp/struct_probe.py` | **구조 진단.** 그룹 오라클(그 구간 자신의 평균) vs 전이(학습 구간 평균)로 남은 점수의 **종류**를 가른다 | 그룹 이름만 |
| `era_screen.py` / `exp/era_check.py` | **시계 누설 스크리닝.** 피처 하나로 "이 행이 Y년인가"를 가르는 AUC + 단조성 | 거의 그대로 |
| `exp/ensemble.py` | 앙상블 크기 곡선·가중치를 재학습 0회로 | 거의 그대로 |
| `exp/card_probe.py` | 범주형 인코딩 정하기 전에 카디널리티·범주당 표본 수부터 셈 | 거의 그대로 |
| `make_submit.py` | zip 생성 + 구조 검증 | 경로만 |

**예측을 캐시하는 것이 핵심이다.** 점수만 캐시하면 앙상블·가중치·캘리브레이션을
다시 볼 때마다 재학습해야 한다. 검증 예측(float32 25만행 = 1MB)을 저장하면
그 전부가 **재학습 0회**가 된다. Phase 2 후반의 결론은 거의 다 여기서 나왔다.

---

## 4. 함정 체크리스트

### 4-1. 측정 함정 — 다섯 개 다 실제로 걸렸다

> **① 단일 폴드·단일 시드를 믿지 말 것.** fold2024 seed42 에서 +20.49 를 보고
> 채택 직전까지 갔는데 seed43 이 −2.74 였다.

> **② 경로 사이의 편향.** 실험 경로(캐시·float32·스레드 고정)와 제출 경로(pandas·
> 기본 스레드)는 **같은 설정에서 13점** 어긋났다. 실험에서 10점 미만으로 나온 이득은
> 제출 경로에서 다시 재기 전까지 채택 금지. `rstr_3` 은 실험 +13.37 → 제출 **+1.39**

> **③ 폴드 안에서 맞춘 계수는 계수가 아니다.** 폴드마다 다른 계수를 쓰면 폴드마다
> 다른 모델이다. **3폴드 전부 양수여도** 워크포워드에서 죽는다 — 실제로 +33.76 (3/3)
> 이 −88.89 가 됐다. 계수가 있는 후보는 **반드시 이전 폴드에서만** 맞춰 볼 것

> **④ 정렬 기준이 결론을 만든다.** 최근 폴드로 정렬하면 부호갈림이 1위로 올라온다.
> 실제로 걸렸다 (2024 에서 +25.0 이라 1위였는데 2021 에서 −47.0, 평균 −0.4)

> **⑤ 델타는 대체 대상에 대해 재라.** 기준 대비 +8.87 이어도 이미 채택한 것(+5.83)
> 위에 얹는 것이면 그 둘을 직접 짝지어야 한다. 그렇게 하니 미달이었다

### 4-2. 제출 사고 — 셋 다 환경 문제였다

- **zip 안에 `model/` 이 폴더로 풀리는가.** 역슬래시 경로로 압축하면 파일명이
  `model\rf.pkl` 인 단일 파일이 된다. 서버에서 죽는다
- **`requirements.txt` 에 numpy 를 핀할 것.** 학습 환경보다 **낮은 numpy** 로
  `joblib.load` 가 되는지 반드시 확인한다. `HistGradientBoosting` 은 pkl 안에
  `PCG64` 난수 객체를 들고 있어서 낮은 numpy 에서 열리지 않는다
- **커스텀 클래스를 pkl 에 넣지 말 것.** 평가 서버가 클래스를 못 찾는다.
  라이브러리 객체와 순수 dict 만
- **콘솔 인코딩.** 비ASCII 출력이 있는 스크립트를 파이프/리다이렉트로 받으면
  기본 디코더가 cp949 라 죽는다. `PYTHONIOENCODING=utf-8` 과 `encoding="utf-8"` 을
  subprocess 에 명시할 것 (여기서 하루에 세 번 걸렸다)
- **스크립트 인자가 실제로 파싱되는지 확인할 것.** `--zip` 을 줬는데 argparse 가
  없어 조용히 무시되고 **엉뚱한 파일이 "점검 통과"** 로 찍힌 적이 있다

### 4-3. 판정 규율

채택하려면 **네 개를 다** 넘어야 한다.

1. 평균이 표준오차의 **2배** 초과
2. 폴드별 **부호 일치** (갈리면 그 변동폭만큼 할인)
3. **워크포워드** 로 양수 (계수·상수가 있는 것은 필수)
4. **제출 경로**에서 재측정 (10점 미만 이득이면 특히)

---

## 5. Phase 2 에서 나온 가장 값진 한 문장

> **한 구간에서 맞춘 관계는 다른 구간으로 안 넘어간다.**

네 번을 서로 다른 각도로 확인했다 — 잔차 타깃, 투수 수준 선형보정, 외부 데이터 상한,
조건부 아핀. **전부 폴드 안에서는 양수고 워크포워드에서 음수였다.**

그러면 **왜 캘리브레이션만 통했는가.** 그것만 유일하게 로컬에서 **전이시키지
않았기 때문이다** — LB 점으로 평가 구간 위에서 직접 풀었다.

Phase 3 에서 시간 분할 문제가 또 나오면:

- **전이가 필요한 개선**(피처, 계수, 그룹별 보정)은 기대값을 낮게 잡는다
- **전이가 필요 없는 개선**(평가 구간 위에서 직접 푸는 상수)을 먼저 찾는다
- 그리고 **폴드 안 적합값 = 직접 풀었을 때 얻을 값**이므로, 폴드 내 수치는
  버리는 숫자가 아니라 **"직접 풀 수 있다면 얼마인가"의 견적**으로 읽는다

---

## 6. 값싼 검산 먼저 (원칙 ①)

가설의 전제가 **셀 수 있는 것**이면 학습 전에 세어본다. Phase 2 에서 이걸로
학습 0회로 끝난 축이 여럿이다 — 외부 데이터 축은 인터넷도 연결 작업도 없이
`exp/extern_probe.py` 하나로 닫혔다.

> 단, **전제 검사는 가설을 죽일 수는 있어도 살리지는 못한다.** 전제를 통과하고도
> 실패한 축이 있었다.
