# 수준(level) 축 원장 — LV-001 ~ LV-005 (2026-08-24)

## 왜 이 원장이 생겼는가

55회차에서 타자 **수준**축 하나가 +24.5 를 냈다. 그 축은 8개월 전에 **대비**로
재고 닫혔던 것이다. CLAUDE.md §5-c 9 가 이미 경고를 적어 뒀다.

> 어떤 축을 닫을 때 "대비로 닫았는가 수준으로 닫았는가"를 구분해 적어라.

닫힌 축 60여 건이 대부분 **대비로만** 측정됐다. 그래서 남은 최대 미탐색 면적은
새 컬럼이 아니라 **이미 닫은 축들의 수준 판본**이고, 이 원장이 그것을 전수로 센다.

---

## 0. 결론 한 줄

```
후보 36개 중 위약 문턱을 넘은 것은 **카운트(12) 수준축 하나뿐**이다  (z = +6.1)
나머지 35개는 전부 귀무 대역. 탐욕 선택이 쌓은 +22.37 은 대부분 선택 잡음이었다
```

---

## 1. 측정 규약 (`research/lv001_level_sweep.py`)

```
표 원천   walk-forward 만.  목표 2023 <- 표 2022 · 목표 2024 <- 표 2022+2023
기준선    FULL = champ_oof + 현 Champion 후처리 9축   (증분 판정)
          LV0  = 후처리 축 0~6 만 (수준 2축 제거)      (대조군 검증용)
이득      rho^2(w) = (a+wb)^2 / ((Vp+2wC+w^2Vv)Vy) 를 **해석적으로** 푼다
          w* = (aC − bVp)/(bC − aVv)                 격자 탐색 없음, 근사 없음
```

🚩 **첫 판본은 대조군이 0.00 이 나왔다.** 버그가 아니라 기준선에 이미 투수·타자
수준축이 들어 있었기 때문이다. **증분만 재면 지표 자체를 검증할 수 없다** —
그래서 기준선을 둘로 갈랐다. LV0 에서 대조군 타자 **+3.45** (원장 실측 +4.2 와
일치) 가 나오면서 지표가 살아났다.

---

## 2. 🔴 유일한 생존 — 카운트(12) 수준축

| 검정 | 결과 |
|---|---|
| 단독 이득 (FULL 기준선, 2024) | **+7.21** |
| 귀무 "36개 중 최대" (키 시즌내 치환 12회) | +2.80 ± 0.72 (최대 +4.39) → **z = +6.1** |
| 자기 귀무 | +0.79 ± 0.91 → **z = +7.0** |
| walk-forward, **w=1 고정** | 목표 2023 **+5.56** · 목표 2024 **+5.57** |
| 3시즌 셀 부호 일치 | **9/12** · corr(2022+23 셀평균, 2024) = **+0.53** |
| 중심화 불변 | +5.57 → +5.57 (리그 드리프트 상수가 아니다) |
| 무작위 12셀 위약 20회 | **전부 음수** (평균 −3.19) |
| 현행 후처리 9축과 중복 | corr = **0.006** |
| 셀당 행수 | 최소 6,072 · 중앙 35,370 → 추정 잡음 사실상 0 |
| w* | **1.66** (제정신 대역. 축퇴된 큰 값이 아니다) |

셀별 보정값 (원천 2023+2024 OOF 잔차, 중심화):

```
0-0 −0.0016   0-1 +0.0063   0-2 +0.0017
1-0 −0.0050   1-1 −0.0024   1-2 +0.0061
2-0 −0.0034   2-1 −0.0041   2-2 +0.0033
3-0 **+0.0174**   3-1 +0.0001   3-2 −0.0088
```

**구조 — 2스트라이크 쪽은 과소예측, 3볼 0스트라이크는 크게 과소예측이다.**
모델이 `balls_before`·`strikes_before` 를 입력으로 갖고 있는데도 12셀 보정이
남는다는 뜻이고, 셀당 3.5만 행이라 추정 잡음으로는 설명되지 않는다.

### 원장 §4 의 "카운트 −4.1, 회수 완료"와 충돌한다 — 해소

옛 오라클(`exp/oracle_probe.json`)은 **기준선 944.0** 에서 `count(12)` 의
cv2 939.9 (−4.1) · transfer 916.9 였다. 지금 기준선은 **1110.3** 이고 모델도
후처리도 다르다. 그리고 옛 `transfer` 는 모델 잔차가 아니라 **라벨 편차**로
만든 대용물이었다(스크립트 주석에 명시). 즉 **다른 것을 잰 것**이다.
현행 구성에서는 위 표대로 재현된다.

---

## 3. 나머지 35개 — 전부 위약 대역

`research/lv005_null.json` 에 전체 표. 상위만 옮긴다 (실측 / 자기귀무평균 / z).

| 축 | 실측 | 귀무 | z | 판정 |
|---|---:|---:|---:|---|
| base_state | +3.11 | +1.14 | +3.2 | ⚠️ 경계 — "36개 중 최대" 문턱(2.80+2sd=4.24) 미달 |
| 주자수 | +2.38 | +0.79 | +1.8 | 위약 이하 |
| 타자 x game_type | +2.25 | +0.80 | +2.9 | 〃 |
| game_type · 월 · 타자경력십분위 · 요일 | +1.5~2.0 | — | — | 〃 |
| 투수 x 타자 (매치업) | +1.12 | +0.69 | +0.7 | 〃 |
| **타자 x 맥락 9종** | +0.09~+0.44 | — | — | 〃 **수준으로도 비어 있다** |
| **투수 x 맥락 8종** | +0.15~+0.60 | — | — | 〃 |
| 투수팀 · 타자팀 | +0.33 / +0.22 | — | — | 〃 |

🚩 **가장 중요한 음성 결과** — "타자 x 맥락"을 **수준**으로 다시 열어도 비어 있다.
타자축의 +24.5 는 *타자 x 맥락* 이 아니라 **타자 그 자체(수준)** 에 있던 것이고,
그 몫은 이미 회수됐다. CLAUDE.md §5-c 7("타깃은 투수 쪽 운동 결과다")과 부합한다.

---

## 4. 탐욕 전진 선택은 +22.37 을 쌓았지만 대부분 잡음이다

`research/lv004_greedy.py` — 8단계 전부 2023·2024 양수였다.

```
카운트 +7.21 -> base_state +3.21 -> 타자xgame_type +2.70 -> 월 +1.97
-> 타자경력십분위 +2.12 -> 타자x월 +2.44 -> 요일 +1.47 -> 투수x타자 +1.26
```

그런데 3단계부터 w* 가 0.06 · −0.10 · −0.41 · 198.09 로 **의미 없는 값**이고,
귀무 "36개 중 최대"가 이미 +2.80 이다. **§5-c 4 와 EXP055 가 경고한 그대로**이므로
1단계(카운트)만 남기고 나머지는 채택하지 않는다.

---

## 5. 산출물

```
research/lv001_level_sweep.py / .json    36축 전수 (해석적 w 곡선)
research/lv002_count_verify.py           카운트 정밀 검증 (셀 표·부호·walk-forward)
research/lv003_count_placebo.py          위약·중심화·셀별 기여 분해
research/lv004_greedy.py / .json         탐욕 전진 선택 (겹침 구조)
research/lv005_null.py / .json           귀무분포 "36개 중 최대"
exp/build_level.py                       10번째 후처리 축 빌더 (학습 0회)
submissions/cand_cnt1.zip                w=+1 판본  sha 58ce734d7dea5196  관문 3종 ✅
```

---

## 6. 다음 — 제출은 **LB 좌표로** 연다

37회차(볼우위)가 로컬 3폴드 양수 + 겹침 0.02 로 **−7.07** 이었다. 그러므로 이
축도 로컬 값을 근거로 채택하지 않는다. w 를 3점 해법으로 연다.

```
탐침 1  w=+1  (cand_cnt1, 빌드 완료)
탐침 2  w=−1  (미러. 48·53회차가 이 규칙으로 성공했다)
-> 포물선 확정 -> 정점 제출.  최고점 채점이므로 하방은 0
```

**단 이 축은 표 원천이 2023+2024 라 champ_oof 에 의존한다.** 투수축(내일)이
성공해 Champion 이 바뀌면 **그 위에서 다시 빌드**해야 한다 (`--base`).
