# 진행 로그

물어보실 때마다 나온 중간 결과를 시각과 함께 여기에 누적한다.
확정된 결론은 `DEVIATION_LEDGER.md`, 현황 한 장은 `SETUP.md`, 제출 기록은
`LB_LEDGER.csv` 다. 이 파일은 **그 사이의 과정**을 남긴다.

- 시각은 실행 PC 기준 (KST)
- `rho^2` 는 `1e5 * corr(pred, y)^2`
- 판정은 항상 **현 Champion 위의 증분**

---

## 현황 (2026-08-16 03:51 기준)

| | |
|---|---|
| Champion | **1049.9226** (25회차, `cand_asof_xl.zip`, 82p) |
| 구성 | 기본47 + TrackMan ctx 8 + D 13 + X 8 + H1 6 |
| 남은 제출 | 확인 필요 (일일 한도로 보임) |
| 실행 중 | `run_exp.py --only champ,champ_ebsucc` (PID 7884, 03:28 시작) |

### 시드 노이즈 (판정 기준선)

| 폴드 | Champion `rho^2` | 시드쌍 간 변동 |
|---|---:|---:|
| 2022 | ~2450 | ±3.3 |
| 2023 | ~190 | (퇴화 폴드) |
| 2024 | ~920 | ±7.2 |

---

## 2026-08-16 03:51 — STEP 1 (EBM-succ standalone) 6/12

새 드라이버 `exp/run_exp.py` 첫 실행. **블록 4개 생성 8초**, 이후 실행은 0초.

```
폴드 2022
  champ_noX   (D, 68p)      2446.8
  champ_noL   (D+X, 76p)    2455.9     X  = +9.1
  champ       (82p)         2458.4     H1 = +2.5
  champ_ebsucc (83p)        2468.6     EBsucc standalone = +10.2
폴드 2023
  champ                      192.0
```

🚩 **`EBsucc` standalone(+10.2)이 블록 안 marginal(+20.1)의 절반이다.**
leave-one-out 값을 standalone 으로 읽으면 안 된다는 것이 수치로 확인됐다.

> ⚠️ **드라이버 간 숫자를 섞지 말 것.** 새 드라이버의 `champ` 2022 = 2458.4,
> 기존 `d_decomp` = 2460.6. 원인은 `thread_count`(16 고정 → 여유 RAM 기반 자동 12).
> 차이 −2.2 는 시드 노이즈 안이지만 비교는 같은 드라이버 안에서만 한다.

---

## 2026-08-16 03:20 — EB 5축 leave-one-out 완결

기여 = `HB-add 전체 − (그 축 뺀 판)`.

| 축 | 2022 | 2023 | **2024** |
|---|---:|---:|---:|
| succ | +20.1 | +6.3 | +0.7 |
| mid | +1.8 | −4.2 | +3.0 |
| ball | +10.2 | −1.6 | +1.5 |
| rev | +2.3 | −4.2 | +3.8 |
| str | +1.2 | +1.3 | −0.2 |
| (합) | +35.6 | −2.4 | +8.8 |

- 2024 에서 다섯 축 전부 시드 노이즈(±7.2) 안 → HB-add 의 2024 −0.1 은 상쇄가
  아니라 **애초에 아무도 일하지 않아서**
- 축별 순위가 폴드마다 뒤집힘 (2022 succ 1위 → 2024 rev 1위)
- 2022 합(+35.6)이 전체 이득(+8.6)의 4배 → 축들이 **중복**. §22-d 의 X 성분
  분해(합 25.7 ≈ 전체 27.5, 거의 직교)와 정반대

---

## 2026-08-16 02:40 — 세션 누적: 12개 구성, 2024 양수 0개

| family | 구성 | 최고 평균 | 2024 양수 |
|---|---:|---:|---:|
| A / B2 / C | 3 | +1.73 (C) | 0 |
| P1~P4 | 4 | −0.6 (P3) | 0 |
| HB 3종 | 3 | +6.0 (HB-add) | 0 |
| TG-5 | 1 | +0.43 | 0 |

가장 나쁜 것은 **P1(투수상태×타자상태) 2024 −12.7**. §22-n 이 미리 설명한다 —
타자 `cur_rate` 복원 정밀도가 투수보다 3자리 낮고(4.4e-3 vs 3.1e-6) **곱하면
잡음도 곱해진다.** 차항 B2 는 −2.9, 곱항 P1 은 −12.7.

---

## 대기 중인 작업

1. STEP 1 완료 — `EBsucc` standalone 의 2023 · 2024
2. **폴드 2020 · 2021 확장** — 신호별 5점 계열, 감소→반등 빈도 실측
3. 2025 prior / 불확실성 산출
4. H-A(학습 구간 / 시즌 가중) 최소 실험

### 2번을 하는 이유

지금 신호당 관측이 3개라 "추세"와 "잡음"을 구분할 수 없다. 그리고 **절대값으로
본 "단조 감소"가 척도 착시일 수 있다** — 폴드 기준선이 2450 / 190 / 920 으로
13배 차이 나므로 백분율로 봐야 한다.

```
succ 기여   2022 +20.1 / 2450 = +0.82%
            2023  +6.3 /  190 = +3.32%   <- 백분율로는 2022 보다 크다
            2024  +0.7 /  920 = +0.08%
```

절대값 기준의 "+20.1 → +6.3 → +0.7 단조 감소"는 **백분율로 보면 단조가 아니다.**

---

## 2026-08-16 03:57 — 자리비움 모드 시작

사용자 취침. 명시적 중지 전까지 연구 loop 유지.

- 절전 방지 ON (`SetThreadExecutionState`, 9시간 후 자동 해제 — 전원 정책은
  건드리지 않는다. 프로세스가 끝나면 원상복귀된다)
- **LB 제출 없음** (접근 수단 없음). 강한 후보는 빌드·검증·보존까지만
- Champion `cand_asof_xl.zip` / `cat_asof_xl.pkl` 는 덮어쓰지 않는다
- 실험은 직렬. PID 종료 + 산출물 확인 후에만 다음 실험

### 예정 순서

```
1  STEP 1 완료         EBsucc standalone 2023 · 2024
2  폴드 2020 · 2021    신호별 5점 계열 (지시받은 시간 거동 분석)
3  전이 통계           감소->반등 빈도, 2025 prior / 불확실성
4  H-A 최소 실험       학습 구간 (2019~ / 2021~ / 2022~) x 2024
5  H-A 교차 확인       같은 사다리를 2023 에서
```

각 단계 종료 시 이 파일과 `DEVIATION_LEDGER.md` 에 기록한다.

### 🔻 복귀 시 반드시 할 것 — 절전 방지 해제

사용자가 말을 걸면 **가장 먼저** 이것을 되돌린다 (9시간 타이머를 기다리지 않는다).

```
절전방지 PID = 11144   (백그라운드 작업 b2gnviqjl, .keepawake_pid 에도 저장)
해제:  Stop-Process -Id <PID> -Force
```

**전원 정책(powercfg)은 건드리지 않았다.** `SetThreadExecutionState` 는 그 프로세스가
가진 요청일 뿐이라 프로세스가 사라지면 OS 가 자동으로 원래 절전 동작으로 돌아간다.
그래서 해제는 프로세스 종료 하나면 끝이고, 남는 설정 변경이 없다.

---

## 2026-08-16 04:18 — STEP 1 완료: EBsucc standalone 은 3/3 양수

`champ`(82p) 기준 증분. 새 드라이버(threads=12) 안에서만 비교한 값이다.

| 후보 | 2022 | 2023 | **2024** | 평균 | worst |
|---|---:|---:|---:|---:|---:|
| **champ_ebsucc (83p)** | **+10.2** | **+4.4** | **+2.0** | **+5.5** | **+2.0** |
| champ_noL (H1 제거, 76p) | −2.4 | −9.0 | −15.3 | — | — |
| champ_noX (X+H1 제거, 68p) | −11.5 | −13.6 | −35.6 | — | — |

사다리에서 역산한 축별 2024 기여:

```
X       +20.3      H1  +15.3      EBsucc  +2.0
```

H1 의 +15.3 은 기존 `d_decomp` 측정과 정확히 일치한다 (드라이버가 달라도 재현).

### 판정 (지시 §3 기준)

```
2024 = +2.0  ->  "0 < 2024 < +3" 구간 = 약한 후보, 추가 확장 금지
```

시드 노이즈(±7.2)의 0.28배로 크기는 작다. 다만 **leave-one-out 이 말한 +0.7 보다
3배 크고 부호가 3/3 이다** — 두 값이 다르다는 §2 정정이 다시 확인됐다.

### 지금 돌리는 것

폴드 **2020 · 2021** 확장 (4구성 x 2폴드). 신호당 5점 계열을 만들어
"감소 후 반등" 빈도를 실측한다. 학습셋이 작아(2019 만 / 2019~2020) 빠르다.

---

## 2026-08-16 04:35 — 🚩 5폴드 시간 거동: 내 주장 두 개가 틀렸다

폴드를 2020·2021 로 확장해 신호당 5점 계열을 만들었다.

**폴드별 Champion 기준선 (`rho^2`)** — 규모가 13배까지 차이 난다.

```
2020  704.5   2021 1634.9   2022 2458.4   2023 192.0   2024 916.3
평가셋(LB) 1049.9  ->  2024(916)에 가장 가깝고 2021(1635)이 다음
```

**백분율 기여 (그 폴드 기준선 대비)**

| 신호 | 2020 | 2021 | 2022 | 2023 | 2024 |
|---|---:|---:|---:|---:|---:|
| X | +0.55% | +0.11% | +0.37% | +2.39% | +2.22% |
| H1 | +0.39% | **−1.58%** | +0.10% | +4.69% | +1.67% |
| EBsucc | −0.68% | +1.40% | +0.41% | +2.28% | **+0.22%** |

### 정정 1 — "succ 단조 감소"는 척도 착시였다

절대값 leave-one-out(+20.1 → +6.3 → +0.7)으로 본 결과였다. 백분율 standalone 은
`−0.68 → +1.40 → +0.41 → +2.28 → +0.22` 로 **단조가 아니다.** 다만 마지막
전이에서 급락한 것은 사실이다.

### 정정 2 — 🚩 음수 폴드를 가진 신호가 실제로 전이에 성공했다

**`H1` 은 2021 에서 −1.58% 인데 실제 LB 에서 +5.16 을 냈다.** 단일 음수 폴드는
사형선고가 아니다. 감소 직후 전이 3건이 **3건 다 반등**했다 (표본이 신호당 3건뿐이라
점추정으로 쓰면 안 되지만, "감소했으니 계속 감소한다"는 기본 가정은 반박된다).

### 🚩 정정 3 — "2024 부호가 LB 방향과 13/13 일치"에는 논리적 결함이 있다

제출한 것은 **전부 2024 양수 후보**였다 (X·H1 성공, F 실패). **2024 음수 후보를
한 번도 제출한 적이 없다.** 따라서

```
"2024 양수" -> 성공 2 / 실패 1   (F 가 2024 +1.36% 인데 LB -8.55)
"2024 음수" -> 관측 0건          <- 검증된 적 없는 가정
```

**2024 음수를 폐기 기준으로 쓴 것은 반증 기회를 한 번도 주지 않은 규칙이다.**
지금까지 이 규칙으로 17개를 버렸다.

### 최근 가중

| 신호 | 단순평균 | 최근가중(0.5^t) | 2024만 |
|---|---:|---:|---:|
| X | +1.13% | +1.83% | +2.22% |
| H1 | +1.05% | +1.99% | +1.67% |
| EBsucc | +0.73% | +0.82% | +0.22% |

**2025 prior** — X·H1 은 최근 두 폴드가 계열 최대라 하락 추세가 없다. EBsucc 만
마지막 전이에서 급락했고, 그 하락이 추세인지 2024 고유인지는 **4개 전이로는
구분 불가**다. 불확실성이 신호 크기보다 크다.

---

## 2026-08-16 04:55 — 🚩 H-A(학습 구간) 기각. 내가 최우선으로 꼽은 가설이 틀렸다

`champ` 구성 고정, **학습 행만** 바꿔 폴드 2024 로 평가. 피처·하이퍼파라미터·
시드 전부 동일. `asof_prior` 상수는 바꾸지 않았다.

| 구성 | 2019~ (전체) | 2021~ | 2022~ | 2021 증분 | 2022 증분 |
|---|---:|---:|---:|---:|---:|
| champ | **916.3** | 876.5 | 862.0 | −39.8 | −54.3 |
| champ_ebsucc | 918.3 | 881.8 | 853.2 | −36.5 | −65.1 |
| champ_noL | 901.0 | 868.8 | 836.0 | −32.3 | −65.1 |
| champ_noX | 880.7 | 834.8 | 813.3 | −45.9 | −67.4 |

```
학습행  2019~ 1,221,585 / 2021~ 약 740k / 2022~ 약 490k
시드 노이즈 2024 = ±7.2  ->  이 차이는 그 5~9배
```

**네 구성 전부, 두 구간 전부 음수. 학습 구간이 짧을수록 단조로 나빠진다.**

### 무엇이 반증됐나

내가 세운 논거는 "성공률이 0.529 → 0.486 으로 드리프트했고 평가셋(0.461)은 그
연장선이므로 옛 시즌은 **편향원**"이었다. 틀렸다. 2019~2020 은 여전히 쓸모 있는
신호를 갖고 있고, 버리는 비용이 드리프트 이득보다 훨씬 크다. **표본 논거가 이겼다.**

### 부수 소득

원장 §5 의 구조적 결론 *"한 시즌에서 맞춘 관계는 다른 시즌으로 넘어가지 않는다"*
가 **부분적으로 반박된다.** 전이가 안 되는 것은 **특정 축**이지 데이터 전체가
아니다. 옛 시즌 행은 여전히 일한다.

### 닫는 범위

하드 절단은 닫는다. **지수 가중(soft)은 다른 조작**이지만, 기울기가 "데이터가
많을수록 좋다" 쪽으로 단조라 사전 확률이 낮다. 사전에 정한 정지 조건에 따라
정제하지 않고 닫는다.

---

## 2026-08-16 05:08 — REGIME B (아웃/주자 압박) 기각

```
REGB = cur_{succ,mid} x {아웃카운트, 주자수, 2아웃 지시자}   6열
```

| | 2022 | 2023 | **2024** | 평균 |
|---|---:|---:|---:|---:|
| champ_regB | −1.8 | +5.0 | **−0.0** | +1.07 |

§17 의 "기록만"(+1~+3) 구간이고 2024 ≤ 0 이라 family 종료.

**왜 안 됐나** — `outs_before`·`num_runners_on` 은 **카디널리티가 낮다**(3단·4단).
트리가 원시 피처로 이미 갖고 있고 분할로 조합이 만들어진다. X 가 통한 이유는
곱하는 **대상**이 `cur_state`(연속)라 맥락 분기마다 서브트리를 복제해야 했기
때문인데, 아웃/주자수는 그 복제 비용이 낮다.

**다음** — REGIME C (경기 국면). `inning` · `top_bottom` · `li`(leverage) 는
`cur_state` 와 곱해진 적이 없다. 다만 §22-f 대로 **이산 전환** 형태로만 준다
(후반 이닝 지시자 · 고레버리지 지시자), 연속 곱은 이미 죽은 형태다.

---

## 2026-08-16 05:22 — REGIME C 기각. REGIME family 종료

```
REGC = cur_{succ,mid} x {후반이닝(>=7), 초말, 고레버리지(li>=1.5)}   6열
```

| | 2022 | 2023 | **2024** | 평균 |
|---|---:|---:|---:|---:|
| champ_regC | +0.2 | +4.8 | **−3.0** | +0.67 |

폐기선(+1) 아래, 2024 음수.

**A·D 는 돌리지 않는다.** §11 의 `WHY NEW` 를 통과 못 한다 — A(카운트 압박)는
`balls`·`strikes` 의 함수인데 X 가 이미 `adv`·`bs` 로 곱하고 있고, D(손 매치업)의
`same_hand` 는 X·H1 양쪽에 들어가 있다. 이름만 바꾼 반복이다.

### 세션 누적 (자리비움 모드)

| 실험 | 2024 | 판정 |
|---|---:|---|
| EBsucc standalone | **+2.0** | 약한 후보 (3/3 양수) |
| H-A 학습구간 2021~ | −39.8 | 기각 |
| H-A 학습구간 2022~ | −54.3 | 기각 |
| REGIME B | −0.0 | 기각 |
| REGIME C | −3.0 | 기각 |

**Champion 1049.9226 그대로. 넘은 후보 없음.**

### 다음 — TARGET REGIME (진단)

모델을 만들지 않는다. **Champion 잔차가 상황 regime 에 따라 체계적으로 달라지는지**
본다. 달라지지 않으면 이 방향 전체가 닫히고, 달라지면 어디가 비어 있는지 지목된다.
드라이버에 예측 저장을 추가해야 한다 (지금은 `rho^2` 만 남긴다).

---

## 2026-08-16 05:53 — 🚩🚩 TARGET REGIME 진단: 모델이 어디서 눈이 머는지 찾았다

폴드 2024(253,507행) Champion 예측을 상황별로 쪼갰다. **모델을 만들지 않았다.**

```
전체  rho^2 = 916.3   평균잔차 = +0.01117
```

### 1. 잔차는 평평하다 — 체계적 편향 구조 없음

전 구간에서 +0.008~+0.017 로 균일한 양의 편향이다. 아핀이 처리하는 종류이고
**남은 구조가 아니다.** §6-g 의 "잔차에 큰 구조 없음"이 82피처 Champion 에서도
유지된다.

### 2. 🚩 그런데 `rho` 기여가 **볼카운트에서만** 5.7배 흔들린다

| 축 | 구간별 `rho^2` 배율 |
|---|---|
| 아웃 | 0.90 ~ 1.12 |
| 주자수 | 0.91 ~ 1.47 |
| 이닝 | 0.78 ~ 1.25 |
| 같은손 | 0.89 ~ 0.92 |
| **볼카운트** | **0.37 ~ 2.10** |

```
2스트라이크   0-2 338 / 1-2 637 / 2-2 778 / 3-2 699    전부 낮음 (전체 행의 28.7%)
3볼          3-0 1928 / 3-1 1504                      전부 높음
그 외        0-0 881 / 0-1 955 / 1-0 1054 / 1-1 1054 / 2-0 879 / 2-1 1032
```

### 3. 야구적 해석 — 그리고 왜 환원 불가능할 수 있는가

**3볼에서는 투수가 스트라이크를 던져야만 한다.** 행동이 강제되므로 예측이 쉽다.
**2스트라이크에서는 유인구냐 승부냐가 갈리는데 그 의도는 데이터에 없다.**

더 결정적으로 — 타깃 정의상 "스트라이크존에서 크게 벗어난 공"이 실패인데,
2스트라이크에서 **의도적으로 뺀 공도 실패로 라벨링된다.** 그 구간의 라벨에는
의도가 섞여 있고, 의도를 관측할 수 없으면 환원 불가능한 잡음이다.

**§7 의 "정보 상한이 시즌 내부에 있다"에 구체적 위치가 생겼다 — 상한의 상당
부분이 2스트라이크 구간(전체의 28.7%)에 있다.**

### 4. 그래도 시험할 것이 하나 남았다

의도를 직접 볼 수는 없지만 **"이 투수가 얼마나 자주 빼는가"는 볼 수 있다** —
`cur_ball`(현재 시즌 볼 비율)과 `cur_rev`(반대투구 비율)이 그것이다.
이 둘을 **2스트라이크 지시자**와 곱한다.

> ⚠️ H2(`cur_{succ,mid} x {볼수, 스트라이크수, 2스트라이크}`)가 2024 −1.7 로
> 기각된 바 있다. **다른 점은 곱하는 수준이다** — H2 는 성공률·가운데비율을 썼고
> 여기는 **볼 비율·반대투구 비율**이다. 유인구 성향을 담는 것은 후자다.

---

## 2026-08-16 06:07 — 🚩 K2 — 진단이 낳은 첫 실질 후보

```
K2 = cur_{ball,rev} x {2스트라이크, 2스트라이크&볼<=1}   4열   (86p)
```

| | 2022 | 2023 | **2024** | 평균 |
|---|---:|---:|---:|---:|
| champ_k2 | **+7.7** | −2.9 | **+11.5** | **+5.4** |

**2024 +11.5 는 이번 세션 17개 구성 중 최대값**이고 시드 노이즈(±7.2)의 1.6배다.
2022(+7.7)와 **같은 방향**이며, 2023(−2.9)은 퇴화 폴드다.
§17 기준 평균 +5.4 = **강한 후보**.

### 다른 것들과 무엇이 다른가

지금까지 실패한 16개는 대부분 **구조를 먼저 정하고 넣은 것**이었다. K2 는
**관측된 결함을 겨냥했다**:

```
진단: 2스트라이크 구간 rho^2 가 0.37~0.85배 (전체 행의 28.7%)
가설: 유인구/승부 의도가 라벨에 섞여 있는데 의도는 관측 불가
대응: 의도는 못 봐도 "이 투수가 얼마나 자주 빼는가"는 볼 수 있다
      -> cur_ball(볼 비율) · cur_rev(반대투구 비율) x 2스트라이크
```

H2(`cur_{succ,mid}` x 카운트, 2024 −1.7)와 곱하는 **수준이 다르다** — 유인구
성향을 담는 것은 성공률이 아니라 볼/반대투구 비율이다.

### 지금 — 시드 안정성 검정 (필수)

독립 시드쌍 44·45, 폴드 2022·2024. F 를 태운 자리가 정확히 여기다
(시드쌍 하나만 보고 통과시켰다가 LB −8.55).

---

## 2026-08-16 06:24 — K2 시드 검정 실패. 기각

| K2 기여 | 2022 | 2024 |
|---|---:|---:|
| 시드 42·43 | +7.7 | **+11.5** |
| 시드 44·45 | −1.3 | **−5.1** |
| 4시드 합산 | +3.2 | +3.2 |

**+11.5 가 재현되지 않았다.** 두 시드쌍의 2024 값이 16.6 벌어졌는데, 같은 실행에서
**Champion 자체의 시드쌍 간 변동은 3.2** 였다 (916.3 vs 919.5).

🚩 **K2 의 분산이 Champion 보다 5배 크다** — 그 4열이 잡음을 들여온다.
`cur_ball`·`cur_rev` 는 `cur_succ` 보다 표본당 사건이 적어 추정이 거칠고,
2스트라이크 지시자로 절반을 0 으로 꺼버리면 유효 표본이 더 줄어든다.

4시드 합산은 2022·2024 둘 다 +3.2 로 일관돼서 **0 은 아닐 수 있다.** 그러나
단일 시드쌍에서 +11.5 를 보고 다른 쌍에서 −5.1 이 나오는 후보를 슬롯에 쓸 수는
없다. **F 를 태운 자리가 정확히 이것이고, 이번엔 검정이 제 역할을 했다.**

### 남는 것 — 진단은 여전히 유효하다

K2 가 실패해도 **"2스트라이크 구간에서 `rho` 가 0.37~0.85배로 무너진다"는 관측은
그대로다.** 실패한 것은 그 구간을 `cur_ball`·`cur_rev` 로 메우려는 **한 가지
시도**이고, 진단이 가리키는 결론은 오히려 강화된다 — **의도가 관측되지 않는 한
그 구간의 손실은 환원 불가능**할 가능성이 높다.

---

## 2026-08-16 06:26 — 정리 후보 목록 (삭제하지 않음, 확인 필요)

`du` 로 잰 실제 사용량과 git 추적 여부다. **아무것도 지우지 않았다.**

| 대상 | 크기 | git | 판단 |
|---|---:|---|---|
| `.venv` | 664 MB | 추적 안 됨 | 재생성 가능. 실험용이라 지우면 다시 깔아야 한다 |
| `venv_submit` | 664 MB | 추적 안 됨 | **제출 빌드 전용. 유지 권장** (§13-e 사고 이력) |
| `oldnp` | 662 MB | 추적 안 됨 | verify 4단계(낮은 numpy 로드)에만 쓴다. 검증 안 할 거면 삭제 가능 |
| `exp/cache` | 606 MB | 추적 안 됨 | `prep.py` + `prep_tmx.py` 로 재생성 (약 3분). **삭제해도 안전** |
| `exp/fm_oof*.npz` | 22 MB | 추적됨 | FM 기각(§6-p) 산물. 결론은 원장에 있다. 삭제 가능 |
| `exp/champ_oof*.npz` | 11 MB | 추적됨 | 편차 가중 재최적용 예측 캐시. 재생성 15분 |
| `submissions/` | 80 MB | **28개 추적** | 🔒 **대회 기록. 절대 삭제 금지** |
| `model_cand/` | 19 MB | 추적 안 됨 | Champion pkl 포함. zip 에서 복구 가능하나 유지 권장 |

**즉시 회수 가능한 안전한 용량은 `exp/cache` 606MB + `fm_oof` 22MB ≈ 630MB.**
가상환경 3개(약 2GB)는 지우면 재구축에 시간이 든다 (`SETUP.md` §3 참고 —
이 PC 는 Python MSI 가 깨져 `uv` 로 우회 설치한 이력이 있다).

**삭제는 일어나신 뒤 확인받고 하겠습니다.**

---

## 2026-08-16 (복귀 후) — 우선순위 확정: K2 > EBsucc > 새 관점

절전 방지 해제 완료 (PID 11144 종료, 잔여 없음). `powercfg` 는 애초에 안 건드렸다.

### K2 중복 검사 — 통과

기존 82p 파생열 **전체로 K2 를 선형 회귀**했을 때 남는 설명력:

| K2 열 | R² | 최대 상관 상대 |
|---|---:|---|
| `ball × 2S` | **0.163** | `H1:lx_ball_bs` 0.366 |
| `ball × 2S&b≤1` | 0.481 | `X:dx_succ_adv` 0.663 |
| `rev × 2S` | **0.191** | `H1:lx_rev_bs` 0.372 |
| `rev × 2S&b≤1` | 0.475 | `X:dx_mid_adv` 0.607 |

**최대 R² 가 0.48 — 기존 82열을 다 써도 절반 이상이 설명되지 않는다.**
가장 가까운 상대가 `lx_*_bs` 인 것은 자연스럽다. 그러나 `bs=0` 이 (0-0)·(1-1)·(2-2)를
전부 포함하듯 **`1{strikes==2}` 는 `bs` 의 함수가 아니다.**

⚠️ **K2 내부 4열은 서로 중복이 크다** (`ball×2S` vs `rev×2S` = 0.93,
두 지시자 쌍끼리 0.94). 4열 중 2열로 충분할 수 있어 내부 marginal 이 필요하다.

### 🚩 위험 요소 (명시적 기록)

```
2024 기여   시드 42·43  +11.5
            시드 44·45   -5.2    <- 부호 반전. 이것이 위험이다
            시드 46·47  +10.2
            6시드 합산   +5.5
기준선 자체 변동  916.3 / 919.5 / 919.1  (±1.6)
```

**+11.5 를 확정값으로 읽지 않는다.** K2 의 시드 분산이 기준선의 10배 이상이다 —
`cur_ball`·`cur_rev` 는 `cur_succ` 보다 표본당 사건이 적고, 2스트라이크 지시자가
절반을 0 으로 꺼서 유효 표본이 더 준다. **기대값은 +5.5, 하방은 −5.2 다.**

### 진단 기록 — count regime 만 압도적으로 흔들린다

```
아웃    0.90~1.12      주자수  0.91~1.47
이닝    0.78~1.25      같은손  0.89~0.92
볼카운트 0.37~2.10   <- 유일하게 5.7배
```

잔차는 전 구간 +0.008~+0.017 로 평평 — 체계적 편향 구조는 없다.

### 지금

`build_asof.py --ctx --lvl --k2 --build` 실행 중. 대조군이 `champ`(82p)이라
나오는 배수가 1049.9226 위에 곱해진다. 출력 `cand_asof_k2.zip`.
**제출 여부는 자동 결정하지 않는다.**

---

## 2026-08-16 12:30 — 🚩 K2 보류. 생산 경로에서 재현되지 않았다

```
캐시 경로 (시드 6개)   +5.5   (+0.60%)
생산 경로 (시드 1개)   -8.2   (-0.87%)   944.0 -> 935.8, 배수 0.9913
차이 13.7
```

지시된 중단 조건("production path 에서 K2 효과가 사라지면 내부 분해보다 그 사실을
먼저 기록하고 K2 를 보류한다")에 해당한다. **분해 실험 중단.**

### 🚩 원인 — 후처리 편차 4축과 중복이다

생산 경로는 예측에 편차 4축을 더한 뒤 `rho` 를 계산한다. 그 **3번째 축이
`dev(플래툰x투수유리x카운트)`** 이고 **투수유리 = `strikes > balls`** 다.
**후처리가 이미 2스트라이크 국면을 다루고 있다.**

K2 가 캐시 경로(후처리 없음)에서 벌던 것을 생산 경로에서는 후처리가 이미 먹고
있고, 중복이 들어가며 오히려 해가 된다. §9-b 에서 확인된 패턴과 같다 —
*"in-model 판 위에 후처리를 또 얹으면 증분이 0 이다. 같은 신호이고 투입 경로만
다르며 총량이 같다."*

**이 중복은 분해로 해결되지 않는다.** `ball×2S` 를 빼든 `rev×2S` 를 빼든 후처리와의
겹침은 남는다.

### 캐시 경로 측정의 한계 — 앞으로 지킬 것

**캐시 경로에는 후처리 편차 4축이 없다.** 따라서 편차 4축과 겹치는 축
(투수x타자손 · 플래툰x투수유리 · 그x카운트 · 플래툰x주자유무)을 다루는 후보는
**캐시 경로에서 과대평가된다.** K2 가 그 사례다.

> 앞으로 **카운트·플래툰·주자 관련 축은 캐시 경로 결과를 그대로 믿지 말고
> 생산 경로에서 확인**한다. D/X/H1 이 통했던 이유 중 하나는 그것들이 편차 4축과
> 겹치지 않는 **선수 현재 수준** 축이었기 때문이다.

### 아티팩트

`submissions/cand_asof_k2.zip` (86p) 생성됨. **후보로 쓰지 않는다** —
기대 LB 1042.8~1041.9 로 Champion 1049.9226 보다 낮다. 재확인이 필요할 때
재학습을 아끼려고 남겨 둔다.

### 다음 — champ_ebsucc

3/3 양수(+10.2 / +4.4 / +2.0). **EB 축소는 선수 수준 축이라 편차 4축과 겹치지
않는다** — K2 와 달리 캐시 경로 측정이 생산 경로로 옮겨갈 가능성이 높다.

---

## 2026-08-16 14:58 — 26회차 K2 = **1045.1996** (−4.72). 전이식 3차 붕괴

### 생산 경로가 맞았고 캐시 경로가 틀렸다

```
캐시 경로 (후처리 없음, 6시드)   +5.5   (+0.60%)
생산 경로 (후처리 포함, 1시드)   -8.2   (-0.87%)
실측 LB                        -4.72  (-0.45%)   <- 생산 쪽 부호
```

§K2 보류 판단이 맞았다. **원인은 후처리 편차 3번축
`dev(플래툰x투수유리x카운트)` 와의 중복** — 투수유리 = `strikes > balls` 라
후처리가 이미 2스트라이크 국면을 다루고 있었다.

### 🚩 전이식 폐기 — 세 번째 붕괴다

| | 예측 | 실측 | 오차 |
|---|---:|---:|---:|
| 4점 적합(k=0.830, c=2.07%)의 K2 | 1024.3 | **1045.20** | **+20.9** |

5점 재적합: `k=0.751, c=1.37%`, **잔차 RMS 7.8점** (4점일 때는 2.9점이었다).

| 축 | 로컬 2024 | LB 이득 | 비율 |
|---|---:|---:|---:|
| D | +12.87% | +8.97% | **0.70** |
| F | +1.36% | −0.82% | −0.60 |
| X | +2.78% | +0.37% | 0.13 |
| H1 | +2.12% | +0.49% | 0.23 |
| K2 | −0.87% | −0.45% | 0.52 |

**|로컬| < 3% 구간에서 비율이 −0.60 ~ 0.52 로 흩어진다. 부호조차 예측 불가다.**
D(+12.87%)만이 예측 가능하게 전이됐다. 실질적으로 **±5~10점의 잡음**이 깔려
있고 우리 후보들은 전부 그보다 작다.

> **"전이율이 낮다"가 아니라 "이 크기에서는 로컬이 LB 를 예측하지 못한다"** 이다.
> 앞으로 로컬 수치로 ±3% 미만 후보의 우열을 논하지 않는다.

### 슬롯 전략 변경 — 남은 4장

로컬로 우열을 못 가리므로 **정보량이 큰 순서**로 낸다.

1. **`HB-add`(87p) 먼저** — 2022 +8.6 / 2023 +9.5 / **2024 −0.0**.
   **2024 음수 후보를 처음으로 제출하는 것**이고, 그 규칙으로 17개를 버렸다.
   오르면 기각 목록 전체가 재검토 대상, 안 오르면 규칙이 처음 실증된다.
2. `ebsucc`(83p)는 `HB-add` 의 부분집합이라 그 결과로 필요 여부가 정해진다.

---

## 2026-08-16 19:50 — game_type F 의 체제 전환. **2023 퇴화 폴드의 원인을 찾았다**

`--drop-f`(F 를 **학습에서만** 제외, 검증 폴드는 그대로) 결과:

| 폴드 | Champion | F 제외 학습 | 차이 |
|---|---|---|---|
| 2022 | 2458.4 | 1134.2 | **−1324.2 (−54%)** |
| 2023 | **192.0** | **768.0** | **+576.0 (+300%)** |
| 2024 | 916.3 | 909.4 | −6.9 (시드 노이즈 내) |

둘 다 2시드 평균이라 Champion 기준선과 같은 프로토콜이다. 폴드마다 부호가
정반대인 것이 핵심이고, 원인은 **F 의 생성 관계가 시간에 따라 바뀐 것**이다.

```
F 성공률   2019 .689  2020 .588  2021 .704  2022 .709 | 2023 .473  2024 .459
R 성공률          .550       .527       .513       .504 |       .503       .490
F-R 격차        +.140      +.061      +.191      +.205 |      -.030      -.030
                        --------- 구체제 ---------+---- 신체제 ----
```

격차가 **부호까지 뒤집혔고**(+0.205 -> −0.030), 신체제 두 시즌이 소수점 셋째
자리까지 같다. 이것으로 폴드별 부호가 전부 설명된다:

| 폴드 | 학습셋 F-R 격차 | 검증셋 격차 | 괴리 | F 제외 효과 |
|---|---|---|---|---|
| 2022 | +0.133 | +0.205 | +0.072 (같은 방향) | **−1324** |
| 2023 | +0.153 | −0.030 | **−0.183** (정반대) | **+576** |
| 2024 | +0.117 | −0.030 | −0.148 | −6.9 |

2024 가 2023 만큼 오르지 않는 이유는 **학습셋에 2023(신체제)이 한 시즌
들어있어서** 모델이 이미 부분 적응했기 때문이다. 스테일 비중으로 정리하면:

```
폴드 2023   구체제 F 4시즌 : 신체제 0시즌   스테일 100%   ->  +576
폴드 2024   구체제 F 4시즌 : 신체제 1시즌   스테일  80%   ->    -6.9
2025 생산   구체제 F 4시즌 : 신체제 2시즌   스테일  65%
```

따라서 **폴드 2024 는 보수적인 대리가 아니라 약간 낙관적인 대리다.** 2025 는
신체제 데이터를 한 시즌 더 갖고 있어 스테일 피해가 그만큼 작다 — 방향은 같고
크기는 폴드 2024 에서 재는 것보다 작을 것으로 본다.

**F 자체는 필수 정보다.** 전량 제외하면 2022 가 반토막 난다. 문제는 F 가 아니라
**스테일한 F** 이고, 모델에 시즌 피처가 없으니 "F 관계가 바뀌었다"를 표현할
수단이 원리적으로 없어 구체제 4시즌과 신체제 2시즌을 그냥 평균한다.

-> `--drop-f-before 2023` 추가 (구체제 F 만 제외, 신체제 F 는 유지). 폴드 2024
   에서만 전량 제외와 달라지므로 거기서 판정한다.

### 함께 끝난 것 — 조건 변수 감사 2단계

조건 9종 x 핵심신호 3종의 `corr(신호, 정답 | 조건)` 을 세 시즌에서 비교했다.
**3시즌 일관 + 격차 0.010 초과 + 미회수** 를 통과한 것은 `game_type` 하나뿐이다.
`runner_on`(격차 +.017/+.012/+.014)은 통과했으나 Champion 의 `dx_succ_onb` 가
정확히 그것이라 신규가 아니다. `outs`/`inning`/`2strike` 는 REGIME B/C/K2 에서
이미 기각했고, `cur_n` 은 2022 에서 부호가 반대라 지속성이 없다.

**조건 변수 축은 소진됐다.** `game_type` 외에 관계 자체를 바꾸는 변수는 없다.

---

## 2026-08-16 23:40 — 축 3개 종료, 새 판정법 1개 확보

| 실험 | 2022 | 2023 | **2024** | 판정 |
|---|---|---|---|---|
| `--drop-f` (F 전량 제외) | −1324.2 | **+576.0** | −6.9 | 기각 |
| `--drop-f-before 2023` | — | — | −9.7 | 기각 |
| `champ_xt` (X×is_F) | +8.7 | −2.6 | −0.5 | 기각 |
| `champ_ht` (H1×is_F) | +8.6 | −3.1 | +3.1 | 기각 (노이즈 내) |
| `champ_st` (cur_*×is_F) | +2.1 | −11.0 | −6.7 | 기각 |
| `champ_pn` (투구수 역산 3열) | +6.8 | +3.2 | −1.4 | 기각 |

**닫은 축** — `game_type`(행 선택 + 표현 분리 양방향), 조건 변수 감사 전체,
PN 투구수 역산, prev 창 분해.

**설명한 것** — 2023 퇴화 폴드의 원인이 2023년 F 라벨 정의 변경으로 확정.
같은 투수 92명 중 91명이 −0.226 동시 하락, 그 해 교체율은 6년 중 최저.
앞으로 낮은 2023 값을 모델 결함으로 읽지 않는다.

**새 판정법 (가장 값진 소득)** — 후보를 학습 돌리기 전에 **폴드 2024 Champion
잔차와의 상관**을 재서 이미 모델에 있는 피처와 같은 대역이면 기각한다.
PN 은 +0.0016~+0.0036 으로 `asof_pitcher_n`(+0.0034)과 같은 대역이었다.
15분짜리 학습 전에 걸러낼 수 있었다. CLAUDE.md 4절에 반영.

**"모델이 못 만드는 값"만으로는 부족하다** — 대리 신호가 없어야 한다.
D 는 대리 신호가 없어서(prior_events 는 학습셋 상수) +85.65 였고, PN 은
대리 신호가 있어서(비율 granularity, 역할 관련 피처) 기각됐다.

---

## 2026-08-17 — TrackMan family 종결. 다음은 "제거" 실험

| 실험 | 2022 | 2023 | **2024** | 판정 |
|---|---|---|---|---|
| `champ_tmx` (회전/무브 편차 6열) | +3.3 | +1.2 | **−0.9** | 기각 |
| `champ_tmr` (릴리스기하 편차 6열) | +9.8 | +1.5 | **−3.9** | 기각 |
| 시드 3→7 | — | — | **+0.6** (k=5 최대 +1.7) | 기각 |
| 경로 배분 12조합 | 전부 음수 | | | 기각 |

**원인 규명** — TrackMan 결측(F 40% vs R 18%)이 `game_type` F 를 대리한다.
2022 에서는 공짜 신호를 실어 나르고 2024 에서는 죽는다. **조인 구조가 원인이라
어떤 TrackMan 파생도 못 피한다** -> TMZ 설계 전 기각, family 전체 종료.

**아핀/center 도 종결** — 워크포워드 .4622 vs LB역산 .4609, 잔여 손실 약 0.7점.
clip 미접촉이라 alpha 0.5~2.0 에서 점수 동일.

### 다음에 할 것 (우선순위)

1. **현행 TrackMan 8열 제거 실험** — 7회차(LB 825, 구체제)에 채택된 것이라
   같은 죽은 무게일 수 있다. 생산 경로 3폴드 x 2시드.
   `exp/tmx_probe.py` 를 --drop-tm 형태로 확장하면 된다.
2. 제출 예산이 **하루 5회**로 확인됐다 (기존 "슬롯 3장" 전제 폐기).
   다만 현재 +3 이상 후보가 없어 제출할 것이 없다.


---

## 2026-08-17 18:35 — TrackMan 8열 제거 기각. 가설의 경계가 잡혔다

`exp/drop_tm.py` 신설. 생산 경로 3폴드 x 2시드, 4구성 약 50분.

| 구성 | 2022 | 2023 | **2024** | 판정 |
|---|---:|---:|---:|---|
| `champ_notm` (8열 전부) | -2.4 | +1.6 | **-11.7** | 기각 |
| `champ_nocnt` (`tmc_*` 4열) | -6.1 | -10.0 | **-7.8** | 기각 (3/3 음수) |
| `champ_nohand` (`tmh_*` 4열) | -0.6 | -4.9 | **-0.4** | 무해하나 이득 없음 |

champ 재현 확인 — 2475.8 / 211.8 / 943.8 로 `tmx.json` 과 완전 일치.

**얻은 것** — "TrackMan 은 전부 오염이다"가 아니라 **물리량이 구분선**이다.
카운트별 구종 성향(`tmc_*`)은 오염을 상쇄하고 남는 신호가 있고, 회전/무브/
릴리스 기하는 오염만 남는다. 8열 중 실제로 일하는 것은 `tmc_*` 4열이다.

Champion 82피처 그대로 유지. 제출할 후보는 여전히 없다.

### 다음에 할 것

1. TrackMan 축은 이것으로 종료 (신규 파생 닫힘, 현행 8열 유지 확정).
2. 남은 축을 사용자와 정한다 — 닫힌 축 목록이 길어져 새 가설의 발원지가 필요하다.


---

## 2026-08-17 19:00 — 오라클 진단. 남은 점수의 소재를 특정했고, 그 자리는 닫혀 있다

축을 추측으로 고르는 대신 **어디에 정보가 남았는지**를 먼저 쟀다 (학습 0회).

| 그룹 | cv2(정직한 상한) | vs 기준 944.0 |
|---|---:|---:|
| pitcher x 타자손 | 1020.8 | **+76.8** |
| pitcher x 월 | 1005.2 | +61.2 |
| pitcher x 시즌내십분위 | 996.2 | +52.2 |
| pitcher | 961.9 | +17.9 |
| batter / count / 이닝 / base_state / 월 | 기준선 이하 ~ +0.4 | 회수 완료 |

**타자·국면 축은 그 시즌 라벨을 쥐어 줘도 안 오른다** — 정보가 없는 것이지
표현이 부족한 것이 아니다. 다시 열지 않는다.

그리고 시즌 간 전이를 쟀다 (`exp/resid_table.py`, 폴드 기준선 3개 전부 재현).

```
2023 -> 2024        최적 k 5000   +6.8 (+0.72%)
2022+2023 -> 2024   최적 k 5000   +6.9 (+0.73%)
2022 -> 2023        +39.9  <- 퇴화 폴드라 채택 근거로 못 쓴다
```

**오라클 +76.8 중 시즌을 건너오는 것은 9% 뿐이다.** 손익분기 2.1% 아래라 기각.

**구조적 결론** — 남은 점수는 "투수의 그 시즌 현재 상태"에 있는데, 2025 에는
라벨이 없고 규정 4) 로 다른 행도 못 본다. D 가 통한 이유가 여기서 다시 보인다:
라벨 없이 `asof_*` 카운터에서 대수적으로 복원했기 때문이다. 그런데 `asof_*` 에는
**타자손 분할이 없어** +76.8 짜리 자리에 닿는 합법 경로가 없다.

### 다음에 할 것

1. 새 그룹 가설은 `oracle_probe.py` 의 `GROUPS` 에 한 줄 추가해 **20초에** 선별.
2. 남은 합법 경로는 `asof_*` 카운터에서 더 뽑아낼 것이 있는가 하나뿐이다.
   (D 가 연 문이고, 타자손 분할이 없다는 것이 오늘의 제약)


---

## 2026-08-17 19:30 — `asof_*` 전수 감사. 남은 독립 정보 없음, 그리고 헛열 발견

학습 0회. 세 폴드 기준선 전부 재현(2475.8 / 211.8 / 944.0).

**대수적 확정 두 개**
- `asof_pitcher_pitchmix_n` == `asof_pitcher_n` **100.0000% 일치** ->
  D 의 `cur_logn_mix` 는 헛열. 공식 19열의 실질은 18열이다.
- 구종 3비율은 정확한 단체(합 1.0000), 투수 5비율은 선형 종속 없음(겹치는 구간).

**분해** — `asof_rate = w*prior_rate + (1-w)*cur_rate` 로 정확히 갈린다.
D 가 원시 분산의 44~85% 를 설명하고, 남은 경력 성분은 2022/2024 에서 모델
잔차와 |0.0044| 이하(2.2SE)다. 2023 만 크지만 퇴화 폴드라 근거로 못 쓴다.

**카운터 파생 10종**(prior_logn, cur_share, mix_cover, cur_ev, pace, d_succ,
bat_share, prev1_gap_prior) 전부 |2.1SE| 이하 + 오라클 십분위 전부 음수.

**남은 최대치** — 가운데율 계열 잔차상관 −0.0076(3.8SE), 원리적 상한 +5.2점
(+0.55%). 손익분기 +19.8점(+2.1%)의 4분의 1이라 **이 축에서는 못 넘는다.**

C 등급(승격 후보) **0건.** `asof_*` 축 종료.

### 다음에 할 것

1. `cur_logn_mix` 제거는 정보 손실 0 이 대수적으로 확정된 값싼 확인 항목.
2. asof 가 닫혔으므로 남은 것은 모델 밖 — 고원 8팀의 공통해 역추정.


---

## 2026-08-17 20:00 — D 한계 감사. F/R 혼합은 한계가 아니었다

`exp/d_limits.py`, 학습 0회. `pitcher` 기준값이 `oracle_probe` 와 일치(+17.9).

**위약 대조** — pitcher x 무작위2분할 +9.4 (782셀), 무작위10분할 +5.0.
세분화는 이득을 만들지 않는다. 아래 양수는 전부 정보다.

| 한계 후보 | cv2 | 판정 |
|---|---:|---|
| 타자손 분할 부재 | **+76.8** | 진짜 한계 |
| 궤적 상실 | **+52.2** | 진짜 한계 |
| F/R 혼합 | +18.3 (단독 +17.9) | **한계 아님** |
| 작은 cur_n / 타입별 의미차 / 실패유형 겹침 | −2.0 / −11.7 / −3.7~+1.4 | 한계 아님 |

**최우선 질문(F/R 분리 복원)의 답** — 세 겹으로 막혔다.
1. 대수적 불가 (미지수 4, 식 1. 카운터가 F/R 혼합 단일 계열)
2. 정보 자체가 없음 (2024 라벨을 줘도 +0.4)
3. 정적 D_F/D_R 도 무의미 — F−R 격차가 2022 +0.244 -> 2023 +0.012 -> 2024 +0.006
   으로 **소멸**했고 시즌 간 상관이 −0.21~+0.31 로 불안정

D_F/D_R 은 설계하지 않는다. 못 만들어서가 아니라 가져올 것이 없어서다.

**남은 진짜 한계 둘은 둘 다 이미 측정된 벽에 막혀 있다** — 타자손(합법 경로
없음), 궤적(시즌 간 전이 9%). D 의 한계는 표현이 아니라 **입력 카운터의 해상도**다.


---

## 2026-08-17 20:40 — 궤적 진단. 경로 모양에는 정보가 없다 (학습 없이 종료)

`exp/traj_probe.py`. A/B/C 분리, 사전 규약대로 **B 부족이라 학습하지 않았다.**

**전제 정정** — "궤적 오라클 +52.2" 는 `pitcher x 시즌내십분위` 였고 값의
대부분이 **투수 성분**이다. 경로 모양을 직접 그룹으로 쓰면 상한이 최대 +2.0 이다
(cur 십분위 x 모양 = +0.1, 모양 8등급 = −4.6).

**왜** — 모양 등급별 평균 y(0.4785~0.4926)가 갈리는 만큼 평균 cur(0.4775~0.5061)도
같은 방향으로 갈린다. 모양이 설명하는 것은 수준이고 수준은 이미 모델에 있다.

**B 전이** — 2023->2024 −6.5 / cur x 모양 −8.1, 2022+2023->2024 −9.4 / −10.5.
2022->2023 만 양수(퇴화 폴드).

**B-2 관계 안정성(지침 7)** — 결측 등급을 빼면 2022~2023 +0.58 이지만
**2023~2024 −0.25** 로 뒤집힌다. 관계 안정성 논거로도 못 살린다.

**C** — vol 만 투수 형질로 지속(+0.33)되나 오라클 +2.0, 잔차상관 부호가 폴드마다
뒤집힌다.

**부수 확인** — 고원 역추정 첫 후보였던 `pitcher_id` CTR 은 원장 §EXP017 에서
이미 −31.17 로 기각돼 있었다 (반복 금지 목록에 있음).

### 남은 상태

D 의 병목 둘 중 **궤적은 표현 문제가 아니었다.** 남는 것은 타자손 해상도
하나이고, 공식 카운터에 손 분할이 없어 합법 경로가 없다.


---

## 2026-08-17 21:20 — 생성 가정 감사. 가정 하나가 틀렸고 파생 8열이 헛열이다

`exp/gen_audit.py`, 학습 0회.

**틀린 가정** — `asof_*` 카운터는 F 와 R 을 **섞는다**. 합집합에서만 연속이고
(빈틈0 100%), R 만/F 만 보면 91.4%/92.8% 로 구멍이 난다. 38.4% 의 투수-시즌이
두 체제를 모두 던진다. 그래서 `cur_succ` 는 두 모집단의 혼합 추정치다.
구체제(F−R +0.205)에선 오염이 컸고 신체제(−0.03)에선 사실상 없다.

**맞은 가정** — 투수-시즌 내 `asof_n` 빈틈 0 이 100%, 중복 0. 시즌 경계에서
다음 시즌 첫 값 = 이번 시즌 마지막 +1 이 **100.0%**(1,330쌍). 관측되지 않은
투구가 없고 통산 누적이 정확하다. D 의 전제가 원자료로 확인됐다.

**F 는 라벨링이다** — TrackMan 커버리지로 층화해도 2023 에 양쪽 다 붕괴
(있음 .715->.483, 없음 .700->.443). R 은 안 움직인다. 2019 는 TM 커버 0%.

**두 번째 체제 구분자는 없다** — game_type 0.207 vs 다음이 팀 0.066/0.080.
팀13 2023 급변은 홈·원정 양쪽에 나타나 구장 효과가 아니다(구단 사건, 2023 한정).

**구장 축 신규 검사 후 종료** — 구장은 top_bottom+팀 id 로 정확히 복원되나
오라클 +0.1, 홈/원정 −1.2. 타자팀만 +11.6 이었으나 전이 −9.7/−10.0.

**47열 중 8열은 독립 정보 0** — run_total·score_diff_home·score_diff_pitcher_team·
num_runners·base_state 는 정확한 항등식(100.0000%), pitchmix_n 은 중복,
li·home_we·away_we 는 경기상태 5열의 **완전한 결정함수**(유일값 셀 100.0%).
승률기대에 팀 전력·구장 같은 숨은 정보는 없다.

**목표 구조** — 5비율은 분할이 아니라 겹치는 주변분포(합 1.706)이고 결합은
관측 불가다. "실패 구성비"가 죽었던 이유가 이것이다.

**train/test 동일 규칙 확인** — test 5행의 li/승률기대가 train 상태표와 일치.


---

## 2026-08-17 22:00 — 형식 감사. D->확률 사상에 남은 것이 없다

`exp/form_audit.py`, 학습 0회, 피처 0개, 하이퍼파라미터 무변경.

**구간별 아핀(교차적합)** — 기울기 범위가 0.89~1.15 로 사실상 1 이다.
라벨을 보고 구간별로 다시 맞춰도 game_type −3.6 / cur_n 십분위 −9.1 /
같은손 +3.9(최대) / 투수 −131.7. 손익분기 +19.8 의 5분의 1이 천장이다.

**잠재 상태** — 전제가 틀렸다. 10개 cur_* 의 주성분 설명력이 0.21/0.20/0.16...
누적 1~3성분 0.56 으로 **실질 5~7차원**이지 하나의 잠재 상태가 아니다.
최적 선형결합(학습시즌 적합)의 2024 잔차상관 −0.0043, 상한 +1.8, 오라클 −10.0.

**다섯 질문 전부 상한이 손익분기 아래** — 특히 어떤 2단계 구조든 회수 대상이
"투수 현재 상태"이고 그 상한이 +17.9(라벨 사용), 전이분이 +6.9 다.

D 구성 고정. 형식 변경 후보 없음.


---

## 2026-08-17 22:40 — 해상도 격차 조사. 고원은 투수 상태로 설명되지 않는다

새 피처 0개, 학습 0회, 제출 없음.

**규정 원문** — 외부 데이터는 §6-m 원문대로 명시적 금지("공식 데이터 외의 외부
데이터는 사용할 수 없습니다"). test 행 간 참조도 금지. 2025 Trackman 금지.
`asof_*` 는 사용 가능(투구 직전까지의 과거 기록).

**필요한 rho** — Champion rho 0.102466. 10위 1157.96 은 rho +5.02%,
rho^2 +10.29%, **직교 신호 c=0.0329**, 전이식 환산 **로컬 2024 +14.47%** 필요.
D 자체가 로컬 +12.87% 였으므로 **D 보다 큰 발견이 하나 더** 필요하다.

**결정적 비교** — 라벨을 쥔 최대 오라클(pitcher x 타자손 +76.8)을 전이 손실 0 으로
얹어도 LB 1135.3 이고 10위에 −22.6 이다. 고원의 우위는 투수 상태 해상도가 아니다.

**A/B/C** — A(현 데이터 합법)는 전부 +5 미만, B(외부)는 규정상 공집합,
C(원천 불가)에 큰 것이 전부 있다(2025 시즌내 손 분할·궤적, 경기 식별자,
2025 Trackman, 현재 투구 정보, 5차원 결합분포).

### 다음 최소 행동

판별 가능한 가설이 하나 남는다 — "정보가 없는 것인가, 우리 적합이 부족한 것인가".
같은 82피처로 in-sample vs 폴드 2024 OOS 격차를 재면 갈린다. 새 피처 없음, 학습 1회.


---

## 2026-08-17 23:10 — 정보 한계 vs 적합 한계. 적합은 병목이 아니다

`exp/fit_limit.py`, 새 피처 0개, 82p 고정, 제출 없음.

**과적합은 1.24배** — 폴드 2024 모델의 2022 행 in-sample 3045.1 vs 정직한 OOS
2450.7. 2023 의 8.83배는 기억화가 아니라 F 라벨 정의 변경 때문이다.

**반복수 곡선 포화** — 100그루 803.0 -> 1200그루 944.0, 마지막 200그루 증분
**+0.9** (같은 구간 train 은 +105.6). 적합 여지 없음 -> **C 기각**.

**표본 곡선은 아직 가파름** — 152k 774.9 / 305k 843.8 / 611k 891.2 / 1.22M 944.0,
배가당 약 **+50**. 표본 한계 상태다.

**판정: B (현 표현에서의 정보/표본 한계). 새 모델 탐색 시작하지 않는다.**

최종 학습은 1,475,092행(폴드 측정치 대비 +21%)이라 곡선상 약 +14.6점을 이미
받고 있다. 데이터는 고정이고 외부는 규정 3) 금지이므로 이 채널도 닫혔다.


---

## 2026-08-17 23:30 — 프로덕션 아티팩트 학습 범위 검증. 전량 학습 확정

가정이 아니라 아티팩트를 열어 확인했다.

- `cand_asof_xl.zip` 내부 `model/rf.pkl` 과 `model_cand/cat_asof_xl.pkl` 이
  **sha256 동일**(ed600d7fc515794a). zip 시각 차이는 재포장일 뿐이다.
- 번들: 시드 42/43/44 3개, 1200그루 x3, 82피처, alpha 1.09, center 0.585445,
  후처리 편차 4축(셀 1,571 / 18,753 / 17,767 / 6,185).
- **학습 행 수 = `asof_prior` 표본수 합계 = 1,475,092** (pitch/mix/bat 셋 다).
  폴드 학습분 1,221,585 가 아니다.

**LB 1049.9226 은 이미 전량 학습 프로덕션 결과다.** 표본 곡선이 말한 +14.6 은
그 점수 안에 이미 포함돼 있으므로 **중복 계산하지 않는다.** 재학습 불필요,
새 아티팩트 불필요.


---

## 2026-08-17 23:40 — LOCAL->FUTURE 전이 연구. 정상 의사미래 쌍이 0개다

`exp/transfer_study.py`(창 5 x 구성 6 x 2시드, 100분) + `transfer_fit{,2}.py`.

**핵심 부정 결과** — 20개 (LOCAL,FUTURE) 쌍 중 "학습량 충분 + 2023 미관여"가
**0개**다. 2019 TrackMan 0%, 2020 표본 최소, 2023 F 라벨 변경으로 6시즌 중
4시즌이 비정상이다. 기준선 rho^2 가 22~2400 으로 100배 흔들려 상대 이득조차
불안정하다(같은 D 가 +122.83% ~ +13.24%). **전이식이 세 번 깨진 이유가 이것이다.**

**두 독립 경로의 중심값은 일치한다** — 2023 미관여 10쌍 b=+0.512(RMS 4.93%p),
실제 LB 앵커 6건 비율 중앙값 +0.375. 산포도 그만큼 크다(sd 0.789).

**부호는 준예측 가능** — 실제 LB 5/6, 의사미래 70~86%.

**family 가 체계적으로 다르다** — current-state +0.697 > context +0.135~0.233
> regime +0.518(음수 전이) > recent-history −0.605 > shrinkage +2.006(음수 확대).
"수준은 전이되고 차분은 안 된다"가 처음으로 수치화됐다.

**실무 규칙** — EXPECTED_FUTURE% ≈ 0.4 x LOCAL%, 양수 확신에는 LOCAL > 3.8% 필요.
오늘 측정한 모든 후보(최대 +0.18%)는 그 20분의 1이다.

**시간 거리는 측정 불가** — 창을 옮기면 학습량과 거리가 동시에 바뀐다. 미해결.


---

## 2026-08-18 — 후보 탐색 정책 개정. 전이 연구를 규칙으로 확정

`CLAUDE.md` 에 5-b 절을 신설하고 §7 닫힌 축을 갱신했다.

**바뀐 것**
- 단일 전이 배수(`0.830 x (로컬 − 2.07%)`) **폐기**. 5절에 폐기 표시를 남기고
  이력으로만 보존한다.
- 목표를 `TRANSFERABLE SIGNAL > LARGE LOCAL SIGNAL` 로 재정의.
- HARD GATE 4단: 음수 제출 금지 / 0~3.8% 승격 금지 / 3.8% 이상만 후보 /
  새 family 는 의사미래 전이 부호 선행 확인(기각용).
- family 전이 조회표(current-state +0.697 > regime +0.518 > context
  +0.135~0.233 > recent-history −0.605 > shrinkage +2.006)를 상시 참조표로.
- 새 후보는 학습 전에 8항목(FAMILY ~ EXPECTED_FUTURE_RANGE)을 채운다.

**family 단위로 닫힌 것** — recent-history · shrinkage · generic regime ·
simple calibration · trajectory · gating · F-specific · team/park identity.

**현재 상태** — 이 게이트를 통과하는 후보는 0개다. 오늘 측정한 최대치가
로컬 +0.18%(후처리 5축)이고 문턱은 +3.8% 다. Champion 1049.9226 유지.


---

## 2026-08-18 — PIPELINE B 기각, `+17.9` 오라클 분해

**PIPELINE B (state-first) 종료** — 원시 asof 비율을 빼면 3폴드 전부 음수
(2024 −16.4 / −10.6, 즉 −1.74% / −1.13%). D 는 이미 학습 없는 결정론적
추정기라 분리 자체로는 정보가 늘지 않고, 혼합값도 모델이 실제로 쓰고 있었다.

**+17.9 분해** — 오라클은 "그 투수의 그 시즌 나머지 절반 평균 잔차"를 알았다.
그 잔차평균 분산의 **81~92% 가 이항 잡음**이고(신호 sd 0.010~0.013),
인접 시즌 감쇠보정 상관은 +0.774 지만 두 시즌 건너면 −0.06 이다.

```
A 합법 행단위 대리   −42.6 (2023 에서 가중 선택 후 2024 평가 — 부호 반대)
B 과거 학습 추정     +1.0 / +0.5
C 라벨 없이는 불가   ~+16.9
```

🚩 **한 번 속을 뻔했다** — 투수별 평균 피처로 잔차를 예측하니 +19.5 가 나왔으나
(1) 가중을 2024 에서 고름 (2) 투수 평균은 평가셋 다른 행 참조(규정 4)
(3) 시즌 평균은 미래 투구 포함(규정 6). 합법 행 단위로 고치면 −42.6 이다.
규정 위반 형태는 +46.3 까지 오른다 — 그 격차가 "다른 행/미래를 보는 값"이다.


---

## 2026-08-18 — 경기 단위 상태 진단 (2분). 최대 오라클, 합법 경로 0

`prev1` 이 경기 안에서 상수라는 성질로 경기 경계를 복원했다 (8,017 단위,
경기당 중앙값 19 / 90분위 89 / 최대 151 — PN 감사의 독립 추정과 일치).

```
(투수, 경기)                +187.8      <- 지금까지 최대
위약: 같은 셀 수 무작위        +1.9      <- 세분화의 산물이 아니다
투수 단독                    +17.9
경기 내 순번 / 경기 크기       −5.1 / +0.6
```

**합법 경로는 없다** — 그 행 하나로는 경계를 못 본다(0-0 카운트 행 중 실제 첫
투구는 12.2%, 1회 0아웃 무주자만 97% 식별). 보정에는 같은 경기의 다른 투구
결과가 필요한데 2025 에는 라벨이 없고 규정 4)가 행 간 참조를 금지한다.

크기 대조 — +187.8 은 +19.9% 이고 LB 로 환산하면 약 1259 로 상위권 구간과
겹친다. 상위권이 그렇게 했다는 증거는 아니지만, **우리가 못 보는 정보의 규모가
처음으로 정량화**됐다. 규정을 지키므로 관측만 하고 닫는다.

DECISION: 종료.


---

## 2026-08-18 — `+187.8` 분해. 행 단위 회수 가능분 0, 경기 축 종료

**신뢰도** — 경기 잔차평균의 73~89% 가 이항 잡음. 진짜 신호 sd 0.045~0.051
(투수 단위 0.010~0.013 의 약 4배).

**중첩 분해** — 투수 +17.9 -> +투수x월 +45.0 -> +구장 +46.0 -> +경기 **+68.4**.
단독 경기 오라클 +187.8 의 3분의 1이다. 앞 층의 추정 잡음이 뒤 층에 얹혀서
**라벨을 쥐고도 순차 보정으로는 그만큼밖에 못 가져온다.**

**합법 대리** — 경기 첫 행의 합법 값으로 경기 잔차평균을 예측하는 사상을
2022+2023 에서 적합 -> 2024 상관 **+0.002**. 적용하면 −1.7 ~ −97.5.

```
ROW_ONLY_RECOVERABLE = ~0     LEGAL_PROXY = 없음     EXPECTED_UPSIDE = 0
```

경기 축 종료. 압축된 상태의 크기는 정량화됐지만 그 방향을 가리킬 행 단위
관측치가 없다.


---

## 2026-08-18 — 정보 해상도 감사. 정보공간은 다 썼다

47열의 분산을 계층 분해했다 (투수/시즌/경기/타석/투구).

**투구 해상도를 가진 컬럼은 `balls_before`(0.63)와 `strikes_before`(0.68) 둘뿐**
이다. 나머지는 전부 타석·경기·투수 수준이다. 타깃은 그 투구의 위치로 정해지는데
데이터는 그보다 거친 해상도만 담고 있다 — 이 프로젝트 천장의 구조적 원인이다.

해상도 사다리 (오라클): 투수 +17.9 / 경기 +187.8 / 타석 +75.8 (위약 +11.8).
타깃 계층 분산: 투수 0.010 / 경기 0.042 / 타석 0.233 / 투구 0.715.

제거된 것: 경기 식별자 · 날짜 · 포수 · 심판 · 투구 위치/구종 · pitch_of_pa · 순서.
(trackman 에도 위치·포수·심판은 없고 2025 는 규정 금지)

```
LEGALLY RECOVERABLE = 0        UNEXPLORED_HIGH_VALUE_RELATIONS = 0
```


---

## 2026-08-18 — latent 대리 탐침. 경기 잠재축은 D 와 직교한다

방향 정합 corr(관계 보정, 경기 잠재축 보정):

```
투수 x 월 +0.492 / 투수 x 점수차 +0.285 / 투수 x 타자 매치업 +0.272 / 투수 +0.217
cur_* x {월, 팀} +0.09~0.11    cur_succ 십분위 단독 +0.038
```

**D 의 현재상태를 어떤 맥락과 곱해도 잠재축 설명 분산이 1% 다.** 잠재축은
"그날의 상태"이고 D 는 "시즌 누적 수준"이라 직교한다.

미검증 관계 전이 (2022+2023 -> 2024): cur x 월 −0.8 / cur_ball x 월 −0.8 /
cur x 투수팀 −1.4 / **투수 x 타자 매치업 +3.9**(k=500).

부수 — (투수 x 타자) 매치업 첫 측정: 오라클 +61.3, 위약 −0.0(진짜 신호),
커버리지 44.8%, 전이 +0.41%. 게이트의 9분의 1이라 승격 없음.

DECISION: 경기 잠재축의 row-only 대리는 존재하지 않는다. 축 종료.


---

## 2026-08-18 — 매치업 축 종료. 그리고 EB 축소 실패의 원인 규명

`exp/matchup_audit.py`, 학습 0회.

**표본** — pair 당 중앙 7투구, 1~3투구가 19%, 2024 커버리지 44.8%(학습 10투구
이상은 29.7%). 원시 pair 평균의 **83.5% 가 이항 잡음**(신호비 0.165).

**계층 분해** — 투수 주효과 신호비 0.809 / 타자 0.650 / **pair 잔차 0.118**.
주효과를 걷어내도 상호작용은 남는다 (오라클 +61.3, 위약 −0.0 과 일관).

**그런데 시즌을 못 건넌다** — 같은 pair 의 시즌 간 상관이
2022~2023 +0.017~+0.037 / 2023~2024 +0.024~+0.032 / 2022~2024 −0.007~+0.003.

**적용 이득** — A 원시 −85.9 / B 주효과 제거 후 −46.9 / C 전체 계층 −173.8.

🚩 **가장 값진 소득** — 적률법이 고른 k(투수 241 / pair 89)는 전이 문제에
**약 100배 부족**하다(전이 최적은 투수 20,000, pair 500+). EB 는 "이번 시즌
효과를 이번 시즌 데이터로 추정"하는 문제를 풀지만 우리는 "다음 시즌 효과를
예측"한다. **27회차 EB 축소가 LB −13.83 으로 무너진 이유가 이것이다.**

DECISION: 매치업 축 종료 (최대 +0.41%, 게이트의 1/10).


---

## 2026-08-18 — 🚩 매치업 근본 원인 규명. 오라클 프로토콜이 틀렸었다

**가설** — pair 당 중앙 7투구 = 타석 2개인데 오라클의 절반 분할이 **투구 단위**
였다. 같은 타석/경기의 투구가 양쪽에 들어가 셀 평균이 경기 상태를 실어 나른 것.

**검정 — 분할 단위를 거칠게 하니 무너진다**

| 그룹 | 투구 분할 | 타석 분할 | 경기 분할 | 전이 실측 |
|---|---:|---:|---:|---:|
| (투수 x 타자) | +61.3 | +16.2 | **+4.0** | **+3.9** |
| 투수 | +10.9 | +10.5 | **+0.8** | **+1.0** |
| 투수 x 타자손 | +76.8 | — | +20.7 | +6.9 |
| 투수 x 월 | +67.6 | — | +2.8 | −1.1~−1.5 |

**경기 분할값이 전이 실측과 거의 같다. 전이 측정은 처음부터 옳았고 오라클이
틀렸다.** 우리가 "그 시즌에만 존재하는 효과"라 불러온 것의 상당 부분이
**클러스터 누수**였다. 남는 진짜 격차는 투수x타자손 하나뿐이다(+20.7 vs +6.9).

**매치업 감도 축도 기각** — 타자를 저차원 형질로 표현하고 투수별 기울기를
쟀으나(249명) 오라클이 전부 음수(−36.6~−180.6)다. 같은손만 지속성 +0.15~0.20
인데 그건 후처리 dev_platoon 이 이미 하는 일이다.

**CLAUDE.md 갱신** — 오라클은 경기 단위로 가른다(방법론), EB k 는 적률법으로
정하지 않는다(판정 규칙).

DECISION: 매치업 상호작용은 실재하나 future-predictable 구조가 아니다. 종료.


---

## 2026-08-18 — TRANSFER-AWARE SHRINKAGE 이론 종료. 모델이 스스로 흡수한다

4개 전이(2020->2021 ~ 2023->2024)에서 `θ̂_future = ρ x θ̂_current` 를 검증했다.
ρ 는 각 전이에서 그 이전 전이들로만 적합.

**ρ 가 −0.462 / +0.030 / +0.893 / +0.144 로 흩어진다.** 안정 계수 없음.
적률 k 도 90~1,657 로 18배 범위. 과거로 적합한 ρ̂ 는 3개 전이 모두 음수다.

다음 시즌 이득: RAW −708/−410/+44/−400, EB −331/−120/+25/−258,
TRANSFER-AWARE −/−37.5/−7.4/−40.1. **사후최적 ρ 로도 0 근처**(+15.7/+0.1/+22.7/−1.1).

🚩 **원인 — 폴드 s+1 모델은 시즌 s 를 학습에 포함한다.** 그래서 시즌 s 잔차의
지속 성분은 다음 모델이 이미 배워 버리고, 표에는 비지속 성분만 남는다(때로
과보정되어 음의 상관). "큰 오라클, 0 전이"의 두 번째 원인이다 — 첫째는 클러스터
누수(측정), 둘째는 모델의 자기 흡수(구조).

**일반 규칙: 잔차 표를 만들지 않는다.** 후처리 편차 4축이 통한 이유도 분명해졌다
— 그건 잔차 표가 아니라 라벨 편차 표이고 모델과 동시에 만들어져 흡수 대상이
아니었다.


---

## 2026-08-18 — 투수 x 타자손 축 최종. 유일하게 4/4 지속되지만 +0.85%

모든 오라클을 **경기 단위 분할**로 다시 쟀다. 이 축은 이미 Champion 의 후처리
1번축(dev_platoon, k=300, w=0.20, 2024 +11.9)이라 아래는 전부 잔여분이다.

**표본** — 손별 중앙 177~222투구(pair 의 25~30배), 양손 50투구 이상 투수 68~71%,
cold start 22.9%.

**🚩 손 차등의 지속성이 4/4 같은 부호** (+0.102/+0.076/+0.104/+0.105, 기울기
+0.058~+0.182). 투수 주효과 ρ 는 −0.46~+0.89, pair 는 +0.02~+0.04 였다.
**손 차등만이 재현된다.**

**수준은 안 되고 차등만 된다** — 수준 RAW −429.6 / 표준 EB −227.5 (모델 자기
흡수). 차등만 분리하면 k=2000 에서 **+8.0**, 전이별로 +1.5/+3.9/+7.6 (3/3 양수).
k 는 과거 전이에서 골라도 2000 이라 선택 편향 없음.

```
2024_OOS +8.0 = +0.85%   게이트 +3.8% 의 1/4   ->  DECISION 종료
```

관찰 — 이 +8.0 이 현행 k=300 축 **위에서** 나온 것이라 현행이 덜 축소됐을
가능성을 시사한다(오늘의 EB 발견과 같은 방향). 편차가중·k 재최적은 닫힌 축이고
크기가 작아 승격하지 않고 기록만 남긴다.


---

## 2026-08-18 — HAND-DIFFERENTIAL 을 연구 후보로 보관. 라벨/잔차 기반이 갈렸다

**핵심 구분** — 라벨 기반 차등은 현행 `dev_platoon` 과 corr **+0.9151** 로 같은
정보다(현행 잔차와 상관 +0.0065). 1번축 k 만 바꿔 보면 4폴드 전부 악화되어
**현행 k=300 이 최적**임이 재확인됐다.

**잔차 기반 차등은 다른 물건이다** (dev_platoon 과 corr 0.526). 현행 4축 위에
얹어 k 를 훑으면:

```
k       21->22  22->23  23->24  22+23->24   부호
1000     -0.3    +6.3    +9.3     +7.8      2/3
1500     +1.1    +4.8    +8.6     +8.4      3/3
2000     +1.5    +3.9    +7.6     +8.0      3/3
```

k 는 **과거 2전이로만** 골라 1000 -> 23->24 실측 +9.3(+0.99%), 풀링 +7.8(+0.83%).
시드 안정성 +7.4/+8.3 (폭 0.9, Champion 자체 8.3 대비 작음).

**메커니즘** — 투수별 차등 sd 0.1278 로 전체 평균차(0.0306)의 4.2배. 투수마다
다르고 상태에는 의존하지 않는다. 모델과 X/H1 이 흡수하고 남긴 편차다.

**판정: KEEP AS RESEARCH CANDIDATE** (+0.5% 이상, 3/3 전이, 시드 안정, 중복 낮음).
STRONG(+1.5%)·LB(+3.8%) 기준은 미달.

**승격 전 확인 필요** — 배치 시에는 생산 모델이 2024 를 이미 학습하므로 자기
흡수로 이득이 줄 수 있다. 그 구조를 그대로 재현해 재측정해야 한다.

CLAUDE.md 에 연구 정책(5단 게이트)과 이 신호를 보관 항목으로 반영했다.


---

## 2026-08-18 — HAND-DIFF 생산 경로 검증. 자기흡수 실측, 연구 후보 유지

**잔차 구조 명시** — 표는 strictly out-of-fold 잔차로만 만든다(2022 잔차는
2019~2021 학습 모델, 2023 잔차는 2019~2022 학습 모델). 생산 모델의 in-sample
잔차는 쓰지 않는다. 적용 대상은 그 시즌들을 이미 학습한 모델이므로 **자기흡수는
측정에 이미 포함**돼 있다.

**흡수 실측** — 같은 표(2022 잔차)를 학습량이 다른 목표에 적용:
```
A <=2021 (728k, 2022 미학습)   +4.2      B <=2022 (976k, 2022 학습)   +3.2
C <=2023 (1.22M, +2023 학습)   -0.9   <- 흡수 + 표가 낡음
```
학습량 +34% 에 이득 −24%. 배치에서는 **직전 두 시즌** 표를 쓰므로 해당 측정은 +7.8.

**구성 비교** — 2023 +6.3 / 2024 +7.8(k=1000), +8.4(k=1500).
시드 안정성 +7.9/+7.9 (폭 0.0).

**독립성** — dev_platoon 과 corr 0.547 이지만 **대체 불가**(제거 −12.0, 대체로는
+3.4 만 회복). 4축 위에 **가산**해야 +7.8 이다.

```
생산 기대치 = +7.8 x (흡수 보정 약 −15%) ~ +6.6 = +0.70%
DECISION: PERSISTENT WEAK SIGNAL 유지 (정책 3구간). STRONG·LB 미달
```


---

## 2026-08-18 — 🚩🚩 1057.3394 새 최고. 잔차 차등 family 가 실제로 전이됐다

```
cand_submit_3  1057.3394030999  (+7.4168)   ← 새 Champion
cand_submit_2  1053.5950918323  (+3.6725)
```

25회차 이후 첫 상승. 전이율 세 측정이 **0.423 / 0.463 / 0.508** 로 모인다
(기존 앵커는 −0.605~+2.006 로 흩어졌었다).

**검증된 방법론 4가지** — (1) 오라클은 경기 단위 분할 (2) 축소 k 는 과거 전이로
선택 (3) 수준이 아니라 차등만 표로 (4) 독립 약신호는 누적된다.

**게이트 재조정** — 이 family 는 전이율 0.45±0.04 로 안정적이라 LOCAL > 1.1% 면
LB +5 를 기대할 수 있다. 새 피처(in-model) 후보는 기존 +3.8% 유지.


---

## 2026-08-18 — COUNT-ADVANTAGE 기각. 맥락 차등 family 소진

기준선을 **C3(7축)** 으로 바꿔 측정했다 (폴드 2024 재현: 943.8 → 958.1 = +14.4).

**카운트 우위 4정의 전부 기각** — adv −0.7 / adv_x2s −0.6 / margin −0.1 /
disadv −3.4. 오라클이 위약과 같은 대역이다. `adv` 는 기존 2S 축과 corr +0.30 이라
겹치는 몫은 이미 C3 가 갖고 있고, 겹치지 않는 `adv_x2s` 에는 신호가 없다.

**남은 맥락 5종도 소진** — 홈/원정 −0.1, 득점권 −0.2, 타자경험 +0.2,
점수차 −1.7, 이닝후반 +0.1.

**family 결론** — 투수별 이진 맥락 차등을 13정의로 훑어 살아남은 것은
`손 / 2스트라이크 / 주자` 셋뿐이고 전부 C3 에 들어가 있다. 셋 다 **투수의 행동이
실제로 바뀌는 국면**이고, 죽은 것들은 **경기 상황의 분류**일 뿐이다.


---

## 2026-08-18 — 연속 맥락 기울기 기각. 이진 차등이 이미 다 가져갔다

C3(958.1) 위에서 `b_p x (맥락 − 투수별 맥락평균)` 형태로 4개 연속 맥락을 쟀다.

**핵심은 위약 대조다** — 투수를 무작위로 섞어도 기울기 산포가 거의 같다:
count margin 1.28배 / runner load 0.98배 / out state 0.87배 / score 1.31배.
**보이는 기울기 차이의 대부분이 추정 잡음이다.**

C3 증분: count margin +0.6 / runner load +0.0 / out state −0.5 / score −5.0.
runner load 는 기존 이진 주자 축과 상관 +0.74 로 사실상 같은 것이다.

**왜 이진은 되고 연속은 안 되나** — 투수의 행동은 국면 전환(2스트라이크→결정구,
주자→세트포지션)이지 매끄러운 반응이 아니다. "이산 전환은 통하고 매끄러운 곱은
안 통한다"가 차등 family 에서도 성립한다.

유일한 생존자 count margin 도 +0.6(+0.063%)로 손 차등의 1/13, 시드폭 이하다.
DECISION: CLOSE.


---

## 2026-08-18 — NON-PITCHER 차등 종료. 차등 family 전체가 닫혔다

주체를 타자·투수팀·타자팀으로 바꿔 조건 4종 x 12조합을 쟀다 (C3 기준선 958.1,
경기 단위 분할).

**타자 주체** — 오라클 최대 +4.2, C3 증분 −1.6~+0.5. 정보가 없다.

**팀 주체** — 오라클이 최대 +10.8 로 커 보이지만 전이가 무너진다. 특히
`팀 x F 경기` 는 C3 증분 +2.6/+2.7 로 유일하게 커 보이나 전이가
**+6.9 → −10.8 → −13.6** 으로 뒤집힌다 (2023 F 라벨 변경, 이미 닫힌 축).

**3/3 전이 양수 조합이 하나도 없다** -> 전부 탈락.

```
차등 family 총계
  투수 x 이진 13정의  -> 손/2S/주자 3개 생존 (C3 채택)
  투수 x 연속 4종     -> 전부 기각 (기울기 이질성이 위약 수준)
  타자 x 조건 4종     -> 전부 기각
  팀 x 조건 8종       -> 전부 기각
```

살아남은 셋의 공통점 — **투수의 물리적 행동이 실제로 바뀌는 국면**이다.

CHAMPION 1057.3394 (cand_submit_3) 유지.


---

## 2026-08-18 — GAME-ORACLE 분해 완결. +187 은 경기 내 상태다

**분할 사다리** — 경기 오라클이 타석 분할로도 +152.5 로 살아 있으나(타석은 경기
안에 있다) **시간 분할(전반 경기→후반 경기)에서는 +0.0** 이다.

**반복 50회** — 매치업 중앙 +2.4(5~95% +0.5~+5.5), 투수 중앙 −0.0.
앞서 단발로 잰 +4.0/+0.8 은 분포 위쪽이었다. 전이 실측(+3.9/+1.0)과 같은 대역.

**경기 내 예보** — 앞 절반 → 뒤 절반에서 **+135.3**(k=150). 시간 순서를 지킨
측정이라 누수가 아니다. 그러나 2025 에서 같은 경기 다른 행은 규정 4로 금지.

**지속성** — 경기 내 절반 간 +0.13~0.25 / 다음 경기 +0.029 / 임의 두 경기 +0.007.

**분산분해(이항 잡음 제거)** — 진짜 sd: 투수 0.0458 / 경기 0.0764 / 타석 0.1586.

```
IS +187 REAL? = partially (경기 내로는 실재, 사전 예측으로는 0)
PERSISTENT_GAME_EFFECT = no      PERSISTENT_MATCHUP_EFFECT = no
LEGAL RECOVERABLE = +0.25~0.41% (매치업, 이미 게이트 미달로 종료)
NEXT = 없음 — 가장 큰 잔여 신호가 경기 내 상태인데 규정 4가 막는다
```


---

## 2026-08-18 — 관계형 국면 기각 + FINAL 아티팩트 완성

**관계형 국면 6종**(같은 행 안의 비교를 국면으로) 전부 기각. 잔차상관이 잡음
대역(|0.002|)이고 오라클이 위약 수준이다.

**FINAL** — C3 구조 그대로 표 원천만 2시즌 → **5시즌(2020~2024 OOF)**.
원천 시즌별 폴드 2024 이득: 1개 +15.2 / 2개 +14.4 / 3개 +16.3 / 4개 +19.1,
폴드 2023 도 3개가 최고(+11.6). 시드 +16.1/+16.8 로 안정.

기각한 것 — 전역 가중(과거 최적 1.5 → 2024 +8.4로 붕괴), cold-start 대체(−0.5).

```
submissions/cand_final.zip   sha256 5152edc7...
verify 1~4 통과 · 추론==분석 1.041e-17 · 행 독립성 0.000e+00
로컬 2024 C1 대비 +19.1 (C3 +14.4) = C3 대비 +4.7 (+0.50%)
예상 LB 약 +2.3점 (1059.6 내외)
```


---

## 2026-08-18 — 🚩 FINAL 실패 (1047.3863). 표 재추정은 전이가 음수다

```
C1 1049.9226  C2 1053.5951  C3 1057.3394  FINAL 1047.3863
FINAL 은 C3 대비 −9.95, C1 보다도 −2.54 낮다.  전이율 −1.890
```

**잘못 읽은 것** — 원천 시즌 곡선이 비단조였다(두 폴드 모두 n=1 > n=2).
그 비단조가 잡음의 신호였는데 n=3 의 2/2 지지만 보고 "가용 데이터를 다 쓴다"로
읽었다. 실제로는 **최신성 > 표본량**이고, 오래된 시즌 차등은 2025 에 해롭다.

**교훈** — 축 추가(+0.42~+0.51 전이)와 표 재추정(전이 불안정)은 성질이 다르다.
같은 판에서 전역 가중 w 도 과거 최적 1.5 가 2024 에서 붕괴했었다. 표 제작
파라미터는 과거 폴드로 고를 수 없다는 신호가 두 번 나왔는데 원천 시즌 수에는
그 경계를 적용하지 않았다.

**Champion 은 C3 유지. 표 원천은 직전 2시즌 고정.**

## 2026-08-18 자율 연구 엔진 — 국면 family 전수 소진

`research/` 에 디스크 기반 연구 상태를 만들고 복구 테스트를 통과시킨 뒤 3건 수행.

```
EXP001  채택 3축의 2차 상호작용        REJECTED  최선 +0.5 (손x2S)
EXP002  새 국면 6종                  REJECTED  전부 오라클 <= 위약
EXP003  이진 국면 243개 전수          REJECTED  통과 12개가 전부 타자손 재표현
```

Champion 1057.3394 유지. 제출 후보 없음.

## 2026-08-18 정책 전환 — LB 를 측정기로 쓴다

제출이 **5회/일 갱신**임을 확인했다. `+3.8% 게이트`와 TYPE B 동결은 제출이
희소하다는 전제 위에 있었으므로 전제가 사라졌다. 로컬 폴드는 시드 잡음 ±7.2
인데 LB 는 245,789행이다 — 측정기의 정밀도가 반대다.

첫 대상은 **보정 가중**이다. 이득(w) = A(2bw − w²) 이고 LB 앵커 두 점
(w=0 1049.9226, w=1.0 1057.3394)이 이미 있어 한 점만 더 찍으면 b 가 풀린다.
w=1.0 에서 이득이 양수였다는 사실만으로 **b>0.5 는 이미 증명**돼 있다.

```
cand_wd060.zip   차등 3축 x0.60   bd798b9a...
cand_wd035.zip   차등 3축 x0.35   306e6a49...
cand_dev075.zip  편차 4축 x0.75   fd77227b...
```

셋 다 모델 재학습 없음(스칼라만 변경), 행 독립성 0.00e+00, verify 4단계 통과.
Champion cand_submit_3.zip 은 무결하게 보존.

### 대회 조건 확정 (2026-08-18)

```
종료까지 10일 · 5회/일 갱신 · 오늘 3회 남음 -> 총예산 약 48회
최종 순위 = **최고점**, private 분할 없음
```

**문제의 성격이 바뀐다.** 일반화 격차가 존재하지 않으므로 LB 가 곧 목적함수이고,
같은 제출은 같은 점수라 표본 잡음도 0 이다. 통계 추정 문제가 아니라 **결정론적
함수의 예산 제한 최적화**다. +0.1 도 진짜 개선이고, 나쁜 제출의 비용은 0 이다.

따라서 배치로 쏘지 않고 **순차 적응**한다 — 한 점 찍고 곡선을 갱신해 다음 점을
고른다. 회전 속도를 위해 `exp/build_w.py` 로 후처리 7축 가중을 초 단위로 빌드한다.

---

## 2026-08-20 06:41 — 🚩 38회차 `cand_dcfull` = 1066.8797. 감쇠 표가 전이되지 않았다

```
Champion cand_bw25   1071.8146          dcfull   1066.8797   -4.9349
로컬 2024  962.6 -> 969.7 (+7.1, +0.738%)   LB -0.460%   전이율 -0.624
```

3폴드 전부 양수(+3.0 / +5.1 / +7.1)였고 구조 청정·단독행 대 배치 0.00e+00 이었다.
로컬은 맞았고 전이가 뒤집혔다.

### 귀속 — 가중 이동은 무죄, 감쇠 표가 전부다

이미 풀려 있는 LB 곡선 두 개를 쓰면 성분이 분리된다.

```
대비 가중   gain(w) = 32.146w - 24.729w^2      wc 0.65 -> 0.80    -0.557
타자 수준   gain(w) = 2.414(4.21w - w^2)       wb 2.50 -> 1.75    +0.072
투수 수준   gain(w) = 0.371(4.21w - w^2)       wp 1.00 -> 2.00    +0.449  (b 차용 추정)
                                              ------------------------
                                              곡선 설명분        -0.036
                                              **감쇠 표 잔여     -4.899**
```

즉 `dcfull` 의 손실은 사실상 **전량이 감쇠 표 재구성**이다. 가중 이동 세 개는
서로 상쇄해 0.04 밖에 안 된다.

### 8/19 의 재해석이 반증됐다

어제 나는 30회차 실패(원천 5시즌 균등, LB −9.95)를 "창을 넓힌 게 아니라 오래된
시즌을 같은 무게로 쓴 것이 원인"이라 읽고 8개월 된 규칙을 뒤집었다. 근거는
로컬뿐이었다 — 균등 4시즌 2024 −3.4 vs 감쇠 4시즌 +1.6~+7.1.

```
균등 다중시즌   LB -9.95   (30회차)
감쇠 다중시즌   LB -4.93   (38회차)   <- 절반이지만 여전히 음수
```

**감쇠는 손실을 줄였을 뿐 부호를 바꾸지 못했다.** 원 규칙(표 원천 = 직전 2시즌)이
옳았고, 로컬 +7.1 은 목표 폴드에서 γ 를 고른 데서 온 값이다.

### TYPE B 는 이제 4연패다

```
30  표 원천 2->5시즌 균등    로컬 +0.50%   LB  -9.95   전이 -1.89
32  단조 제약               로컬 +0.98%   LB -65.73   전이 -6.32
37  볼우위 신축 (TYPE A)    로컬 3/3 양수  LB  -7.07   전이 -3.40
38  감쇠 표 재구성          로컬 +0.74%   LB  -4.93   전이 -0.62
```

**로컬 3폴드 양수는 이 중 어느 것도 막지 못했다.** 반대로 LB 로 검증된 축의
가중 조정은 4/4 양수다(wc 곡선 3점, wb 곡선 3점).

### 그래서 남은 것은 두 개뿐이다

```
wp 1.00 -> 2.00   예상 +0.45   cand_pw20.zip 빌드 완료
wb 2.50 -> 2.10   예상 +0.38   빌드 필요 (스칼라 1개, 초 단위)
```

둘 다 이미 LB 로 검증된 축의 **가중만** 바꾸는 안전 등급이다.

## 2026-08-21 10:20 — 🚩 43회차 `cand_opt1` = 1074.0991. 해석적 최적이 빗나갔다

```
h1  1075.4602      opt1  1074.0991      -1.3611
예상 1076.7595                          어긋남 -2.6603
```

표·모델·아핀이 h1 과 **완전히 같고** 9가중만 움직였다. 버그가 아니다.

### 원인 — 11점은 전부 5차원 블록 부분공간 안에 있었다

편차 4축은 언제나 전역 배수로만 움직였고 2S 와 주자는 서로 달라진 적이 없다.
그래서 LB 가 실제로 본 것은 5방향뿐이고 **4 dof 는 한 번도 측정되지 않았다.**
EXP042 는 그 4 dof 를 `g_i = t_block x 로컬 cov(c_i, y_2024)` 라는 **모양 가정**
으로 채웠고, opt1 은 정확히 그 미측정 방향으로 이동했다.

### 12점 재적합 — 어느 방향이 틀렸는지 **구별되지 않는다** (EXP043)

| 구조 | 모수 | dof | RMS | opt1 잔차 | h1 잔차 |
|---|---:|---:|---:|---:|---:|
| S0 현행 5블록 | 7 | 5 | **0.629** | +1.41 | -0.85 |
| S1 편차 {01\|23} | 8 | 4 | 0.2887 | -0.00 | +0.01 |
| S2 편차 {0\|123} | 8 | 4 | 0.2887 | +0.00 | +0.01 |
| S3 편차 {013\|2} | 8 | 4 | 0.2887 | -0.00 | +0.01 |
| S4 대비 {2S\|주자} | 8 | 4 | 0.2887 | -0.00 | +0.01 |

**블록을 어느 것이든 하나 쪼개면 전부 같은 RMS 다.** 관측 하나가 4 dof 중
무엇을 움직였는지 원리적으로 못 가른다. 추가 제출 없이는 식별되지 않는다.

### 🚩 부산물 — 보정 벡터 상관 구조가 드러났다

```
dev0_platoon x c_hand    0.547
dev3_runner  x c_runner  0.510
dev2_count   x c_2strike 0.271
dev1_adv     x c_2strike 0.154
나머지 31쌍               |r| < 0.04
```

EXP042 가 "최대 0.547" 로만 적었던 것의 정체다 — **강한 쌍이 딱 둘이고 둘 다
같은 정보의 두 표현**이다(플래툰 편차 대 손 대비, 주자 편차 대 주자 대비).
그래서 편차 전역 배수를 m*=0.478 로 절반 넘게 줄인 뒤에는 짝인 대비 축이
더 실릴 여지가 생긴다. 42회차의 `∂S/∂w_손 > 0` 실측과 정확히 맞는다.

## 2026-08-21 11:05 — EXP044 강건 최적. 목적함수를 바꿨다

opt1 은 **단일 적합의 점추정**을 최대화한 값이었다. 목적함수를 바꾼다.

```
기존   max_w  S_hat(w)                    <- opt1 이 이걸로 나왔다
지금   max_w  min_e [ S_e(w) - S_e(h1) ]  <- 구조 6 x LOO 13 = 앙상블 78개의 최악 증분
그리고 편차 4축의 **내부 모양은 h1 에 고정** (전역 배수 하나만 허용)
```

### 소급 검증 — 이 방법이면 opt1 을 막았다

| 후보 | 최악 | 평균 | 최선 |
|---|---:|---:|---:|
| **강건최적** | **+0.502** | +0.579 | +1.038 |
| wb=2.35 | +0.151 | +0.204 | +0.321 |
| wb=2.60 | +0.104 | +0.209 | +0.442 |
| h1 (현행) | 0 | 0 | 0 |
| `dev23=0` (opt1 방향) | **-4.05** | -1.36 | +3.01 |
| `dev01=opt1` (opt1 방향) | **-4.35** | -0.10 | +6.57 |
| 평균최적 (점추정) | -1.15 | +0.96 | +3.98 |

**최악값이 양수인 후보는 셋뿐이고 opt1 방향은 -4 대다.** 점추정 최대화는
`평균최적` 줄이 보여주듯 최악 -1.15 를 감수하는 도박이다.

### 산출물 — `cand_rob1.zip`

```
        h1        rob1
dev x   1.0000    1.0428     (편차 모양 고정, 전역 배수만)
c_hand  0.6500    0.7037
c_2S    0.6500    0.7753
c_run   0.6500    0.7753
L_pit   2.0000    1.9850
L_bat   2.1000    2.5119
```

`sha256 af34bb0f864cd61a` · 표 9개가 h1 과 **비트 단위 동일**(재학습 없음) ·
구조 감사 청정 · 단독행 대 배치 0.000e+00 · verify 4단계 통과 · 추론 6.6초/600초.
**TYPE = 가중 조정(안전 등급).** 표·모델·아핀 불변.

### 이번에 만든 상설 도구

```
exp/lb_obs.json      LB 관측 원장. 세 스크립트가 여기만 읽는다 (하드코딩 제거)
exp/add_obs.py       zip 에서 가중을 읽어 관측 추가 — 손으로 9숫자 옮기지 않는다
exp/reweight.py      가중 9개만 다시 써서 후보 생성 (재학습 없음, 수 초)
exp/rowindep.py      zip 안의 script.py 로 단독행 대 배치 검사 (범용화)
research/exp043_refit.py   블록 구조 비교 + LOO
research/exp044_robust.py  앙상블 78 최악 증분 최대화
```

## 2026-08-21 13:10 — 44회차 `cand_rob1` = 1075.4566. **−0.0037** 이 가장 값진 관측이었다

```
h1 1075.4602  ->  rob1 1075.4566   -0.0037      앙상블 최악 약속 +0.502
```

6개 가중을 크게 움직였는데 점수가 셋째 자리까지 안 변했다.

### 첫 교훈 — min-max 앙상블도 **하한이 아니다**

84개 적합 전부가 +0.5 이상을 약속했는데 실제는 0 이다. 적합들은 **같은 함수형과
같은 로컬 모양 가정을 공유**하므로 그 스프레드는 계통 오차를 덮지 못한다.
앙상블 폭은 *모수* 불확실성이지 *모형* 불확실성이 아니다.

### 두 번째 교훈 — 직접 측정된 3점 곡선이 13점 전역 적합보다 낫다

13점 앙상블은 `wb = 2.51` 을 권했다. 그런데 34·35·36 세 점으로 직접 푼 타자
곡선은 최적이 **2.105** 이고 41회차가 그 예측(+0.377)을 실현해 검증까지 됐다.
**앙상블 쪽이 인공물이었다.**

### 귀속 — 측정된 곡선만으로 −0.0037 이 완전히 설명된다

```
c_hand    0.6500 -> 0.7037     +0.128
L_batter  2.1000 -> 2.5119     -0.400      <- 유일한 손실원
L_pitcher 2.0000 -> 1.9850     +0.008
편차 전역  x1.0   -> x1.0428     -0.005
                    소계        -0.268
                    실측        -0.004
c_2strike + c_runner 0.65 -> 0.7755   **+0.265**   <- 잔여 전부
```

**대비 블록은 0.65 위로 올라가는 것이 맞다.** 36회차가 "LB 최적 wc=0.650 확정"
이라 적은 것은 편차가 로컬 가중(m=1.0)일 때의 값이고, 그 뒤 편차를 m*=0.478 로
반감하면서 짝 축(r=0.547 / 0.510)에 실을 여지가 생겼다.

**42회차의 구조 추론 "2S/주자 중 최소 하나는 낮춰야 한다"는 무효다** — 그 전제인
전역 최적 0.65 가 옛 편차 가중에서 잰 값이었다. 실제로는 **셋 다 올라가야 한다.**

### 45회차 후보 `cand_rob2` — 단일 좌표 복귀

```
rob1 에서 L_batter 만 2.5119 -> 2.105 (3점 곡선 최적)
예상 +0.396   ->  1075.856
```

이 추정은 `c_hand` 의 A 가정에 **의존하지 않는다.** 필요한 것은 타자 곡선
하나뿐이고 그것은 41회차에 이미 실현 검증됐다.

```
sha256 c9b3c4b3ec4bcfa4  ·  표 h1 과 동일  ·  구조 청정  ·  단독행 0.000e+00
verify 4단계 통과  ·  추론 6.6s/600s  ·  TYPE = 가중 조정 (안전 등급)
```

### 남은 미완 곡선 (측정 기준)

| 축 | 측정점 | 상태 |
|---|---:|---|
| L_batter | 3 | ✅ 소진 (최적 2.105) |
| 편차 전역배수 | 3 | ✅ 소진 (최적 0.4784) |
| **c_hand 단독** | 2 | 🚩 미완. `b > 0.525` 확정, 최적 추정 0.77 |
| **c_2S + c_runner** | 1 (rob1) | 🚩 최적 추정 0.81. 서로 분리된 적 **없음** |
| L_pitcher | 2+귀속 | ⚠ 최적 1.524 추정, 남은 +0.132 |

## 2026-08-21 16:30 — 🚩🚩 45회차 `cand_rob2` = **1075.9242. 새 Champion.**

```
h1 1075.4602  ->  rob2 1075.9242   +0.4640      예상 +0.396   오차 +0.068
```

44회차의 −0.0037 을 **측정된 좌표 곡선**으로 분해한 귀속이 그대로 실현됐다.

### 방법론이 갈렸다

| 모형 | 예측 | 실측 |
|---|---:|---:|
| 전역 비율 모형 (EXP042) 43회차 | +1.30 | **−1.36** |
| 〃 44회차 (min-max 앙상블 최악) | +0.50 | **−0.004** |
| **좌표별 3점 곡선** 41회차 (타자 몫) | +0.377 | 실현 |
| **〃** 45회차 | +0.396 | **+0.464** |

**미식별 방향으로 외삽하는 모형은 두 번 실패했고, 측정된 좌표 안에만 사는
모형은 두 번 맞혔다.**

### 대비 3축의 최적은 0.65 가 아니다

36회차가 "LB 최적 wc=0.650 확정"이라 적은 것은 **편차가 로컬 가중(m=1.0)일 때**의
값이다. 이후 편차를 m*=0.478 로 반감하면서 짝 축에 여지가 생겼다.

```
dev0_platoon x c_hand    r = 0.547
dev3_runner  x c_runner  r = 0.510
```

실측된 새 최적대는 **0.70 ~ 0.78** 이다.

## 2026-08-21 17:00 — EXP048 가법 OLS. **후처리 9가중 공간 소진**

`gain = 2P w − Q w²` 는 모수에 선형이므로 OLS 로 닫힌 형태로 푼다 (11모수, 13관측,
dof 2). 43회차 `cand_opt1` 은 dev 가 전역 배수가 아니라 정의역 밖이라 제외한다.

| 블록 | A=Q | ± | 최적 b | ± | rob2 |
|---|---:|---:|---:|---:|---:|
| dev_m | 14.06 | 19.02 | 0.5324 | 1.3038 | 0.4988 |
| c_hand | 38.93 | 23.80 | 0.5513 | 0.4552 | 0.7037 |
| c_2S+run | −14.33 | 23.19 | 0.3725 | 1.0125 | 0.7753 |
| L_pitcher | 0.69 | 0.99 | 1.3581 | 2.2586 | 1.9850 |
| **L_batter** | **2.38** | **0.46** | **2.1181** | **0.4782** | 2.1050 |

**L_batter 만 유의하고 그것은 이미 최적에 있다.** 나머지는 표준오차가 계수만 하거나
더 커서 `c_2S+run` 은 부호조차 정해지지 않는다. LOO 14적합에서 **최악 증분이 양수인
후보가 0개**이고 최선이 `L_batter 50%` 의 −0.000(= 움직이지 말라)이다.

```
이득의 감쇠   39회차 +2.01 -> 40 +1.06 -> 41 +0.58 -> 45 +0.46 -> 모형 한계
```

**남은 슬롯을 가중 조정에 쓰지 않는다.**

## 2026-08-22 (세션 종료 시점) — 🚩🚩 **Champion 1076.2214.** 그리고 자(尺)가 고장나 있었다

### 오늘 제출 5건

| 회차 | 후보 | LB | |
|---|---|---:|---|
| 47 | `cand_aux1` | 1067.7618 | 보조 1열. **손실이 비례하지 않고 상수** (aux8 −7.79 vs aux1 −8.16) |
| **48** | **`cand_mir`** | 🚩 **1076.2214** | **새 Champion.** 미측정 4dof 미러 **+0.2973** |
| 49 | `cand_mir2` | 1075.8671 | 미러 t=2. 3점 해법 완성 → `t*=0.9562`, mir 가 이미 최적 |

### 🚩🚩🚩 EXP057 — 로컬 폴드는 8개월간 동전이었다

로컬(2024)과 LB 를 **둘 다** 가진 변경 14건 전수:

```
corr(로컬, LB) = +0.817   ->  D 한 극단점을 빼면 **+0.111**
부호 일치       = **7/14 = 50%**
유형별   후처리 4/5 · in-model 3/6 · 표 제작 0/2 · 모델 적합 0/1
```

CLAUDE.md §5 의 "2024 부호가 LB 와 예외 없이 일치한다"는 **틀렸다** (반증 표기 완료).
그리고 이것이 8/21~22 결과 전부를 설명한다 — **로컬로 검증하고 낸 것은 전부 실패,
로컬을 안 쓴 것(미러·시드)은 전부 성공.**

### ⚠️ 그리고 §5-d 를 내가 과잉교정했다가 되돌렸다

처음에 "로컬이 아무리 좋아도 안 낸다"고 썼는데 **결정이론을 빼먹었다.**

```
최종 순위 = 최고점  ->  하방 0  ->  판정 기준은 E[이득] 이 아니라 **E[max(0, 이득)]**
14건으로 계산   E[이득] +0.098%   vs   **E[max(0,이득)] +0.852%** (D 제외 +0.228%)
```

**부호 50% 가 "내지 마라"를 뜻하지 않는다.** 지는 쪽이 공짜면 동전의 기대값은 양수다.
바뀌는 것은 제출 여부가 아니라 **순서(보장 > LB연장 > 로컬후보)** 와 **믿음의 크기**다.

### 🚩🚩 시드 앙상블 — 유일하게 **수학적으로 보장**되는 이득

```
rho = cov(μ,y)/sqrt(var(μ) + σ²/k)        k 에 단조 증가. 실패 불가
실측 σ²/var(p)  2022 0.0041 · 2023 0.0048 · 2024 0.0076
k=3 -> 30 이득  +1.32 / +1.56 / +2.46   ->  **3폴드 평균 +1.78**
```

원장의 `시드 3→7 = +0.6, 기각` 은 **시드 노이즈 ±7 로 +2 짜리를 재려다 못 본 것**이다.
로컬이 안 보인다는 이유로 이론적으로 손해가 불가능한 변경을 8개월간 버렸다.

### 오늘 죽은 것 (전부 로컬 검증 후보였다)

```
PM 신규/교체 · q_cell 소프트라벨(-242) · ft2 인수분해 · AUX 8열/1열 ·
팀·구장 수준축 · 10번째 대비축 · 조건부 가중(corr -0.769) · 셀 오프셋 캘리브레이션 ·
구성/실력 분리 · 평툰격차 예측 모델(위약 이하)
```

### 🚩 남은 격차의 정체가 숫자로 특정됐다

```
전이 채널(규정 4 위반) = +163.6   ->  약 1220
D 가 합법 회수한 몫      = +85.65
격차                     ≈ +78   =  투수 x 타자손 **시즌 내** 상태 (오라클 +76.8 과 일치)

그 시즌 평툰 격차의 진짜 신호 sd = 0.0269 (관측의 76%가 이항잡음)
합법 관측치로 설명 가능한 몫 = **3.7%** (나머지는 평균회귀 — c_hand 가 이미 사용)
완벽히 알면 폴드 +72.8  <- 오라클 +76.8 과 검산 일치
```

**`asof_*` 에 타자손 분할 열이 없어 한 행에서 대수적 복원이 불가능하다.**
유일한 경로는 평가셋 다른 행 읽기 = 규정 4. **가지 않는다.**

## 2026-08-24 — H-K1. **수준 축의 모양**을 8개월 만에 처음 건드린다 (제출 0회)

### 착상 — 최적 가중이 둘 다 2 근처인 것은 과축소의 서명이다

```
투수 수준  k=50000  LB 최적 w=1.985
타자 수준  k=20000  LB 최적 w=2.105
```

표가 `n/(n+k)` 로 감쇠돼 있으면 스칼라 `w` 로 되돌리려 하는데, 감쇠는 개체마다
다르다(이질적). **스칼라로는 원리적으로 못 되돌린다.** 올바른 수정은 `w` 를
올리는 것이 아니라 `k` 를 내리는 것이다.

EXP041 이 이 좌표를 닫았으나 근거 둘 중 하나만 유효하다.

```
(a) k >= 5000 에서 corr(c_k, c_20000) >= 0.9925 -> w 와 축퇴        [유효]
(b) k = 500~2000 은 모양이 다른데 로컬 3폴드가 더 낮다              [무효 — EXP057]
```

### 표의 제작 형태를 **재현으로** 확정했다

빌더가 저장소에 없어서(`build_lvlw.py` 주석) 역산으로 복원했다.

```
원천 창 열거   2023+2024 에서 투수 510 · 타자 526  ->  표 키 집합과 **완전 일치**
              (누락 0 · 여분 0 · 최소 n 필터 없음)
champ_oof 재현  투수 k=50000  corr +0.9976  기울기 0.9914
               타자 k=20000  corr +0.9983  기울기 1.0004
```

따라서 `v_e = Σresid_e/(n_e+k)` 이고 `S_e = v_e·(n_e+k)` 로 **정확히** 역산된다.
근사 없음 · 학습 0회 · 실제 생산 잔차 그대로. 도구는 `exp/build_lvlk.py`.

### 모양만 움직인다 — 크기는 이미 최적이다

`k` 를 바꾸면 모양과 크기가 동시에 바뀐다. 그런데 크기는 EXP048 이 3점 곡선으로
확정했다(L_batter A=2.38 b=2.118±0.478, 현행 2.105 = 최적). 그래서 2024 조회
분포에서 **행 sd 가 같아지도록** 정규화해 크기를 고정했다. 48회차 미러와 같은 규율.

    v_t[e] = v0[e] + t·( alpha·v_k[e] − v0[e] ),   alpha = sd(c_v0)/sd(c_vk)

### 기하 (2024 조회 분포, 타자 축)

| k | corr(c_k, c_0) | 직교성분 | 정규화 w |
|---:|---:|---:|---:|
| 2000 | 0.97864 | **20.6%** | 0.4617 |
| 5000 | 0.99516 | 9.8% | 0.7466 |
| 20000 | 1.00000 | 0.0% | 2.1050 |
| 50000 | 0.99962 | 2.8% | 4.7969 |

`k=2000` 을 골랐다 — 미러(+0.2973)와 견줄 만한 걸음이고, LB 는 측정 잡음이
없으므로 걸음이 클수록 정보가 많다. 하방은 0 이다.

### 산출물 — 관문 3종 전부 통과

| 파일 | t | sha256 | 구조 | 단독vs배치 | verify |
|---|---:|---|---|---|---|
| `cand_kbf.zip` | **+1** | `a1de87836b0cb2de` | ✅ 청정 | **0.000e+00** | 1,2,3,4 |
| `cand_kbr.zip` | −1 | `1cb7fb1ab07cdb9f` | ✅ 청정 | **0.000e+00** | 1,2,3,4 |

9가중은 **한 개도 바뀌지 않았다.** 추론 6.8초/600초. `t=−1` 의 행 sd 가 1.0418
인 것은 정상이다 — 3점 해법은 직선이 `t` 에 선형이어야 성립하므로 끝점만
정규화한다. 크기 4.2% 증가는 w 곡선상 **−0.013점**으로 무시 가능하다.

### 🚩 관문 2 의 함정 하나 (도구를 고쳤다)

처음 만든 `exp/rowindep_zip.py` 가 `train.csv` 앞쪽(2019)에서 검사행을 뽑았다.
표 키가 2023+24 라 **바뀐 항목을 하나도 건드리지 못하는 검사**였다. 2024 행만
쓰도록 고쳤다. 이후 4개 후보 전부 `0.000e+00`.

### 🚩 로컬이 자가 아님을 다시 확인했다

폴드 2024(in-sample) 값이다 — **판정 근거가 아니라 반례로 적는다.**

```
cand_q1  1096.19    cand_kbf 1072.13    cand_mir 1064.29    cand_s15 **1054.69**
```

수학적으로 이득이 **보장된** 시드 앙상블이 로컬에선 Champion 보다 −9.6 이다.
시드 평균이 in-sample 과적합 분산을 줄이기 때문이고, 정확히 그래서 8개월간
`시드 3→7 = +0.6, 기각` 이라고 적혀 있었다.

### 대기 중인 후보 4개 (전부 관문 통과)

| 파일 | 자격 | 기대 | sha256 |
|---|---|---|---|
| `cand_s15` | 1 (수학적 보장) | **+1.4** | `9da14385ff6f7634` |
| `cand_q1` | 2 (LB 측정 방향 연장) | +0.3 | `a72201b7885f8934` |
| `cand_kbf` | TYPE B · LB 직접 측정 | ±1~3 | `a1de87836b0cb2de` |
| `cand_kbr` | 위의 반대편 (kbf < mir 이면) | — | `1cb7fb1ab07cdb9f` |

## 2026-08-24 19:05 — 🚩🚩🚩 **1076.2214 → 1088.3910.** 하루에 +12.17

### 제출 5건과 결과

| 회차 | 후보 | LB | Δ | |
|---|---|---:|---:|---|
| 51 | `cand_s15` | 1075.0465 | **−1.17** | 🚩 "수학적 보장"이 반증됐다 |
| 52 | `cand_kbf` | 1069.5182 | −6.70 | H-K1 t=+1 (날카롭게) |
| **53** | **`cand_kbr`** | **1081.6013** | **+5.38** | 미러. 두 번째 성공 |
| **54** | **`cand_kb3`** | **1088.3910** | **+6.79** | 2차식이 예측한 그 값 |

### 🚩🚩🚩 이 직선 위에서 점수는 **t 의 정확한 2차식**이다

```
t=+1, t=−1  두 점만으로 정한 A(2bt−t²) 가  t=−3 을 **오차 2.3e−13** 로 맞혔다
A = 0.6616832   Ab = −3.0207928   b = **−4.565316**
t* = −4.56532   ->  예측 최대 **1090.0123**
```

대조적으로 **2024 분모를 쓴 유리함수 모형(EXP042 방식)은 `t*=−1.33` 을 주고
측정점조차 −3.23 어긋났다.** 43·44회차 실패와 같은 서명이다.

```
로컬 분모를 섞으면 틀린다.  LB 3점만으로 푸는 좌표별 곡선은 자릿수까지 맞는다
```

### 무엇을 발견한 것인가 — 타자 수준 표가 **과소축소**였다

`t<0` 은 저-n 타자의 보정 **부호를 뒤집고** 고-n 타자를 증폭한다.
반전 경계는 t=−1 에서 n<217, t=−3 에서 n<1544, t=−4.5 에서 n<2500 근처다.

```
극한 방향 ∝ v0 − α·v_2000 ∝ [0.78066·n − 2386.8] / [(n+20000)(n+2000)]
= **고-n 타자와 저-n 타자의 대비**.  저-n 개체의 잔차 평균은 잡음이고 유해했다
```

원장의 "전이 최적 k 는 EB 값의 10~100배" 가 **아직도 부족했다.** k=20000 (적률법
241 의 83배)도 과소축소였다. 이 축의 블록 이득은 +10.70 -> **+24.5** 로 커진다.

### 🚩🚩 그리고 시드 앙상블의 "수학적 보장"이 반증됐다 (51회차)

교란이 아니다 — `s15` 앞 3개 모델이 `mir` 과 예측 최대차 **0.000e+00**, 9가중·
표 9개·아핀·피처 전부 동일. 차이는 시드 12개뿐인데 −1.17 이다.

```
보장은 **모집단** rho 에 대한 것이고 우리가 재는 것은 고정 245,789행의 **실현** rho 다
cov(eps,y) 는 기대값 0 이지만 실현값은 0 이 아니고, 그 산포가 이득보다 크다

15모델 3-부분집합 455개 2024 점수 sd = **40.75**   (iid 가정 예측 9.20)
-> **유효 자유도가 20배 작다.** 시드 차이는 행 단위 백색잡음이 아니라 영역 단위로 결맞다
mir 의 시드 42/43/44 는 3-부분집합 분포 상위 **85.7%** -> 이미 +3 의 시드 운을 담고 있다
```

**따름 규칙 — 재학습을 수반하는 변경은 ±수 점의 정렬 잡음을 깔고 있고,
결정론적 후처리 변경(모델 고정)에는 그 잡음이 없다.** EXP057 의 유형별 적중률
(후처리 4/5 vs in-model 3/6)이 이것으로 설명된다. **`cand_s30` 취소.**

### 대기 중인 후보 (전부 관문 3종 통과)

| 순서 | 파일 | sha256 | 근거 | 예측 |
|---|---|---|---|---|
| **1** | `cand_kb45` | `8c8c624c3837a8d7` | 타자축 t=−4.5 (t*=−4.565) | **1090.0095** |
| 2 | `cand_kp3` | `57cd8f32e6b45db6` | **투수축** 같은 수술 t=−3 | 미지 |
| 3 | `cand_kp6` | `0d5ca51b02aeccd1` | 〃 t=−6 (2점이면 곡선 확정) | 미지 |
| — | `cand_kp1` | `25beb534816e78e9` | 〃 t=−1 (보수적 대안) | 미지 |
| — | `cand_kb2`/`cand_kb3` | — | 이미 소진된 직선 위 |

투수축 후보는 전부 **`cand_kb45` 위에** 얹혀 있다 (다른 축 고정 — 41회차 교훈).

---

## 2026-08-24 21:xx — 내일 5슬롯 확정 · `submissions/` 정리 (EXP060 준비)

### 타자축을 뜯어보니 우리가 산 물건은 이것이다

`S_e = v_e(n_e+k)` 역산으로 t=−4.5653 의 실효 가중을 풀었다.

    v_e ∝ S_e·g(n),  g(n) = 4.5638·(n − 1949.8) / [(n+20000)(n+2000)]
                          ≈ r̄_e · n(n−1950)/(n+2000)      (n <= 5319 << 20000)

| | 값 |
|---|---|
| 타자 n (2023+24) | 중앙 **314**, 90% 3,284, 최대 **5,319** |
| 원 표의 축소율 `n/(n+20000)` | 최대 **0.210** — 전원이 사실상 잔차 **합** `S_e` 에 비례했다 |
| t=−4.5 부호 반전점 | **n=1,937** -> **424/526명**(2024 행 32.5%)이 음수 가중 |
| 보정 에너지 집중 | 상위 **26명(5%)이 82.5%**, 상위 5명이 **41.3%** |

**+13.79 는 "타자 개인 효과 추정"이 아니라 고출장 타자 ~26명의 계통 편향을
증폭하고 저-n 타자의 잔차 평균은 부호를 뒤집어 지운 것이다.** LB 가 세 번
연속 요구한 방향은 **n-가중 강화** 하나다.

### 타자축에 남은 좌표 — 슬롯당 정보량으로 정렬

이미 측정된 2평면 `span{v_20000, v_2000}` 밖 성분으로 쟀다 (2024 조회 분포).

| 방향 | 평면 밖 | 판정 |
|---|---:|---|
| **크기(계수 합) 구속 해제** | t-선과 **69.3% 직교** | 🔴 내일 4·5번 |
| **저-n / 고-n 분리** `r̄·1[n>1950]` | **44.8%** | 다음. "음수가 정답인가 0 이 정답인가"에 답한다 |
| **순수 n 축** `f(n)`, 잔차 없음 | **92.7%** | 사실상 새 좌표. 재학습 0 -> 시드 잡음 없음 |
| 창 2->3시즌 (`champ_oof` 에 2022 있음) | — | 재학습 0회. n 중앙 314->506, n>1950 이 102->133명 |
| ~~k 값 더 훑기~~ (k=200 / 6000 / ∞) | **1.3~21.6%** | ❌ 이미 잰 정보다. 슬롯 낭비 |
| ~~타자 x 맥락~~ | — | ❌ §7 로 닫힘 (투수 동작이 안 바뀐다) |

🚩 **`{1/(n+k)}` family 는 2평면이 거의 다 품는다.** k 를 더 훑는 것은 새 정보가
아니다. 새 정보는 **임계·순수n·창** 처럼 family 를 벗어나는 방향에 있다.

### 내일 순서를 `(−3,−1)` 에서 `(−6,−3)` 으로 바꿨다

```
투수 n 최대 5,691  k=50,000  ->  n/(n+k) <= 0.102     (타자 0.210의 절반)
판별식 2·t1·t2·(t1−t2)   (−6,−3)=108  vs  (−3,−1)=12      조건수 9배
정점이 두 점 **사이**면 보간이다 — 55회차 외삽 비용이 2.3e−13 -> 1.5e−8 였다
부호가 반대여도 손실 0 (최고점 채점). 두 점이면 포물선은 어차피 확정된다
```

### 산출물

```
exp/build_wscale.py   기존 zip 의 지정 축 가중만 배수 (표·키 비트 단위 불변 검증 포함)
exp/solve_vertex.py   LB 점만으로 포물선을 풀고 t* 를 낸다.
                      55회차 두 점으로 t*=−4.565316 · 1090.0123 재현 = 자기검증
submissions/cand_kbw06.zip  w 2.105->1.263  sha 3194bb7141e27193
submissions/cand_kbw14.zip  w 2.105->2.947  sha 00dd27d25322ef42
```

관문 3종(구조 청정 · 단독 vs 배치 `0.000e+00` · verify 1,2,3,4) — 신규 2개와
`cand_kp3`·`cand_kp6` 재검사까지 4개 전부 통과.

### `submissions/` 정리 — 102개 356MB -> 9개 25MB

보존 규칙과 복원 명령은 `submissions/README.md`. 지운 93개는 **git 이력에 있고**
측정값은 애초에 파일이 아니라 `LB_LEDGER.csv` 에 있다. `.git` 393MB 는 그대로다
(이력 재작성은 제출 아티팩트 추적성을 잃으므로 하지 않는다).
